# Codex Protocol Event Coverage / Codex 协议事件覆盖

## Metadata / 元数据
- Owner: repository maintainers / 负责人：仓库维护者
- Status: draft / 状态：草案
- Last updated: 2026-07-30 / 最近更新：2026-07-30
- Related spec: / 相关规格：
  - `docs/product-specs/session-transcript-analyzer.md`
- Related design: / 相关设计：
  - `docs/design-docs/logical-event-timeline.md`
- Related runbook: / 相关运行手册：
  - `docs/design-docs/schema-update-runbook.md`
- Upstream references checked on 2026-07-30: / 2026-07-30 检查的上游参考：
  - `https://github.com/openai/codex/blob/b545c94041017d000e2c8b2f6272705d21b85dfb/codex-rs/docs/protocol_v1.md`
  - `https://github.com/openai/codex/blob/b545c94041017d000e2c8b2f6272705d21b85dfb/codex-rs/protocol/src/protocol.rs`
  - Pinned revision: `b545c94041017d000e2c8b2f6272705d21b85dfb`; local post-refactor baseline: `eee7663`. / 固定 revision：`b545c94041017d000e2c8b2f6272705d21b85dfb`；本地重构后基线：`eee7663`。

## Context / 背景

Codex rollout JSONL rows are protocol records, not a frozen public analytics schema. Local rollout rows flatten a top-level `RolloutItem` beside `timestamp`; paginated rollouts may also carry an optional top-level zero-based `ordinal`, while legacy rollout rows omit it. A protocol event therefore normally appears as `{"timestamp":"...","ordinal":42,"type":"event_msg","payload":{"type":"..."}}` (with `ordinal` omitted for legacy rows). `EventMsg` itself is a tagged Rust enum with `payload.type` encoded in `snake_case`.

Codex rollout JSONL 行是协议记录，不是固定不变的公开分析 schema。本地 rollout 行会把顶层 `RolloutItem` 和 `timestamp` 展平；分页 rollout 还可能带有可选的顶层零基 `ordinal`，而 legacy rollout 行不会带它。因此协议事件通常形如 `{"timestamp":"...","ordinal":42,"type":"event_msg","payload":{"type":"..."}}`（legacy 行省略 `ordinal`）。`EventMsg` 本身是带 tag 的 Rust enum，`payload.type` 使用 `snake_case` 编码。

The current protocol documentation also warns that these Rust protocol types are primarily in-process types rather than a stable serde wire contract. The analyzer should use the source as a trusted shape reference, not as permission to close the local parser over a finite list of variants. / 当前协议文档还特别说明，这些 Rust protocol type 主要是进程内类型，而不是稳定的 serde wire contract。Analyzer 应把上游源码作为可信的形态参考，但不能因此把本地 parser 封闭成一个有限 variant 列表。

Upstream explicitly treats `EventMsg` as extensible. This viewer should therefore keep its existing loose parser model: accept unknown event variants, preserve raw JSON, and add strong behavior only for event types that affect reading, search, grouping, metadata, severity, or metrics.

上游明确把 `EventMsg` 视为可扩展类型。因此本查看器应保持现有宽松解析模型：接受未知 event variant，保留原始 JSON，只对会影响阅读、搜索、分组、元数据、严重级别或指标的事件类型补充强行为。

## Event Families / 事件族

Current upstream `EventMsg` variants fall into these practical parser families:

当前上游 `EventMsg` variant 可按解析器实践分成以下事件族：

- Lifecycle and session metadata: `task_started`, `task_complete`, `context_compacted`, `thread_rolled_back`, `turn_aborted`, `session_configured`, `thread_settings_applied`, `thread_goal_updated`, `shutdown_complete`, and `safety_buffering`. / 生命周期与 session 元数据：`task_started`、`task_complete`、`context_compacted`、`thread_rolled_back`、`turn_aborted`、`session_configured`、`thread_settings_applied`、`thread_goal_updated`、`shutdown_complete` 和 `safety_buffering`。
- Conversation and reasoning: `user_message`, `agent_message`, `agent_reasoning`, raw reasoning/content delta events, `raw_response_item`, `item_started`, `item_completed`. / 对话与推理：`user_message`、`agent_message`、`agent_reasoning`、原始推理或内容 delta 事件、`raw_response_item`、`item_started`、`item_completed`。
- Tools and external actions: MCP startup/tool events, web search, image generation, exec command begin/output/interaction/end, patch apply begin/update/end, `view_image_tool_call`, dynamic tool request/response. / 工具与外部动作：MCP 启动和工具事件、Web search、图像生成、exec command begin/output/interaction/end、patch apply begin/update/end、`view_image_tool_call`、dynamic tool request/response。
- Approval and user interaction: exec/apply-patch approval requests, permissions requests, user input requests, elicitation requests, guardian assessment. / 审批与用户交互：exec/apply-patch approval request、permissions request、user input request、elicitation request、guardian assessment。
- Warning and error surfaces: `error`, `warning`, `guardian_warning`, `deprecation_notice`, `stream_error`, model reroute/verification. / 警告与错误表面：`error`、`warning`、`guardian_warning`、`deprecation_notice`、`stream_error`、model reroute/verification。
- Review, plan, hooks, realtime, and collaboration: review mode enter/exit, `plan_update`, `plan_delta`, hook events, realtime conversation events, and collab agent lifecycle events. / Review、计划、hook、实时会话与协作：review mode 进入/退出、`plan_update`、`plan_delta`、hook 事件、realtime conversation 事件、collab agent 生命周期事件。

The parser does not need to model every field in these families. It should extract only the fields needed for user-facing summaries, search, grouping, and structured detail, while raw JSON remains the source of truth for everything else.

解析器不需要建模这些事件族中的每个字段。它只应提取面向用户摘要、搜索、分组和结构化详情所需的字段；其他内容继续以原始 JSON 作为事实来源。

## Implemented Coverage Notes / 已实现覆盖记录

- Existing parser behavior already matches the right compatibility posture: unknown `event_msg` rows fall back to protocol/raw visibility instead of being rejected. / 现有解析行为已经符合正确的兼容姿态：未知 `event_msg` 行会回退到 protocol/raw 可见性，而不是被拒绝。
- `task_started` and `task_complete` are current v1 wire names. Upstream also accepts `turn_started` and `turn_complete` as aliases, so the viewer should normalize both aliases into the same protocol-layer lifecycle behavior. These routine runtime markers should not add noise to the Main timeline. / `task_started` 和 `task_complete` 是当前 v1 wire 名称。上游也接受 `turn_started` 和 `turn_complete` 作为别名，因此查看器应把这两个别名归一到相同的 protocol 层生命周期行为。这些常规运行时标记不应给主时间线增加噪声。
- `thread_name_updated` is not the same wire event as current `thread_goal_updated`. Keep `thread_name_updated` only as legacy transcript compatibility for `payload.thread_name`. Current protocol title metadata should come from `session_configured.thread_name`; `thread_goal_updated.goal` is a structured long-running goal snapshot, not a thread-name replacement. Its first appearance and semantic lifecycle changes become Main timeline `goal` events, while accounting-only heartbeats remain protocol. User-side injected goal context stays in the protocol layer as `goal_context`, and `create_goal`, `get_goal`, and `update_goal` tool calls remain Main timeline goal status events. / `thread_name_updated` 不是当前 `thread_goal_updated` 的同一个 wire 事件。`thread_name_updated` 只应作为旧转录兼容，用于 `payload.thread_name`。当前协议下的标题元数据应来自 `session_configured.thread_name`；`thread_goal_updated.goal` 是结构化长期 goal 快照，而不是 thread name 替代品。它的首次出现和语义生命周期变化会成为 Main timeline `goal` 事件，只更新计量数据的 heartbeat 则留在 protocol。用户侧注入的 goal context 作为 `goal_context` 留在 protocol layer，`create_goal`、`get_goal` 和 `update_goal` 工具调用也继续作为 Main timeline goal 状态事件。
- Warning-like events should not be treated as neutral protocol noise when they communicate user-visible risk. `warning`, `guardian_warning`, and `stream_error` should surface warning/error severity, while `deprecation_notice` can remain protocol unless it affects the user's current work. / 当 warning 类事件传达用户可见风险时，不应把它们当作中性协议噪声。`warning`、`guardian_warning` 和 `stream_error` 应暴露 warning/error 严重级别；`deprecation_notice` 可继续留在 protocol，除非它影响用户当前工作。
- Plan events currently arrive through full proposed-plan records, tool-call-shaped `update_plan` records, and protocol-shaped `plan_update` / `plan_delta` records. Planning metrics and reading filters should count both proposed plans and these plan update events. / 计划事件当前可能通过完整 proposed-plan 记录、工具调用形态的 `update_plan` 记录和协议形态的 `plan_update` / `plan_delta` 记录出现。计划指标和阅读筛选应同时统计 proposed plan 和这些 plan update 事件。
- Tool operations should prefer complete end events when present, but begin/update/delta rows must remain readable for incomplete or interrupted transcripts. Status values such as `declined` should not be inferred as success only because an exit code is absent. Grouped generic tool-family labels should use the representative terminal lifecycle row, preferring declined rows, then end rows, then the latest update/delta, and finally begin rows. Raw records keep their original one-row subtype labels. / 工具操作在存在完整 end 事件时应优先使用 end 事件，但 begin/update/delta 行必须在不完整或中断转录中保持可读。`declined` 等状态不应仅因为缺少 exit code 就被推断为成功。分组后的 generic 工具族 label 应使用代表性的终态生命周期行，优先选择 declined 行，其次 end 行，然后最新 update/delta，最后才是 begin 行。Raw record 保持原始逐行 subtype label。

The implementation covers these notes with focused synthetic fixtures: task lifecycle events and their aliases are routed through the protocol layer; `session_configured` contributes current title and cwd/project metadata without overriding first-`session_meta` identity; injected `goal_context` and accounting-only goal heartbeats remain protocol; meaningful structured goal snapshots and goal lifecycle tools become Main timeline status events; explicit matching tool/snapshot records merge their Raw refs; `warning`, `guardian_warning`, and `stream_error` surface main-layer severity; `plan_update` and `plan_delta` become planning events; tool-family begin/declined/incomplete rows are grouped by `call_id` when present; and dynamic tool, image generation, approval, hook, and collaboration begin/end or declined pairs choose terminal grouped labels without changing raw-row traceability.

实现已用聚焦合成 fixture 覆盖上述记录：task 生命周期事件及其别名归入 protocol 层；`session_configured` 补充当前标题和 cwd/project metadata，但不覆盖第一条 `session_meta` 身份；注入的 `goal_context` 和只更新计量数据的 goal heartbeat 保持在 protocol；有意义的结构化 goal 快照与 goal 生命周期工具成为 Main timeline 状态事件；明确匹配的工具/快照记录会合并 Raw refs；`warning`、`guardian_warning` 和 `stream_error` 暴露到 main 层并带严重级别；`plan_update` 和 `plan_delta` 成为计划事件；工具族 begin/declined/incomplete 行在存在 `call_id` 时按调用分组；dynamic tool、image generation、approval、hook 和 collaboration 的 begin/end 或 declined 配对会选择终态分组 label，同时不改变 raw-row 可追踪性。

Goal normalization accepts both camelCase and snake_case forms for the currently modeled goal and response-envelope fields. Status spellings normalize to stable snake_case logical values before transition comparison, signature matching, filtering, preview generation, or detail extraction. Original payload shape and unknown fields remain available in structured inspector payloads and Raw refs. / Goal 规范化接受当前已建模 goal 与 response-envelope 字段的 camelCase 和 snake_case 形态。Status 拼写会在 transition 比较、签名匹配、筛选、preview 生成或 detail 提取之前统一为稳定 snake_case logical value。原始 payload 形态和未知字段仍保留在结构化 Inspector payload 与 Raw refs 中。

The current upstream shape adds storage and lifecycle metadata that is intentionally not yet promoted by this parser: paginated `RolloutLine.ordinal`; turn start/complete trace, timing, context-window, collaboration-mode, and terminal-error fields; a full `thread_settings_applied` snapshot; process/timing/source fields on exec lifecycle rows; item/delta identifiers and timestamps; and richer collaboration payloads. Review enter/exit lifecycle payloads are now promoted to the same `review` Main lifecycle as the legacy dedicated rows (see the 2026-08-08 pass); other review and collaboration fields remain lossless through the parsed raw record and generic protocol fallback until sanitized fixtures and product semantics justify stronger behavior. / 当前上游形态增加了一批本 parser 有意尚未提升为规范字段的 storage 和生命周期 metadata：分页 `RolloutLine.ordinal`；turn start/complete 的 trace、计时、context-window、collaboration-mode 和终态错误字段；完整的 `thread_settings_applied` snapshot；exec 生命周期行上的 process/计时/source 字段；item/delta 的 identifier 和 timestamp；以及更丰富的 collaboration payload。review 进入/退出生命周期 payload 现已提升为与旧专用行相同的 `review` Main 生命周期（见 2026-08-08 检查记录）；其余 review 与 collaboration 字段继续通过 parsed raw record 和 generic protocol fallback 无损保留，直到有脱敏 fixture 且产品语义足以支持更强行为。

## Schema review notes / Schema 审查记录

2026-06-12 runbook pass: checked upstream `protocol.rs` and `protocol_v1.md`. The protocol doc continues to identify `protocol.rs` as the complete `EventMsg` reference and marks the enum as non-exhaustive, so this repository should still expect new variants over time. The current pass found no reason to change parser behavior; it only updates the coverage matrix to make fallback or partial coverage explicit for routine metadata, token count, raw/item/delta, realtime, model-routing, moderation, diff, and subagent activity event families.

2026-06-12 运行手册检查：已检查上游 `protocol.rs` 和 `protocol_v1.md`。协议文档仍将 `protocol.rs` 标为完整 `EventMsg` 参考，并说明该 enum 非穷尽，因此本仓库仍应预期未来会新增 variant。本次检查没有发现需要改变 parser 行为的理由；仅更新覆盖矩阵，明确 routine metadata、token count、raw/item/delta、realtime、model-routing、moderation、diff 和 subagent activity 事件族的 fallback 或部分覆盖状态。

2026-06-18 runbook pass: checked upstream `protocol.rs` and `protocol_v1.md` again. The current `EventMsg` set includes additional or more granular families that are not fully modeled by this parser, including realtime SDP/list-voices payloads, request-permissions and request-user-input events, MCP elicitation, raw response item and item/delta streaming events, hook start/complete payloads, collaboration resume lifecycle rows, and path-based `sub_agent_activity`. Unknown and weakly modeled rows remain visible through raw/protocol fallback, so this pass is classified as `fixture TODO / coverage update`, not a parser behavior change proposal.

2026-06-18 运行手册检查：再次检查上游 `protocol.rs` 和 `protocol_v1.md`。当前 `EventMsg` 集合包含一些本解析器尚未完整建模的新增或更细粒度事件族：realtime SDP/list-voices 和 realtime streaming payload、request-permissions 与 request-user-input 事件、MCP elicitation、raw response item 与 item/delta streaming 事件、hook start/complete payload、collaboration resume 生命周期行，以及基于路径的 `sub_agent_activity`。未知或弱建模行仍会通过 raw/protocol fallback 可见，因此本次归类为 `fixture TODO / coverage update`，不是 parser 行为变更提案。

2026-06-19 local-corpus pass: scanned 142 real `session-analyzer` transcript files without persisting private rows. Approval behavior is common through `response_item.function_call` rows whose `shell_command` arguments request `sandbox_permissions: require_escalated`, but no `event_msg` row with `payload.type = request_permissions` was observed. Multi-agent behavior is similarly recorded through tool calls such as `spawn_agent`, `wait_agent`, `send_input`, and `close_agent`, while path-based `sub_agent_activity` protocol rows were not observed. `request_user_input` is observed as a `response_item.function_call`, not as an `event_msg`. Non-empty `review_output.findings[]` rows are now observed in real transcripts, so review finding support is no longer blocked on real-data availability; remaining work should focus on sanitized fixture strengthening and rendering regressions.

2026-06-19 本地语料检查：已扫描 142 个真实 `session-analyzer` transcript 文件，未持久化私有原始行。Approval 行为常见于 `response_item.function_call` 行中，即 `shell_command` 参数请求 `sandbox_permissions: require_escalated`，但没有观察到 `payload.type = request_permissions` 的 `event_msg` 行。Multi-agent 行为同样通过 `spawn_agent`、`wait_agent`、`send_input` 和 `close_agent` 等工具调用记录，而未观察到基于路径的 `sub_agent_activity` 协议行。`request_user_input` 以 `response_item.function_call` 形式出现，而不是 `event_msg`。真实 transcript 现已观察到非空 `review_output.findings[]` 行，因此 review finding 支持不再被真实数据可用性阻塞；剩余工作应聚焦脱敏 fixture 加强和渲染回归。

2026-06-19 targeted real-data pass: a separate real local transcript outside this repository contains 7 `event_msg` rows with `payload.type = image_generation_end` and 7 mirrored `response_item` rows with `payload.type = image_generation_call`. The observed event row keys are `call_id`, `result`, `revised_prompt`, `saved_path`, `status`, and `type`; the response item keys are `id`, `result`, `revised_prompt`, `status`, and `type`. `result` is a bare base64-encoded PNG payload, not a `data:image/...` URL. The parser now treats `response_item.image_generation_call.id` as the call id, groups each mirrored pair into a Main timeline `image_generation` tool event with raw refs preserved, externalizes the bare image payload for preview rehydration, and shows a compact generated-image summary instead of a raw response dump. A current escalated shell approval was also validated in a real `session-analyzer` transcript: it appears as `response_item.function_call` / `function_call_output` for `shell_command`, with `sandbox_permissions: require_escalated` in arguments, and no extra approval or permission `event_msg` row in that transcript.

2026-06-19 定向真实数据检查：仓库外另一个真实本地 transcript 包含 7 条 `payload.type = image_generation_end` 的 `event_msg` 行，以及 7 条镜像的 `payload.type = image_generation_call` 的 `response_item` 行。观察到的 event 行字段为 `call_id`、`result`、`revised_prompt`、`saved_path`、`status`、`type`；response item 字段为 `id`、`result`、`revised_prompt`、`status`、`type`。`result` 是裸 base64 编码的 PNG payload，不是 `data:image/...` URL。解析器现在会把 `response_item.image_generation_call.id` 作为 call id，并把每组镜像行合并为 Main timeline 的 `image_generation` 工具事件，同时保留 raw refs；裸图片 payload 会外部化以供预览按需重新读取，Main timeline 显示紧凑的生成图片摘要而不是 raw response dump。另一次当前真实 `session-analyzer` transcript 中的 escalated shell approval 也已验证：它以 `shell_command` 的 `response_item.function_call` / `function_call_output` 出现，`sandbox_permissions: require_escalated` 位于 arguments 中，该 transcript 没有额外 approval 或 permission `event_msg` 行。

2026-07-13 upstream/schema pass: compared the current official `protocol.rs` and `protocol_v1.md` against the 2026-06-18 coverage pass. Recent upstream work makes paginated rollout ordering explicit with optional zero-based `RolloutLine.ordinal`, and the current event payloads expose more terminal error/timing data plus `safety_buffering` and richer command/item/review/collaboration fields. The source remains non-exhaustive and the local parser already keeps unknown rows visible through raw/protocol fallback. This pass is classified as `fixture TODO / coverage update`, not a parser behavior change proposal: no sanitized fixture currently proves the new fields should alter layer placement, grouping, failure metrics, search, or detail semantics. / 2026-07-13 上游/schema 检查：将当前官方 `protocol.rs` 和 `protocol_v1.md` 与 2026-06-18 的覆盖检查进行对比。最近的上游工作通过可选的零基 `RolloutLine.ordinal` 明确了分页 rollout 的持久排序；当前 event payload 还暴露了更多终态错误/计时数据，以及 `safety_buffering` 和更丰富的 command/item/review/collaboration 字段。上游仍保持 non-exhaustive，本地 parser 也已经通过 raw/protocol fallback 让未知行保持可见。本次归类为 `fixture TODO / coverage update`，不是 parser behavior change proposal：目前没有脱敏 fixture 能证明这些新字段应改变 layer 归属、分组、失败指标、搜索或 detail 语义。

2026-07-30 pinned lifecycle/schema pass: ran the accepted schema-update workflow after the lifecycle-family contract extraction, using local baseline `eee7663` and pinned OpenAI Codex revision `b545c94041017d000e2c8b2f6272705d21b85dfb`. That revision is 18 commits ahead of the lightweight inventory revision `406dc92`, but both authoritative files are byte-identical across the two revisions (`protocol.rs` blob `c17ada934220c035752a9296b47004150c02aafe`; `protocol_v1.md` blob `f16da209f60a5353b1ee6cd8c358c45d519c246a`). The current `EventMsg` enum still has 80 variants, so the earlier inventory did not drift before formal review. / 2026-07-30 固定 lifecycle/schema 检查：在生命周期族契约提取完成后，使用本地基线 `eee7663` 和固定 OpenAI Codex revision `b545c94041017d000e2c8b2f6272705d21b85dfb` 执行已接受的 schema 更新流程。该 revision 比轻量盘点使用的 `406dc92` 前进 18 个 commit，但两份权威文件在两个 revision 间逐字节一致（`protocol.rs` blob 为 `c17ada934220c035752a9296b47004150c02aafe`；`protocol_v1.md` blob 为 `f16da209f60a5353b1ee6cd8c358c45d519c246a`）。当前 `EventMsg` enum 仍有 80 个 variant，因此早期盘点在正式审查前没有发生漂移。

2026-07-30 real-corpus follow-up: a read-only exact-shape scan covered 478 active and archived local Codex JSONL files, approximately 980 MB and 313,519 rows at the frozen snapshot, without persisting or displaying private payload values. Only two current unadmitted shapes were observed. `view_image_tool_call` appeared 132 times in 9 files; every occurrence carried `call_id` and `path` and was mirrored by an independently supported same-file `response_item.function_call(name = view_image)` plus output. `sub_agent_activity` appeared 871 times in 68 files at the frozen snapshot and continued growing during verification; all rows carried non-empty `event_id`, `agent_thread_id`, and `agent_path`. A later structural pass found 533 distinct event IDs, all matching real Agent Coordination call IDs somewhere in the corpus, while 340 of 873 observation occurrences were cross-Session replicas without the owner call in that file. The other 17 reviewed current shapes and explicit hook lifecycle rows had zero exact `event_msg` observations. / 2026-07-30 真实语料跟进：一次只读精确形态扫描覆盖 478 个活动与归档本地 Codex JSONL 文件；固定快照约 980 MB、313,519 行，且未持久化或展示私有 payload 值。只观察到两个当前未准入形态。`view_image_tool_call` 在 9 个文件中出现 132 次；每次都携带 `call_id` 与 `path`，并在同文件内由独立受支持的 `response_item.function_call(name = view_image)` 加 output 镜像。固定快照中 `sub_agent_activity` 在 68 个文件中出现 871 次，并在复核期间继续增长；所有记录都携带非空 `event_id`、`agent_thread_id` 与 `agent_path`。后续结构检查发现 533 个不同 event ID，全部能在语料某处匹配真实 Agent 协调 call ID；而后续 873 条观察中的 340 条属于所在文件不含 owner call 的跨会话副本。其余 17 个已审查当前形态与显式 hook lifecycle 记录的精确 `event_msg` 观察数均为零。

2026-08-08 real-corpus pass: review lifecycle rows now persist through two envelopes. Legacy dedicated rows (`event_msg` with `payload.type = entered_review_mode` / `exited_review_mode`) appear in older transcripts, while current providers persist the canonical completed TurnItem envelope (`event_msg` with `payload.type = item_completed` and `payload.item.type = EnteredReviewMode` / `ExitedReviewMode`, carrying `target` / `user_facing_hint` / `review_output`). The parser now normalizes both envelopes to the same `review` Main lifecycle subtype (`entered_review_mode` / `exited_review_mode`) through one shared helper used by marker extraction, logical-event construction, preview, and detail. Review children whose metadata classifies them as review also accept the top-level `parent_thread_id` as explicit parent evidence before temporal inference; canonical rows with a `thread_id` are owner-filtered to the indexed session. Sanitized fixture coverage locks legacy inference, canonical item + explicit parent linkage, explicit-wins-over-temporal, non-review demotion guards, owner filtering, and index reuse. / 2026-08-08 真实语料检查：review 生命周期记录现在以两种 envelope 持久化。旧转录使用专用行（`event_msg` 的 `payload.type = entered_review_mode` / `exited_review_mode`），当前 provider 则持久化 canonical 的 completed TurnItem envelope（`event_msg` 的 `payload.type = item_completed`，`payload.item.type = EnteredReviewMode` / `ExitedReviewMode`，携带 `target` / `user_facing_hint` / `review_output`）。解析器现在通过同一个共享 helper（marker 提取、逻辑事件构建、preview 与 detail 共同消费）把两种 envelope 归一化为相同的 `review` Main 生命周期 subtype（`entered_review_mode` / `exited_review_mode`）。metadata 已判定为 review 的子会话也会在时间推断之前接受顶层 `parent_thread_id` 作为显式父会话证据；带 `thread_id` 的 canonical 记录只接受属于当前索引会话的那一份。脱敏 fixture 覆盖锁定：legacy 推断、canonical item + 显式父链路、显式优先于时间推断、非 review 降级防护、owner 过滤与索引复用。

The current MCP startup, web-search begin, command/patch progress, image begin, dynamic request/response, approval/request, and collaboration-resume shapes remain losslessly visible through Protocol/Raw fallback and stay at `fixture TODO / coverage update`. Real `view_image_tool_call` rows keep their existing same-`callId` traceability ownership without semantic admission. Exact `sub_agent_activity.event_id` now has one bounded typed-correlation path: when the same Session contains an exact Agent Coordination Direct Tool Call owner, the observation becomes a traceability-only Raw Reference on that event; otherwise it remains Protocol/Raw fallback. The identity is not copied into generic `callId`, and no cross-Session grouping is inferred. Hook lifecycle membership is supported while `run.id` correlation remains unmodeled. Historical or locally observed exact variants already admitted by the 42-member contract remain supported for transcript compatibility even when absent from the current upstream `EventMsg` enum. / 当前 MCP startup、web-search begin、command/patch progress、image begin、dynamic request/response、approval/request 与 collaboration-resume 形态继续通过协议层／原始层兜底无损可见，并保持 `fixture TODO / coverage update`。真实 `view_image_tool_call` 记录继续使用既有 same-`callId` traceability ownership，不获得语义准入。精确 `sub_agent_activity.event_id` 现在具有一条有界的带类型 correlation 路径：当同一会话包含精确 Agent 协调直接工具调用 owner 时，观察记录会成为该事件上仅用于可追溯性的原始引用；否则继续进入协议层／原始层兜底。该 identity 不会复制进泛化 `callId`，也不会推断跨会话分组。Hook lifecycle membership 已支持，但 `run.id` correlation 仍未建模。即使当前上游 `EventMsg` enum 不再列出，42-member contract 中已经准入的历史或本地观察精确 variant 仍为 transcript compatibility 保持支持。

## Coverage matrix / 覆盖矩阵

This compact matrix summarizes Codex event shapes already discussed in this document and high-risk ambiguous families. It is not an exhaustive closed protocol schema. Support status values are `supported`, `partially supported`, `raw-only`, `unsupported by design`, and `unknown / needs fixture`.

本紧凑矩阵总结本文已讨论的 Codex event shape 以及高风险、易混淆事件族。它不是穷尽且闭合的协议 schema。支持状态值为 `supported`、`partially supported`、`raw-only`、`unsupported by design` 和 `unknown / needs fixture`。

| Protocol record / event shape | Current support status | Raw event outcome | Logical event kind / subtype | Layer | Detail rendering | Raw refs / traceability | Search / filter relevance | Folding relevance | Fixture coverage | Docs status | Notes / TODO |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Paginated `RolloutLine.ordinal` | raw-only | Preserved inside the parsed raw record; no typed ordinal field | Existing row/event subtype unchanged | main, protocol, and raw | Raw JSON only | Source locator remains the physical JSONL line; ordinal is not in `rawRefs` | Not indexed as a field | None | Focused fixture TODO | Documented | Optional zero-based storage ordering for paginated rollouts; legacy rows omit it and current parser still walks physical line order |
| `session_meta` with newer identity, source, agent, history, and context-window fields | partially supported | Preserved as raw/protocol rows | `protocol` session metadata subtype | protocol and raw | Metadata fallback | Raw refs preserved | Searchable flattened metadata | Not profile-editable | Existing identity fixture; newer fields TODO | Documented | First-session identity remains stable; `session_id`, `thread_source`, agent path/role, history mode, multi-agent version, and context-window fields are not promoted |
| `task_started`, `task_complete`, `turn_started`, `turn_complete` | partially supported | Preserved as raw rows | `protocol` lifecycle subtype | protocol | Protocol text/metadata fallback | Raw refs preserved | Searchable protocol text | Not profile-editable | Alias fixture; timing/error/context fields TODO | Documented | Runtime lifecycle remains out of Main; current upstream adds trace/timing/context-window/collaboration fields and terminal error data |
| `session_configured` | supported | Preserved as metadata row | `protocol` session metadata subtype | protocol | Metadata fields and protocol text | Raw refs preserved | Title/cwd/project metadata searchable | Not profile-editable | Synthetic fixture coverage | Documented | Contributes current title/cwd metadata without replacing session identity |
| `context_compacted`, `turn_aborted`, `thread_rolled_back` | supported | Preserved as raw rows | Lifecycle warning/error subtypes | main with protocol/raw traceability | Notice/protocol fallback | Raw refs preserved | Searchable lifecycle text | Error-focus relevance when warning/error | Fixture coverage for compacted and rollback paths; abort path relies on parser coverage | Documented | Main timeline only when the event communicates reading-impacting lifecycle state |
| `thread_settings_applied`, `shutdown_complete` | raw-only | Preserved as raw/protocol rows | Generic `protocol` subtype | protocol and raw | Bounded protocol fallback | Raw refs preserved | Searchable flattened text | Not profile-editable | Generic fallback; settings snapshot TODO | Matrix documented | `thread_settings_applied` carries a full effective settings snapshot; add stronger behavior only if it affects session metadata or user-visible state |
| `thread_name_updated` legacy rows | partially supported | Preserved as raw rows | `protocol` legacy metadata subtype | protocol | Metadata fallback | Raw refs preserved | Searchable legacy title text | Not profile-editable | Legacy-focused coverage through parser tests | Documented | Legacy compatibility only; current title metadata should prefer `session_configured.thread_name` |
| `thread_goal_updated`, injected `goal_context`, and `create_goal` / `get_goal` / `update_goal` tool rows | supported | Preserved as raw rows | `protocol` for injected context and accounting heartbeats; `goal` main kind for semantic snapshot transitions and lifecycle tools | protocol for injected/accounting context; main for user-visible lifecycle changes | Goal objective, status, budget, usage, request/response, and protocol fallback | Raw refs preserved; identity-, proximity-, turn-, and single-consumption-gated tool/snapshot matches merge refs | Goal objective/status searchable; `kind:goal` filters lifecycle transitions | Planning and narrative relevance for lifecycle changes | Synthetic current-shape snapshot, heartbeat, detail, partial-response fallback, causal merge, and false-merge coverage | Documented | Do not use goal objective as a thread title; keep injected audit text and accounting-only churn out of Main timeline |
| `token_count` | supported | Preserved as raw row | `protocol` token count subtype; Main warning only if a usage limit is reached | protocol plus conditional main warning | Token/rate-limit preview and protocol fallback | Raw refs preserved | Usage/rate-limit text searchable | Error-focus relevance only for reached usage limits | Synthetic fixture and replay coverage | Documented in product/logical docs | Routine token counts stay out of Main timeline |
| `safety_buffering` | raw-only | Preserved as raw/protocol row | Generic `protocol` subtype; warning candidate | protocol and raw | Bounded protocol fallback | Raw refs preserved | Searchable model/use-case/reason text when present | Not profile-editable | Focused fixture TODO | Matrix documented | Current payload carries model, use cases, reasons, UI flag, and optional faster model; do not infer failure or promote without product semantics |
| `warning`, `guardian_warning`, `stream_error` | supported | Preserved as raw rows | Warning/error event subtype | main and protocol/raw traceability | Notice/protocol fallback | Raw refs preserved | Affects issue search/filter and metrics | Error-focus folding relevance | Synthetic fixture coverage | Documented | User-visible risk should not remain neutral protocol noise |
| `deprecation_notice` | raw-only | Preserved as raw/protocol row | `protocol` notice subtype | protocol | Protocol fallback | Raw refs preserved | Searchable protocol text | Not profile-editable | Generic fallback coverage | Documented | Promote only if it affects current user work |
| `model_reroute`, `model_verification`, `turn_moderation_metadata` | raw-only | Preserved as raw/protocol rows | Generic `protocol` subtype | protocol and raw | Bounded protocol fallback | Raw refs preserved | Searchable flattened text when present | Not profile-editable | Needs focused synthetic fixture if product semantics emerge | Matrix documented | Fixture TODO only; no parser proposal until real payloads show UI/search/metrics impact |
| Proposed plan records, `update_plan`, `plan_update`, `plan_delta` | supported | Preserved through raw refs | `proposed_plan` / planning subtypes | main when planning context matters; protocol/raw traceability | Plan/update sections or protocol fallback | Multi-row raw refs preserved where applicable | Planning metrics and filters | Conversation/planning folding relevance | Synthetic and replay coverage | Documented | Count planning context across tool-shaped and protocol-shaped records |
| `raw_response_item`, `item_started`, `item_completed`, `agent_message_content_delta`, `reasoning_content_delta`, `reasoning_raw_content_delta`, streaming content/reasoning delta rows | partially supported | Preserved as raw rows | Generic protocol or conversation/reasoning-derived subtype when text is promoted | protocol by default; main only for promoted readable content | Bounded protocol/text fallback | Raw refs preserved | Extracted text searchable when available | Conversation folding only for promoted readable content | Generic fallback plus item-completed parser coverage; new field-shape TODO | Matrix documented | Current item rows carry thread/turn/item identity and timestamps; delta rows add item and index fields. Keep deltas bounded; add behavior only for stable readable content or traceability needs |
| `exec_command_begin`, `exec_command_end`, and admitted compatibility `exec_command_update` / `exec_command_delta` / `exec_command_declined` rows | supported | Preserved as raw rows | `command` | main with protocol/raw traceability | Command/stdout/stderr sections | Grouped raw refs preserved by call id | Command text/output searchable; status filters | Narrative/error/change-review relevance | Existing exact-membership, lifecycle, command-output, and incomplete/declined coverage; timing/source/process fields remain TODO | Documented | End rows are preferred when available; locally admitted historical or observed exact variants remain supported even when absent from the current upstream enum |
| Current upstream `exec_command_output_delta`, `terminal_interaction` | raw-only; fixture TODO | Preserved losslessly as raw/protocol rows | Generic `protocol` subtype | protocol and raw | Bounded protocol/raw fallback | Own raw refs preserved; not joined to the command call group | Flattened payload text may be searchable, but no command-output or status semantics are assigned | Not profile-editable | Negative exact-admission coverage for `exec_command_output_delta`; focused payload fixtures for both shapes TODO | Matrix documented | A future proposal must prove stream-byte decoding, stdin/process presentation, command grouping, and search ownership without treating suffix similarity as admission |
| `patch_apply_begin`, `patch_apply_end`, and admitted compatibility `patch_apply_update` / `patch_apply_delta` / `patch_apply_declined` rows | supported | Preserved as raw rows | `patch` | main with protocol/raw traceability | Patch sections with file summaries | Grouped raw refs preserved by call id | Touched-file and patch text searchable | Change-review folding relevance | Existing exact-membership, patch-shape, incomplete/declined, and detail coverage | Documented | Locally admitted historical or observed exact variants remain supported for transcript compatibility |
| Current upstream `patch_apply_updated` | raw-only; fixture TODO | Preserved losslessly as a raw/protocol row | Generic `protocol` subtype | protocol and raw | Bounded protocol/raw fallback | Own raw ref preserved; not joined to the patch call group | Flattened change payload may be searchable, but no touched-file or patch-progress semantics are assigned | Not profile-editable | Negative exact-admission coverage; focused current-payload fixture TODO | Matrix documented | A future proposal must establish whether updated changes belong in patch preview, touched-file search, and representative selection |
| `turn_diff` | raw-only | Preserved as raw/protocol row | Generic `protocol` subtype | protocol and raw | Bounded protocol fallback | Raw refs preserved | Searchable flattened diff text when present | Not profile-editable | Needs focused fixture if observed | Matrix documented | Potential future change-review relevance, but no parser proposal without observed payloads |
| MCP startup/tool events, `elicitation_request` | partially supported | Preserved as raw rows | Exact MCP call begin/end rows become `mcp_call`; `mcp_startup_update`, `mcp_startup_complete`, and elicitation remain generic protocol fallback | main for exact admitted MCP calls; otherwise protocol | Structured Main timeline summaries for admitted MCP request code/arguments and text results, with full request/response JSON retained in the inspector; bounded fallback for startup/elicitation payloads | Raw refs preserved | Tool/search text when available | Narrative/error relevance only for modeled Main calls | Existing focused MCP call detail and generic fallback coverage; current startup/elicitation payload fixtures TODO | Documented | Keep MCP startup state separate from tool-call lifecycle; add field-specific detail only when observed payloads establish user-visible startup or elicitation semantics |
| `response_item.web_search_call`, `event_msg.web_search_end`, and open-page snapshots | supported | Preserved as raw rows | Web search subtype | main with protocol/raw traceability | Structured web search detail | Adjacent mirrored raw refs merged when appropriate | Search target searchable | Narrative relevance | Existing fixture coverage | Documented in logical timeline | Adjacent end/call snapshots merge by canonical action target |
| Current upstream `web_search_begin` | raw-only; fixture TODO | Preserved losslessly as a raw/protocol row | Generic `protocol` subtype | protocol and raw | Bounded protocol/raw fallback | Own raw ref preserved; not joined to the independently supported end/call snapshot path | Flattened payload may be searchable, but no web-search ownership or incomplete-state semantics are assigned | Not profile-editable | Focused current-payload fixture TODO | Matrix documented | A future proposal must establish begin/end identity, incomplete-state value, and whether begin payload text should affect the existing web-search event |
| Current upstream `view_image_tool_call` | partially supported as mirror traceability | Preserved losslessly as a raw row | No independent semantic subtype when a supported response-item `view_image` call owns the same call ID; otherwise generic `protocol` | Existing same-call owner in main plus raw traceability; otherwise protocol and raw | Existing structured `view_image` detail plus Raw References, or bounded protocol/raw fallback | Same-`callId` rows remain Raw References on the response-item-owned event; the event-message row contributes no outcome, preview, or search semantics | Supported response-item request/output text remains searchable; the mirror adds no search ownership | Not profile-editable | Real-shape-derived synthetic mirror fixture | Matrix documented | The 132 observed rows were all mirrored by same-file response-item calls and outputs, so no new lifecycle or Main admission is justified |
| Dynamic tool request/response, image generation, approval/request-permissions, user-input request, hook, and collaboration lifecycle rows | partially supported | Preserved as raw rows when present; approval and user-input behavior may also appear as tool-call rows; unwrapped developer stdout remains visible in the protocol layer without being asserted as hook | Exact admitted lifecycle rows receive their documented Main kinds; current unadmitted shapes use generic `protocol` fallback; unwrapped developer stdout becomes a protocol-layer `developer_message` with a possible-hook-output tag | main only for exact admitted or independently supported action shapes; unwrapped developer stdout is protocol-only; otherwise protocol/raw | Bounded request/response fallback; real `image_generation_end` rows group with mirrored `response_item.image_generation_call` rows and show a generated-image summary plus preview | Exact admitted call-id rows group; hook rows without modeled `call_id` remain single-row events; unknown current shapes retain own raw refs | Exact admitted payload text is searchable through its logical event; unknown current shapes retain bounded flattened fallback text; bare image bytes are externalized from indexed DTOs | Narrative/error relevance only for modeled Main events; possible hook output is uncertainty-tagged | Exact 42-member lifecycle table and negative lookalike coverage; minimized real image-generation pair; current `image_generation_begin`, dynamic request/response, approval/request variants, collab resume, and hook `run.id` correlation need fixtures | Documented | Current upstream `dynamic_tool_call_request` / `dynamic_tool_call_response`, `image_generation_begin`, `exec_approval_request`, `apply_patch_approval_request`, `request_permissions`, `request_user_input`, `elicitation_request`, `guardian_assessment`, and `collab_resume_begin` / `collab_resume_end` remain unadmitted. `hook_started` / `hook_completed` membership is supported, but `run.id` correlation is intentionally deferred. None receives parser behavior from name similarity alone. |
| Realtime conversation/list-voices rows, including SDP rows | raw-only | Preserved as raw/protocol rows | Generic `protocol` subtype | protocol and raw | Bounded protocol fallback | Raw refs preserved | Searchable flattened text when present | Not profile-editable | Needs fixture if real transcript rows appear | Matrix documented | Keep as observation target until user-visible transcript/audio/session semantics are clear |
| `sub_agent_activity` | partially supported through bounded typed correlation | Preserved losslessly as a raw row | Traceability-only observation on an existing `agent_coordination` event when an exact same-Session owner exists; otherwise generic `protocol` subtype | Existing owner in main plus raw traceability, or protocol and raw fallback | Existing collaboration detail plus Raw References; orphan uses bounded protocol fallback | Exact typed `event_id` may attach to an exact Agent Coordination Direct Tool Call in the same Session; it is not copied into generic `callId`, and no cross-Session grouping is inferred | Attached observation fields do not enter Main search; orphan flattened text remains searchable in Protocol/Raw as before | No new folding fact | Synthetic exact-match, orphan, collision, missing-identity, multiple-observation, and build-index coverage derived from aggregate real shapes | Product, logical model, and matrix documented | High-frequency real rows prove correlation value, but cross-Session replication requires owner-local fail-closed handling; `agent_thread_id` and `agent_path` remain Raw-only evidence |
| Review mode enter/exit and review output | supported | Preserved as raw rows | `review` lifecycle subtype normalized from legacy dedicated rows and canonical completed TurnItem rows | main lifecycle plus protocol/raw traceability | Review lifecycle/detail fallback | Raw refs preserved | Review text/findings searchable when present | Conversation/planning relevance when applicable | Legacy regression, canonical item envelope, explicit parent linkage, owner-filter, and reuse coverage | Documented | Legacy `entered_review_mode` / `exited_review_mode` and canonical `item_completed` + `EnteredReviewMode` / `ExitedReviewMode` normalize to the same logical lifecycle; remaining risk is field-shape coverage and rendering regression |
| Unknown future `event_msg` variants | supported as fallback | Preserved losslessly | Generic `protocol` / raw fallback | protocol and raw | Bounded generic protocol/raw fallback | Raw refs preserved | Searchable flattened text where extracted | Not profile-editable unless later modeled | Generic fallback coverage | Documented | Add parser behavior only when product semantics require it |

## Maintenance Rules / 维护规则

When Codex protocol changes or real transcripts expose new event shapes:

当 Codex 协议变化或真实转录暴露新的事件形态时：

1. Check upstream `codex-rs/protocol/src/protocol.rs` first; treat generated or hand-copied schemas as secondary evidence. / 先检查上游 `codex-rs/protocol/src/protocol.rs`；生成或手抄的 schema 只能作为次级依据。
2. Do not hard-code an exhaustive closed `EventMsg` schema in this repository. If an exact schema is needed for investigation, generate it locally from upstream Rust types with `schemars::schema_for!(RolloutLine)`. / 不要在本仓库中硬编码穷尽且闭合的 `EventMsg` schema。如果调查需要精确 schema，应从上游 Rust 类型用 `schemars::schema_for!(RolloutLine)` 本地生成。
3. Add parser behavior only when an event changes the main/protocol layer choice, severity, session metadata, tool grouping, search text, metrics, or structured details. / 只有当某个事件会改变 main/protocol 层归属、严重级别、session 元数据、工具分组、搜索文本、指标或结构化详情时，才新增解析行为。
4. Keep raw fallback lossless for every row and add focused fixtures before broadening normalization rules. / 对每一行保持无损 raw fallback，并在扩大归一化规则前添加聚焦 fixture。
5. Use `docs/design-docs/schema-update-runbook.md` for source trust, decision tiers, fixture privacy rules, and the schema-review output template. / 使用 `docs/design-docs/schema-update-runbook.md` 获取来源信任等级、决策分层、fixture 隐私规则和 schema 审查输出模板。
6. Update bilingual docs together: this document for protocol coverage, `logical-event-timeline.md` for model changes, product specs only for changed user-visible behavior, and `tech-debt-tracker.md` for known gaps left intentionally open. / 双语文档要同步更新：协议覆盖写入本文；模型变化写入 `logical-event-timeline.md`；只有用户可见行为变化才更新产品规格；有意保留的已知缺口写入 `tech-debt-tracker.md`。

## Follow-up Candidates / 后续候选工作

- Broaden goal field-specific presentation if future real transcripts expose new semantic fields beyond objective, status, token budget, tokens used, time used, creation/update identity, and completion budget reports. / 如果未来真实转录暴露出超过 objective、status、token budget、tokens used、time used、创建/更新 identity 和 completion budget report 的新语义字段，再扩展 goal 的字段级展示。
- Add more real-data fixtures for newly observed MCP, dynamic tool, hook, approval, image generation, or collaboration payload variants. Synthetic lifecycle coverage protects grouped labels and status, hook alias coverage protects `hook_started` / `hook_completed`, minimized fixture coverage includes the observed image-generation pair, and aggregate real-shape evidence now backs synthetic `view_image_tool_call` mirror and `sub_agent_activity` typed-correlation fixtures. The current local corpus still lacks real dynamic tool, approval/request-permissions, explicit hook lifecycle, and the other upstream-only lifecycle shapes reviewed on 2026-07-30. Approval and multi-agent behavior may also appear through tool-call rows, so avoid treating missing protocol rows as proof that user-visible behavior never occurred. Unobserved protocol payload shapes intentionally keep bounded fallback summaries until real payloads justify field-specific presentation. / 为新观察到的 MCP、dynamic tool、hook、approval、图像生成或协作 payload variant 增加更多真实数据 fixture。Synthetic 生命周期覆盖会保护这些事件族的分组 label 与 status，hook alias 覆盖会保护 `hook_started` / `hook_completed`，最小化 fixture 已包含观察到的图像生成配对；聚合真实形态证据现在也支持合成 `view_image_tool_call` 镜像与 `sub_agent_activity` 带类型 correlation fixture。当前本地语料仍缺少真实 dynamic tool、approval/request-permissions、显式 hook lifecycle，以及 2026-07-30 审查的其他仅由上游得知的生命周期形态。Approval 与 multi-agent 行为也可能通过工具调用记录出现，因此不要把缺少协议记录误判为用户可见行为从未发生。这些尚未观察的协议 payload 形态会有意继续使用受限兜底摘要，直到真实 payload 足以支持字段级展示。
- Add a synthetic paginated-rollout fixture with optional `ordinal`, legacy rows without it, a gap/resume case, and terminal lifecycle metadata (`error`, timing, source, and process fields). Verify that raw detail and source locators remain lossless before deciding whether ordinal-aware ordering or field-specific metrics belong in the parser. / 增加一个合成的分页 rollout fixture，覆盖可选 `ordinal`、不带 `ordinal` 的 legacy 行、gap/resume 场景，以及终态生命周期 metadata（`error`、计时、source 和 process 字段）。在决定是否需要 ordinal 感知排序或字段级指标前，先验证 raw detail 与 source locator 仍然无损。
