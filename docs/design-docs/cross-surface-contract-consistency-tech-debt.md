# Cross-surface contract consistency technical debt / 跨表面契约一致性技术债

- Status: open; items 1, 2, and 3 complete, and item 4 deferred / 状态：开放；事项 1、2 与 3 已完成，事项 4 已推迟
- Recorded: 2026-07-30 / 记录日期：2026-07-30
- Scope: Codex transcript classification and Code Mode display contracts / 范围：Codex 转录分类与 Code Mode 展示契约
- Source: read-only consistency audit after the Code Mode tool-metadata registry consolidation, plus the item 1 re-audit and lightweight upstream inventory / 来源：Code Mode 工具 metadata 注册表收敛后的只读一致性审计，以及事项 1 复核与轻量上游盘点

## Purpose / 目的

This document records four high-confidence risks where one semantic contract is represented by several consumers. It is a prioritised refactoring queue, not evidence of a current user-visible defect. The Code Mode tool registry already removed one such duplication; the items below deliberately retain parsing, rendering, and security decisions that are not mechanically interchangeable.

本文记录四项高置信度风险：同一语义契约被多个消费方分别表达。它是一份按优先级排序的重构队列，而非当前已有用户可见缺陷的证据。Code Mode 工具注册表已经消除了一处此类重复；下列事项会刻意保留那些不能机械互换的解析、渲染和安全决策。

## Decision rule / 决策规则

Extract a shared descriptor only when multiple consumers need the same stable semantic fact. Keep source-specific parsing, payload extraction, presentation layout, and safety-sensitive allowlists at their existing boundaries unless the descriptor can state the shared fact without hiding those distinctions.

只有当多个消费方需要同一项稳定的语义事实时，才提取共享 descriptor。除非 descriptor 能在不掩盖差异的前提下表达共享事实，否则 source 专属解析、payload 提取、展示布局和安全敏感 allowlist 应保留在现有边界中。

## 1. Tool-lifecycle family taxonomy / 工具生命周期族分类法

- Status: completed 2026-07-30 / 状态：已于 2026-07-30 完成
- Priority: high / 优先级：高
- Evidence: lifecycle admission is declared in `src/codex.js`; exact wire enrichment classifications live in `src/codex-source.js`; logical grouping, family recognition, hook aliases, representative selection, and standalone terminal handling live in `src/codex-logical.js`; and focused logical tests supply a reduced private admission set. Repository history shows that adding `hook_started` / `hook_completed` and the real `image_generation_end` shape required synchronized changes across several of those sites. / 证据：生命周期准入在 `src/codex.js` 中声明；精确 wire enrichment 分类位于 `src/codex-source.js`；逻辑分组、family 识别、hook alias、代表记录选择和独立终态处理位于 `src/codex-logical.js`；聚焦 logical 测试还提供了一份缩减的私有准入集合。仓库历史表明，增加 `hook_started` / `hook_completed` 与真实 `image_generation_end` 形态时，需要同步修改上述多个位置。
- Upstream result: the formal runbook pass pinned OpenAI Codex revision `b545c94041017d000e2c8b2f6272705d21b85dfb`. Its authoritative `protocol.rs` and `protocol_v1.md` blobs are identical to the lightweight `406dc92` inventory, confirming the same nonuniform current types: MCP startup rows, `web_search_begin`, `exec_command_output_delta`, `terminal_interaction`, `patch_apply_updated`, `image_generation_begin`, `view_image_tool_call`, `dynamic_tool_call_request` / `dynamic_tool_call_response`, current approval/request variants, `collab_resume_begin` / `collab_resume_end`, and `sub_agent_activity`. Hook records correlate by `run.id`, while `sub_agent_activity` uses `event_id` and path identity; a generic `call_id` assumption remains invalid. Every unadmitted current shape is classified as `fixture TODO / coverage update`, not a parser change proposal. / 上游结果：正式 runbook 检查固定 OpenAI Codex revision `b545c94041017d000e2c8b2f6272705d21b85dfb`。其权威 `protocol.rs` 与 `protocol_v1.md` blob 和轻量盘点的 `406dc92` 完全一致，确认相同的非统一现行类型：MCP startup rows、`web_search_begin`、`exec_command_output_delta`、`terminal_interaction`、`patch_apply_updated`、`image_generation_begin`、`view_image_tool_call`、`dynamic_tool_call_request` / `dynamic_tool_call_response`、当前 approval/request variant、`collab_resume_begin` / `collab_resume_end` 与 `sub_agent_activity`。Hook 记录使用 `run.id` correlation，而 `sub_agent_activity` 使用 `event_id` 与 path identity；泛化 `call_id` 假设仍不成立。每个尚未准入的当前形态均归类为 `fixture TODO / coverage update`，而不是 parser change proposal。
- Risk: a new lifecycle family or alias can remain visible as a Raw Record while missing logical grouping, representative selection, or enrichment semantics. Prefix or suffix inference can create the opposite failure by accidentally admitting a lookalike or conflating wire spelling, Lifecycle Phase, Lifecycle Outcome Status, and representative priority. / 风险：新增生命周期族或 alias 可能作为原始记录可见，却缺少逻辑分组、代表记录选择或 enrichment 语义。前缀或后缀推断还可能造成相反故障：意外准入相似类型，或混淆 wire 拼写、生命周期阶段、生命周期结果状态与代表优先级。
- Refined model: preserve the exact source wire type; map it explicitly to a Tool Lifecycle Family and canonical Lifecycle Phase; keep representative priority independent of outcome; keep Lifecycle Correlation Identity typed and separate from logical admission. Canonical phases are source-neutral positions such as `start`, `progress`, `interaction`, `terminal`, or `single`, not raw suffixes such as `updated`, `output_delta`, `request`, or `completed`. / 细化模型：保留精确来源 wire type；把它显式映射到工具生命周期族与规范生命周期阶段；让代表优先级独立于结果；让带类型的生命周期关联标识与逻辑准入相互分离。规范 phase 是 `start`、`progress`、`interaction`、`terminal` 或 `single` 等来源中立位置，而不是 `updated`、`output_delta`、`request` 或 `completed` 等原始后缀。
- Bounded direction: preserve all currently admitted behavior while extracting exact membership, family, canonical phase, representative rank, call-group admission, and intentional standalone admission into one backend-safe data-only descriptor. Retain source-specific payload extraction, outcome/status interpretation, existing `callId` extraction, typed-identity expansion, Logical Event construction, card/detail rendering, localization, and safety-sensitive allowlists at their current boundaries. Exact semantic admission is distinct from traceability ownership: isolated unknown lookalikes remain Protocol/Raw fallback, while an unregistered same-`callId` row may remain in a grouped event's Raw References but must not influence lifecycle family, outcome, representative, preview, or search semantics. / 有界方向：在保持全部当前已准入行为的同时，把精确 membership、family、规范 phase、代表优先级、调用组准入与有意的独立准入提取到一份仅后端使用、仅含数据的 descriptor。source 专属 payload 提取、outcome/status 解释、现有 `callId` 提取、typed identity 扩展、逻辑事件构造、card/detail 渲染、本地化与安全敏感 allowlist 继续保留在当前边界。精确语义准入不同于 traceability ownership：孤立未知相似类型继续进入 Protocol/Raw fallback；未注册但具有相同 `callId` 的记录可以继续留在分组事件的 Raw References 中，但不得影响 lifecycle family、outcome、代表记录、preview 或 search 语义。
- Delivered schema review: after behavior equivalence was proven, `schema-update-runbook.md` was run against pinned revision `b545c94041017d000e2c8b2f6272705d21b85dfb`. Exact current shapes, compatibility-only admitted variants, typed correlation pressure, impact surfaces, and fixture gaps are recorded in the completed plan and protocol coverage matrix. No behavior-changing or Planning follow-up was justified. / 已交付 schema 审查：证明行为等价后，已针对固定 revision `b545c94041017d000e2c8b2f6272705d21b85dfb` 执行 `schema-update-runbook.md`。当前精确形态、仅兼容的已准入 variant、typed correlation 压力、影响表面与 fixture 缺口均记录在已完成计划和协议覆盖矩阵中。没有证据支持行为变更或 Planning 后续。
- Subsequent local evidence: a later read-only scan of 478 real Codex JSONL files found high-frequency `sub_agent_activity` observations whose typed `event_id` values consistently matched Agent Coordination call IDs somewhere in the corpus, alongside cross-Session replicas without local owners. This does not reopen Tool Lifecycle Family membership; it triggered a separate, exact, Session-local traceability-correlation plan. The same scan found only fully mirrored `view_image_tool_call` rows, which receive regression coverage without semantic admission. / 后续本地证据：随后对 478 个真实 Codex JSONL 文件进行的只读扫描发现高频 `sub_agent_activity` 观察，其带类型的 `event_id` 在语料某处始终能匹配 Agent 协调 call ID，同时也存在没有本地 owner 的跨会话副本。这不会重新打开工具生命周期族 membership；它触发了一份独立、精确、限于同会话的 traceability correlation 计划。同一扫描只发现完全镜像的 `view_image_tool_call` 记录，因此该形态只增加回归覆盖而不获得语义准入。
- Required proof: table-driven coverage for every currently admitted exact type, family, phase, rank, and admission policy; isolated and mixed same-call negative lookalikes; alias and start/progress/terminal transitions; intended representatives; unchanged admitted Logical Event, Raw Reference, Code Mode association, search, metrics, folding, and detail outputs; followed by a pinned runbook report before new types are admitted. / 必需证明：表驱动覆盖每个当前准入的精确类型、family、phase、rank 与 admission policy；覆盖孤立与 mixed same-call 负向相似类型、alias 与 start/progress/terminal 转换、预期代表记录，以及不变的已准入逻辑事件、原始引用、Code Mode 关联、搜索、指标、折叠和 detail 输出；随后在准入新类型前提供固定 revision 的 runbook 报告。
- Delivered result: `src/codex-tool-lifecycle-contract.js` owns immutable descriptors for all 42 currently admitted exact types. Parser generic enrichment, logical family/rank/admission decisions, hook/collaboration membership, Code Mode lifecycle association, package coverage, and tests consume its exact projections where meanings are shared. Prefix/suffix inference no longer owns those facts; source-specific extraction, outcome interpretation, existing `callId` correlation, response-item image handling, rendering, and `web_search_end` remain local. The extraction closes the bounded same-call lookalike leak, and the pinned schema review records current upstream gaps without admitting them. Full Node, browser, generated-asset, and package validation passed. / 已交付结果：`src/codex-tool-lifecycle-contract.js` 负责当前准入的全部 42 个精确 type 的不可变 descriptor。parser 通用 enrichment、logical family/rank/admission 决策、hook/collaboration membership、Code Mode 生命周期关联、package 覆盖与测试会在含义相同时消费其精确投影。前缀/后缀推断不再拥有这些事实；source 专属提取、outcome 解释、现有 `callId` correlation、response-item image 处理、渲染与 `web_search_end` 继续保持局部。本次提取关闭有界的 same-call lookalike 泄漏，固定 schema 审查则记录当前上游缺口而不准入。完整 Node、browser、生成资产与 package 验收已通过。
- Completed plan: `docs/exec-plans/completed/2026-07-30-tool-lifecycle-family-contract.md` / 已完成计划：`docs/exec-plans/completed/2026-07-30-tool-lifecycle-family-contract.md`
- Completed typed-correlation follow-up: `docs/exec-plans/completed/2026-07-30-subagent-activity-correlation.md` / 已完成带类型 correlation 后续：`docs/exec-plans/completed/2026-07-30-subagent-activity-correlation.md`

## 2. Planning semantic facet / 规划语义 facet

- Status: completed 2026-07-31 / 状态：已于 2026-07-31 完成
- Priority: high / 优先级：高
- Scope decision: a Plan Event is a narrow Logical Event category, not every event used as a planning-reading anchor. Every Plan Event has exactly one semantic category: `artifact` for a complete Plan Artifact or `update` for a Plan Update. Goal events, `request_user_input`, compaction, and other planning-context anchors remain independently composed by Folding Strategies and do not enter the Plan metric or plan navigation through this facet. / 范围决策：计划事件是一类边界窄的逻辑事件，并不包含计划阅读中用作锚点的所有事件。每个计划事件恰好具有一个语义类别：完整计划产物使用 `artifact`，计划更新使用 `update`。Goal event、`request_user_input`、compaction 与其他规划上下文锚点继续由折叠策略独立组合，不会通过该 facet 进入 Plan 指标或计划导航。
- Audited normalized admission baseline: the shared facet admits exact canonical field combinations and never derives semantics from a display label. The four normalized forms remain in existing canonical fields rather than being copied into the facet. / 已审计的归一化准入基线：共享 facet 只接受精确的 canonical 字段组合，绝不从展示 label 派生语义。四种归一化形态继续保留在既有 canonical 字段中，不复制进 facet。

  | Normalized form / 归一化形态 | Exact canonical evidence / 精确 canonical 证据 | Plan Event category / 计划事件类别 |
  | --- | --- | --- |
  | Proposed plan artifact / Proposed plan 产物 | `kind === 'proposed_plan'` and `subtype === 'proposed_plan'` | `artifact` |
  | Direct or observed nested `update_plan` / 直接或已观测嵌套 `update_plan` | `kind === 'other_tool_call'`, `toolName === 'update_plan'`, and `subtype === 'update_plan'` | `update` |
  | Protocol plan update / 协议计划更新 | `kind === 'plan_update'` and `subtype === 'plan_update'` | `update` |
  | Protocol plan delta / 协议计划增量 | `kind === 'plan_update'` and `subtype === 'plan_delta'` | `update` |

- Code Mode ownership decision: an independently persisted Observed Nested Activity that satisfies the exact `update_plan` Logical Event combination is a Plan Update under its own event identity. Its owning Code Mode Operation, Declared Nested Calls, and Nested Tool Projections are not Plan Events and add no Plan metric or navigation membership. One operation containing two independently observed `update_plan` Logical Events therefore contributes two Plan Updates, not a parent or projection count. / Code Mode ownership 决策：独立持久化的已观测嵌套活动如果满足精确的 `update_plan` 逻辑事件组合，就凭自身 event identity 成为计划更新。其所属 Code Mode 操作、声明的嵌套调用与嵌套工具投影都不是计划事件，也不会增加 Plan 指标或计划导航 membership。因此，一个 operation 如果包含两条独立观测到的 `update_plan` 逻辑事件，就贡献两条计划更新，而不是父 operation 或 projection 计数。
- Consumer decision: the Plan metric counts every Plan Event exactly once. The compatibility `planArtifacts` count includes only `artifact`. `Plans / updates` navigation contains both categories, while `Plan updates` contains only `update`; a Plan Update belongs to both navigation categories and a Plan Artifact only to the aggregate category. / 消费方决策：Plan 指标对每个计划事件恰好计数一次。兼容性 `planArtifacts` 计数只包含 `artifact`。`计划 / 更新` 导航包含两个类别，`计划更新` 只包含 `update`；计划更新同时属于两个导航类别，计划产物只属于聚合类别。
- Evidence: raw-to-logical recognition lives in `src/codex-logical.js`; provenance-specific detail extraction lives in `src/codex-detail.js`; metrics independently reconstruct Plan membership in `src/codex.js`; and `src/shared/folding.js` supplies a partial update-only predicate that browser navigation already reuses. Proposed-plan membership and the aggregate navigation union remain separately reconstructed. / 证据：raw-to-logical 识别位于 `src/codex-logical.js`；provenance 专属 detail 提取位于 `src/codex-detail.js`；指标在 `src/codex.js` 中独立重建 Plan membership；`src/shared/folding.js` 提供一份浏览器导航已经复用的局部 update-only predicate。Proposed-plan membership 与聚合导航 union 仍被分别重建。
- Risk: a newly supported planning provenance can render correctly while being absent from metrics, folding, or navigation. / 风险：新支持的规划来源可能渲染正确，却没有进入指标、折叠或导航。
- Bounded direction: add a shared post-normalization `planFacetForEvent` projection returning no facet or only `{ category: 'artifact' | 'update' }`, plus derived predicates where useful. It consumes exact canonical Logical Event fields, rejects label-only and conflicting lookalikes, and does not copy normalized form or raw provenance. Raw wire-shape parsing, logical construction, and provenance-specific detail presentation remain independent so the facet does not become a second parser or a layout contract. / 有界方向：新增共享的归一化后 `planFacetForEvent` 投影；它要么不返回 facet，要么只返回 `{ category: 'artifact' | 'update' }`，并在有用时提供派生 predicate。它消费精确的 canonical 逻辑事件字段，拒绝仅靠 label 或字段冲突的 lookalike，也不复制归一化 form 或 raw provenance。原始 wire shape 解析、逻辑构造与 provenance 专属 detail 展示继续保持独立，避免该 facet 成为第二个 parser 或布局契约。
- Required proof: table-driven admission and negative-lookalike coverage for every exact normalized combination; isolated `item_completed Plan` and `<proposed_plan>` message fixtures plus their mirrored single-event collapse; direct and observed nested `update_plan`; protocol `plan_update` and `plan_delta`; declaration/projection/parent Code Mode exclusions; unchanged provenance-specific detail; and explicit assertions for Plan metric, compatibility artifact count, folding, aggregate navigation, and update-only navigation. Raw-to-logical assertions are upstream compatibility proof rather than facet-owned parsing behavior. / 必需证明：以表驱动方式覆盖每个精确归一化组合的准入与负向 lookalike；覆盖独立的 `item_completed Plan`、`<proposed_plan>` message fixture 及其镜像折叠为单一事件；覆盖直接与已观测嵌套 `update_plan`、协议 `plan_update` 与 `plan_delta`、声明/projection/父级 Code Mode 排除、不变的 provenance 专属 detail，以及 Plan 指标、兼容 artifact 计数、折叠、聚合导航和仅更新导航的显式断言。Raw-to-logical 断言属于上游兼容性证明，而不是 facet 自有的解析行为。
- Delivered result: `src/shared/plan-facet.js` now owns immutable `artifact` / `update` facets, exact post-normalization admission, and derived Plan predicates. Session metrics, folding conditions and important-event recognition, aggregate/update-only navigation, browser defaults, runtime packaging, and generated assets consume that contract where meanings are shared. Raw parsing, Logical Event construction, normalized form, Code Mode association, and provenance-specific detail remain local. Exact negative coverage rejects incomplete, label-only, conflicting, and presentation-only lookalikes; isolated and mirrored Plan Artifact fixtures plus Code Mode declaration/projection fixtures preserve the confirmed ownership and counts. A dedicated sanitized raw lifecycle fixture also proves that an observed nested `update_plan` is associated with its parent operation yet keeps the only Plan count and aggregate/update-only navigation membership. Full Node, browser, package, generated-asset, syntax, diff, and restarted-server validation passed. / 已交付结果：`src/shared/plan-facet.js` 现在负责不可变的 `artifact` / `update` facet、精确归一化后准入与派生 Plan predicate。Session 指标、folding condition 与 important-event 识别、聚合/仅更新导航、浏览器默认项、运行时 packaging 和生成资产会在含义相同时消费该契约。Raw 解析、逻辑事件构造、归一化 form、Code Mode association 与 provenance 专属 detail 继续保持局部。精确负向覆盖会拒绝不完整、仅 label、字段冲突与仅 presentation lookalike；独立与镜像 Plan Artifact fixture 以及 Code Mode 声明/projection fixture 保持已确认 ownership 与计数。一份专用的脱敏 raw lifecycle fixture 还证明：已观测嵌套 `update_plan` 会与父 operation 建立关联，但只有该子事件获得 Plan 计数以及聚合/仅更新导航 membership。完整 Node、browser、package、生成资产、语法、diff 与重启服务验收均已通过。
- Completed plan: `docs/exec-plans/completed/2026-07-31-planning-semantic-facet.md` / 已完成计划：`docs/exec-plans/completed/2026-07-31-planning-semantic-facet.md`

## 3. Code Mode detail-presentation DTO vocabulary / Code Mode detail 展示 DTO 词汇表

- Status: completed 2026-07-30 / 状态：已于 2026-07-30 完成
- Priority: medium; completed as a bounded refactor / 优先级：中；已作为有界重构完成
- Scope decision: the next increment covers `presentation.variant`, `presentation.requestEvidence`, and `presentation.resultAssociation`. The stable `declared_source` semantic value may be shared with `presentationFacts.codeModeDeclaredRequests.requestEvidence`, but the timeline fact and detail presentation remain separate DTO shapes with separate admission boundaries. / 范围决策：下一增量覆盖 `presentation.variant`、`presentation.requestEvidence` 与 `presentation.resultAssociation`。稳定语义值 `declared_source` 可以与 `presentationFacts.codeModeDeclaredRequests.requestEvidence` 共享，但 timeline fact 与 detail presentation 仍是具有独立准入边界的不同 DTO 形态。
- Evidence: the declared-call projector currently emits `declared_source` plus `none` or `bounded`; the domain glossary classifies Nested Tool Projection result association as `exact`, `bounded`, or `none`; the detail sanitizer additionally admits values such as `observed_lifecycle`, `exact_identity`, and `bounded_order`; and browser app and section renderers duplicate evidence-to-badge interpretation. An admission list therefore cannot by itself establish the stable vocabulary. / 证据：声明调用 projector 当前生成 `declared_source` 以及 `none` 或 `bounded`；领域词汇表将 Nested Tool Projection 的结果关联分类为 `exact`、`bounded` 或 `none`；detail sanitizer 还额外接受 `observed_lifecycle`、`exact_identity` 和 `bounded_order` 等值；浏览器 app 与 section renderer 则重复解释 evidence 到 badge 的映射。因此，不能仅凭准入列表确定稳定词汇。
- Risk: a newly introduced value can be silently sanitized away or receive an inconsistent badge, title, or variant across detail surfaces. Conversely, mechanically extracting the current sanitizer allowlists can promote a compatibility-only, reserved, or stale value into a permanent public contract. / 风险：新引入的值可能被静默清洗掉，或在不同 detail 表面获得不一致的 badge、标题或 variant；反过来，机械提取当前 sanitizer allowlist 也可能把仅兼容、预留或陈旧的值提升为永久公开契约。
- Audited baseline: every value that was produced, admitted, documented, or rendered before extraction now has one lifecycle classification. / 审计基线：提取前所有会被生成、准入、记录或渲染的值现在都具有唯一生命周期分类。

  | Dimension / 维度 | Value / 值 | Classification / 分类 | Rationale / 理由 |
  | --- | --- | --- | --- |
  | `presentation.variant` | `single_tool` | `producer-supported` | Produced for exactly one safe declared request and consumed by detail plus browser presentation. / 为恰好一个安全声明 request 生成，并由 detail 与浏览器展示消费。 |
  | `presentation.variant` | `multi_tool` | `producer-supported` | Produced for two or more safe declared requests. / 为两个及以上安全声明 request 生成。 |
  | `presentation.variant` | `raw_code_mode` | `producer-supported` | Produced as the fail-closed fallback for unsupported programs. / 作为不受支持程序的保守失败回退生成。 |
  | `requestEvidence` | `declared_source` | `producer-supported` | Produced by the bounded declared-call projector; the same semantic value is also used by the separate name-only timeline fact. / 由有界声明调用 projector 生成；独立的仅名称 timeline fact 也使用同一语义值。 |
  | `requestEvidence` | `observed_lifecycle` | `rejected` | No producer or persisted compatibility need exists, and treating Observed Nested Activity as projection evidence would blur its independent canonical ownership. / 不存在 producer 或持久化兼容需求；把 Observed Nested Activity 当作投影证据会模糊其独立 canonical ownership。 |
  | `resultAssociation` | `bounded` | `producer-supported` | Produced only for supported static one-to-one output order. / 只为受支持的静态一一对应输出顺序生成。 |
  | `resultAssociation` | `none` | `producer-supported` | Produced when no supported result association is established. / 未建立受支持结果关联时生成。 |
  | `resultAssociation` | `exact` | `compatibility/reserved` | The domain model reserves it for a future persisted exact identity edge; it remains admitted and has an intentional exact-match badge, but no current producer fabricates it. / 领域模型将其预留给未来持久化的精确 identity 边；它继续被准入并具有有意的精确匹配 badge，但当前 producer 不会伪造它。 |
  | `resultAssociation` | `exact_identity` | `rejected` | Undocumented alias with no producer or persisted compatibility requirement. / 没有文档定义、producer 或持久化兼容要求的 alias。 |
  | `resultAssociation` | `bounded_order` | `rejected` | Undocumented alias superseded by canonical `bounded`, with no producer or persisted compatibility requirement. / 已由 canonical `bounded` 取代，且没有 producer 或持久化兼容要求的未记录 alias。 |

- Implemented direction: `src/shared/code-mode-presentation-contract.js` now owns the stable values, strict normalization, the detail sanitizer's vocabulary projection, and semantic badge identity. Declared-call producers, the name-only timeline request fact, detail construction, the detail sanitizer, browser app, and section renderer consume that contract where meanings are identical. Renderer markup and layout remain local. / 已实施方向：`src/shared/code-mode-presentation-contract.js` 现在负责稳定值、严格规范化、detail sanitizer 的词汇投影以及语义 badge identity。声明调用 producer、仅名称 timeline request fact、detail 构造、detail sanitizer、浏览器 app 与 section renderer 会在含义相同时消费该契约；renderer markup 与布局仍保持局部。
- Explicit exclusions: this increment does not consolidate canonical `kind: code_mode_operation`, the synthetic `code_mode_script_operation` query facet, or `presentationContext.relation: enclosed_by_code_mode_operation`. Those contracts retain their existing canonical, query, and navigation ownership and must not be folded into one central Code Mode registry. / 明确排除：本增量不收敛 canonical `kind: code_mode_operation`、合成查询 facet `code_mode_script_operation` 或 `presentationContext.relation: enclosed_by_code_mode_operation`。这些契约继续保留现有的 canonical、查询与导航 ownership，不得被折叠进一个中央 Code Mode registry。
- Expansion trigger: revisit a multi-family contract model only when a second canonical Code Mode kind or presentation-context relation is introduced, a new evidence provenance must span timeline and detail, or another consumer begins independently interpreting one of those contracts. If triggered, define separate canonical-identity, timeline-fact, detail-presentation, and presentation-context families rather than one catch-all vocabulary. / 扩展触发条件：仅当引入第二种 canonical Code Mode kind 或 presentation-context relation、新 evidence provenance 必须横跨 timeline 与 detail，或新增消费方开始独立解释其中某项契约时，才重新评估多 family 契约模型。触发后应分别定义 canonical identity、timeline fact、detail presentation 与 presentation context family，而不是建立一个包罗万象的词汇表。
- Delivered proof: table-driven contract coverage verifies producer-supported, reserved, and rejected values; producer tests verify `declared_source`, `bounded`, and `none`; sanitizer coverage retains `exact` while dropping rejected aliases; renderer coverage verifies shared badge eligibility and stable omission; and cross-layer coverage proves that sharing `declared_source` does not merge timeline and detail DTO ownership. / 已交付证明：表驱动契约覆盖验证 producer-supported、reserved 与 rejected 值；producer 测试验证 `declared_source`、`bounded` 与 `none`；sanitizer 覆盖保留 `exact` 并丢弃 rejected alias；renderer 覆盖验证共享 badge 资格与稳定省略；跨层覆盖证明共享 `declared_source` 不会合并 timeline 与 detail DTO ownership。

## 4. Structured detail-section type contract / 结构化 detail section 类型契约

- Priority: medium; defer until a new section type is needed / 优先级：中；推迟到确有新 section 类型需求时
- Evidence: section-type admission and dropping occur in `src/codex.js`; section construction occurs in `src/codex-detail.js`; browser rendering and inspector supplementation are handled separately in browser code. / 证据：section 类型的准入与丢弃发生在 `src/codex.js`；section 构建发生在 `src/codex-detail.js`；浏览器渲染与 inspector 补充则由浏览器代码分别处理。
- Risk: a new allowed section can be silently removed, fall back inconsistently, or duplicate a timeline summary in the inspector. / 风险：新增的允许 section 可能被静默移除、发生不一致回退，或在 inspector 中重复时间线摘要。
- Bounded direction: when a new type is introduced, add a section descriptor with `allowed`, `inspectorSupplement`, and `ownsTimelinePreview`, and derive backend admission plus browser fallback behavior from it. / 有界方向：引入新类型时，增加含 `allowed`、`inspectorSupplement` 和 `ownsTimelinePreview` 的 section descriptor，并从中派生后端准入及浏览器回退行为。
- Required proof: every allowed type has either a dedicated renderer or an intentional fallback, and tests establish timeline-preview ownership and inspector supplementation. / 必需证明：每个允许类型都具有专用 renderer 或有意的回退，且测试明确时间线预览 ownership 与 inspector 补充。

## Sequencing / 排序

1. Item 1 is complete. Keep current upstream-only lifecycle shapes as fixture TODOs and open a separate implementation plan only when focused evidence establishes user-facing semantics. / 事项 1 已完成。当前仅由上游得知的 lifecycle 形态继续作为 fixture TODO；只有聚焦证据明确建立用户可见语义时，才另开实现计划。
2. Item 3 is complete. Keep its bounded shared contract separate from canonical kind and presentation context, and revisit the multi-family expansion only when one of its recorded triggers occurs. / 事项 3 已完成。继续让其有界共享契约与 canonical kind、presentation context 保持分离，并且只在已记录触发条件出现时重新评估多 family 扩展。
3. Item 2 is complete. Keep the shared facet limited to exact post-normalization Plan Event category and preserve separate planning-reading anchors, Code Mode declaration facts, raw parsing, normalized form, and detail presentation. / 事项 2 已完成。继续把共享 facet 限定为精确归一化后的 Plan Event 类别，并保持计划阅读锚点、Code Mode 声明事实、raw 解析、归一化 form 与 detail 展示相互独立。
4. Keep item 4 deferred until an actual new structured section type establishes its required semantics. / 事项 4 保持推迟，直到实际新增结构化 section 类型并明确所需语义。

## Related documents / 相关文档

- `CONTEXT.md`
- `docs/exec-plans/tech-debt-tracker.md`
- `docs/design-docs/code-mode-operations.md`
- `docs/design-docs/code-mode-structured-display-catalog.md`
- `docs/design-docs/logical-event-timeline.md`
- `docs/design-docs/codex-protocol-event-coverage.md`
- `docs/design-docs/schema-update-runbook.md`
- `docs/exec-plans/completed/2026-07-30-tool-lifecycle-family-contract.md`
