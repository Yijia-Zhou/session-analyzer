# v0.2.0 release plan / v0.2.0 发布计划

## Objective / 目标

Freeze the accepted feature set at `e9b7a1923c12f18d2f283dacb998bc51a11cfa66` as
`session-analyzer@0.2.0`, converge release metadata and the matching consumer
documentation, run the unprivileged release gates, and hand off at the
maintainer-owned publication boundaries defined by
[`docs/design-docs/npm-release-runbook.md`](../../design-docs/npm-release-runbook.md).
No new feature work is in scope. / 将已接受的功能冻结基线
`e9b7a1923c12f18d2f283dacb998bc51a11cfa66` 冻结为
`session-analyzer@0.2.0`，收口发布 metadata 与匹配的消费端文档，运行无特权
release gate，并在[发布运行手册](../../design-docs/npm-release-runbook.md)规定的
维护者所有发布边界前交接。不新增功能。

This plan is the durable public version-specific record. The runbook is the
authoritative publication procedure; this plan does not copy or weaken its
publication commands. / 本计划是长期公开的版本专属记录。发布运行手册是权威发布
流程；本计划不复制或削弱其中的发布命令。

## Status and identity / 状态与身份

- Owner: repository maintainers / 负责人：仓库维护者
- Status: completed; publication, public verification, tag, Release, and post-publication docs closeout complete / 状态：completed；发布、公共验证、tag、Release 与发布后文档收口均已完成
- Started: 2026-09-09 / 开始日期：2026-09-09
- Package: `session-analyzer`
- Target version: `0.2.0`
- Git tag: annotated `v0.2.0`, targeting `c5bc304898ddccdf2cb4700a2e571e5079d342bb`
- Source branch: `towards-0.2.0`
- Accepted feature-freeze baseline: `e9b7a1923c12f18d2f283dacb998bc51a11cfa66`
- Baseline remote: `origin/towards-0.2.0` at the same SHA
- Release-preparation commit: `4b14bc0ab364e28822f60937bfabecefff0f694f`
- Final evidence commit: `581d6f986f0674e23bb9feebaed8c9786f0a0ace`
- Intended release commit: `c5bc304898ddccdf2cb4700a2e571e5079d342bb` on `main`
- Required CI and merge record: PR [#23](https://github.com/Yijia-Zhou/session-analyzer/pull/23); merged commit `c5bc304898ddccdf2cb4700a2e571e5079d342bb`
- Trusted Publishing stage run: [34360332640](https://github.com/Yijia-Zhou/session-analyzer/actions/runs/34360332640)
- Public verification run: [34364548202](https://github.com/Yijia-Zhou/session-analyzer/actions/runs/34364548202)
- GitHub Release: `https://github.com/Yijia-Zhou/session-analyzer/releases/tag/v0.2.0`
- Related runbook: `docs/design-docs/npm-release-runbook.md`
- Related product spec: `docs/product-specs/session-transcript-analyzer.md`
- Maintainer-local appendix, when needed: `tmp/release-evidence/v0.2.0.md`

The working branch is not `origin/main`; the baseline must not be presented as
the final exact-main release source. / 当前工作分支不是 `origin/main`；不得把该基线
冒充最终 exact-main release source。

## Frozen scope and non-goals / 冻结范围与非目标

- Included: the accepted baseline's Codex, Claude Code, DeepSeek Harness,
  Timeline/Trajectory, source selection, materialization, package-reading,
  diagnostics, documentation, and generated assets.
- Included release preparation: package/lock identity, bilingual changelog,
  package metadata assertions, README and online-guide convergence, release
  evidence, and only release-blocking validation corrections.
- Excluded: new product behavior, performance experiments, architecture
  redesign, unrelated cleanup, and non-blocking technical debt. Existing debt
  remains open unless the runbook requires a release-specific record.
- No real transcripts, credentials, tokens, OTPs, recovery codes, or personal
  absolute paths may enter the plan, package, or public evidence.

## Toolchain and registry contract / 工具链与 registry 契约

- Node.js release line: `^22.22.2 || ^24.15.0`; local validation target:
  Node.js `24.18.1`.
- npm: exactly `12.0.2`.
- Registry for every npm invocation in this execution: explicitly pass
  `--registry='https://registry.npmjs.org/'`. Do not rely on user config or a
  mirror, and do not mutate the user's normal npm configuration.
- `strict-allow-scripts=true`; never use `--dangerously-allow-all-scripts`.
- `publishConfig.registry` must remain
  `https://registry.npmjs.org/`, with public access.
- Supported CI/runtime matrix: Linux Node.js 22, Linux Node.js 24, Windows
  Node.js 24, Linux and Windows package smoke, and Ubuntu browser coverage.

The preliminary official-registry slot check for `session-analyzer@0.2.0`
returned explicit `E404` on 2026-09-09. This is not a reservation; the
committed `release:preflight` must repeat the check immediately before any
authorized workflow dispatch. / 2026-09-09 对官方 registry 的
`session-analyzer@0.2.0` 初步版本槽位检查得到明确 `E404`。这不构成预留；在任何
获授权 workflow dispatch 前，已提交的 `release:preflight` 必须立即重查。

## Release phases / 发布阶段

### Phase 1 — Metadata freeze / 阶段 1：metadata freeze

- [x] Create this active plan from the accepted baseline.
- [x] Update `package.json`, both `package-lock.json` identity locations, and
  package metadata version assertions to `0.2.0`.
- [x] Close the bilingual `Unreleased` changelog into the dated `0.2.0`
  entry while retaining an empty bilingual `Unreleased` section.
- [x] Converge both READMEs and the online usage/development guides on the
  intended 0.2.0 capabilities; retain a clear pre-publication warning until
  the release is publicly installable, and pin release links to `v0.2.0`.
- [x] Update only relevant current product/design version boundaries; do not
  rewrite historical completed plans or close unrelated debt.
- [x] Recheck package allowlist, third-party notices, dependency placement,
  `publishConfig`, `devEngines`, and lockfile install-script coverage.
- [x] Record the frozen metadata diff and confirm no production feature edit.

### Phase 2 — Candidate gates / 阶段 2：候选 gate

- [x] Bootstrap/verify the exact toolchain from outside the checkout, using the
  explicit official registry on every npm command.
- [x] Run clean strict install, pending install-script check, `release:check`,
  browser tests, both audits, whitespace check, and final clean-tree check.
- [x] Generate and inspect a candidate outside the worktree; verify manifest,
  allowlist, notices, sensitive paths, hashes, installed CLI, and packaged
  server behavior. Do not pass the inspection tarball to publish.
- [x] Run the guarded directory `npm publish --dry-run` with no positional
  package/tarball argument and observe `prepublishOnly`/`release:check`.

### Phase 3 — Exact `main` source / 阶段 3：精确 `main` 来源

- [x] Use the normal review path to get the frozen change onto `main`; PR
  [#23](https://github.com/Yijia-Zhou/session-analyzer/pull/23) merged at
  `c5bc304898ddccdf2cb4700a2e571e5079d342bb`.
- [x] Record the pushed exact `main` SHA and required CI result; all required
  merge checks completed successfully.
- [x] Stop if local source, pushed `main`, CI, metadata, registry, or manifest
  differs from this approved plan.

### Phase 4 — Trusted Publishing handoff / 阶段 4：Trusted Publishing 交接

- [x] Run `npm run release:preflight -- '0.2.0'` with the explicit official
  registry configuration and require the sole GO result.
- [x] Stop before dispatch as required; the maintainer dispatched the workflow,
  approved the protected Environment/staged artifact, and completed WebAuthn
  approval through the Trusted Publishing path.
- [x] Do not use the manual fallback unless staged publishing is unavailable,
  the reason is recorded here, and the maintainer explicitly authorizes it.

### Phase 5 — Public verification and closeout / 阶段 5：公共验证与收尾

- [x] After maintainer approval, require the read-only Ubuntu/Windows public
  verification evidence before any tag or GitHub Release. Windows passed; the
  Ubuntu job failed only in the POSIX temporary-prefix harness tail.
- [x] Record the Linux substitute evidence for explicit maintainer exception
  acceptance: the exact
  public version, source SHA, candidate SHA-256, anonymous `ENEEDAUTH`,
  global install, global CLI help, and packaged server `/` plus `/api/state`
  all passed in a disposable Linux Codespace. The original Ubuntu job remains
  recorded as failed, not passed; the maintainer accepted this exception.
- 2026-09-10 — The maintainer created annotated `v0.2.0` targeting
  `c5bc304898ddccdf2cb4700a2e571e5079d342bb` and published the
  non-draft, non-prerelease GitHub Release. / 2026-09-10 — 维护者创建了精确
  指向 `c5bc304898ddccdf2cb4700a2e571e5079d342bb` 的 annotated
  `v0.2.0`，并发布了非 draft、非 prerelease 的 GitHub Release。
- 2026-09-10 — After publication, both bilingual README branch warnings were
  removed; package commands and immutable `v0.2.0` links remain aligned.
  / 2026-09-10 — 发布后已移除双语 README 的分支 warning；package
  命令与不可变 `v0.2.0` 链接保持一致。
- [x] The maintainer created annotated `v0.2.0` at the exact release
  source and published the GitHub Release; tag and Release identities agree.
- [x] Archive this plan only after all runbook-required publication, public
  verification, tag, and GitHub Release evidence is complete.

## Mandatory STOP conditions / 强制 STOP 条件

Stop immediately and report without improvising a workaround when any of the
following occurs: / 遇到以下任一情况立即停止并报告，不得自行发明 workaround：

- Any package, lockfile, changelog, metadata assertion, intended tag, plan,
  source SHA, or manifest disagreement.
- Any npm command is not explicitly pinned to
  `https://registry.npmjs.org/`, or the response is not an explicit expected
  result (including an ambiguous/network version-slot response).
- Dirty worktree at a required clean boundary, missing/failed/incomplete CI,
  unsupported Node/npm, pending install scripts, failed audit, stale generated
  asset, or failed package/browser smoke.
- A candidate contains an unapproved file, source map, fixture, plan,
  credential, transcript, log, personal path, or incomplete third-party notice.
- A release workflow, source, dependency, generated asset, or metadata changes
  after a recorded gate without returning to the invalidated phase.
- Any request to fix non-blocking technical debt or add product scope during
  this freeze.
- Any maintainer-owned action: workflow dispatch, Environment approval, staged
  approve/reject, npm authentication or registry mutation outside the exact
  authorized workflow, dist-tag mutation, remote tag, or GitHub Release.

## Progress log / 进度日志

- 2026-09-09 — Confirmed baseline
  `e9b7a1923c12f18d2f283dacb998bc51a11cfa66`; `HEAD` and
  `origin/towards-0.2.0` match; worktree was clean. `origin/main` remains a
  different older commit, so exact-main release identity is pending normal
  review.
- 2026-09-09 — Initial registry check encountered the user's npm mirror and
  stopped per the runbook. Resumed only after explicit instruction to pass the
  official registry manually on every npm invocation.
- 2026-09-09 — Explicit official-registry query for
  `session-analyzer@0.2.0` returned `E404`; no registry write occurred.
- 2026-09-09 — Metadata freeze completed. Package/lock identity, bilingual
  changelog, README/guide release paths, current product/design version
  boundaries, and package metadata assertions agree on `0.2.0`; no production
  feature path or non-blocking technical debt was changed. Release-link
  contract assertions were updated to require the intended immutable `v0.2.0`
  URLs.
- 2026-09-09 — External toolchain evidence: Node.js `v24.18.1`, npm
  `12.0.2`, explicit registry `https://registry.npmjs.org/`, strict mode
  `true`, and official-registry `PONG`.
- 2026-09-09 — Candidate gates on the frozen content: strict install passed;
  `npm install-scripts ls --json` reported no pending entries;
  `release:check` passed generated-currentness, Node `1070/1070`, and all
  three installed-package source smokes; browser passed `228/228`; production
  and full audits each reported zero vulnerabilities; `git diff --check`
  passed.
- 2026-09-09 — Outside-worktree candidate inspection passed with 75 entries,
  exact manifest/extract parity, no forbidden or sensitive paths, complete
  Highlight.js third-party notice, and byte identity for packed repository
  files. The inspection candidate SHA-256 was
  `48bd47222d347a22dfa08a270f22f12663a441e412b3415981a5ac27cced2b71`.
- 2026-09-09 — Frozen metadata and gate evidence were committed as
  `4b14bc0ab364e28822f60937bfabecefff0f694f`; the worktree is clean. This is
  a preparation commit only. Exact `main` source, CI, and final guarded
  directory dry-run remain pending.
- 2026-09-09 — Guarded directory dry-run on the clean preparation content
  passed with the official registry and no positional package/tarball
  argument. `prepublishOnly` and `release:check` were observed; generated
  assets were current, Node `1070/1070` passed, all three package smokes
  passed, npm identity was `session-analyzer@0.2.0`, and npm reported 75 files
  with the recorded SHA-1 and integrity. A subsequent identity readback also
  confirmed package/lock version `0.2.0`, zero worktree paths, and no `.tgz`.
- 2026-09-09 — Final evidence commit `581d6f986f0674e23bb9feebaed8c9786f0a0ace`
  remained clean; the no-change final dry-run reconfirmed the same guarded
  lifecycle, package identity, 75-file manifest, and zero leftover tarballs.
- 2026-09-09 — PR [#23](https://github.com/Yijia-Zhou/session-analyzer/pull/23)
  merged the frozen release source into `main` at
  `c5bc304898ddccdf2cb4700a2e571e5079d342bb`; required CI completed
  successfully. / 2026-09-09 — PR #23 将冻结 release source 合入 `main`，
  required CI 全部成功。
- 2026-09-09 — `release:preflight` returned the sole GO decision; the
  maintainer-owned Trusted Publishing stage, staged review, and WebAuthn
  approval completed successfully. / 2026-09-09 — `release:preflight`
  返回唯一 GO；维护者所有的 Trusted Publishing staging、staged review 与
  WebAuthn approval 均成功完成。
- 2026-09-09 — Public verification run
  [34364548202](https://github.com/Yijia-Zhou/session-analyzer/actions/runs/34364548202)
  passed on Windows and failed on Ubuntu only at the POSIX temporary-prefix
  global-install tail; the package identity/signature/provenance checks had
  completed before that failure. / 2026-09-09 — 公共验证 run 在 Windows 通过，
  Ubuntu 仅在 POSIX 临时 prefix 的 global-install 尾段失败；此前 package
  identity、signature 与 provenance 检查已完成。
- 2026-09-09 — A disposable Linux Codespace substitute supplied the evidence
  for the public-smoke exception: it verified Node.js `v24.20.0`,
  npm `12.0.2`, anonymous `ENEEDAUTH`, the exact public
  package/source/hash, global install, global CLI help, and packaged server
  root plus `/api/state`. The original Ubuntu job remains failed and
  is not relabeled as passed; the maintainer accepted the exception.
  / 2026-09-09 — 一次性 Linux Codespace substitute 提供了 public-smoke
  例外证据：验证了 Node.js `v24.20.0`、
  npm `12.0.2`、匿名 `ENEEDAUTH`、精确公共 package/source/hash、
  global install、global CLI help 与 packaged server 根页及 `/api/state`。
  原 Ubuntu job 仍记录为失败，不改标为通过；维护者已接受该例外。

## Public evidence record / 公开证据记录

To be completed only with sanitized, durable outcomes; raw output and
account/machine-specific details belong only in the optional ignored appendix. /
仅记录脱敏且长期有效的结论；原始输出及账户／机器特有信息只放入可选的 Git 忽略
附录。

```text
Release identity:
- Package: session-analyzer
- Version: 0.2.0
- Release commit: `c5bc304898ddccdf2cb4700a2e571e5079d342bb`
- Source branch: `main`
- Git tag: annotated `v0.2.0` targeting `c5bc304898ddccdf2cb4700a2e571e5079d342bb`
- GitHub Release: published at https://github.com/Yijia-Zhou/session-analyzer/releases/tag/v0.2.0

Toolchain:
- Node.js: workflow Node 24; substitute Linux Node.js `v24.20.0`
- npm: 12.0.2
- Registry: https://registry.npmjs.org/
- strict-allow-scripts: true
- Publication path: staged OIDC

Repository state:
- git status: clean at release commit `c5bc304898ddccdf2cb4700a2e571e5079d342bb`; no release tarball remains
- package.json version: `0.2.0`
- package-lock.json version: `0.2.0`
- changelog entry: dated `0.2.0` entry with empty bilingual `Unreleased` section
- publishConfig: public access, official npm registry
- devEngines: release toolchain contract retained
- allowScripts: reviewed; no pending install scripts

CI and local gates:
- Required CI: [run 34358763304](https://github.com/Yijia-Zhou/session-analyzer/actions/runs/34358763304)
  for PR #23; all merge checks passed on `c5bc304898ddccdf2cb4700a2e571e5079d342bb`
- npm ci --strict-allow-scripts: passed
- pending install scripts: none (`allowScripts: []`)
- release:check: passed; Node `1070/1070`, generated assets current, Codex/Claude/DeepSeek smoke passed
- browser tests: passed `228/228`
- production audit: passed; zero vulnerabilities
- full audit: passed; zero vulnerabilities
- git diff --check: passed
- final guarded publish dry-run: passed; directory root, no positional argument, official registry
- prepublishOnly observed: yes; `release:check` observed and passed

Inspection candidate:
- Entry count: `75`
- SHA-256: `48bd47222d347a22dfa08a270f22f12663a441e412b3415981a5ac27cced2b71`
- Allowlist result: passed; manifest and extracted files match
- Third-party notices result: passed; complete Highlight.js notice present
- Sensitive-path result: passed; no forbidden or sensitive paths
- Installed CLI/server result: passed for Codex, Claude Code, and DeepSeek Harness; Timeline → Detail → Raw readback passed

Publication and public verification:
- Trusted Publisher/Environment review: passed; stage-only OIDC path
- publish.yml source SHA and run URL: `c5bc304898ddccdf2cb4700a2e571e5079d342bb`;
  [34360332640](https://github.com/Yijia-Zhou/session-analyzer/actions/runs/34360332640)
- Verify-job candidate SHA-256: `3754f694f1d32743d6c8eaa71e88e73200abc4f9e12d102799ea0f4eee43385b`
- Stage-job reproduced SHA-256: `3754f694f1d32743d6c8eaa71e88e73200abc4f9e12d102799ea0f4eee43385b`
- Staged identity/manifest/provenance/source review: GO; trusted automation,
  `0.2.0`, `75` entries, `latest`, source and hash matched;
  pre-approval provenance visibility limit recorded
- Downloaded staged tarball SHA-1: `dc544f7a796f6e49f4ecc87ab8c7305c3872b959`
- Downloaded staged tarball SHA-256: `3754f694f1d32743d6c8eaa71e88e73200abc4f9e12d102799ea0f4eee43385b`
- Maintainer approval result: completed with phishing-resistant WebAuthn; factor/account details not recorded
- Anonymous verification: `npm whoami` returned `ENEEDAUTH`
- Exact-version registry metadata: `0.2.0` public; `latest=0.2.0`;
  repository metadata matched
- Windows public smoke: passed; signatures, provenance, npx, global install,
  CLI, root and `/api/state` passed
- Linux public smoke: original Ubuntu job failed at the POSIX temporary-prefix
  global-install tail; accepted Codespace substitute passed the missing global
  install, CLI and packaged server checks
- Public tarball SHA-256: `3754f694f1d32743d6c8eaa71e88e73200abc4f9e12d102799ea0f4eee43385b`
- Final dist-tags: `latest=0.2.0`; `next=0.1.2`

Exceptions and recovery actions:
- Ubuntu public-verification harness exception: the committed verifier failed
  while initializing a disposable POSIX global prefix because `lib` was
  absent; no package or release identity change was made. A Linux Codespace
  substitute supplied concrete evidence for the missing tail; the maintainer
  accepted the exception. The original failed job remains failed. / Ubuntu
  公共验证 harness 例外：提交的
  verifier 在一次性 POSIX global prefix 初始化时因缺少 `lib` 失败；
  未改变 package 或 release identity。Linux Codespace substitute 补齐了缺失
  尾段证据；维护者已接受该例外，原失败 job 仍保持失败记录。

Maintainer-local appendix:
- Relative path: tmp/release-evidence/v0.2.0.md
- Completion status: optional raw-output appendix; public plan above is the
  sanitized record and contains no credential or personal path
```
