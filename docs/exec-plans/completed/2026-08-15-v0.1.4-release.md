# v0.1.4 freeze and release / v0.1.4 冻结与发布

## Objective / 目标

Freeze the implemented v0.1.4 increment as `session-analyzer@0.1.4`, take it through the normal pull-request and CI path, and release the exact verified `main` commit through the repository's staged OIDC workflow. The increment includes runtime Codex/Claude Code source switching, source-neutral runtime ownership cleanup, compact Codex resident indexing with source-backed detail hydration and freshness protection, and the associated capacity/reliability documentation. Complete staged-artifact review, maintainer approval, anonymous public verification, and only then create `v0.1.4` and the GitHub Release. / 将已经实现的 v0.1.4 增量冻结为 `session-analyzer@0.1.4`，通过正常 pull request 与 CI 流程，并通过仓库的 staged OIDC workflow 发布精确验证过的 `main` commit。本增量包括 Codex／Claude Code 运行期来源切换、来源中立运行时 ownership 清理、带来源支撑详情 hydration 与 freshness 保护的 Codex 紧凑常驻索引，以及配套的容量／可靠性文档。完成 staged 制品审查、维护者审批、匿名公共验证后，才创建 `v0.1.4` 与 GitHub Release。

This plan is the durable public version-specific release record. It references `docs/design-docs/npm-release-runbook.md` as the authoritative procedure and does not redefine or weaken that runbook. Raw command output, account-side readback, machine-specific details, and transient diagnostics belong only in the Git-ignored maintainer-local appendix. / 本计划是长期公开的版本专属发布记录。权威流程以 `docs/design-docs/npm-release-runbook.md` 为准；本计划不重新定义或削弱该运行手册。原始命令输出、账户侧 readback、本机特有信息与临时诊断只进入 Git 忽略的维护者本地附录。

## Status and ownership / 状态与负责人

- Owner: repository maintainers / 负责人：仓库维护者
- Status: completed; released on 2026-08-15 after exact-main gates, staged OIDC publication, maintainer/2FA approval, anonymous public verification, the annotated tag, and the GitHub Release / 状态：已完成；2026-08-15 在 exact-main 门禁、staged OIDC 发布、维护者／2FA 审批、匿名公共验证、annotated tag 与 GitHub Release 完成后发布
- Started: 2026-08-15 / 开始日期：2026-08-15
- Actual release date: 2026-08-15 / 实际发布日期：2026-08-15
- Package and version: `session-analyzer@0.1.4`
- Source branch: `v0.1.4-development`
- Pre-freeze source baseline: `v0.1.4-development@084eb64` / 冻结前来源基线：`v0.1.4-development@084eb64`
- Released main commit: `main@83d7b682f078cf09a86eb1b29b81960a89d332ca` / 已发布 main commit：`main@83d7b682f078cf09a86eb1b29b81960a89d332ca`
- Published tag: annotated `v0.1.4`; tag object `faa95626e902bf86c30333138c2e1592087c1dde` / 已发布 tag：annotated `v0.1.4`；tag object 为 `faa95626e902bf86c30333138c2e1592087c1dde`
- GitHub Release: published at `https://github.com/Yijia-Zhou/session-analyzer/releases/tag/v0.1.4` / GitHub Release：已发布于 `https://github.com/Yijia-Zhou/session-analyzer/releases/tag/v0.1.4`
- Release toolchain: Node.js 24.18.1 locally and exact npm `12.0.2`; CI uses Node.js 22/24 as specified below / 发布工具链：本地 Node.js 24.18.1 与精确 npm `12.0.2`；CI 使用下述 Node.js 22/24 矩阵
- Installed runtime support: `package.json#engines.node` is `>=22` / 安装后运行时支持：`package.json#engines.node` 为 `>=22`
- Source and CI runtime support: `package.json#devEngines.runtime.version` is `^22.22.2 || ^24.15.0` / 源码与 CI 运行时支持：`package.json#devEngines.runtime.version` 为 `^22.22.2 || ^24.15.0`
- Supported CI matrix: Linux Node.js 22, Linux Node.js 24, Windows Node.js 24; package smoke on Linux and Windows; browser on Ubuntu / 支持的 CI 矩阵：Linux Node.js 22、Linux Node.js 24、Windows Node.js 24；Linux 与 Windows package smoke；Ubuntu browser
- Publication path: staged OIDC through `.github/workflows/publish.yml`; manual fallback requires the runbook's explicit outage record and maintainer authorization / 发布路径：通过 `.github/workflows/publish.yml` 执行 staged OIDC；手动 fallback 必须有运行手册要求的明确故障记录与维护者授权
- Pre-staging public-registry check on 2026-08-15 recorded `0.1.2` and `0.1.3` as public and `0.1.4` as unused; the post-approval public verification later confirmed `0.1.4` and final `latest=0.1.4`. / 2026-08-15 staging 前公共 registry 检查记录 `0.1.2` 与 `0.1.3` 已公开、`0.1.4` 尚未使用；审批后的公共验证随后确认 `0.1.4` 已发布且最终 `latest=0.1.4`。
- Maintainer-local evidence appendix: `tmp/release-evidence/v0.1.4.md` (Git ignored; supporting material only) / 维护者本地证据附录：`tmp/release-evidence/v0.1.4.md`（Git 忽略；仅作为支撑材料）
- Related runbook: `docs/design-docs/npm-release-runbook.md`
- Related product spec: `docs/product-specs/session-transcript-analyzer.md`
- Related source design: `docs/design-docs/transcript-source-adapters.md`
- Related memory/reliability plans: `docs/exec-plans/completed/2026-08-12-index-memory-and-runtime-reliability.md`, `docs/exec-plans/completed/2026-08-13-memory-runtime-reliability-review-followup.md`
- Related feature plans: `docs/exec-plans/completed/2026-08-09-codex-materialized-fork-ownership.md`, `docs/exec-plans/completed/2026-08-10-claude-compatibility-hardening.md`, `docs/exec-plans/completed/2026-08-14-source-neutral-runtime-cleanup.md` / 相关功能计划：`docs/exec-plans/completed/2026-08-09-codex-materialized-fork-ownership.md`、`docs/exec-plans/completed/2026-08-10-claude-compatibility-hardening.md`、`docs/exec-plans/completed/2026-08-14-source-neutral-runtime-cleanup.md`

## Frozen release decisions / 已冻结的发布决策

1. `0.1.4` is now in feature freeze. Only release metadata, documentation convergence, package-contract tests, validation fixes, and evidence recording remain in scope; no new user-facing feature is added during release preparation. / `0.1.4` 现在进入功能冻结。发布准备期间只允许修改 release metadata、文档收敛、package-contract 测试、验证修复与证据记录；不再增加面向用户的新功能。
2. Package identity must converge on `0.1.4`: `package.json`, both lockfile identity locations, the bilingual dated changelog entry, package metadata version assertions, the published tag, and the published GitHub Release must agree. / Package identity 必须收敛到 `0.1.4`：`package.json`、lockfile 两个 identity 位置、双语带日期 changelog entry、package metadata 版本断言、已发布 tag 与已发布 GitHub Release 必须保持一致。
3. The source branch is reviewed through a normal pull request. Do not modify or merge local `main` directly. / 来源分支通过正常 pull request 接受审查。不得直接修改或合并本地 `main`。
4. The exact release source is determined only after the pull request is merged, the resulting `main` commit is pushed, required CI is green, and a clean checkout identifies that exact SHA. / 只有 release pull request 合入、生成的 `main` commit 已 push、必要 CI 全绿且干净 checkout 确认精确 SHA 后，才能确定 release source。
5. The staged OIDC path is mandatory unless this plan records a concrete staged-publishing outage and the maintainer explicitly authorizes the runbook's manual fallback. No local tarball may be published directly. / 除非本计划记录具体的 staged-publishing 故障且维护者明确授权运行手册中的手动 fallback，否则必须使用 staged OIDC 路径。不得直接发布本地 tarball。
6. `.github/workflows/publish.yml` may be manually dispatched from `main` only with exact input `0.1.4`, after a fresh official-registry availability check and explicit maintainer authorization. The staged artifact uses immutable intended tag `latest`. / 只有在重新检查官方 registry 版本可用性并得到维护者明确授权后，才能从 `main` 手动 dispatch `.github/workflows/publish.yml`，且输入必须精确为 `0.1.4`。Staged 制品使用不可变的预期 tag `latest`。
7. The staged package must be reviewed for identity, manifest, workflow source SHA, downloaded tarball SHA-256, and any provenance npm exposes before WebAuthn/hardware-key approval. / 在通过 WebAuthn／硬件安全密钥审批前，必须检查 staged package 的 identity、manifest、workflow source SHA、下载 tarball SHA-256，以及 npm 暴露的 provenance（如有）。
8. Anonymous exact-version public verification on the supported matrix must pass after approval and before the annotated tag or GitHub Release. Any unavailable platform requires an explicit, narrowly scoped exception with substitute evidence recorded here; it must not be reported as passed. / 审批后、annotated tag 与 GitHub Release 之前，必须在支持的矩阵上完成匿名精确版本公共验证。任何不可用平台都必须在此记录明确且范围受限的例外与替代证据，不得表述为已通过。
9. v0.1.4 supports one selected source at a time and runtime switching between Codex and Claude Code; it does not support one mixed Codex-and-Claude index or a source-filtered mixed view. / v0.1.4 支持一次选择一个来源，并支持在 Codex 与 Claude Code 之间运行期切换；不支持一个混合 Codex／Claude 索引或按来源筛选的混合视图。
10. A published `session-analyzer@0.1.4` can never be reused. The pre-staging public-registry check was repeated before staging, followed by final anonymous verification after approval. / 已发布的 `session-analyzer@0.1.4` 永不复用。Staging 前已重复执行公共 registry 检查，审批后也已完成最终匿名验证。

## Version-specific risks / 版本特有风险

### Large Codex index reliability / Codex 大型索引可靠性

The compact resident index must keep source JSONL as the lossless source of truth, avoid a reachable full parsed-record graph, and preserve source-backed detail hydration, Raw readback, image preview, exact transcript fingerprinting, append-only handling, and stale-source/reindex correctness. The completed memory/reliability plans provide the implementation evidence; this release must rerun the relevant gates on the exact release commit. / 紧凑常驻索引必须继续以来源 JSONL 作为无损事实来源，避免可到达的完整 parsed-record 对象图，并保持来源支撑的详情 hydration、Raw 回读、图片预览、精确 transcript 指纹、append-only 处理以及过期来源／重建索引正确性。已完成的内存／可靠性计划提供实现证据；本次发布仍必须在精确 release commit 上重跑相关门禁。

The 800 MiB candidate-byte warning is advisory and non-blocking. Release notes should communicate the material resident-memory and freshness benefit without copying local benchmark values or sensitive corpus details. / 800 MiB candidate-byte warning 是提示性的、非阻断的。Release note 应传达常驻内存与 freshness 的实质收益，但不复制本地 benchmark 数字或敏感语料细节。

### Runtime source switching and stale discovery / 运行期来源切换与过期发现

The active source can change without restarting the server, and source configuration/ownership must remain registry-driven and source-neutral. Stale discovery responses must not commit an old source or old project selection. Existing browser coverage exercises invalidated discovery failures and source transitions; the remaining tracked case is the successful stale full-scan response after summary selection, including the old auto-restore path. / 当前来源可以在不重启 server 的情况下切换，来源配置／ownership 必须继续由 registry 驱动并保持来源中立。过期发现响应不得提交旧来源或旧项目选择。现有 browser coverage 已覆盖失效 discovery failure 与 source transition；剩余跟踪 case 是 summary 选择之后成功返回的过期 full-scan 响应，包括旧 auto-restore 路径。

### Package and cross-platform contract / Package 与跨平台契约

The package manifest, generated browser asset, Codex and Claude installed-package smoke paths, strict npm 12 install-script policy, and Linux/Windows CI must remain aligned. Any package allowlist or generated-asset change invalidates candidate hashes and requires fresh inspection. / Package manifest、生成的 browser asset、Codex 与 Claude 安装后 package smoke、npm 12 strict install-script policy，以及 Linux／Windows CI 必须保持一致。任何 package allowlist 或生成资产变化都会使候选哈希失效，并要求重新检查。

## Work phases / 工作阶段

### Phase 1: Open the plan and close metadata / 阶段 1：建立计划并收口 metadata

- During preparation, keep this plan in `docs/exec-plans/active/` and keep the docs index and `AGENTS.md` active-plan anchors accurate; at closeout, archive it under `completed/` and restore the no-active-plan indexes. / 准备期间将本计划保留在 `docs/exec-plans/active/`，并保持 docs 索引与 `AGENTS.md` active-plan anchor 准确；收尾时将其归档到 `completed/`，并恢复无 active plan 的索引状态。
- Update `package.json`, `package-lock.json`, the package metadata version assertions, and the bilingual changelog to `0.1.4`; retain an empty bilingual `Unreleased` section. / 将 `package.json`、`package-lock.json`、package metadata 版本断言与双语 changelog 更新到 `0.1.4`；保留空的双语 `Unreleased` 区段。
- Converge the product-spec non-goal with the supported runtime-switching boundary and keep the README known limit semantically aligned. / 使产品规格中的非目标与受支持的运行期来源切换边界收敛，并保持 README known limit 语义一致。
- Narrow the project-chooser debt wording to the one remaining successful stale full-scan/auto-restore case without closing the broader debt. / 将项目选择器技术债文字收窄到剩余的成功 stale full-scan／auto-restore case，但不关闭整个技术债。

### Phase 2: Validate the candidate branch / 阶段 2：验证候选分支

- Use Node.js 24.18.1 or the supported Node.js 24 line, npm `12.0.2`, `strict-allow-scripts=true`, and the official registry. / 使用 Node.js 24.18.1 或支持的 Node.js 24 版本线、npm `12.0.2`、`strict-allow-scripts=true` 与官方 registry。
- Run `npm run build:check`, `npm test`, `npm run test:package`, `npm run test:browser`, both audits, package inspection, and the guarded directory `npm publish --dry-run` required by the runbook. / 运行 `npm run build:check`、`npm test`、`npm run test:package`、`npm run test:browser`、两类 audit、package inspection，以及运行手册要求的受保护目录式 `npm publish --dry-run`。
- Run focused compact-index and hydration/freshness tests; source-switch/project-chooser race tests; Codex materialized-fork/review-parent regressions; and Claude 2.1.220 compatibility/derived-session regressions; record outcomes as candidate evidence only. / 运行 compact-index 与 hydration／freshness 聚焦测试；source-switch／project-chooser 竞态测试；Codex 物化式分叉／review-parent 回归；以及 Claude 2.1.220 兼容性／派生会话回归；这些结果只作为候选证据记录。
- Confirm package contents, third-party notices, sensitive-path hygiene, generated assets, and installed Codex/Claude server smoke. / 确认 package 内容、第三方 notice、敏感路径卫生、生成资产，以及安装后 Codex／Claude server smoke。

### Phase 3: Establish exact `main` release source / 阶段 3：确定精确的 `main` release source

- Open the normal release pull request from `v0.1.4-development`; do not merge local `main` directly. / 从 `v0.1.4-development` 建立正常 release pull request；不得直接合并本地 `main`。
- After merge, record the pushed `main` SHA and CI run URL, then repeat the strict install, release, browser, audit, package, hash, and guarded dry-run gates on a clean checkout of that SHA. / 合入后记录已 push 的 `main` SHA 与 CI run URL，再在该 SHA 的干净 checkout 上重复 strict install、release、browser、audit、package、hash 与受保护 dry-run 门禁。

### Phase 4: Stage and approve through OIDC / 阶段 4：通过 OIDC staging 并 approve

- Recheck that `session-analyzer@0.1.4` is unused in the official public registry and recheck final dist-tag state immediately before dispatch. / Dispatch 前重新检查官方公共 registry 中 `session-analyzer@0.1.4` 未被使用，并重新检查最终 dist-tag 状态。
- With explicit maintainer authorization, dispatch **Stage npm release** from `main` with exact input `0.1.4`. / 得到维护者明确授权后，从 `main` dispatch **Stage npm release**，输入精确 `0.1.4`。
- Confirm verify-job gates, source SHA, candidate SHA-256, stage-job byte reproduction, immutable `latest` intent, and the single OIDC staging mutation. / 确认 verify-job 门禁、source SHA、候选 SHA-256、stage-job 字节复现、不可变 `latest` 意图与唯一一次 OIDC staging mutation。
- Review staged identity, manifest, downloaded hash, source provenance, and any npm-provided attestation; approve only with the maintainer-controlled WebAuthn/hardware-key path. / 审查 staged identity、manifest、下载哈希、来源 provenance 与 npm 提供的 attestation（如有）；只能通过维护者控制的 WebAuthn／硬件密钥路径 approve。

### Phase 5: Verify publicly, tag, and release / 阶段 5：公共验证、tag 与 release

- Run the anonymous exact-version public verification workflow on Ubuntu and Windows with the exact source SHA and candidate SHA-256. / 使用精确 source SHA 与候选 SHA-256，在 Ubuntu 与 Windows 运行匿名精确版本公共验证 workflow。
- Confirm public tarball hash, provenance/signatures, package metadata, npx/global-install/CLI help, packaged server root, `/api/state`, and final dist-tags. / 确认公共 tarball 哈希、provenance／signature、package metadata、npx／全局安装／CLI help、打包 server root、`/api/state` 与最终 dist-tag。
- Only after public verification succeeds, create and push annotated `v0.1.4` on the exact release commit and publish the bilingual GitHub Release. / 只有公共验证成功后，才在精确 release commit 上创建并 push annotated `v0.1.4`，并发布双语 GitHub Release。

### Phase 6: Close the plan / 阶段 6：收尾计划

- Record public verification outcomes, artifact hash continuity, URLs, any sanitized exception, and final identity consistency in this plan. / 在本计划中记录公共验证结论、制品哈希连续性、URL、任何脱敏例外与最终 identity 一致性。
- Move this plan to `docs/exec-plans/completed/` only after staged review/approval, public verification, tag, and GitHub Release are complete. / 只有 staged 审查／审批、公共验证、tag 与 GitHub Release 全部完成后，才能将本计划移入 `docs/exec-plans/completed/`。

## Acceptance criteria / 验收标准

1. Package, lockfile root, package metadata version assertions, dated bilingual changelog, published tag, and release notes agree on `0.1.4` / `v0.1.4`; the bilingual `Unreleased` section is empty. / Package、lockfile root、package metadata 版本断言、双语带日期 changelog、已发布 tag 与 release note 对 `0.1.4` / `v0.1.4` 保持一致；双语 `Unreleased` 区段为空。
2. Product-spec wording distinguishes supported runtime source switching from unsupported mixed indexing/source-filtered mixed views, and docs anchors list no active plan while pointing to this completed record. / 产品规格文字区分受支持的运行期来源切换与不受支持的混合索引／按来源筛选混合视图；文档 anchor 不再列出 active plan，并指向本 completed record。
3. The release note names the material Codex resident-memory and source-freshness benefit without embedding sensitive corpus data or local benchmark output. / Release note 概括 Codex 常驻内存与来源 freshness 的实质收益，不包含敏感语料数据或本地 benchmark 输出。
4. The project-chooser tracker records existing invalidated-discovery/source-transition coverage and keeps only the successful stale full-scan/auto-restore sequence open. / 项目选择器 tracker 记录既有失效 discovery／source transition 覆盖，只保留成功 stale full-scan／auto-restore 序列为开放项。
5. Generated assets, Node/browser/package gates, audits, manifest/notice/hygiene checks, and exact-main CI pass with the approved Node/npm toolchain. / 生成资产、Node／browser／package 门禁、audit、manifest／notice／hygiene 检查，以及 exact-main CI 使用批准的 Node／npm 工具链通过。
6. The staged and public artifacts are byte-identical to the verified candidate, provenance/source identity is correct, maintainer approval is recorded, and anonymous public verification succeeds before tag/Release. / Staged 与公共制品与已验证候选逐字节一致，provenance／source identity 正确，维护者审批已记录，并在 tag／Release 前完成匿名公共验证。
7. Final evidence is complete and this record is archived in `completed/`; no v0.1.4 release plan remains active. / 最终证据已完整，本记录已归档到 `completed/`；不再有 v0.1.4 release plan 处于 active 状态。

## Release evidence / 发布证据

- Release identity: `session-analyzer@0.1.4` / 发布身份：`session-analyzer@0.1.4`
- Release commit: `83d7b682f078cf09a86eb1b29b81960a89d332ca` / Release commit：`83d7b682f078cf09a86eb1b29b81960a89d332ca`
- Source branch and tag: `main@83d7b682f078cf09a86eb1b29b81960a89d332ca`; annotated `v0.1.4`, tag object `faa95626e902bf86c30333138c2e1592087c1dde` / 来源分支与 tag：`main@83d7b682f078cf09a86eb1b29b81960a89d332ca`；annotated `v0.1.4`，tag object 为 `faa95626e902bf86c30333138c2e1592087c1dde`
- GitHub Release: published at `https://github.com/Yijia-Zhou/session-analyzer/releases/tag/v0.1.4` / GitHub Release：已发布于 `https://github.com/Yijia-Zhou/session-analyzer/releases/tag/v0.1.4`
- Registry and toolchain: `https://registry.npmjs.org/`; Node.js 24; npm `12.0.2` / Registry 与工具链：`https://registry.npmjs.org/`；Node.js 24；npm `12.0.2`
- Candidate/staged/public SHA-256 continuity: `de46a9b822f53803b923758e4f12cbd35f8a68ed94fbbb91c7a79b0bf82d89b1` / 候选／staged／公共 SHA-256 连续性：`de46a9b822f53803b923758e4f12cbd35f8a68ed94fbbb91c7a79b0bf82d89b1`
- Candidate-branch gates on 2026-08-15: `npm run release:check` passed with 497 Node tests and Codex/Claude package smoke; `npm run test:browser` passed 112/112; production and full audits reported zero vulnerabilities; `git diff --check` passed. / 2026-08-15 候选分支门禁：`npm run release:check` 通过，包含 497 项 Node 测试与 Codex／Claude package smoke；`npm run test:browser` 以 112/112 通过；production 与 full audit 均报告 zero vulnerabilities；`git diff --check` 通过。
- Exact-main CI and local gates: main push CI [31870382753](https://github.com/Yijia-Zhou/session-analyzer/actions/runs/31870382753) succeeded; exact-main `npm ci --strict-allow-scripts`, `npm run release:check`, `npm run test:browser`, both audits, guarded `npm publish --dry-run`, and package smoke passed with Node.js 24.18.1 and npm `12.0.2`. / Exact-main CI 与本地门禁：main push CI [31870382753](https://github.com/Yijia-Zhou/session-analyzer/actions/runs/31870382753) 成功；exact-main 的 `npm ci --strict-allow-scripts`、`npm run release:check`、`npm run test:browser`、两类 audit、受保护的 `npm publish --dry-run` 与 package smoke 均在 Node.js 24.18.1、npm `12.0.2` 下通过。
- Staged OIDC and approval: [Stage npm release run 31871254021](https://github.com/Yijia-Zhou/session-analyzer/actions/runs/31871254021) succeeded; maintainer WebAuthn/2FA approval completed. / Staged OIDC 与审批：[Stage npm release run 31871254021](https://github.com/Yijia-Zhou/session-analyzer/actions/runs/31871254021) 成功；维护者 WebAuthn／2FA 审批已完成。
- Public provenance/signatures and anonymous verification: passed for `session-analyzer@0.1.4`; public tarball matched the recorded SHA-256, npm signatures were verified, provenance identified this repository, `.github/workflows/publish.yml`, `refs/heads/main`, and source commit `83d7b682f078cf09a86eb1b29b81960a89d332ca`, and npx/global CLI plus packaged server checks passed. Public provenance record: `https://search.sigstore.dev/?logIndex=2473544568`. / 公共 provenance／signature 与匿名验证：`session-analyzer@0.1.4` 已通过；公共 tarball 与记录的 SHA-256 一致，npm signature 已验证，provenance 指向本仓库、`.github/workflows/publish.yml`、`refs/heads/main` 与 source commit `83d7b682f078cf09a86eb1b29b81960a89d332ca`，npx／全局 CLI 与 packaged server 检查均通过。公共 provenance 记录：`https://search.sigstore.dev/?logIndex=2473544568`。
- Sanitized tag note: GitHub reports the annotated tag object `faa95626e902bf86c30333138c2e1592087c1dde` as unsigned. This is independent of npm package provenance/signature verification; the released tag was not recreated or mutated. / 脱敏 tag 说明：GitHub 报告 annotated tag object `faa95626e902bf86c30333138c2e1592087c1dde` 未签名。这与 npm package provenance／signature 验证相互独立；已发布 tag 未被重建或修改。

## Progress log / 进度日志

- 2026-08-15: Opened the v0.1.4 version-specific active plan from the pre-freeze source baseline `v0.1.4-development@084eb64`; at that point no registry mutation, tag, or GitHub Release had yet been performed. / 2026-08-15：从冻结前来源基线 `v0.1.4-development@084eb64` 建立 v0.1.4 版本专属 active plan；当时尚未执行 registry mutation、创建 tag 或 GitHub Release。
- 2026-08-15: The pre-staging official-registry check recorded `0.1.4` as unused; the staged OIDC workflow then produced the reviewed candidate. / 2026-08-15：Staging 前官方 registry 检查记录 `0.1.4` 尚未使用；随后 staged OIDC workflow 生成了经审查的候选制品。
- 2026-08-15: Metadata closure completed for package identity, bilingual changelog, product-spec semantics, docs anchors, and the narrowed project-chooser debt wording. / 2026-08-15：package identity、双语 changelog、产品规格语义、文档 anchor 与收窄后的项目选择器技术债文字均完成 metadata 收口。
- 2026-08-15: Generic release-machinery fixtures now use synthetic version/SHA values; package identity remains the only release-version-specific test contract. The durable runbook no longer points to this short-lived active plan, and the plan now anchors the broader materialized-fork, review-parent, and Claude 2.1.220 regression gates. / 2026-08-15：通用 release machinery fixture 现在使用 synthetic 版本／SHA；只有 package identity 仍由版本专属测试契约约束。Durable runbook 不再指向这个短期 active plan；本计划现在补充 materialized-fork、review-parent 与 Claude 2.1.220 回归门禁的范围 anchor。
- 2026-08-15: The exact released `main` commit `83d7b682f078cf09a86eb1b29b81960a89d332ca` passed main push CI and the exact-main gates. / 2026-08-15：精确发布的 `main` commit `83d7b682f078cf09a86eb1b29b81960a89d332ca` 通过 main push CI 与 exact-main 门禁。
- 2026-08-15: Staged OIDC publication, maintainer WebAuthn/2FA approval, anonymous public verification, npm provenance/signature verification, and npx/global CLI verification passed; the candidate, staged, and public tarballs shared SHA-256 `de46a9b822f53803b923758e4f12cbd35f8a68ed94fbbb91c7a79b0bf82d89b1`. / 2026-08-15：staged OIDC 发布、维护者 WebAuthn／2FA 审批、匿名公共验证、npm provenance／signature 验证与 npx／全局 CLI 验证均通过；候选、staged 与公共 tarball 共享 SHA-256 `de46a9b822f53803b923758e4f12cbd35f8a68ed94fbbb91c7a79b0bf82d89b1`。
- 2026-08-15: Annotated tag `v0.1.4` (tag object `faa95626e902bf86c30333138c2e1592087c1dde`) and the [GitHub Release](https://github.com/Yijia-Zhou/session-analyzer/releases/tag/v0.1.4) were published. The tag points to the exact released commit; the tag object is unsigned, independently of the verified npm provenance/signatures. / 2026-08-15：annotated tag `v0.1.4`（tag object `faa95626e902bf86c30333138c2e1592087c1dde`）与 [GitHub Release](https://github.com/Yijia-Zhou/session-analyzer/releases/tag/v0.1.4) 已发布。Tag 指向精确发布 commit；tag object 未签名，但这与已验证的 npm provenance／signature 相互独立。
