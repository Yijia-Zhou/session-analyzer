# First public npm release / 首次 npm 公开发布

## Objective / 目标

Publish the repository's first public npm package as `session-analyzer@0.1.2`, with a reproducible release artifact, explicit Node.js support, cross-platform CI, a human-controlled 2FA publication boundary, and a documented path to trusted publishing after the package exists. / 将仓库的首个公共 npm package 以 `session-analyzer@0.1.2` 发布；发布制品必须可复现，Node.js 支持范围必须明确，CI 必须覆盖跨平台场景，首次发布必须保留由维护者控制的 2FA 边界，并记录 package 建立后迁移到 trusted publishing 的路径。

## Status and ownership / 状态与负责人

- Owner: repository maintainers / 负责人：仓库维护者
- Status: completed / 状态：已完成
- Started: 2026-07-31 / 开始日期：2026-07-31
- Completed: 2026-08-01 / 完成日期：2026-08-01
- Target version: `0.1.2`
- Target package: `session-analyzer`
- Source branch: `v0.1.2-development`
- Source baseline: `d9ba3f6`
- Release-preparation commit: `1d676fa`
- Review PR: `https://github.com/Yijia-Zhou/session-analyzer/pull/2`
- Initial CI run: `https://github.com/Yijia-Zhou/session-analyzer/actions/runs/30608906152`
- Release-hardening commit: `bf8715c`
- Current review PR: `https://github.com/Yijia-Zhou/session-analyzer/pull/3`
- Failed bootstrap CI run: `https://github.com/Yijia-Zhou/session-analyzer/actions/runs/30691912695` / 失败的 bootstrap CI run：`https://github.com/Yijia-Zhou/session-analyzer/actions/runs/30691912695`
- Bootstrap-repair commit: `4496d04`
- Bootstrap-repair CI run: `https://github.com/Yijia-Zhou/session-analyzer/actions/runs/30692219678` (all six matrix jobs and aggregate `ci` passed) / Bootstrap 修复 CI run：`https://github.com/Yijia-Zhou/session-analyzer/actions/runs/30692219678`（六个 matrix job 与聚合 `ci` 全部通过）
- Release commit: `6304f855a29ce1fa3eade8a74fac2786a7ba7da1`
- Release CI run: `https://github.com/Yijia-Zhou/session-analyzer/actions/runs/30696311244`
- npm package: `https://www.npmjs.com/package/session-analyzer/v/0.1.2`
- Git tag: `v0.1.2`
- GitHub Release: `https://github.com/Yijia-Zhou/session-analyzer/releases/tag/v0.1.2`
- Related product spec: `docs/product-specs/session-transcript-analyzer.md` / 相关产品规格：`docs/product-specs/session-transcript-analyzer.md`
- Related release runbook: `docs/design-docs/npm-release-runbook.md` / 相关发布运行手册：`docs/design-docs/npm-release-runbook.md`
- Related completed hardening: `docs/exec-plans/completed/2026-06-10-v0.1-release-hardening.md` / 相关已完成加固：`docs/exec-plans/completed/2026-06-10-v0.1-release-hardening.md`
- Related debt: `docs/exec-plans/tech-debt-tracker.md#10-release-workflow-and-trusted-publishing--发布流程与-trusted-publishing` / 相关技术债：`docs/exec-plans/tech-debt-tracker.md#10-release-workflow-and-trusted-publishing--发布流程与-trusted-publishing`

## Fixed release decisions / 已确定的发布决策

1. The first public registry version is `0.1.2`; the earlier `0.1.0` changelog entry remains an internal pack-ready milestone rather than a claim that npm versions `0.1.0` or `0.1.1` were published. / 首个公共 registry 版本为 `0.1.2`；较早的 `0.1.0` changelog 条目继续表示内部可打包里程碑，而不声称 npm 上曾发布 `0.1.0` 或 `0.1.1`。
2. The unscoped `session-analyzer` name remains the primary target while the public registry reports it as absent. A scoped-name fallback is out of scope unless that availability changes before publication. / 公共 registry 仍报告该名称不存在时，继续以无 scope 的 `session-analyzer` 为首选。除非发布前可用性发生变化，否则 scoped 名称回退不在本计划范围内。
3. The supported runtime floor becomes Node.js 22, with Node.js 24 LTS used for release production and Node.js 22 plus 24 covered by CI. / 支持的运行时下限调整为 Node.js 22；发布制品使用 Node.js 24 LTS 生成，并由 CI 覆盖 Node.js 22 与 24。
4. The first publication is manual and interactive against `https://registry.npmjs.org/`; the maintainer owns npm login, 2FA, and the irreversible publish action. The registry assigned both `next` and the required inaugural `latest` to `0.1.2`, so no redundant promotion session was performed. The publication used an isolated, short-lived authentication session followed by logout, temporary-userconfig deletion, and an `ENEEDAUTH` proof. / 首次发布通过 `https://registry.npmjs.org/` 手动交互执行；维护者负责 npm 登录、2FA 与不可逆发布动作。Registry 把 `next` 与首发必需的 `latest` 都指向 `0.1.2`，因此没有执行多余的 promotion session。发布使用隔离、短时的认证会话，随后完成 logout、临时 userconfig 删除与 `ENEEDAUTH` 证明。
5. Repository automation stops before public mutation. This plan may prepare and inspect `session-analyzer-0.1.2.tgz` as release evidence, but the irreversible publish must run from the same clean release commit's package root with no positional tarball argument so `prepublishOnly` executes. This plan does not authorize npm publication, remote tags, or GitHub Releases. / 仓库自动化在公共状态变更前停止。本计划可以把 `session-analyzer-0.1.2.tgz` 作为发布证据进行准备与检查，但不可逆 publish 必须从同一干净 release commit 的 package root 执行，且不带位置 tarball 参数，从而确保 `prepublishOnly` 运行。本计划不授权执行 npm 发布、远端 tag 或 GitHub Release。
6. A future trusted-publishing workflow is added only after the package exists. It should use GitHub-hosted runners, OIDC, a protected GitHub Environment, and preferably stage-only permission followed by human 2FA approval. / 只有 package 已存在后才添加未来的 trusted-publishing workflow。该流程应使用 GitHub-hosted runner、OIDC、受保护的 GitHub Environment，并优先采用仅允许 stage、随后由人工 2FA 审批的权限。
7. Installed runtime dependencies are limited to exact `acorn@8.15.0` and `markdown-it@14.3.0`, matching the reviewed release tree. `highlight.js@11.11.1` is an exact build-only dependency because consumers receive the checked-in generated vendor asset rather than loading the package at runtime. / 安装后运行时依赖仅保留与已审查发布树一致的精确版本 `acorn@8.15.0` 与 `markdown-it@14.3.0`。`highlight.js@11.11.1` 是精确的仅构建期依赖，因为用户获得的是已提交的生成 vendor asset，而不是在运行时加载该 package。
8. Source and CI dependency installation use Node.js `^22.22.2 || ^24.15.0` with exact npm `12.0.2`, npm 12 strict default-deny install-script enforcement, exact approval for `esbuild@0.28.1`, and an explicit denial for optional `fsevents`. / 源码环境与 CI 的依赖安装使用 Node.js `^22.22.2 || ^24.15.0` 与精确的 npm `12.0.2`，启用 npm 12 strict 默认拒绝 install-script 策略，精确允许 `esbuild@0.28.1`，并明确拒绝可选的 `fsevents`。

## Implementation phases / 实施阶段

### Phase 1: repository release contract / 第一阶段：仓库发布契约

- Set package and lockfile version metadata to `0.1.2`. / 将 package 与 lockfile 版本元数据设为 `0.1.2`。
- Set the Node.js engine floor to 22 and update both READMEs and the product specification. / 将 Node.js engine 下限设为 22，并同步更新两份 README 与产品规格。
- Add public-registry `publishConfig`, a reusable `release:check` script, and a `prepublishOnly` guard. / 增加指向公共 registry 的 `publishConfig`、可复用的 `release:check` 脚本，以及 `prepublishOnly` guard。
- Update package metadata tests so the release contract cannot silently drift. / 更新 package metadata 测试，避免发布契约静默漂移。
- Pin direct runtime dependencies to their reviewed release versions and move the prebuilt Highlight.js vendor input to `devDependencies`. / 把直接运行时依赖固定为已审查的发布版本，并把预构建 Highlight.js vendor asset 的输入移入 `devDependencies`。
- Add exact `allowScripts` decisions, strict project npm configuration, compatible `devEngines`, and lockfile-wide install-script metadata tests. / 增加精确的 `allowScripts` 决策、strict 项目 npm 配置、兼容的 `devEngines`，以及覆盖整个 lockfile 的 install-script metadata 测试。
- Document the exact npm bootstrap before source installation and package the complete Highlight.js BSD notice beside the redistributed vendor assets. / 记录源码安装前的精确 npm bootstrap，并在再分发的 Highlight.js vendor 资产旁随 package 提供完整 BSD notice。
- Keep a blank bilingual Unreleased section and close the accumulated work into a bilingual `0.1.2` entry. / 保留空的双语 Unreleased 区段，并把累积工作收口到双语 `0.1.2` 条目。

### Phase 2: public CI gate / 第二阶段：公共 CI gate

- Add read-only GitHub Actions CI for pull requests, `main`, and versioned development branches. / 为 pull request、`main` 与版本化 development branch 增加只读 GitHub Actions CI。
- Test Node.js 22 and 24 on Linux, and Node.js 24 on Windows. / 在 Linux 上测试 Node.js 22 与 24，并在 Windows 上测试 Node.js 24。
- Disable `setup-node` package-manager caching so it cannot invoke the bundled npm, then bootstrap exact npm 12.0.2 from `runner.temp` before any repository-local npm command or `npm ci --strict-allow-scripts`. / 禁用 `setup-node` 的 package-manager cache，避免其调用 Node 附带的 npm；随后从 `runner.temp` bootstrap 精确 npm 12.0.2，完成后才能执行任何仓库内 npm 命令或 `npm ci --strict-allow-scripts`。
- Run generated-asset checks and the full Node suite in the runtime matrix. / 在运行时矩阵中执行生成资产检查与完整 Node 测试。
- Run packaged-install smoke on both Linux and Windows, and browser coverage on Linux with Chromium. / 在 Linux 与 Windows 上执行安装后 package smoke，并在 Linux Chromium 上执行 browser coverage。

### Phase 3: official-registry lock and artifact / 第三阶段：官方 registry lock 与制品

- Replace mirror-host lockfile URLs with official registry URLs without changing dependency versions unintentionally. / 将 lockfile 中的镜像 host URL 替换为官方 registry URL，不得无意改变依赖版本。
- Run the release gate, browser tests, package smoke, audit review, generated-asset checks, syntax checks, and diff checks. / 执行 release gate、browser 测试、package smoke、audit 审查、生成资产检查、语法检查与 diff 检查。
- Generate `session-analyzer-0.1.2.tgz`, record its integrity and manifest, extract it into a temporary directory, and inspect the runtime allowlist and sensitive-file exclusions. / 生成 `session-analyzer-0.1.2.tgz`，记录完整性与 manifest，解压到临时目录，并检查运行时白名单及敏感文件排除情况。
- Treat the candidate tarball as inspection evidence only. Stop before npm authentication or publication and hand the manifest, hashes, clean-release-commit identity, and directory-based publish commands to the maintainer. / 把候选 tarball 仅作为检查证据。在 npm 认证或发布前停止，把 manifest、哈希、干净 release commit 身份，以及基于工作树的 publish 命令交给维护者。

### Phase 4: maintainer publication and public verification / 第四阶段：维护者发布与公共验证

- Maintainer confirms that the release commit is pushed, required Linux/Windows CI is green, the local worktree is clean, and no candidate `.tgz` remains inside it. / 维护者确认 release commit 已推送、要求的 Linux/Windows CI 全部通过、本地工作树干净，且其中不再留有候选 `.tgz`。
- From that package root, the maintainer runs `npm publish --dry-run --foreground-scripts --tag='next' --access='public'` and verifies that `prepublishOnly`, `release:check`, and the expected manifest complete. / 维护者从该 package root 执行 `npm publish --dry-run --foreground-scripts --tag='next' --access='public'`，并验证 `prepublishOnly`、`release:check` 与预期 manifest 均完成。
- Maintainer establishes a fresh isolated `NPM_CONFIG_USERCONFIG`, proves no inherited credential can authenticate, logs in with 2FA, and runs `npm publish --foreground-scripts --tag='next' --access='public'` from the same unchanged package root, with no positional package or tarball argument. The session then logs out, deletes its temporary `.npmrc`, and proves `ENEEDAUTH`; a cleanup failure blocks further release work until the session token is revoked at npmjs.com. / 维护者建立全新的隔离 `NPM_CONFIG_USERCONFIG`，证明没有继承凭据能够认证，使用 2FA 登录，并从同一未变化的 package root 执行 `npm publish --foreground-scripts --tag='next' --access='public'`，不带位置 package 或 tarball 参数。随后该会话 logout、删除临时 `.npmrc` 并证明 `ENEEDAUTH`；若清理失败，则必须先在 npmjs.com 撤销该 session token，才能继续发布工作。
- Without publication credentials, verify registry metadata, exact-version `npx`, global installation, CLI help, packaged server startup, root HTML, and `/api/state` on clean Windows. The maintainer explicitly waived a second direct public install on Linux for this inaugural release; the substitute evidence is the green Ubuntu package-smoke CI on the exact release commit plus byte-for-byte identity between the inspected candidate and the public tarball. / 在不携带发布凭据的情况下，于干净 Windows 验证 registry metadata、精确版本 `npx`、全局安装、CLI help、packaged server 启动、根 HTML 与 `/api/state`。维护者明确豁免本次首发的第二次 Linux 直接公共安装；替代证据为精确 release commit 上全绿的 Ubuntu package-smoke CI，以及检查候选与公共 tarball 的逐字节一致性。
- Anonymous registry evidence showed that the inaugural publication automatically assigned both `next` and `latest` to `0.1.2`; no redundant second authenticated promotion was performed. A separately empty userconfig proved `ENEEDAUTH` before the final tag read. Only afterward was annotated `v0.1.2` pushed at the published commit and the bilingual GitHub Release created. / 匿名 registry 证据显示首发自动把 `next` 与 `latest` 都指向 `0.1.2`；没有执行多余的第二次认证 promotion。最终读取 tag 前，单独的空白 userconfig 已证明 `ENEEDAUTH`。随后才把 annotated `v0.1.2` 推送到已发布 commit，并创建双语 GitHub Release。
- Record public verification evidence, then move this plan to `completed/`. / 记录公共验证证据，然后把本计划移动到 `completed/`。

## Acceptance criteria / 验收标准

1. Package metadata, lockfile metadata, package metadata tests, changelog, Git tag, and GitHub Release agree on `0.1.2` / `v0.1.2`. / Package metadata、lockfile metadata、package metadata 测试、changelog、Git tag 与 GitHub Release 对 `0.1.2` / `v0.1.2` 保持一致。
2. `publishConfig` fixes public access and the official registry, while repository install resolution no longer depends on npmmirror URLs. / `publishConfig` 固定公共访问与官方 registry，仓库安装解析不再依赖 npmmirror URL。
3. Linux Node.js 22/24 and Windows Node.js 24 CI pass; browser and packaged-install jobs cover the release surfaces before publication. / Linux Node.js 22/24 与 Windows Node.js 24 CI 通过；browser 与安装后 package job 在发布前覆盖 release surface。
4. `npm run release:check`, `npm run test:browser`, `npm audit --omit=dev`, `npm pack --dry-run`, the final directory-based `npm publish --dry-run`, and `git diff --check` pass or have an explicitly reviewed, documented exception. / `npm run release:check`、`npm run test:browser`、`npm audit --omit=dev`、`npm pack --dry-run`、最终基于工作树的 `npm publish --dry-run` 与 `git diff --check` 通过，或具有经过明确审查并记录的例外。
5. The inspected tarball contains every runtime file required by the allowlist and excludes tests, docs, real transcripts, `.codex`, source maps, CI metadata, temporary files, credentials, and personal logs. / 已检查 tarball 包含白名单要求的全部运行时文件，并排除测试、docs、真实 transcript、`.codex`、source map、CI metadata、临时文件、凭据与个人日志。
6. The first irreversible registry write and every later approval boundary requiring proof of presence remain maintainer-controlled through 2FA. / 首次不可逆 registry 写入，以及之后每个需要 proof of presence 的审批边界，均继续由维护者通过 2FA 控制。
7. Public exact-version installation succeeds on Windows. Direct Linux public installation is explicitly waived for this release, with green Ubuntu package smoke on the exact release commit plus public-tarball hash identity recorded as substitute evidence. The inaugural automatic `latest` exception is documented. / 公共精确版本安装在 Windows 上成功。本次发布明确豁免 Linux 直接公共安装，并记录精确 release commit 上全绿的 Ubuntu package smoke 与公共 tarball 哈希一致性作为替代证据。首发自动 `latest` 例外已记录。
8. The actual `npm publish` runs from the clean release commit's package root without a positional argument or `--ignore-scripts`, and its output proves `prepublishOnly` completed. / 实际 `npm publish` 从干净 release commit 的 package root 执行，不带位置参数，也不使用 `--ignore-scripts`；其输出能够证明 `prepublishOnly` 已完成。
9. Publication uses an isolated `NPM_CONFIG_USERCONFIG` with no inherited npm token and ends with successful `npm logout`, deletion of the exact temporary credential directory, and a post-logout `ENEEDAUTH` result. No promotion session is needed because the registry automatically assigns inaugural `latest`; all public and final dist-tag verification is demonstrably unauthenticated. / 发布使用隔离的 `NPM_CONFIG_USERCONFIG`，且不继承 npm token；会话以成功的 `npm logout`、精确临时凭据目录删除与 logout 后 `ENEEDAUTH` 结果结束。由于 registry 自动分配首发 `latest`，无需 promotion session；全部公共验证与最终 dist-tag 验证均可证明处于无认证状态。
10. Package metadata tests require exact `acorn@8.15.0` and `markdown-it@14.3.0` runtime dependencies and classify exact `highlight.js@11.11.1` as build-only; the installed-package smoke proves that the published CLI/server does not require the Highlight.js package. / Package metadata 测试要求运行时依赖为精确的 `acorn@8.15.0` 与 `markdown-it@14.3.0`，并把精确的 `highlight.js@11.11.1` 归类为仅构建期；安装后 package smoke 证明已发布 CLI/server 不需要 Highlight.js package。
11. Exact npm 12.0.2 strict installs pass on every CI platform; `allowScripts` approves only exact `esbuild@0.28.1`, explicitly denies `fsevents`, and package metadata tests prove every lockfile `hasInstallScript` entry has a decision. / 每个 CI 平台都使用精确 npm 12.0.2 通过 strict 安装；`allowScripts` 只精确允许 `esbuild@0.28.1`、明确拒绝 `fsevents`，且 package metadata 测试证明 lockfile 中每个 `hasInstallScript` 条目都有对应决策。
12. Both READMEs distinguish the installed Node.js runtime floor from the exact source toolchain bootstrap; the package manifest requires `THIRD_PARTY_NOTICES.md`, whose Highlight.js 11.11.1 license matches the reviewed development dependency; and final dist-tag evidence has its own isolated `ENEEDAUTH` proof. / 两份 README 都区分已安装 Node.js 运行时下限与精确源码工具链 bootstrap；package manifest 强制要求 `THIRD_PARTY_NOTICES.md`，其中 Highlight.js 11.11.1 许可证与经过审查的开发依赖一致；最终 dist-tag 证据具有独立、隔离的 `ENEEDAUTH` 证明。

## Failure and recovery policy / 失败与恢复策略

- Before publication, discard the candidate evidence, correct and commit the repository, rerun all invalidated gates, and generate a new inspection candidate. / 发布前若发现问题，丢弃候选证据，修正并提交仓库，重跑全部已失效 gate，并生成新的检查候选制品。
- After `0.1.2` is published, never attempt to reuse that version. Because inaugural `latest` was automatic, a verification failure would require immediate incident assessment, precise deprecation when useful, a repository fix, and publication of `0.1.3`; it could not be contained by withholding promotion. / `0.1.2` 发布后不得尝试复用该版本。由于首发 `latest` 是自动创建的，若验证失败，必须立即进行 incident 评估，必要时精确标记 deprecated，修复仓库并发布 `0.1.3`；不能再通过不执行 promotion 来限制暴露。
- Do not move an already pushed release tag to a different commit. Use a new patch version instead. / 不得把已经推送的 release tag 移到其他 commit；应改用新的 patch 版本。
- Never use a long-lived write token merely to bypass the interactive first-release boundary. / 不得仅为绕过首次发布的交互边界而使用长期 write token。
- If logout or post-logout `ENEEDAUTH` confirmation fails, delete the isolated local credential, revoke the newly created session token through npmjs.com, record both actions, and stop until server-side revocation is confirmed. / 如果 logout 或 logout 后的 `ENEEDAUTH` 确认失败，删除隔离的本地凭据，通过 npmjs.com 撤销新创建的 session token，记录这两项动作，并在确认服务端撤销前停止。

## Prepared candidate artifact / 已准备候选制品

- Status: final inspected evidence for release commit `6304f855a29ce1fa3eade8a74fac2786a7ba7da1`; removed from the worktree after inspection and later proven byte-identical to the public registry tarball. / 状态：release commit `6304f855a29ce1fa3eade8a74fac2786a7ba7da1` 的最终检查证据；检查后已从工作树删除，随后证明与公共 registry tarball 逐字节一致。
- Artifact: `session-analyzer-0.1.2.tgz`
- npm packed size: 223,764 bytes / npm 打包大小：223,764 bytes
- npm unpacked size: 921,166 bytes / npm 解包大小：921,166 bytes
- Manifest entries: 35 / Manifest 条目：35
- SHA-1: `c72019295f79efda80faf03a774d99b2206a7ed8`
- SHA-256: `5e2420fe3fcab4fc30c0a8d6eee3b79c39a512007880922b0f1831ac62b0b6cc`
- npm integrity: `sha512-/mMRZFqhP2QxTXsjnZHACoodBLnJiaCtsFv7tkfunmt/zhe5v0TUwKlO8L3o+aW8rR+xAmPrNAYrqM+bnny/Nw==`
- Inspection result: all 35 packed files were byte-identical to their repository sources; required files, forbidden paths, complete third-party notice, sensitive-pattern scan, and dry-run parity all passed. / 检查结果：全部 35 个打包文件与仓库源文件逐字节一致；必需文件、禁止路径、完整第三方 notice、敏感模式扫描与 dry-run parity 均通过。
- Purpose boundary: this tarball is inspection evidence and must not be passed to `npm publish`; positional tarball publication bypassed `prepublishOnly` in the verified npm 12 workflow. / 用途边界：该 tarball 是检查证据，不得传给 `npm publish`；在已验证的 npm 12 流程中，带位置 tarball 的发布会绕过 `prepublishOnly`。
- Commit boundary: generated from the exact clean release commit, compared against `npm publish --dry-run`, removed before publication, and matched by both the actual publish notice and the anonymously downloaded public tarball. / Commit 边界：从精确干净 release commit 生成，与 `npm publish --dry-run` 比对，发布前删除，并由实际 publish notice 与匿名下载的公共 tarball 共同证明一致。

## Final release evidence / 最终发布证据

- Release identity: `session-analyzer@0.1.2`, source commit `6304f855a29ce1fa3eade8a74fac2786a7ba7da1`, annotated tag `v0.1.2`, GitHub Release `https://github.com/Yijia-Zhou/session-analyzer/releases/tag/v0.1.2`. / 发布身份：`session-analyzer@0.1.2`，来源 commit `6304f855a29ce1fa3eade8a74fac2786a7ba7da1`，annotated tag `v0.1.2`，GitHub Release `https://github.com/Yijia-Zhou/session-analyzer/releases/tag/v0.1.2`。
- Toolchain and authentication: Windows, Node.js `24.18.1`, npm `12.0.2`, official registry, and interactive web/2FA authentication through an isolated temporary userconfig with no inherited credential variables. / 工具链与认证：Windows、Node.js `24.18.1`、npm `12.0.2`、官方 registry，并通过不继承 credential variable 的隔离临时 userconfig 完成交互式 web/2FA 认证。
- CI: main push run `https://github.com/Yijia-Zhou/session-analyzer/actions/runs/30696311244` completed successfully for the exact release commit, including Linux Node 22/24, Windows Node 24, Linux/Windows package smoke, Ubuntu browser, and aggregate `ci`. / CI：main push run `https://github.com/Yijia-Zhou/session-analyzer/actions/runs/30696311244` 在精确 release commit 上成功完成，包括 Linux Node 22/24、Windows Node 24、Linux/Windows package smoke、Ubuntu browser 与聚合 `ci`。
- Final local gates: clean strict install ran only reviewed esbuild; `release:check` passed 326 Node tests plus installed-package CLI/server smoke; Chromium passed 83 tests; production and full audits reported zero vulnerabilities; the guarded directory-based publish dry run observed `prepublishOnly`; `git diff --check` passed. / 最终本地 gate：干净 strict install 只运行获批 esbuild；`release:check` 通过 326 项 Node 测试及安装后 CLI/server smoke；Chromium 通过 83 项测试；production 与完整 audit 均报告零漏洞；基于工作树的受保护 publish dry run 观察到 `prepublishOnly`；`git diff --check` 通过。
- Publication: the first non-interactive attempt completed all gates but was rejected before registry write with `EOTP`; cleanup succeeded and an anonymous query confirmed `0.1.2` remained `E404`. The maintainer then published interactively from the unchanged package root with no positional tarball and received `+ session-analyzer@0.1.2`. / 发布：第一次非交互尝试完成全部 gate，但在 registry 写入前以 `EOTP` 被拒绝；凭据清理成功，匿名查询确认 `0.1.2` 仍为 `E404`。随后维护者从未变化的 package root 交互发布，不带位置 tarball，并得到 `+ session-analyzer@0.1.2`。
- Credential cleanup: explicit recovery was required before further registry work; completion was verified through successful logout, a post-logout `ENEEDAUTH` result, and removal of the isolated credential directory. / 凭据清理：继续 registry 操作前曾需要显式恢复；最终通过成功 logout、logout 后的 `ENEEDAUTH` 结果以及隔离凭据目录已删除确认清理完成。
- Public Windows verification: a fresh empty userconfig proved `ENEEDAUTH`; registry version, repository metadata, and hashes matched; the anonymously downloaded tarball contained 35 files and matched candidate SHA-1/SHA-256; exact-version `npx`, temporary global installation, CLI help, packaged server root HTML, and `/api/state` all passed. / Windows 公共验证：全新空白 userconfig 证明 `ENEEDAUTH`；registry 版本、repository metadata 与哈希一致；匿名下载 tarball 包含 35 个文件，并匹配候选 SHA-1/SHA-256；精确版本 `npx`、临时全局安装、CLI help、packaged server 根 HTML 与 `/api/state` 均通过。
- Linux public verification exception: direct public installation was explicitly waived for this inaugural release. Substitute evidence is the successful Ubuntu package-smoke CI on the exact release commit plus byte identity between the inspected candidate and the public registry tarball. / Linux 公共验证例外：本次首发明确豁免直接公共安装。替代证据为精确 release commit 上成功的 Ubuntu package-smoke CI，以及检查候选与公共 registry tarball 的字节一致性。
- Dist-tags: independent anonymous reads showed both `next: 0.1.2` and `latest: 0.1.2`. npm's inaugural package metadata automatically requires `latest`, so no redundant promotion login or mutation was performed; `next` is retained at the same version. / Dist-tag：独立匿名读取显示 `next: 0.1.2` 与 `latest: 0.1.2`。npm 的首发 package metadata 自动要求 `latest`，因此没有执行多余的 promotion 登录或 mutation；`next` 保留在同一版本。
- Final consistency: GitHub Release is published, non-draft, non-prerelease, and contains the bilingual changelog; local and remote annotated tags dereference to the release commit; anonymous npm evidence reports version `0.1.2` and the expected final dist-tags. / 最终一致性：GitHub Release 已发布，非 draft、非 prerelease，并包含双语 changelog；本地与远端 annotated tag 均解引用到 release commit；匿名 npm 证据报告版本 `0.1.2` 与预期最终 dist-tag。

## Progress log / 进度日志

- 2026-07-31: Confirmed a clean `v0.1.2-development` worktree at `d9ba3f6`, one commit ahead of `origin/v0.1.2-development`, and opened this plan. / 2026-07-31：确认 `v0.1.2-development` 工作树在 `d9ba3f6` 保持干净，比 `origin/v0.1.2-development` 领先一个 commit，并建立本计划。
- 2026-07-31: Closed package metadata, both READMEs, the bilingual changelog, product specification, package metadata tests, and the public CI contract around `session-analyzer@0.1.2` with a Node.js 22 floor. / 2026-07-31：围绕 Node.js 22 下限与 `session-analyzer@0.1.2`，完成 package metadata、两份 README、双语 changelog、产品规格、package metadata 测试与公共 CI 契约的收口。
- 2026-07-31: Normalized all 78 lockfile download URLs to `https://registry.npmjs.org/` and confirmed zero remaining npmmirror URLs. The only dependency-version changes were reviewed security updates: `markdown-it` 14.1.1 to 14.3.0, `linkify-it` 5.0.0 to 5.0.2, and `esbuild` plus its platform packages 0.28.0 to 0.28.1. / 2026-07-31：将 lockfile 中全部 78 个下载 URL 统一到 `https://registry.npmjs.org/`，并确认不再存在 npmmirror URL。唯一的依赖版本变化均为经过审查的安全更新：`markdown-it` 14.1.1 升至 14.3.0、`linkify-it` 5.0.0 升至 5.0.2，以及 `esbuild` 与其平台 package 从 0.28.0 升至 0.28.1。
- 2026-07-31: Verified the production-only and complete dependency graphs with Node.js 24.18.1 and npm 12.0.2 against the official registry; both audits reported zero vulnerabilities. / 2026-07-31：使用 Node.js 24.18.1 与 npm 12.0.2 对官方 registry 验证 production-only 与完整依赖图；两次 audit 均报告零漏洞。
- 2026-07-31: Regenerated the checked-in browser assets after the reviewed dependency updates. The Node.js 24 release gate passed 320 tests plus packaged installation/server smoke, Chromium passed 83 browser tests, and Node.js 22 passed the same 320-test Node suite plus generated-asset verification. / 2026-07-31：在经过审查的依赖更新后重新生成已纳入版本控制的 browser assets。Node.js 24 release gate 通过 320 项测试及安装后 package/server smoke，Chromium 通过 83 项 browser 测试，Node.js 22 通过同一套 320 项 Node 测试及生成资产验证。
- 2026-07-31: Hardened package smoke for the npm 11 array-shaped and npm 12 object-shaped pack manifests, isolated nested smoke commands from inherited publish dry-run state, and normalized the CLI bin target before publication. A final `npm publish --dry-run --tag next` completed through `prepublishOnly` without metadata auto-correction warnings. / 2026-07-31：加固 package smoke，使其兼容 npm 11 的 array-shaped 与 npm 12 的 object-shaped pack manifest；隔离外层 publish dry-run 状态对嵌套 smoke command 的继承，并在发布前规范化 CLI bin target。最终 `npm publish --dry-run --tag next` 完整通过 `prepublishOnly`，且不再出现 metadata 自动修正警告。
- 2026-07-31: Generated and extracted `session-analyzer-0.1.2.tgz`; the 34-entry allowlist, package metadata, byte-for-byte source parity, forbidden-path exclusions, and tested credential/personal-path exclusions all passed inspection. / 2026-07-31：生成并解包 `session-analyzer-0.1.2.tgz`；34 项白名单、package metadata、与源文件逐字节一致性、禁止路径排除，以及已测试的凭据/个人路径排除均通过检查。
- 2026-07-31: Rechecked the official registry immediately before handoff; `npm view session-analyzer version` returned `E404`, so the unscoped package name remained unregistered at that moment. / 2026-07-31：交接前立即复查官方 registry；`npm view session-analyzer version` 返回 `E404`，因此当时该无 scope package 名仍未注册。
- 2026-07-31: Review found that the planned positional tarball publication would bypass the new lifecycle guard. Reproduced with `npm publish .\session-analyzer-0.1.2.tgz --dry-run --tag='next'` under npm 12.0.2: it exited successfully without `prepublishOnly`, `release:check`, or package smoke. Accepted `docs/design-docs/npm-release-runbook.md` and corrected this plan so the candidate is evidence only and publication runs from the clean package root without a positional argument. / 2026-07-31：Review 发现计划中的位置 tarball 发布会绕过新的生命周期 guard。使用 npm 12.0.2 执行 `npm publish .\session-analyzer-0.1.2.tgz --dry-run --tag='next'` 完成复现：命令成功退出，但没有 `prepublishOnly`、`release:check` 或 package smoke。现已接受 `docs/design-docs/npm-release-runbook.md`，并修正本计划：候选制品只作为证据，发布从干净 package root 执行且不带位置参数。
- 2026-07-31: Committed release preparation as `1d676fa`, pushed `v0.1.2-development`, and opened PR #2. Initial CI run `30608906152` passed installed-package smoke on Linux and Windows but exposed three independent gate failures: 11 host-dependent Windows-path assertions in each Linux Node job, CRLF checkout drift for generated bundles in the Windows Node job, and one timing-sensitive keyboard-pagination browser test. / 2026-07-31：将发布准备提交为 `1d676fa`，推送 `v0.1.2-development` 并建立 PR #2。初始 CI run `30608906152` 在 Linux 与 Windows 上均通过安装后 package smoke，但暴露三类独立 gate 失败：每个 Linux Node job 中有 11 项依赖宿主的 Windows path 断言、Windows Node job 中生成 bundle 的 CRLF checkout 漂移，以及一项对时序敏感的键盘分页 browser 测试。
- 2026-07-31: Repaired path handling by selecting `path.win32` or `path.posix` from each absolute path string, retained host semantics for relative paths and legacy raw-ref separators, forced LF checkout for both generated bundles, and made the keyboard-pagination test wait for the committed top viewport and press `End` on the intended pane. Package smoke now pins nested npm operations to `publishConfig.registry` instead of inheriting a maintainer's mirror configuration. Local Windows verification passed the final 321/321 Node release gate plus package smoke, 83/83 browser tests before the test-only stabilization, and the previously failing browser case in five consecutive pre-change plus three consecutive post-change focused runs. The previous candidate artifact is superseded and must be regenerated only after the repaired CI is green. / 2026-07-31：通过根据每个绝对路径字符串选择 `path.win32` 或 `path.posix` 修复 path handling，同时保留相对路径的宿主语义与 legacy raw-ref 分隔符；为两个生成 bundle 强制 LF checkout；并让键盘分页测试等待已提交的顶部 viewport，再在目标 pane 上发送 `End`。Package smoke 现在会把 nested npm 操作固定到 `publishConfig.registry`，不再继承维护者的镜像配置。本地 Windows 验证通过最终 321/321 Node release gate 及 package smoke、测试稳定化前的 83/83 browser 测试，以及原失败 browser case 在变更前连续五次与变更后连续三次的聚焦运行。此前候选制品已失效，只有修复后的 CI 全绿后才能重新生成。
- 2026-07-31: Follow-up CI run `30611582650` passed all five Node and package-smoke jobs but reproduced the same browser failure on Ubuntu after the pane-focus test stabilization. The remaining boundary was platform-native `End` scrolling rather than request timing: the application now owns an unmodified `End` keydown on the focused timeline pane, authorizes the keyboard pagination token, and performs the bottom jump explicitly while leaving interactive descendants untouched. The browser test continues to send a real key and now waits for the exact offset-150 response before checking the appended event count. / 2026-07-31：后续 CI run `30611582650` 的五项 Node 与 package-smoke job 全部通过，但在 pane-focus 测试稳定化后仍于 Ubuntu 复现同一 browser failure。剩余边界是平台原生 `End` 滚动，而不是请求时序：应用现在会接管已聚焦时间线面板上的无修饰键 `End` keydown，授权键盘分页 token，并显式执行跳到底部，同时不影响交互式后代。Browser 测试继续发送真实按键，并在检查追加后的事件数量前等待精确的 offset-150 response。
- 2026-07-31: Local validation for the pane-owned `End` repair passed the focused browser scenario six consecutive times, the complete Chromium suite at 83/83, and the official-registry `release:check` with generated assets current, all 321 Node tests passing, and packaged installation/server smoke passing. `git diff --check` reported no whitespace errors. / 2026-07-31：针对 pane-owned `End` 修复的本地验证中，聚焦 browser 场景连续六次通过，完整 Chromium 套件以 83/83 通过；使用官方 registry 的 `release:check` 也完成通过，包括生成资产保持最新、全部 321 项 Node 测试通过，以及安装后 package/server smoke 通过。`git diff --check` 未报告空白错误。
- 2026-07-31: Hardened the durable runbook and this release plan around two isolated, short-lived npm authentication sessions: publish to `next`, mandatory logout and credential deletion, unauthenticated public verification, then a fresh login for `latest` followed by the same cleanup and proof. This documentation change advances the release commit boundary, so all earlier candidate evidence and final dry-run evidence remain invalid; after the change is committed and CI is green, regenerate the candidate and rerun every final release gate from that exact clean commit. / 2026-07-31：围绕两次隔离、短时的 npm 认证会话加固长期 runbook 与本发布计划：发布到 `next`，强制 logout 并删除凭据，以无认证方式完成公共验证，再为 `latest` 全新登录并执行同样的清理与证明。本次文档变更推进了 release commit 边界，因此更早的候选证据与最终 dry-run 证据继续失效；该变更提交且 CI 全绿后，必须从该精确干净 commit 重新生成候选并重跑全部最终发布 gate。
- 2026-07-31: Pinned the installed runtime dependencies to exact `acorn@8.15.0` and `markdown-it@14.3.0`, moved exact `highlight.js@11.11.1` to `devDependencies` because consumers use the checked-in vendor asset, and added package metadata assertions for that boundary. npm 12 regenerated the lock metadata without changing any resolved package version. Validation passed all 321 Node tests, installed-package CLI/server smoke without Highlight.js installed, all 83 Chromium tests, production-only and complete official-registry audits with zero vulnerabilities, a 34-entry `npm pack --dry-run` manifest, runbook PowerShell parsing, and `git diff --check`. These package and documentation changes advance the release commit boundary again; commit them, require fresh CI, and regenerate the candidate before publication. / 2026-07-31：将已安装的运行时依赖固定为精确的 `acorn@8.15.0` 与 `markdown-it@14.3.0`；由于消费者使用已提交的 vendor asset，将精确的 `highlight.js@11.11.1` 移入 `devDependencies`，并为该边界增加 package metadata 断言。npm 12 重新生成 lock metadata，未改变任何已解析 package 版本。验证通过全部 321 项 Node 测试、不安装 Highlight.js 的安装后 CLI/server smoke、全部 83 项 Chromium 测试、官方 registry 上 production-only 与完整依赖图的零漏洞 audit、包含 34 项的 `npm pack --dry-run` manifest、runbook PowerShell 解析，以及 `git diff --check`。这些 package 与文档变更再次推进 release commit 边界；发布前必须提交它们、要求全新 CI，并重新生成候选制品。
- 2026-08-01: Added an npm 12 default-deny install-script policy: the repository and every CI job enforce `strict-allow-scripts`, exact `esbuild@0.28.1` is the only approved script, `fsevents` is explicitly denied, and metadata tests require a decision for every lockfile `hasInstallScript` entry. A clean npm 12.0.2 install on Node.js 24.18.1 executed only the reviewed esbuild postinstall and left no pending scripts. Validation passed all 323 Node tests plus installed-package CLI/server smoke, all 83 Chromium tests, production-only and complete official-registry audits with zero vulnerabilities, a 34-entry package manifest, all 15 runbook PowerShell blocks, and `git diff --check`. These policy, CI, test, and documentation changes advance the release commit boundary; commit them, require fresh CI, and regenerate the candidate from that exact clean commit before publication. / 2026-08-01：增加 npm 12 默认拒绝的 install-script 策略：仓库与每个 CI job 均强制 `strict-allow-scripts`，只允许精确的 `esbuild@0.28.1` 脚本，明确拒绝 `fsevents`，且 metadata 测试要求 lockfile 中每个 `hasInstallScript` 条目都有对应决策。在 Node.js 24.18.1 上使用 npm 12.0.2 执行干净安装时，仅运行经过审查的 esbuild postinstall，且没有待处理脚本。验证通过全部 323 项 Node 测试及安装后 CLI/server smoke、全部 83 项 Chromium 测试、官方 registry 上 production-only 与完整依赖图的零漏洞 audit、包含 34 项的 package manifest、runbook 中全部 15 个 PowerShell 代码块，以及 `git diff --check`。这些策略、CI、测试与文档变更推进了 release commit 边界；发布前必须提交它们、要求全新 CI，并从该精确干净 commit 重新生成候选制品。
- 2026-08-01: Resolved three release-review findings before candidate regeneration. Both READMEs and the durable runbook now bootstrap exact npm 12.0.2 before any constrained source install while preserving the published CLI's Node.js 22 runtime floor. `THIRD_PARTY_NOTICES.md` reproduces the complete Highlight.js 11.11.1 BSD license for both redistributed vendor assets and is required by package tests and the manifest. Final dist-tag evidence now uses a separate empty userconfig with its own pre-read `ENEEDAUTH` proof and cleanup. Validation passed a clean strict install that ran only reviewed esbuild, all 326 Node tests plus installed-package CLI/server smoke, all 83 Chromium tests, production-only and complete zero-vulnerability audits, a 35-entry dry-run manifest containing the notice and excluding project credentials/internal files, no pending install scripts, all 16 runbook PowerShell blocks, and `git diff --check`. These fixes advance the release commit boundary again; commit them, require fresh CI, and regenerate the candidate only from that exact clean commit. / 2026-08-01：在重新生成候选制品前解决三项 release review finding。两份 README 与长期 runbook 现在都会在任何受约束的源码安装前 bootstrap 精确 npm 12.0.2，同时保留已发布 CLI 的 Node.js 22 运行时下限。`THIRD_PARTY_NOTICES.md` 为两个再分发 vendor 资产完整复现 Highlight.js 11.11.1 BSD 许可证，并由 package 测试与 manifest 强制要求。最终 dist-tag 证据现在使用单独的空白 userconfig，具有读取前独立的 `ENEEDAUTH` 证明与清理流程。验证通过仅执行获批 esbuild 的干净 strict 安装、全部 326 项 Node 测试及安装后 CLI/server smoke、全部 83 项 Chromium 测试、production-only 与完整依赖图的零漏洞 audit、包含 notice 且排除项目凭据/内部文件的 35 项 dry-run manifest、无 pending install script、runbook 中全部 16 个 PowerShell 代码块，以及 `git diff --check`。这些修复再次推进 release commit 边界；发布前必须提交它们、要求全新 CI，并且只能从该精确干净 commit 重新生成候选制品。
- 2026-08-01: PR #3 CI run `30691912695` failed all six matrix jobs before tests because `actions/setup-node@v6` ran its bundled npm 11.16.0 to resolve the requested npm cache; exact npm 12.0.2 had not yet been bootstrapped, so the repository's strict `devEngines` rejected the command with `EBADDEVENGINES`. The aggregate `ci` job then correctly failed. Commit `4496d04` disabled setup-node package-manager caching, moved the exact npm bootstrap to `runner.temp`, and added a metadata regression test plus durable runbook guidance for this ordering. Follow-up run `30692219678` passed Linux Node 22/24, Windows Node 24, Linux/Windows package smoke, the Ubuntu browser suite, and aggregate `ci`. / 2026-08-01：PR #3 的 CI run `30691912695` 中，全部六个 matrix job 均在测试前失败：`actions/setup-node@v6` 为解析所请求的 npm cache，调用了其附带的 npm 11.16.0；此时精确 npm 12.0.2 尚未完成 bootstrap，因此仓库严格的 `devEngines` 以 `EBADDEVENGINES` 拒绝该命令。聚合 `ci` job 随后按预期失败。Commit `4496d04` 禁用了 setup-node package-manager cache，将精确 npm bootstrap 移至 `runner.temp`，并为该顺序增加 metadata 防回归测试及长期 runbook 说明。后续 run `30692219678` 通过 Linux Node 22/24、Windows Node 24、Linux/Windows package smoke、Ubuntu browser suite 与聚合 `ci`。
- 2026-08-01: PR #3 merged as release commit `6304f855a29ce1fa3eade8a74fac2786a7ba7da1`; main CI run `30696311244` passed. From that exact clean commit, final strict install, 326-test release gate, 83-test Chromium suite, both zero-finding audits, 35-file candidate inspection, guarded publish dry run, and candidate/public-tarball hash parity all passed. / 2026-08-01：PR #3 合并为 release commit `6304f855a29ce1fa3eade8a74fac2786a7ba7da1`；main CI run `30696311244` 通过。从该精确干净 commit 执行的最终 strict install、326 项 release gate、83 项 Chromium suite、两类零 finding audit、35 文件候选检查、受保护 publish dry run，以及候选/公共 tarball 哈希一致性均通过。
- 2026-08-01: Maintainer-controlled interactive 2FA publication succeeded as `session-analyzer@0.1.2`. Explicit credential recovery confirmed logout and `ENEEDAUTH`; anonymous Windows public verification passed exact npx, temporary global installation, CLI, packaged server root, and `/api/state`. The maintainer waived a second direct Linux public install with Ubuntu CI plus byte identity retained as substitute evidence. / 2026-08-01：由维护者控制的交互式 2FA 发布以 `session-analyzer@0.1.2` 成功完成。显式 credential recovery 确认 logout 与 `ENEEDAUTH`；匿名 Windows 公共验证通过精确 npx、临时全局安装、CLI、packaged server 根页面与 `/api/state`。维护者豁免第二次 Linux 直接公共安装，并保留 Ubuntu CI 与字节一致性作为替代证据。
- 2026-08-01: Anonymous reads found both inaugural `next` and registry-required `latest` at `0.1.2`, so redundant promotion was skipped. Annotated `v0.1.2` was pushed at the exact release commit, the bilingual non-prerelease GitHub Release was published, final npm/Git/GitHub consistency passed, and the plan was closed. / 2026-08-01：匿名读取发现首发 `next` 与 registry 必需的 `latest` 均指向 `0.1.2`，因此跳过多余 promotion。Annotated `v0.1.2` 已推送到精确 release commit，双语非 prerelease GitHub Release 已发布，npm/Git/GitHub 最终一致性通过，本计划完成收尾。
