# Context reading navigation / 上下文阅读导航

## Review follow-up: bounded terminal origin directory (2026-09-23) / Review 跟进：有界终端起点目录

Reproduced the 5001-request detail-contract failure before the fix. Origin details now project only the first 128 confirmed continuations and add a localized displayed/total notice explaining sequential access to remaining records. Full relation membership and previous/next links are unchanged; shared detail limits are not raised. Synthetic 128/129/5001-request tests validate complete detail DTOs in both locales and links at the directory boundary and chain ends. Browser coverage crosses from request 128 to 129 and returns in Timeline and Trajectory. / 修复前复现 5001 条请求导致详情契约失败。起点详情现在仅投影前 128 条已确认后续请求，并以本地化提示说明展示数量／总数及如何顺序访问剩余记录。完整关系成员和前后链接不变，不提高共享详情限制。128／129／5001 条合成请求测试验证两种语言的完整详情 DTO、目录边界及链首尾链接；浏览器覆盖 Timeline 与 Trajectory 中从第 128 条进入第 129 条并返回。

Validation: 123 focused Node tests and 6 browser tests passed, as did build, generated-asset and whitespace checks. Full suites were not rerun. The local server was restarted as PID 23124 with the same project and Codex root; HTTP passed, indexing remained running with empty error/code, and post-restart real-history reading/counts remain unverified. Changes remain uncommitted. / 验证：123 项聚焦 Node 测试及 6 项浏览器测试通过，构建、生成资产与空白检查通过，未重跑完整套件。本地服务以 PID 23124 重启，保留项目及 Codex 根；HTTP 通过，索引仍在运行且错误／错误码为空，重启后的真实历史阅读／数量尚未核验。改动尚未提交。

## Latest review follow-up: recorded paths through file actions (2026-09-23) / 最新 Review 跟进：文件入口贯穿记录路径

Reproduced the whitespace identity failure in both locales and both presentations before changing production code. Patch files and touched-file entries now carry `recordedPath` separately from display labels, including patch-input and canonical touched-file fallbacks. The additive detail contract validates the field as a string, localization preserves it and does not translate filenames, and both file-activity buttons query by this identity. Query normalization remains unchanged. / 修改生产代码前，在两种语言、两种呈现中复现空白路径身份丢失。补丁文件与涉及文件条目现在独立于显示标签携带 `recordedPath`，包括补丁输入及规范涉及文件回退。新增详情契约字段校验为字符串，本地化保留它且不翻译文件名，两个文件活动按钮均使用该身份查询。查询规范化保持不变。

End-to-end fixtures start from synthetic `patch_apply_end` records and click both buttons, checking the actual HTTP parameter and exact returned event IDs. They cover trailing whitespace versus a different plain filename, absolute unresolved parent segments versus relative equivalent spelling and a different normalized file, and `Status` as a literal filename. This verifies identity across parsing, detail validation/sanitization/localization, rendering and querying, rather than only testing the query helper. Existing parser tests also verify original absolute paths and input fallback identities. / 端到端 fixture 从合成 `patch_apply_end` 开始，点击两个按钮，检查实际 HTTP 参数及返回事件的精确 ID。覆盖尾随空白与不同的普通文件名、绝对未解析父段与相对等价写法及不同的折叠后文件，以及字面文件名 `Status`。这验证身份贯穿解析、详情校验／清洗／本地化、渲染及查询，而不只测试查询 helper。既有解析测试也核验原始绝对路径及输入回退身份。

Validation on the final code: all 1218 Node tests and 25 relevant browser tests passed; generated assets and whitespace checks passed. The full browser suite was not rerun. Restarted the existing local server as PID 15852 with the same project and Codex root; HTTP and exact generated-bundle delivery passed. The bounded real-index check remained running with empty error/code, so final counts, diagnostics and real-history reading after restart remain unverified. Changes remain uncommitted. / 最终代码验证：全部 1218 项 Node 测试及 25 项相关浏览器测试通过，生成资产与空白检查通过；未重跑完整浏览器套件。既有本地服务以 PID 15852 重启，保留项目及 Codex 根；HTTP 和精确生成 bundle 交付通过。有界真实索引检查仍在运行，错误／错误码为空，因此重启后的最终数量、诊断及真实历史阅读仍未核验。改动尚未提交。

## Scope and baseline / 范围与基线

Continue from reviewed collaboration round trips, committed as `952001b` on `feat/context-reading-navigation`. Implement B/C and concrete follow-on D/E/F entrances using existing source facts and shared queries. / 从已 review 的协作阅读往返继续，基线为 `feat/context-reading-navigation` 上的 `952001b`。基于既有来源事实与共享查询，实现 B/C 以及 D/E/F 中可确认的后续入口。

## Work / 工作

All items below are implemented and validated within the stated boundaries. Persistent bookmarks/deep links remain a separate increment: an in-memory index revision is not a durable source-snapshot identity across server restarts, so this change does not publish misleading approximate links. / 下列工作已在所述边界内实现并验证。持久书签／深链接仍是独立增量：内存索引 revision 不是跨服务重启的持久来源快照身份，因此本轮不发布可能误导的近似链接。

- B: patch-local file directory and session file activity, paginated by the shared query layer; project-search handoff. Clearly distinguish recorded path mentions from patch records; never read the current disk as history. / 补丁内文件目录与由共享查询层分页的会话文件活动，提供项目搜索入口。区分已记录路径提及与补丁记录，不把当前磁盘当作历史。
- C: originating terminal command to admitted continuations, previous/next related requests; retain strict admission and independent events. / 起始终端命令到准入后续请求，以及前后相关请求；保留严格准入与独立事件。
- D/E: event-reference and evidence entrances share reading return; expose exact Code Mode/workflow members and owners without inferred execution graphs. / 事件引用与证据入口复用阅读返回；展示精确 Code Mode／workflow 成员与归属，不推断执行图。
- F: confirmed session relationships and sibling entrances with scope checks and reading return; assess exact local link identity separately from session-level relationships. / 已确认会话关系与兄弟入口提供范围核验及阅读返回；精确本地链接身份与会话级关系分别处理。

## Acceptance / 验收

Retain query/profile/presentation/viewport/focus snapshots and failure recovery from A. Test combined filtered/unloaded targets, mobile surfaces, repeated actions, delayed requests, stale responses, empty and failed lists, and localized identity. Run relevant Node/browser tests, rebuild/check generated assets, inspect diffs and synchronize bilingual spec/design. Backend changes require local development server restart and bounded readiness checks. / 保留 A 的查询／策略／呈现／视口／焦点快照及失败恢复。组合测试过滤／未加载目标、移动端界面、重复动作、延迟请求、过时响应、空／失败列表和本地化身份。执行相关 Node／浏览器测试，构建并核验生成资产，审查 diff，同步双语规格／设计。后端修改后重启本地开发服务并进行有界就绪检查。

## Results / 结果

- Full Node suite on the initial implementation before review follow-ups: 1213/1213 passed. Shared query ownership, localization, package allowlist, terminal admission and DeepSeek relation tests are included. / Review 跟进前的初始实现完整 Node 套件 1213/1213 通过，包含共享查询归属、本地化、安装包清单、终端准入与 DeepSeek 关系测试。
- Full browser run: 274 tests, initially 270 passed. Corrected one real regression (clickable file paths lost search highlighting), two old selectors expecting layerless event refs, and a mobile test that switched surfaces before search navigation settled. The resulting layer-aware route adds one indexed Map get; its instrumented test still proves zero canonical array scans. All 31 affected browser tests subsequently passed, including the final focused rerun of that instrumentation assertion. The full browser suite was not repeated after the focused fixes. / 完整浏览器轮次 274 项，初始 270 项通过。修正一项实际回归（可点击文件路径丢失搜索高亮）、两处仍假定事件引用无层的旧 selector，以及搜索导航未结束就切换界面的移动端测试。新跨层路由增加一次索引 Map get，量测测试仍证明没有 canonical 数组扫描。之后 31 项受影响浏览器测试全部通过，包括最后对该量测断言的定向重跑；聚焦修复后未再次重复完整浏览器套件。
- Build, generated assets and whitespace checks passed. Package manifest tests passed; installed-package/release gates were not run. / 构建、生成资产与空白检查通过；安装包清单测试通过，未运行安装后包 smoke／发布门槛。
- Restarted the existing development server on port 17890, preserving `G:\vibe\session-analyzer` and the existing Codex root. PID 22736 serves the exact final bundle. The bounded real-index check remained `running`, error/code empty, with about 2.7 GB of candidates; final count and real-history reading remain unverified. / 已在 17890 端口重启既有开发服务，保留 `G:\vibe\session-analyzer` 及原 Codex 根。PID 22736 提供与最终构建一致的 bundle。有界真实索引检查仍为 `running`，错误／错误码为空，候选记录约 2.7 GB；最终数量与真实历史阅读尚未核验。
- The reviewed A increment is committed as `952001b`. This subsequent B/C/D/E/F work remains uncommitted for review on the same branch. / 已 review 的 A 增量提交为 `952001b`；后续 B/C/D/E/F 工作保留在同分支工作区，尚未提交，供 review。

## Review follow-up: entrance context and path equivalence / Review 跟进：入口上下文与路径等价

Reproduced stale related-Session/project-file navigation and missing dot/separator path variants before fixing them. Collaboration, related-Session and cross-Session file entrances now share one guarded load-and-navigate continuation. It verifies the complete destination reading context before selecting an event or changing the mobile surface, and suppresses errors from obsolete loads. This removes the duplicated partial guards that caused the regression. / 修复前复现相关会话／项目文件导航过时覆盖，以及点段／分隔符路径变体漏匹配。协作、相关会话和跨会话文件入口现在共用同一受保护的加载与导航续接流程，在选择事件或改变移动端界面前核验完整目标阅读上下文，且忽略过时加载错误，消除导致回归的重复、不完整检查。

File activity normalizes query and recorded spelling symmetrically, preserving original strings, POSIX/Windows case behavior, external roots and UNC identity. Only redundant separators and `.` are removed; relative `..` remains unresolved. Tests verify exact exclusion, pagination and one count per event even when it records multiple equivalent spellings. / 文件活动对查询与记录写法对称规范化，保留原始文本、POSIX／Windows 大小写规则、外部根和 UNC 身份；仅移除冗余分隔符与 `.`，相对 `..` 仍不解析。测试核验精确排除、分页，以及单个事件记录多个等价写法时只计一次。

Validation: 25 focused Node tests and 27 relevant browser tests passed. After centralizing the complete load-and-navigate continuation, 10 affected browser tests passed again, including the three-entry matrix with Raw, query, structured-filter, presentation and obsolete-failure cases (15 scenarios). Build, generated assets and whitespace checks passed. Full suites were not rerun for this follow-up. The existing local server was restarted with the same project/source root (PID 16252); exact bundle delivery passed, while the bounded indexing check remained pending without a job error/code. Real-history reading was not reverified. / 验证：25 项聚焦 Node 测试和 27 项相关浏览器测试通过；完整加载与导航续接流程集中后，再次通过 10 项受影响浏览器测试，包括三个入口对 Raw、查询、结构化筛选、呈现和过时失败的矩阵（15 个场景）。构建、生成资产和空白检查通过。本次跟进未重跑全量套件。既有本地服务保留项目／来源根重启（PID 16252），精确 bundle 交付通过，有界索引检查仍在等待且任务错误／错误码为空；未重新核验真实历史阅读。

## Review follow-up: target fetch and recorded path identity / Review 跟进：目标获取与记录路径身份

Reproduced both failures before changing production code: a delayed unloaded target overwrote a newer search, and absolute parent-segment paths matched the wrong file. The guard now belongs to `navigateToLayerEvent` through layer loading and event-envelope fetching, up to its final UI commit. Its own layer transition and selection intent remain valid while newer reading context invalidates the continuation. Current errors remain visible; obsolete success and failure responses cannot select, reveal, scroll or change the surface. / 修改生产代码前复现两项失败：延迟的未加载目标覆盖新搜索，以及包含父段的绝对路径匹配到错误文件。保护现在由 `navigateToLayerEvent` 持有，贯穿层加载、事件封装获取，直至最终提交界面变化；自身层切换与选择意图保持有效，新的阅读上下文则使续接失效。当前错误仍可见；过时成功或失败响应不能选择、展示、滚动或改变界面。

File activity now derives identity directly from recorded spelling, independently of display formatting. Repository-prefix removal is lexical and respects segment boundaries; absolute and relative `..`, filename whitespace, POSIX case and Windows/UNC identity are preserved. No filesystem lookup or symlink equivalence is inferred. / 文件活动直接从记录写法生成身份，独立于显示格式化。仓库前缀移除按文本及路径段边界进行，保留绝对与相对 `..`、文件名空白、POSIX 大小写和 Windows／UNC 身份，不访问文件系统或推断符号链接等价关系。

Prevention and validation: separate asynchronous barriers are tested rather than assuming Session loading is the final await. Fourteen target-fetch scenarios cover same-Session and project-file entrances with unchanged context, query/filter/presentation/mobile changes, and current/obsolete errors. The broader regression run passed 31 Node tests and 46 browser tests, including earlier collaboration return regressions, cross-layer evidence, file navigation and saved related-Session positions. Build, generated assets and whitespace checks passed; full suites were not rerun. / 防回归与验证：分别测试各异步等待边界，不假定会话加载是最后一次等待。14 个目标获取场景覆盖同会话与项目文件入口，以及上下文不变、查询／筛选／呈现／移动端界面变化、当前／过时错误。扩展回归通过 31 项 Node 测试及 46 项浏览器测试，包含此前协作返回回归、跨层证据、文件导航和相关会话阅读位置恢复。构建、生成资产和空白检查通过，未重跑全量套件。

Restarted the existing local server as PID 21280 with the same project, Codex source and root. HTTP and exact generated-bundle delivery passed; the bounded real-index check remained running with empty error/code. Final session count, diagnostics and real-history reading after this restart remain unverified. Changes remain uncommitted on the existing feature branch. / 既有本地服务以 PID 21280 重启，保留项目、Codex 来源及根。HTTP 与精确生成 bundle 交付通过；有界真实索引检查仍为运行中，错误／错误码为空。此次重启后的最终会话数、诊断和真实历史阅读尚未核验。改动保留在既有功能分支，尚未提交。
