# Background terminal closeout / 后台终端收尾

## Follow-up: cross-section PID duplication / 追补：跨 section PID 重复

The user's real-history screenshot exposed a missed case: the original Code Mode browser fixture returned only output, so it did not exercise a repeated response session_id. Native and single-tool Code Mode Detail now compose display-only metadata copies into one panel. Equal IDs appear once; differing IDs retain an explicit Response Process ID; missing request IDs stay unknown. Input, actual stdout, Inspector evidence and Raw References are preserved. No continuation algorithm, canonical status, count, search ownership or folding rule changed. / 用户真实截图暴露了遗漏：原 Code Mode 浏览器 fixture 只返回 output，未覆盖响应 session_id 重复。现将 native 与 single-tool Code Mode Detail 的 metadata 展示副本组合为一个面板；相同 ID 仅显示一次，不同 ID 明确标为响应进程 ID，请求 ID 缺失仍为未知。输入、真实 stdout、Inspector 证据与 Raw References 保留；未改变 continuation algorithm、canonical status、计数、搜索归属或折叠规则。

Expanded acceptance covers same/different/missing/zero IDs, native receipts, empty output, unknown/invalid envelopes, nonmutation and idempotence, both locales, 960/1440px overflow, Trajectory-to-Inspector-to-Raw reading, and mixed direct/variable emission ordering and rejection. A first full Node run caught a forbidden direct require in the dependency-injected detail builder; fixed by injection without weakening the architecture test. The initial Raw References assertion needed to await loaded content rather than just its container. / 扩大验收覆盖相同／不同／缺失／零 ID、native receipt、空输出、未知／无效 envelope、不变性与幂等性、中英双语、960／1440px 溢出、Trajectory 至 Inspector 至 Raw 阅读，以及混合 direct／variable emission 的顺序与拒绝。首轮完整 Node 测试发现 detail builder 不允许直接 require，已恢复依赖注入，未放宽架构测试。初版 Raw References 断言也已改为等待内容加载，而不是只等待容器。

Final local evidence: focused Node 116/116; focused Chromium 6/6; full npm test 1127/1127; full Chromium 239/239 (381111ms), no skips. Build consistency, all 22 changed/untracked JS syntax checks and diff checks passed. Final installed-package smoke passed Codex, Claude Code and DeepSeek including zstd. Logs: output/playwright/pid-dedup-{focused,focused-browser-final,npm-test-final,chromium,package-final}.log. No CI claim. / 最终本地证据：上述 focused／完整测试全通过，无跳过；构建一致性、22 个修改／新增 JS 语法及 diff 检查通过；最终安装包 smoke 覆盖三个来源及 zstd。日志位于上述 ignored 路径，未声称 CI 通过。

Restarted the verified checkout server as PID 23156 at http://127.0.0.1:17890, same repository and Codex root C:/Users/Yijia/.codex. Pinned job 1 exceeded the initial window, then succeeded in 237919ms: 728 sessions, 385130 logical events, 495186 raw records; error/errorCode empty; sourceDiagnostics totalCount 0, counts {}, samples [], truncatedCount 0. A 2542.2MiB history-capacity warning was observed, without OOM or heap override. / 核实并重启同一 checkout 服务为 PID 23156，项目和来源根不变。固定 job 1 超过初次窗口后成功，耗时及数量如上；错误字段为空，diagnostics 四项如上。观察到大历史容量提示，无 OOM，未调 heap。

Read the exact screenshot session 01a08f9f-3a6c-7372-b337-654c3ff2cc91: polls at 16:51:25 and 16:51:51 each show PID 49497 once with wall times 5.0025895 and 5.0057594. JSON-looking cpu-tool-dense stdout remains literal output. Expanded Associated result and read Raw References 350/352 for call_7NJ2D6zsnqxttVoz9XaVz6y2. Real captures: output/playwright/pid-dedup-real-{poll,evidence}.png; synthetic captures: output/playwright/pid-dedup/. / 已阅读截图中的真实会话与两条 poll，各自只显示一次 PID，保留对应耗时及 JSON 形状 stdout；展开关联结果并读取第 350／352 行请求与响应。真实及合成截图路径如上。

Whole-worktree review found no remaining blocker. Continuation algorithm hash remains a816fbc5204cbcf8ee4644f9915650c5ad5ffc3d. Ancestry and backup remain unchanged; no staging, commit, push or merge. Keep the prior three-commit recommendation, including this correction and its tests with presentation changes. / 整体 worktree review 未发现剩余 blocker，算法 hash、ancestry 与备份不变；未暂存、提交、推送或合并。仍建议前述三类 commit，本次修正及测试归入 presentation。

## Scope / 范围

Normalize ancestry, enforce referenced-only origins, simplify presentation, and validate locally. Preserve the already reviewed evidence/model and continuation algorithm. / 规范 ancestry，约束 origins 仅保存被引用项，简化展示并完成本地验证。保留已 review 的 evidence／model 与 continuation algorithm。

## Completed stages / 已完成阶段

- Phase 0: fetched main `507ef27028f1b16b23d004b83b5e5faf5d361859`; its tree equals `a39052a` (`cff1bdea48ebc701ac22ac52b0bbb29f8ed4cdc0`). Feature and local main now use squash ancestry. Binary patches before/after match; all five untracked blobs match the backup. Backup branch: `backup/background-terminal-pre-normalization-20260912` (stash commit, including untracked third parent). / 已 fetch main，确认与旧 HEAD tree 相同；feature 与本地 main 已使用 squash ancestry。迁移前后 binary patch 相同，5 个 untracked blob 与备份匹配。备份分支保留 stash commit 及包含 untracked 的第三 parent。
- Phase 1: bidirectional contract validation; orphan/dangling rejection plus empty/shared-origin acceptance. Initial focused suite: 51 passed. No algorithm change. / 双向 contract 校验，拒绝 orphan／dangling，接受空映射及共享 origin。初次 focused suite 51 项通过，未改 algorithm。
- Phase 2: real Chromium before/after captures in `output/playwright/{before,after}`. Untitled Process ID, input-only escaped input, typed terminal response display, Inspector evidence retention, and nonduplicated Trajectory labels. / 真实 Chromium 前后截图位于上述本地目录。无标题 Process ID、仅 input 显示转义输入、typed terminal response 展示、Inspector 保留证据、Trajectory label 去重。

## Final local validation / 最终本地验证

- Focused Node tests: 114/114; focused Chromium scenarios: 3/3. Full `npm test`: 1125/1125, no skips; full `npm run test:browser`: 236/236, no skips. / Focused Node 114/114，定向 Chromium 3/3；完整 Node 1125/1125、完整 Chromium 236/236，均无跳过。
- `npm run build:check`: generated assets current. Syntax checks: all 22 changed/untracked JavaScript files passed. `git diff --check`: passed. / 构建产物一致；22 个变更／新增 JS 文件语法检查通过，diff 检查通过。
- Installed-package smoke: first sandbox run failed with registry `EACCES`; the same command with escalated network access passed for Codex, Claude Code, and DeepSeek Harness including zstd. Temporary package artifacts were cleaned by the smoke script. / 安装包 smoke 首次因 sandbox registry `EACCES` 失败，同命令提升联网权限后通过，覆盖 Codex、Claude Code、DeepSeek Harness（含 zstd）。Smoke 脚本已清理临时安装包产物。
- Environment: Node `v24.18.1`, npm `12.0.2`; checkout `G:/vibe/session-analyzer`, HEAD `507ef27` plus the uncommitted changes. No CI run or CI success is claimed. / 环境与 checkout 如上，包含未提交修改；未运行或声称 CI 成功。

## Persistent server acceptance / 持久服务验收

Restarted the verified checkout server on `http://127.0.0.1:17890/` (PID 21112), keeping target `G:/vibe/session-analyzer`, source `codex`, root `C:/Users/Yijia/.codex`. Job 1 was still running at the initial bounded deadline, then succeeded in 165409 ms: 728 sessions, empty error/errorCode. Diagnostics: totalCount 0, counts {}, samples [], truncatedCount 0. The separate `SESSION_ANALYZER_LARGE_TRANSCRIPT_HISTORY` warning was observed for approximately 2.5 GB of matching transcripts; no heap override was used. / 已重启并保留相同项目／来源／根。首次有界检查仍 running，随后 job 1 在 165409 ms 成功，728 sessions，error／errorCode 为空。Diagnostics 四项如上。另观察到约 2.5 GB 转录对应的容量提示，未调整 heap。

Read real session `01a09030-1cd5-7533-ac74-28e90be1a4f2` in the browser. Its Code Mode poll at source line 501 displayed Process ID 39335, exit code 0, and multiline historical test output; opened Associated result and both Raw References (lines 501 and 503). Document horizontal overflow was false. The historical output's 1113 tests are not this closeout's 1125-test result. Synthetic before/after captures separately cover native poll/input, origin navigation, Code Mode input/poll and Trajectory. / 浏览器实际阅读上述真实 session，第 501 行 Code Mode poll 显示 Process ID 39335、退出码 0 及多行历史测试输出；已打开 Associated result 与第 501／503 行两条 Raw References，无文档横向溢出。历史输出中的 1113 测试不是本轮 1125 项结果。合成前后截图另覆盖 native poll／input、origin 导航、Code Mode input／poll 与 Trajectory。

Local logs and screenshots: `output/playwright/` (ignored, not package content). Full-suite logs: `npm-test.log`, `chromium.log`, `package-smoke.log` (initial failure), `package-smoke-retry.log` (pass). Real read captures: `local-real-terminal.png`, `local-real-evidence.png`, `local-real-raw-references.png`. / 本地日志／截图在 ignored 目录，不进入安装包；上述文件分别保留完整测试、首轮失败、重试成功与真实阅读证据。

## Review and commit boundaries / Review 与提交边界

No blocking issue found in this closeout diff. The continuation algorithm blob still equals the backup (`a816fbc5204cbcf8ee4644f9915650c5ad5ffc3d`). Preserve three reviewable commits: existing native relation work plus referenced-only contract; existing Code Mode direct-emission coverage; compact presentation plus response/browser tests, generated asset and bilingual docs. Shared-file changes require hunk-level separation. Git normalization itself requires no content commit. No staging, commit, push or merge was performed after normalization. / 本轮 diff 未发现 blocker；algorithm blob 与备份相同。建议三个可 review commit：既有 native relation 加 referenced-only contract；既有 Code Mode direct-emission coverage；紧凑展示及响应／浏览器测试、生成产物、双语文档。共享文件需按 hunk 划分；Git normalization 无需内容 commit。迁移后未 stage／commit／push／merge。
