# v0.2.0 public-package browser acceptance / v0.2.0 公共安装包浏览器验收

Date: 2026-09-10. Result: the M5 reading journey passed in English and Chinese on this Windows workstation, with the known layout limitations below. / 日期：2026-09-10。结果：本 Windows 工作站上，中英文 M5 阅读流程通过；保留下述已知布局限制。

Related plan: [README user entry and discovery](2026-09-08-readme-user-entry-and-discovery.md). Historical publication evidence: [v0.2.0 release](../completed/2026-09-09-v0.2.0-release.md). / 相关计划及历史发布证据见上述链接。

This is an acceptance attachment to M5, not a separate implementation plan. Keep it alongside the owning plan and archive both together when M5 is complete, updating relative links then. / 本文是 M5 的验收附件，不是独立实施计划。随所属计划存放，待 M5 完成后一起归档，并同步更新相对链接。

## Identity and isolation / 身份与隔离

- Package: public npm `session-analyzer@0.2.0`, downloaded with `npm pack session-analyzer@0.2.0 --registry https://registry.npmjs.org/` into a new isolated directory/cache, then installed from that tarball with `--ignore-scripts`. No package source or generated assets were modified. / 安装包：公共 npm `session-analyzer@0.2.0`，以上述命令下载至新建隔离目录／缓存，再以 `--ignore-scripts` 从 tarball 安装；未修改包内源码或生成资源。
- Tarball SHA-256: `3754f694f1d32743d6c8eaa71e88e73200abc4f9e12d102799ea0f4eee43385b`, matching the published artifact recorded in the release plan. This run downloaded and hashed the bytes; it did not repeat signature/provenance verification. / Tarball SHA-256 如上，与发布计划记录的公共制品一致。本次重新下载并计算字节 hash，未重复签名／provenance 验证。
- Runtime: Windows, Node.js `v24.18.1`, npm `12.0.2`, Chrome headless `152.0.0.0`, viewport `1600 × 1000`. Browser automation used the checkout's installed Playwright CLI; application execution used only the isolated public package. / 运行环境如上；浏览器自动化使用 checkout 已安装的 Playwright CLI，应用本身仅运行隔离的公共安装包。
- Launch: from the isolated consumer directory, `npx --offline session-analyzer@0.2.0 --source codex --codex-home <synthetic-codex-home> --port 17926`. `--help` passed first. The package had already been installed from the public tarball; offline launch deliberately resolved that local installation. No `--repo` was supplied: project selection was performed through the browser. / 启动：在隔离消费目录执行上述命令；先检查 `--help`。安装包已从公共 tarball 安装，离线启动明确解析该本地安装。未传 `--repo`，通过浏览器选择项目。
- Listener command-line inspection confirmed the server entry under `consumer/node_modules/session-analyzer/server.js`. HTTP bytes for `/`, `/styles.css`, `/assets/app.js`, `/vendor/highlightjs/highlight.min.js`, and `/vendor/highlightjs/github.min.css` matched installed package files by SHA-256. / 监听进程命令行确认服务器入口位于隔离安装目录；上述五个 HTTP 资源的 SHA-256 与安装包文件一致。
- Data: unchanged scenario and materializer copied from checkout `f4ee929f1d3835a6021db1545488024b0244c57b` into an isolated helper tree, generating a new synthetic `acme/task-board` project and Codex home. Existing `output/showcase` and private transcripts were not used. This is directory/cache/browser isolation, not a fresh OS or VM. / 数据：从上述 checkout 复制未修改的场景与生成器至隔离辅助目录，生成新的合成项目和 Codex 根；未使用既有 `output/showcase` 或私人转录。这是目录／缓存／浏览器隔离，不是全新操作系统或虚拟机。

## Indexing / 索引

Project chooser exposed Codex, Claude Code, and DeepSeek Harness and discovered the intended synthetic project. Selection job `1` settled as `succeeded`, with empty `error` and `errorCode`; pinned-job readback and `/api/state` agreed on the intended source/root/project. / 项目选择器显示三个来源，并发现预期合成项目。选择任务 `1` 为 `succeeded`，`error` 与 `errorCode` 均为空；固定任务回查与 `/api/state` 的来源／根／项目一致。

- Six Sessions: five ordinary plus one review-derived child; six candidate/indexed files, zero skipped or unknown files. / 六个会话：五个普通会话、一个 review 派生子会话；六个候选／已索引文件，跳过和未知文件均为零。
- 28 Logical Events, 77 Raw Records. / 28 个逻辑事件，77 条原始记录。
- `sourceDiagnostics`: `totalCount: 0`, `counts: {}`, `samples: []`, `truncatedCount: 0`. This reports implemented diagnostic coverage, not universal source-health proof. / 诊断结果如上，仅代表实现覆盖内的观测，不证明所有来源情况均健康。

## Browser results / 浏览器结果

Interactions used visible controls and condition-based waits. DOM identifiers and rendered text supplemented screenshots; no mocked API responses, internal application-state mutations, or CSS modifications were used. The documented `sessionAnalyzer.locale` preference plus reload selected each language in the isolated browser. / 操作使用可见控件及按条件等待，以 DOM identity 和呈现文本补充截图；未 mock API 响应、修改应用内部 state 或 CSS。通过隔离浏览器中的既有 `sessionAnalyzer.locale` 偏好及 reload 选择语言。

| Journey / 流程 | Evidence and outcome / 证据与结果 |
| --- | --- |
| Timeline → Trajectory → Timeline | A is `11111111-1111-4111-8111-111111111111`, `Add project-wide search navigation`. All eight ordered Main event IDs matched across presentations. User request, failed-command evidence, and final assistant summary were readable; the five-call tool group expanded, and selecting the passing command in Trajectory exposed `12 tests passed`. Passed in both locales. / A 的八条 Main event ID 和顺序在两种呈现中一致；用户请求、失败命令证据、最终助手总结可读；五调用工具组可展开，在轨迹中选中成功命令能读到结果。双语通过。 |
| Operation → Raw → return | First patch updates `src/browser/search-navigation.js` and `test/browser/search-navigation.test.js`; highlighted code includes `registerSearchMatch(target)`. Supplemental detail reports `Patch applied; search targets now keep their selected event.` Raw refs show request/output rows 9 and 10, with `call-apply-patch-4`; Back returns to the event. Passed in both locales. / 首个补丁的两个文件、高亮代码、结果和原始请求／输出相互对应，返回控件可回到事件。双语通过。 |
| A → project search → B → project results | Query `npm test -- project-switch` has zero targets in A; opening Search options shows `No matches` / `无匹配结果`. Selecting Entire project gives exactly one matching Session and one matching event. Opening B (`22222222-2222-4222-8222-222222222222`, `Fix stale project rows after source switch`) reveals its command, `Project switch suite`, `8 tests passed`, and both neighboring messages: `Fix stale project rows after switching transcript sources.` / `Project rows now refresh from the active transcript source.` Return restores project results. Passed in both locales. / A 无命中，项目范围恰好一个会话和事件命中；打开 B 后命令、结果及前后消息可读，并能返回项目结果。双语通过。 |

This is one synthetic Codex journey in two locales on one workstation, not a fresh cross-platform matrix or complete Claude/DeepSeek browser acceptance. The existing release evidence remains the source for those separate checks. / 本次为单工作站上、两种语言的一条合成 Codex 流程，不是新的跨平台矩阵，也不是 Claude／DeepSeek 完整浏览器验收；其他检查仍以既有发布证据为准。

## Observations and remaining work / 观察与剩余工作

- Known debt #23 reproduced: right-side topbar controls clip at this viewport; project drill-down exposes three return controls in the center pane and another in the inspector. Reading and tested navigation remained functional. No fix was mixed into acceptance, and these screenshots are not evidence of fully accepted responsive layout. / 已复现债务 #23：该视口下顶部右侧控件被裁切；项目下钻后中间面板有三个返回入口，检查器中另有一个。阅读及所测导航仍可用；未在验收中混入修复，截图不代表响应式布局已全面验收。
- Two initial `/api/state` HTTP 409 console entries occurred before project selection, as documented. After selection, inspected console output contained no additional application errors; the Chinese reload's console reported zero errors/warnings. / 选择项目前出现两条指南已说明的 `/api/state` HTTP 409 控制台记录；选择后所查控制台无额外应用错误，中文 reload 后控制台为零错误／警告。
- Three exploratory locator mistakes were corrected before successful steps: expecting Timeline primary message text in supplemental detail, waiting for `No matches` before opening its popover, and treating an empty HTML `open` attribute as closed. These were automation errors, not product failures. / 成功执行前修正了三处探索性定位错误：在补充详情查找 Timeline 主体消息、未打开弹层便等待其中的无匹配文案，以及把空字符串的 HTML `open` 属性误判为关闭。它们属于自动化错误，不是产品失败。
- All 16 distinct version-pinned README document/media targets were retrieved successfully through their raw GitHub URLs. Existence was also checked against the local release tag. The pinned READMEs and startup guide still contain pre-publication wording, so M5 documentation alignment remains open despite reachable links. GitHub rendered-page anchor behavior was not separately exercised. / 16 个去重后的 README 固定版本文档／素材目标均通过 GitHub raw URL 成功读取，也核对了本地 release tag 中的存在性；固定版本 README 和启动指南仍有发布前措辞，因此文档对齐继续开放。未另行验证 GitHub 渲染页面的锚点行为。
- No post-release discovery observation was performed. Completing this browser journey does not close M5 as a whole. / 未执行发布后发现效果观察；浏览器流程完成不等于整个 M5 完成。

## Evidence and reproduction / 证据与复现

Local disposable evidence is retained under `output/playwright/m5-public-0.2.0-20260910/` (gitignored): tarball and installed package, synthetic source tree, server logs, Playwright snapshots, exploratory snippets, `artifacts/identity-and-links.json`, and 14 screenshots (`en-*` / `zh-*`: timeline, trajectory, operation, raw, a-no-match, project-result, b-reading). The JSON records resource/link/screenshot hashes and indexing results. This document is the durable sanitized result; local artifacts are supplementary and are not npm contents or promoted README media. / 本地可丢弃证据保留在上述忽略目录，包括安装包、合成数据、日志、快照、探索脚本、身份／链接 JSON 和 14 张截图；JSON 记录资源／链接／截图 hash 及索引结果。本文是长期保存的脱敏结论，本地工件为补充，不进入 npm 包或替换 README 素材。

Cleanup: the named browser and verified acceptance listener were stopped after collection; port `17926` was confirmed no longer listening. Existing services were left alone (the initially considered port `17892` was occupied and was not used). No evidence files were deleted. / 清理：收集完成后已关闭命名浏览器及经身份核验的验收服务，确认 `17926` 不再监听；未触碰既有服务（最初考虑的 `17892` 已占用，因此未使用）。未删除证据文件。

To reproduce, use a new run directory, download and verify the exact public tarball, install it there, generate the same scenario in a separate tree, and start that installed CLI with the synthetic root and a free port. Confirm listener/asset identity and the bounded indexing result, then follow the three rows above in each locale using the [reading](../../../showcase/captures/readme/reading.json) and [project-search](../../../showcase/captures/readme/project-search.json) storyboards. Do not reuse stale element refs or assume the exploratory snippets form a standalone regression suite. / 复现时使用新的运行目录，下载核验精确公共 tarball 并隔离安装，在独立目录生成同一场景，用合成根及空闲端口启动已安装 CLI。核验监听进程／资源身份和有界索引结果，再按上述三条流程与分镜分别执行两种语言；不要复用过期元素引用，也不要将探索脚本当作独立回归套件。
