# Performance Wave 1A: browser hot path / 性能 Wave 1A：浏览器热路径

## 1. Goal-ready objective / 可直接用于 `/goal` 的目标

Implement the smallest independently measurable first product-optimization wave on top of the accepted Wave 0 baseline. Make exactly two product changes: remove the deterministic second Current Session Scope file-suggestions request after automatic initial Session selection without suppressing Project Scope or explicit refresh ownership, and add a browser-owned `eventId -> event` Map derived from the canonical ordered `currentEvents` prefix so confirmed exact-ID consumers no longer repeat linear scans, especially the per-card lookup in `syncEnclosingOperationAffordances()`. Preserve every user-visible search, filtering, paging, context-reveal, detail, cancellation, locale, Session-switch, and generated-asset contract; extend the accepted Wave 0 synthetic browser evidence only as needed for exact request and lookup-operation proof; do not change server/API/Materialized Session ownership. / 在已接受的 Wave 0 基线上实施最小、可独立量测的首个产品优化 wave。产品变更精确限制为两项：移除自动初始会话选择完成后的第二次确定性当前会话范围文件候选请求，同时不抑制项目范围或显式刷新 ownership；增加由规范有序 `currentEvents` 连续前缀派生、由浏览器拥有的 `eventId -> event` Map，使已确认的精确 ID consumer 不再重复线性扫描，尤其是 `syncEnclosingOperationAffordances()` 中逐 card 的查找。必须保留全部用户可见的搜索、筛选、分页、context reveal、详情、取消、locale、会话切换与生成资产契约；只在精确请求和 lookup operation 证明所需范围内扩展已接受的 Wave 0 合成浏览器 evidence；不得改变 server／API／Materialized Session ownership。

Suggested `/goal` invocation after this draft is reviewed and explicitly approved: / 本草案完成评审并获得明确批准后，建议使用以下 `/goal` 调用：

```text
/goal Execute docs/exec-plans/active/2026-08-27-performance-wave-1a-browser-hot-path.md completely. Revalidate the live towards-0.2.0 ref and relevant-path drift first; implement exactly the duplicate initial Session-suggestion removal and the browser currentEventsById index; preserve the fixed ownership/state invariants and Wave 0 functional/causal contracts; stop at every mandatory reviewer checkpoint; and do not complete the goal until focused/full gates, one smoke, three-process calibration, five-process synthetic comparison, exact changed-path audit, generated-asset equality, independent evidence review, and documentation lifecycle closeout all pass.
```

Implementation was explicitly authorized on 2026-08-27 for execution on the dedicated local branch `perf/wave-1a-browser-hot-path`, with M1 as the first mandatory independent-review stop. The authorization does not permit long profiling groups, generated-asset regeneration, M2 work, commits, pushes, pull requests, or merges before their plan-defined gates. / 2026-08-27 已明确授权在专用本地分支 `perf/wave-1a-browser-hot-path` 上执行实施，并把 M1 作为第一个强制独立评审停止点。该授权不允许在计划规定的 gate 之前运行长 profiling group、重建生成资产、执行 M2、commit、push、创建 pull request 或 merge。

## 2. Status, ownership, target, and inspected ref / 状态、负责人、目标与已检查 ref

- Status: the replacement M5, durable M6 smoke/three-process calibration, and durable replacement M7 five-process comparison have been independently reviewed and accepted for M8 packet assembly. The executable implementation remains frozen at `7b17b4d826e19ffee44af74b7324d66d61df6e35`; the plan-only durable-evidence child candidate is `b1c53157fc9837a4ec9f2303b2ea63b1b820ca12`. M8 packet assembly and independent review are pending; Wave 1A is not integration-ready or complete, and lifecycle closeout remains prohibited. / 状态：replacement M5、durable M6 smoke／三进程 calibration 与 durable replacement M7 五进程 comparison 已通过独立评审，并获准进入 M8 packet assembly。Executable implementation 继续冻结在 `7b17b4d826e19ffee44af74b7324d66d61df6e35`；仅含 plan 的 durable-evidence child candidate 为 `b1c53157fc9837a4ec9f2303b2ea63b1b820ca12`。M8 packet assembly 与独立评审仍待完成；Wave 1A 尚未达到 integration-ready 或 complete，lifecycle closeout 继续被禁止。
- Owner: eventual goal executor, followed by an independent implementation/evidence reviewer. / 负责人：后续 goal 执行者，之后由独立 implementation／evidence reviewer 验收。
- Opened: 2026-08-27. / 建立日期：2026-08-27。
- Repository: exactly `Yijia-Zhou/session-analyzer`, confirmed from `origin` (`git@github.com:Yijia-Zhou/session-analyzer.git`) and `package.json.repository.url` (`git+https://github.com/Yijia-Zhou/session-analyzer.git`). / 仓库：精确为 `Yijia-Zhou/session-analyzer`；已通过 `origin` 与 `package.json.repository.url` 共同确认。
- Target branch: `towards-0.2.0`. / 目标分支：`towards-0.2.0`。
- Inspected target SHA (`inspectedTargetSha`): `afbb30eb7d51ba2594b7dfdd5dbbe7b7991fe988`, verified from the live remote branch ref on 2026-08-27. This equals the expected merge commit supplied to planning, but equality was verified rather than assumed. / 已检查目标 SHA（`inspectedTargetSha`）：`afbb30eb7d51ba2594b7dfdd5dbbe7b7991fe988`，已于 2026-08-27 从实时远端分支 ref 核验。该值与规划输入中的预期 merge commit 相同，但这是核验结果而不是假设。
- The local clean worktree was at `0027b99949bec0dac7dda4e6357aeb272988fe73`, whose tree object is byte-identical to the inspected merge commit's tree (`997d910a64f9760025be11f9f6614a35ffe3d423`). Direct local source reads below are therefore exact reads of the inspected target tree. / 本地干净工作树位于 `0027b99949bec0dac7dda4e6357aeb272988fe73`；其 tree object 与已检查 merge commit 的 tree（`997d910a64f9760025be11f9f6614a35ffe3d423`）逐字节一致。因此下文对本地源码的直接读取就是对已检查目标 tree 的精确读取。
- Latest default branch inspected first: `main@2f282a94cc9eb3ce03fded4f8867e0d120a599aa`; its complete `README.md` and `AGENTS.md` were read before the target-branch dependency audit. Target-branch `README.md` and `AGENTS.md` were then read separately because both differ from `main`. / 首先检查的最新默认分支：`main@2f282a94cc9eb3ce03fded4f8867e0d120a599aa`；在目标分支 dependency 审计前已完整读取其 `README.md` 与 `AGENTS.md`。随后单独读取目标分支的 `README.md` 与 `AGENTS.md`，因为二者均与 `main` 不同。
- All confirmed source facts in this plan bind to `inspectedTargetSha`. A future executor must not silently transplant them to a moving ref. / 本计划全部已确认源码事实都绑定到 `inspectedTargetSha`。后续 executor 不得把这些事实静默移植到移动后的 ref。

Implementation should use a dedicated local branch such as `perf/wave-1a-browser-hot-path`, created only after review authorization. Before editing, resolve the then-live `towards-0.2.0`, require `inspectedTargetSha` to remain an ancestor, and inspect the exact descendant diff over every path named in this plan. Documentation-only descendants are recordable; any descendant change to relevant browser source, query semantics, profiling scripts/tests, package/build inputs, generated runtime assets, or governing performance/lifecycle docs is a mandatory reviewer stop. / 实施应在评审授权后使用专用本地分支，例如 `perf/wave-1a-browser-hot-path`。编辑前必须解析当时实时的 `towards-0.2.0`，要求 `inspectedTargetSha` 仍是其 ancestor，并检查本计划所列全部路径上的精确 descendant diff。纯文档后续可记录后继续；任何涉及相关浏览器源码、查询语义、profiling script／test、package／build input、生成 runtime asset 或 governing performance／lifecycle doc 的后续变化都必须停在 reviewer checkpoint。

## 3. Related repository authority / 相关仓库权威文档

The executor must reread these files at the implementation ref; this plan narrows execution but does not replace their contracts. / Executor 必须在实施 ref 上重新读取以下文件；本计划用于收窄执行范围，但不取代这些文件的契约。

- Domain language: `CONTEXT.md`, especially Session, Logical Event, Main Timeline, Current Session Scope, Project Scope, and Raw Record. / 领域语言：`CONTEXT.md`，尤其是会话、逻辑事件、主时间线、当前会话范围、项目范围与原始记录。
- Product behavior: `docs/product-specs/session-transcript-analyzer.md`. / 产品行为：`docs/product-specs/session-transcript-analyzer.md`。
- Browser lifecycle/performance design: `docs/design-docs/timeline-loading-and-rendering-performance.md`. / 浏览器生命周期／性能设计：`docs/design-docs/timeline-loading-and-rendering-performance.md`。
- Server Indexed/Materialized ownership: `docs/design-docs/indexed-materialized-session-lifecycle.md`. / 服务端 Indexed／Materialized ownership：`docs/design-docs/indexed-materialized-session-lifecycle.md`。
- Accepted measurement authority: `docs/exec-plans/completed/2026-08-24-performance-wave-0-baseline.md`. / 已接受的量测权威：`docs/exec-plans/completed/2026-08-24-performance-wave-0-baseline.md`。
- Build/release commands: target-branch `README.md`, `package.json`, `scripts/build-client.js`, and `docs/design-docs/npm-release-runbook.md` where release semantics are needed. / Build／release 命令：目标分支 `README.md`、`package.json`、`scripts/build-client.js`，以及需要 release 语义时的 `docs/design-docs/npm-release-runbook.md`。

Wave 0 is authoritative for the synthetic fixture, strict adapter build, one commit validation, prebuilt-server ownership, disabled prewarm, five browser scenarios, request normalization, DOM commit ledger, functional-state digests, causal constraints, Long Tasks, Resource Timing, process isolation, three-process calibration, five-process baseline, privacy boundary, and no numerical latency gate. Wave 1A may add exact counters and focused assertions, but must not replace that model. / Wave 0 是合成 fixture、严格 adapter build、一次 commit validation、预建 server ownership、禁用 prewarm、五个浏览器场景、请求规范化、DOM commit ledger、功能状态 digest、因果约束、Long Task、Resource Timing、进程隔离、三进程 calibration、五进程 baseline、隐私边界与无数值 latency gate 的权威。Wave 1A 可以增加精确 counter 和聚焦断言，但不得替换该模型。

## 4. Evidence classification and confirmed current state / 证据分类与已确认当前状态

### 4.1 Evidence classes / 证据类别

| Class / 类别 | What is established here / 本计划建立的内容 | What it does not establish / 不建立的内容 |
| --- | --- | --- |
| Source fact / 源码事实 | Complete relevant functions and their call sites were read at `afbb30eb…`; exact paths/functions are named below. / 在 `afbb30eb…` 上读取完整相关函数及其 call site；下文给出精确路径／函数。 | Runtime magnitude or timing improvement. / Runtime 量级或 timing 改善。 |
| Design-document statement / 设计文档陈述 | Existing canonical-prefix, invalidation, lifecycle, and Wave 0 policies are quoted by path/section and treated as governing intent. / 按 path／section 引用既有 canonical prefix、失效、生命周期与 Wave 0 policy，并作为 governing intent。 | Proof that source still implements every prose statement. / 源码仍实现每条文字陈述的证明。 |
| Static test coverage / 静态测试覆盖 | Existing test functions were read and their asserted contract is summarized. / 已读取既有 test function 并总结其断言契约。 | A pass result for the future Wave 1A candidate. / 未来 Wave 1A candidate 的通过结果。 |
| CI evidence / CI 证据 | GitHub check runs attached to exact merge SHA `afbb30eb…` completed successfully; the target workflow definition was also read. / 绑定精确 merge SHA `afbb30eb…` 的 GitHub check run 已成功完成；也读取了目标 workflow 定义。 | Local Wave 1A validation or proof for files not yet written. / 本地 Wave 1A 验证或尚未编写文件的证明。 |
| Reviewer inference / Reviewer 推断 | Algorithmic consequences such as an `O(L^2)` subpath are derived from confirmed loop and lookup bodies and are labeled as inference. / `O(L^2)` 子路径等算法后果由已确认循环与 lookup body 推导，并明确标为推断。 | Measured latency attribution until the operation seam and post-change evidence exist. / 在 operation seam 与变更后 evidence 建立前的实测 latency 归因。 |

No tests or profiling groups were run during planning. / 规划阶段未运行测试或 profiling group。

### 4.2 Repository, build, and generated-output facts / 仓库、build 与生成输出事实

1. `package.json` names `session-analyzer`, points to `Yijia-Zhou/session-analyzer`, defines `build:client`, `build:check`, `test`, `test:browser`, `test:package`, and `release:check`, and pins the reviewed source-development toolchain policy. / `package.json` 将 package 命名为 `session-analyzer`、指向 `Yijia-Zhou/session-analyzer`，定义 `build:client`、`build:check`、`test`、`test:browser`、`test:package` 与 `release:check`，并固定经评审的源码开发 toolchain policy。
2. `scripts/build-client.js::buildApp()` bundles `src/browser/entry.js` through esbuild into `public/assets/app.js`, minified and without source maps. `src/browser/entry.js` loads `src/browser/app.js` after shared/browser dependencies. The generated file is therefore an obligation, not source truth, and must be regenerated only through `npm run build:client`. / `scripts/build-client.js::buildApp()` 通过 esbuild 把 `src/browser/entry.js` bundle 到 `public/assets/app.js`，进行 minify 且不生成 source map。`src/browser/entry.js` 在 shared／browser dependency 后加载 `src/browser/app.js`。因此 generated file 是交付义务而非源码权威，只能通过 `npm run build:client` 重建。
3. Direct generated-output inspection found the minified `/api/file-suggestions` request construction and the minified enclosing-affordance card loop corresponding to source. It supplies no independent semantic authority. / 对生成输出的直接检查发现了 minified `/api/file-suggestions` 请求构造与对应源码的 minified enclosing-affordance card 循环；它不提供独立语义权威。

### 4.3 File-suggestion source facts / 文件候选源码事实

1. Initial selected-project startup is `src/browser/app.js::init()` → `/api/state` → `applyAppState()` → `loadSessions()`. Completed project selection uses `finishProjectSelection()` → `resetProjectViewState()` → `applyAppState()` → `loadSessions()`. Project refresh completion uses `finishProjectRefresh()` → request-owner abort/context retirement → `applyAppState()` → `loadSessions()`. Locale and revision recovery also re-enter `loadSessions()` through `changeLocale()` and `recoverIndexRevision()`. / 初始已选项目启动链是 `src/browser/app.js::init()` → `/api/state` → `applyAppState()` → `loadSessions()`。已完成项目选择使用 `finishProjectSelection()` → `resetProjectViewState()` → `applyAppState()` → `loadSessions()`。项目刷新完成使用 `finishProjectRefresh()` → request owner 取消／context retire → `applyAppState()` → `loadSessions()`。Locale 与 revision recovery 也分别通过 `changeLocale()` 与 `recoverIndexRevision()` 重新进入 `loadSessions()`。
2. `src/browser/app.js::loadSessions()` selects the first visible Session when needed and, in Current Session Scope, awaits `selectSession(state.selectedSessionId)`. It then reaches a second unconditional `refreshFileSuggestions()` after the Session/Project branch. / `src/browser/app.js::loadSessions()` 在需要时选择第一个可见会话；处于当前会话范围时，它会 await `selectSession(state.selectedSessionId)`；随后在 Session／Project 分支之后到达第二次无条件 `refreshFileSuggestions()`。
3. `src/browser/app.js::selectSession()` resets selected-timeline state, renders the pending surface, and awaits `Promise.all([loadAnalysis(sessionId), loadTimeline(false), refreshFileSuggestions()])`. Therefore the later `loadSessions()` invocation starts only after the first Session suggestion request has settled. This confirms candidate findings 1 and 2 more strongly than a timing observation: the ordering is an explicit await dependency. / `src/browser/app.js::selectSession()` 重置已选 timeline state、渲染 pending surface，并 await `Promise.all([loadAnalysis(sessionId), loadTimeline(false), refreshFileSuggestions()])`。因此 `loadSessions()` 中后续 invocation 只会在第一次会话候选请求 settle 后启动。这比 timing 观测更强地确认候选 finding 1 与 2：该顺序是显式 await dependency。
4. `src/browser/app.js::refreshFileSuggestions()` constructs parameters from `activeLayerId()` and includes `sessionId` only in Current Session Scope. It owns one independent `requestOwners.fileSuggestions` generation, `fileSuggestionRequestId`, `fileSuggestionContextKey()`, Index-revision acceptance, intentional-abort handling, and final `state.fileSuggestions` commit. / `src/browser/app.js::refreshFileSuggestions()` 从 `activeLayerId()` 构建参数，并且只在当前会话范围包含 `sessionId`。它独立拥有一套 `requestOwners.fileSuggestions` generation、`fileSuggestionRequestId`、`fileSuggestionContextKey()`、Index revision 接受、intentional abort 处理与最终 `state.fileSuggestions` commit。
5. Legitimate Project Scope ownership is distinct: `setSearchScope('project')` runs `loadProjectResults()` and `refreshFileSuggestions()` together; `loadSessions()` in Project Scope awaits `loadProjectResults()` and then performs its one post-branch suggestion refresh; an empty Session list switches to Project Scope and uses that same post-branch refresh. `changeLayer()` explicitly refreshes suggestions in either scope. / 合法的项目范围 ownership 相互独立：`setSearchScope('project')` 会同时运行 `loadProjectResults()` 与 `refreshFileSuggestions()`；项目范围中的 `loadSessions()` 先 await `loadProjectResults()`，再执行其一次分支后候选刷新；空 Session list 会切到项目范围并使用同一次分支后刷新。`changeLayer()` 会在任一 scope 中显式刷新候选。
6. Explicit later Session selection is the Session-list click handler → `selectSession()` and therefore currently produces the single request owned inside `selectSession()`; it does not pass through the duplicate tail in `loadSessions()`. Project-result drill-down has its own `drillDownProjectResult()` `Promise.all` with exactly one `refreshFileSuggestions()`. / 显式后续会话选择链是 Session list click handler → `selectSession()`，因此当前只产生 `selectSession()` 内部拥有的一次请求；它不会经过 `loadSessions()` 的重复 tail。项目结果下钻由 `drillDownProjectResult()` 的 `Promise.all` 独立拥有精确一次 `refreshFileSuggestions()`。
7. `server.js` route `/api/file-suggestions` interprets an absent `sessionId` as `query.projectFileSuggestions()` over the resident packed ProjectQueryStore. A present `sessionId` resolves and materializes the selected strict Indexed Session under the captured revision, then calls `query.sessionFileSuggestions()`. Request parameters therefore encode a real scope distinction; Wave 1A must not merge them. / `server.js` 的 `/api/file-suggestions` route 把缺少 `sessionId` 解释为在常驻 packed ProjectQueryStore 上执行 `query.projectFileSuggestions()`；存在 `sessionId` 时则在捕获 revision 下解析并物化所选严格 Indexed Session，再调用 `query.sessionFileSuggestions()`。请求参数因此编码了真实 scope 差异；Wave 1A 不得合并二者。
8. `src/session-query.js::sessionFileSuggestions()` delegates to `residentFileSuggestions()` for one explicit Materialized Session. `residentFileSuggestions()` validates and scans the selected layer's events, counts each event/file once, sorts, and slices. Because `loadSessions()` awaits the first selection group, the second normal request is a warm Materialized Session hit followed by another full Session suggestion scan; describing that scan as `O(N)` is reviewer inference from this function body, not a planning-time runtime measurement. / `src/session-query.js::sessionFileSuggestions()` 针对一个显式 Materialized Session 委托给 `residentFileSuggestions()`。`residentFileSuggestions()` 会校验并扫描所选 layer 的 event、按 event／file 计数一次、排序后切片。因为 `loadSessions()` 会 await 第一组 selection，正常的第二次请求是一次 warm Materialized Session hit，随后再次执行完整 Session suggestion scan；把该 scan 描述为 `O(N)` 是由函数 body 得出的 reviewer inference，不是规划阶段 runtime 量测。
9. `src/browser/app.js::refreshFileSuggestions()` allocates every invocation with `requestId = state.fileSuggestionRequestId + 1`, publishes that generation to state, and calls `requestOwners.fileSuggestions.start(requestContext)`. `src/browser/transition-safety.js::createRequestOwner().start()` unconditionally aborts the prior current controller; it does not exempt an equal context. / `src/browser/app.js::refreshFileSuggestions()` 为每次 invocation 分配 `requestId = state.fileSuggestionRequestId + 1`、把该 generation 发布到 state，并调用 `requestOwners.fileSuggestions.start(requestContext)`。`src/browser/transition-safety.js::createRequestOwner().start()` 无条件 abort 先前 current controller；相同 context 也不例外。
10. The `src/browser/app.js` `el.sessionList` click handler calls `selectSession(item.dataset.sessionId, ...)` without rejecting the already selected Session. Therefore a same-Session click may advance the file-suggestion request generation while automatic `loadSessions()` selection is still awaiting; context equality alone cannot prove that the delegated generation is still current. The existence of this schedule is a reviewer concurrency inference from the confirmed handler, generation, abort, and await bodies. / `src/browser/app.js` 的 `el.sessionList` click handler 会调用 `selectSession(item.dataset.sessionId, ...)`，且不会拒绝已经 selected 的 Session。因此，在自动 `loadSessions()` selection 仍 await 时，same-Session click 可以推进 file-suggestion request generation；仅凭 context 相等不能证明被委托 generation 仍 current。该 schedule 的存在是 reviewer 根据已确认 handler、generation、abort 与 await body 得出的 concurrency inference。

Conclusion: findings 1–3 are confirmed, and the same-context generation race narrows the safe handoff: both context and the delegated request generation must remain current. The optimization is removal of one redundant browser invocation in the successful automatic Current Session Scope path; it is not request coalescing, a server cache, a query change, or deletion of any Project/explicit refresh boundary. / 结论：finding 1 至 3 已确认；same-context generation race 进一步收窄安全 handoff：context 与被委托 request generation 都必须仍 current。优化内容是在成功的自动当前会话范围路径中移除一个冗余 browser invocation；它不是请求合并、server cache、query 变更，也不是删除任何 Project／显式刷新边界。

### 4.4 Timeline state and exact-ID lookup source facts / Timeline state 与精确 ID lookup 源码事实

1. `src/browser/app.js` stores `state.currentEvents`, `offset`, `timelineTotal`, and `timelineDataContext`; it has no browser event-ID Map at the inspected ref. / `src/browser/app.js` 存储 `state.currentEvents`、`offset`、`timelineTotal` 与 `timelineDataContext`；已检查 ref 上不存在浏览器 event-ID Map。
2. `loadTimeline()` replaces `currentEvents` with `data.events` for page zero and uses `state.currentEvents.concat(data.events)` for append after request-owner, request-ID, selected-Session, context, and append-offset gates. `loadTimelineThroughIndex()` and `refreshTimelineFindState()` construct a local ordered array and replace `currentEvents` only after the same ownership/context checks. `selectSession()`, `drillDownProjectResult()`, the no-Session branch in `loadSessions()`, and `resetProjectViewState()` clear the array. / `loadTimeline()` 在 page zero 时用 `data.events` 替换 `currentEvents`，在通过 request owner、request ID、selected Session、context 与 append offset gate 后通过 `state.currentEvents.concat(data.events)` 追加。`loadTimelineThroughIndex()` 与 `refreshTimelineFindState()` 构建局部有序 array，并且只在相同 ownership／context check 后替换 `currentEvents`。`selectSession()`、`drillDownProjectResult()`、`loadSessions()` 的无 Session 分支与 `resetProjectViewState()` 会清空该 array。
3. `docs/design-docs/timeline-loading-and-rendering-performance.md` states that `currentEvents` is the committed contiguous filtered prefix, append preserves numeric offset order, query refresh refetches a prefix, temporary event-reference and Code Mode context rows remain presentation-only, and current append has no browser overlap/duplicate-ID validation. This is governing design text and matches the inspected mutation sites. / `docs/design-docs/timeline-loading-and-rendering-performance.md` 规定 `currentEvents` 是已提交的连续筛选前缀、append 保持数字 offset 顺序、query refresh 重取前缀、临时事件引用与 Code Mode context row 保持 presentation-only，并指出当前 append 没有 browser overlap／duplicate-ID 校验。这是 governing design text，并与已检查 mutation site 一致。
4. `src/browser/app.js::currentTimelineEvent(eventId)` currently calls `state.currentEvents.find(...)` and then falls back to `temporaryEventReveal`. This confirms candidate finding 4. / `src/browser/app.js::currentTimelineEvent(eventId)` 当前调用 `state.currentEvents.find(...)`，随后回退到 `temporaryEventReveal`。候选 finding 4 已确认。
5. `src/browser/app.js::syncEnclosingOperationAffordances()` iterates every mounted `.event[data-event-id]` card and calls `currentTimelineEvent()` once per card before updating its affordance. With `L` committed/mounted cards and an `Array.find()` over up to `L` entries, an `O(L^2)` exact-ID lookup subpath follows. This confirms finding 5 as reviewer algorithmic inference; Wave 1A must add operation evidence before making a measured causal claim. / `src/browser/app.js::syncEnclosingOperationAffordances()` 遍历每个已挂载 `.event[data-event-id]` card，并在更新 affordance 前逐 card 调用一次 `currentTimelineEvent()`。当有 `L` 个已提交／挂载 card，且每次 `Array.find()` 最多检查 `L` 个 entry 时，会形成 `O(L^2)` 的精确 ID lookup 子路径。这以 reviewer 算法推断形式确认 finding 5；Wave 1A 在作实测因果声明前必须增加 operation evidence。
6. `renderTimeline()` calls `syncEnclosingOperationAffordances()` after replacing the complete timeline DOM. `loadVisibleExpandedDetails()` separately iterates visible mounted cards and also calls `currentTimelineEvent()`. Centralizing `currentTimelineEvent()` on the Map therefore removes confirmed scans from both paths without changing their ordered traversal. / `renderTimeline()` 在替换完整 timeline DOM 后调用 `syncEnclosingOperationAffordances()`；`loadVisibleExpandedDetails()` 会另外遍历可见已挂载 card，并同样调用 `currentTimelineEvent()`。因此让 `currentTimelineEvent()` 集中使用 Map 可以移除两条路径中已确认的 scan，同时不改变其有序遍历。
7. Additional confirmed canonical exact-ID retrievals exist in `setActiveSearchTarget()`, `materializeSearchEvent()`, `resolveSearchTargetNode()`, `restoreFocus()` after loading, `drillDownProjectResult()`, `openInheritedSourceSession()`, the structured-filter selection restoration in `loadTimeline()`, and `revealEnclosingOperation()`. These are eligible only through the reviewed canonical lookup helper. / 其它已确认的 canonical 精确 ID retrieval 位于 `setActiveSearchTarget()`、`materializeSearchEvent()`、`resolveSearchTargetNode()`、加载后的 `restoreFocus()`、`drillDownProjectResult()`、`openInheritedSourceSession()`、`loadTimeline()` 的 structured-filter selection restoration 与 `revealEnclosingOperation()`。这些 consumer 只能通过经评审的 canonical lookup helper 迁移。
8. Ordered and noncanonical consumers must remain arrays: `resolveFocusTarget()` may operate on `navigationCache.events` and uses ordering/nearest-neighbor traversal; `navigationApi.withTemporaryEventReveal()` inserts relative to a source index; navigation category/filter construction, search target discovery, rendering, paging, and `findIndex()`-based positions are order-sensitive; `currentNavigationCache()` is a separate complete result. / 有序与非 canonical consumer 必须继续使用 array：`resolveFocusTarget()` 可能作用于 `navigationCache.events` 并使用顺序／nearest-neighbor 遍历；`navigationApi.withTemporaryEventReveal()` 相对 source index 插入；navigation category／filter 构造、search target discovery、rendering、paging 与基于 `findIndex()` 的位置都依赖顺序；`currentNavigationCache()` 是独立完整结果。
9. `src/canonical-contract.js::validateCompleteSessionEventOwnership()` already rejects duplicate Logical Event IDs and duplicate Raw Record IDs in a Materialized Session. That server/source contract lowers expected duplicate risk but does not validate browser page overlap. A browser append/replacement guard is still required; silent Map last-write-wins is forbidden. / `src/canonical-contract.js::validateCompleteSessionEventOwnership()` 已拒绝 Materialized Session 中重复的逻辑事件 ID 与原始记录 ID。该 server／source contract 降低了预期 duplicate 风险，但不会校验 browser page overlap。仍必须增加 browser append／replacement guard；禁止静默 Map last-write-wins。
10. `src/browser/navigation.js` proves temporary referenced events are inserted into a derived presentation array without mutating the input; `reconcileContextReveal()` validates Session/Layer/data/folding/detail ownership against canonical events. These presentation rows are not Map members. / `src/browser/navigation.js` 证明 temporary referenced event 会被插入派生 presentation array 而不修改输入；`reconcileContextReveal()` 会针对 canonical event 校验 Session／Layer／data／folding／detail ownership。这些 presentation row 不是 Map member。
11. The followed auxiliary dependency chain does not introduce another canonical event owner: `src/browser/transition-safety.js::createRequestOwner()` and `createPaginationIntentState()` supply cancellation/current-owner and replacement/append epochs; `src/browser/search-targets.js::discover()`/`targetOrder()` own ordered target discovery and `liveBinding()` owns DOM binding lookup; `src/browser/search-controls.js::structuredSearchKey()` owns filter-context identity; `src/browser/navigation.js::withTemporaryEventReveal()`/`contextRevealSourceIndex()` own presentation insertion/order; and `src/shared/project-root.js::sameProjectRoot()` compares project identity. These responsibilities stay unchanged and do not write `currentEventsById`. / 已跟随检查的辅助 dependency chain 不会引入另一 canonical event owner：`src/browser/transition-safety.js::createRequestOwner()` 与 `createPaginationIntentState()` 提供取消／current-owner 与 replacement／append epoch；`src/browser/search-targets.js::discover()`／`targetOrder()` 拥有有序 target discovery，`liveBinding()` 拥有 DOM binding lookup；`src/browser/search-controls.js::structuredSearchKey()` 拥有 filter-context identity；`src/browser/navigation.js::withTemporaryEventReveal()`／`contextRevealSourceIndex()` 拥有 presentation insertion／order；`src/shared/project-root.js::sameProjectRoot()` 比较 project identity。这些职责保持不变，且不写入 `currentEventsById`。

Conclusion: findings 4–8 are confirmed with one refinement: exact-ID consumers are not migrated mechanically; the Wave 1A list is the canonical retrieval set named above plus central helper callers, while ordered/navigation-cache/presentation traversal remains unchanged. No server-side Map is justified. / 结论：finding 4 至 8 已确认，并作一项收窄：精确 ID consumer 不做机械迁移；Wave 1A 列表只包括上文命名的 canonical retrieval 集与 centralized helper caller，而有序／navigation-cache／presentation 遍历保持不变。没有证据支持 server-side Map。

### 4.5 Static test coverage and live CI evidence / 静态测试覆盖与实时 CI 证据

Existing static coverage read at the inspected target includes: / 在已检查目标上读取的既有静态覆盖包括：

- `e2e/browser.test.js::openApp()` proves the browser loads generated `/assets/app.js`, automatically selects a Session, and commits a non-empty initial timeline. Prewarm tests additionally cover first Session paint and no duplicate server materialization, but they do not assert file-suggestion request count. / `e2e/browser.test.js::openApp()` 证明浏览器加载生成的 `/assets/app.js`、自动选择会话并提交非空初始 timeline。Prewarm test 还覆盖首次 Session paint 与 server materialization 不重复，但不断言 file-suggestion request count。
- `browser project scope renders cards...`, `browser project result drill-down...`, `browser discards revision-mismatched file suggestions...`, and `browser retries coalesced recovery...` cover Project Scope result behavior, drill-down, revision-safe suggestion rejection, and nested recovery. They do not prove Project/Session suggestion counts. / 对应 browser test 覆盖项目范围结果行为、下钻、revision-safe suggestion 拒绝与嵌套恢复，但不证明 Project／Session suggestion count。
- `browser find keeps loaded timeline range...`, `browser canonical Load more...`, `browser search navigation loads only the next hit page...`, and `browser previous search navigation...` cover replacement/append prefix semantics and search navigation. / 对应 browser test 覆盖 replacement／append prefix 语义与 search navigation。
- `browser search discovery waits for a structured result view...`, `...excludes the previous timeline while switching sessions`, `...localized timeline content`, `browser rapid query filter and Session replacement...`, and detail/Raw/Layer abort tests cover stale DOM/request ownership across filters, Session, locale, detail, and Layer transitions. / 对应 browser test 覆盖 filter、会话、locale、detail 与 Layer transition 中的 stale DOM／request ownership。
- `browser nested Code Mode context reveals...` and its late-response companion prove one isolated presentation row, unchanged canonical cards/targets/folds, and stale owner invalidation. / 两个 nested Code Mode context browser test 证明一条隔离 presentation row、不变的 canonical card／target／fold 与 stale owner invalidation。
- `test/navigation.test.js`, `test/transition-safety.test.js`, and `test/search-targets.test.js` cover temporary reveal nonmutation, context reconciliation, request-owner aborts, pagination epochs, and canonical target ordering. / 对应 Node test 覆盖 temporary reveal 非 mutation、context reconciliation、request owner abort、pagination epoch 与 canonical target 排序。
- `test/timeline-profile.test.js` covers the strict 1,800/40-event fixture, late hit at ordinal 1,650, context relation, request constraints, final-state acceptance, and no latency gate. `test/performance-wave-0-runner.test.js` covers independent aggregation, calibration, exact/observational classifications, and sibling request order remaining unconstrained. / 对应 profiling test 覆盖严格 1,800／40-event fixture、ordinal 1,650 靠后命中、context relation、request constraint、final-state acceptance、无 latency gate、独立 aggregation、calibration、exact／observational 分类与 sibling request order 不受约束。
- `test/build-boundary.test.js` and `scripts/build-client.js --check` establish generated runtime currentness; they do not prove semantic parity for the new Map until the bundle is rebuilt and browser tests run. / `test/build-boundary.test.js` 与 `scripts/build-client.js --check` 建立 generated runtime currentness；在 bundle 重建并运行 browser test 前，它们不证明新 Map 的语义等价。

Live CI evidence for exact `afbb30eb…` is GitHub Actions run `33031984089`: aggregate `ci`, Browser on Ubuntu, Node 22 on Ubuntu, Node 24 on Ubuntu, Node 24 on Windows, and package smoke on Ubuntu/Windows all completed with `success`. `.github/workflows/ci.yml` statically shows build currentness plus Node tests in the Node matrix, package smoke on two operating systems, real Chromium browser coverage on Ubuntu, and an aggregate required-success job. This is target evidence only; the future Wave 1A candidate must rerun all required local gates and obtain its own CI/reviewer evidence. / 精确 `afbb30eb…` 的实时 CI evidence 是 GitHub Actions run `33031984089`：聚合 `ci`、Ubuntu Browser、Ubuntu Node 22、Ubuntu Node 24、Windows Node 24，以及 Ubuntu／Windows package smoke 均以 `success` 完成。`.github/workflows/ci.yml` 静态表明 Node matrix 中执行 build currentness 加 Node test、两个操作系统执行 package smoke、Ubuntu 执行真实 Chromium browser coverage，并由聚合 job 要求全部成功。这只属于目标 ref evidence；未来 Wave 1A candidate 必须重跑全部本地必需 gate，并取得自身 CI／reviewer evidence。

The completed Wave 0 plan reports executor-run Node `770/770`, browser `121/121`, lifecycle/source `115/115`, build/package/release/generated/privacy/scope gates and an independent reviewer rerun of syntax `4/4` plus focused tests `38/38`. Those are accepted historical Wave 0 evidence on its named candidate/tree, not a planning-time rerun or a hard-coded expected total for Wave 1A. / 已完成 Wave 0 计划报告 executor-run Node `770/770`、browser `121/121`、lifecycle／source `115/115`、build／package／release／generated／privacy／scope gate，以及独立 reviewer 重跑 syntax `4/4` 与 focused test `38/38`。这些是其具名 candidate／tree 上已接受的历史 Wave 0 evidence，不是规划阶段重跑结果，也不是 Wave 1A 的硬编码预期总数。

## 5. Included scope and non-goals / 包含范围与非目标

### Included / 包含

Wave 1A contains exactly two product changes: / Wave 1A 精确包含两项产品变更：

1. Remove only the successful automatic Current Session Scope duplicate suggestion invocation at the tail of `loadSessions()`, with an explicit outcome handoff from `selectSession()` so an aborted/stale/noncommitting first request retains the existing fallback opportunity. / 只移除 `loadSessions()` tail 中成功自动当前会话范围的重复候选 invocation；由 `selectSession()` 显式返回 outcome，使 aborted／stale／未 commit 的第一次请求继续保留既有 fallback 机会。
2. Add and synchronize browser `state.currentEventsById`, route the reviewed canonical exact-ID consumers through Map lookup, and prove the enclosing-affordance path no longer scans the canonical array per card. / 增加并同步浏览器 `state.currentEventsById`，让经评审的 canonical 精确 ID consumer 使用 Map lookup，并证明 enclosing-affordance 路径不再逐 card 扫描 canonical array。

Supporting work is allowed only where it directly proves or ships those changes: one narrow browser timeline-event-state module, focused Node/browser tests, content-free Wave 0 profiler/runner counter extensions, normal bundle regeneration, bilingual timeline-performance design updates, active/completed plan lifecycle, three-process calibration, five-process synthetic browser evidence, exact gates, and independent review. / 仅允许直接证明或交付上述变更的 supporting work：一个窄化 browser timeline-event-state module、聚焦 Node／browser test、不含内容的 Wave 0 profiler／runner counter 扩展、正常 bundle 重建、双语 timeline performance design 更新、active／completed plan 生命周期、三进程 calibration、五进程合成 browser evidence、精确 gate 与独立评审。

### Explicit non-goals / 明确非目标

Do not implement, prototype in production, or opportunistically refactor any of the following: / 不得在 production 中实施、原型化或顺带重构以下内容：

- Materialized Session derived query indexes; server-side event/raw ID Maps; ProjectQueryStore changes. / Materialized Session 派生 query index；server-side event／raw ID Map；ProjectQueryStore 变更。
- Raw DTO paging/slicing changes, cursor or sparse timeline loading, preload render batching, incremental/keyed rendering, detail-card patching, virtualization, or owner-scoped incremental highlighting. / Raw DTO paging／slicing 变更、cursor／稀疏 timeline loading、preload render batching、增量／keyed rendering、detail-card patching、virtualization 或 owner-scoped 增量 highlighting。
- Validator/fingerprint fast paths, private-validator contract changes, relationship-only scanners, or Claude/Codex/DeepSeek relationship algorithm changes. / Validator／fingerprint fast path、private-validator contract 变更、relationship-only scanner，或 Claude／Codex／DeepSeek relationship algorithm 变更。
- Cache, scheduler, revision lease, prewarm, materialization owner, source adapter, source-specific parser, or canonical DTO changes. / Cache、scheduler、revision lease、prewarm、materialization owner、source adapter、source-specific parser 或 canonical DTO 变更。
- API request/response, search/count/paging, suggestion-result, sorting, locale, detail ownership, or error-copy semantics changes. / API request／response、search／count／paging、suggestion result、排序、locale、detail ownership 或 error copy 语义变更。
- Request coalescing, debounce, throttling, or a new server/browser cache as a substitute for deleting the redundant invocation. / 用 request coalescing、debounce、throttling 或新的 server／browser cache 代替删除冗余 invocation。
- A numerical millisecond/percentage regression threshold or a claim that unrelated server domains improved. / 数值毫秒／百分比回归阈值，或无关 server domain 获得改善的 claim。

Adjacent findings go to a later-wave candidate or the tech-debt tracker; they do not enter Wave 1A merely because they are visible during implementation. / 相邻 finding 必须进入后续 wave candidate 或 tech-debt tracker；不能仅因实施期间可见就进入 Wave 1A。

## 6. Fixed decisions and non-negotiable invariants / 固定决策与不可妥协的不变量

1. `currentEvents` remains the sole canonical ordered contiguous-prefix array. `currentEventsById` is derived lookup state, never an independently mutated data source. / `currentEvents` 继续作为唯一 canonical 有序连续前缀 array。`currentEventsById` 是派生 lookup state，绝不是独立 mutation 的 data source。
2. The Map holds the exact event object references from `currentEvents`; it does not clone DTOs, store detail DTOs, or materialize presentation rows. / Map 保存 `currentEvents` 中精确的 event object reference；不 clone DTO、不存 detail DTO，也不物化 presentation row。
3. The committed timeline pair consists of its committed context (`state.timelineDataContext`), canonical `currentEvents`, and derived `currentEventsById`. All context/array/Map replacement and append commits pass through one narrow state boundary in a new `src/browser/timeline-event-state.js`; asynchronous request orchestration remains in `src/browser/app.js`. / 已提交 timeline pair 由其 committed context（`state.timelineDataContext`）、canonical `currentEvents` 与派生 `currentEventsById` 组成。全部 context／array／Map replacement 与 append commit 都通过新的 `src/browser/timeline-event-state.js` 中一个窄化 state boundary；异步 request orchestration 继续位于 `src/browser/app.js`。
4. A replacement validates/builds the next Map before atomically publishing its next context, array, and Map. An invalid replacement leaves the prior committed pair intact and follows the existing replacement error/retry path. / Replacement 在原子发布 next context、array 与 Map 前先校验／构建 next Map。无效 replacement 保持此前已提交 pair 不变，并沿用既有 replacement error／retry path。
5. An append validates the entire new page against itself and the existing Map before mutating either structure. Only after validation may it append the page in order and insert those exact new objects. / Append 在修改任一 structure 前，先针对 page 自身与既有 Map 校验完整新 page。只有通过校验后才能按序追加 page，并插入这些精确新 object。
6. Duplicate or empty event IDs fail closed with a stable internal invariant error. Silent overwrite, silent deduplication, or last-write-wins is forbidden. Existing user-visible error/retry behavior is retained; no fabricated event or fallback to an earlier Session is permitted. / 重复或空 event ID 必须以稳定 internal invariant error fail closed。禁止静默覆盖、静默去重或 last-write-wins。保留既有用户可见 error／retry 行为；不得虚构 event，也不得回退到较早会话。
7. Existing request-owner, request-ID, selected-Session, `timelineDataContextKey()`, Index-revision, append-offset, and pagination-epoch checks remain before every state commit. The Map introduces no new authority that can bypass them. / 既有 request owner、request ID、selected Session、`timelineDataContextKey()`、Index revision、append offset 与 pagination epoch check 继续位于每次 state commit 之前。Map 不引入可绕过它们的新 authority。
8. During a Layer/filter/query replacement, the prior committed context/array/Map remains the rendered, lookup-capable pair until success. A difference between `state.timelineDataContext` and the newly desired `timelineDataContextKey()` marks pending replacement; it must not globally disable `currentTimelineEvent()` or the still-visible cards. Only a consumer whose existing contract requires the desired query context may perform a local committed-context check. After all existing response gates pass, success atomically publishes the new committed context/array/Map. / Layer／filter／query replacement 期间，此前已提交 context／array／Map 继续作为已渲染、可 lookup 的 pair，直至 replacement 成功。`state.timelineDataContext` 与新期望的 `timelineDataContextKey()` 不同只表示 replacement pending；它不得全局禁用 `currentTimelineEvent()` 或仍可见的 card。只有既有契约要求 desired query context 的 consumer 才能执行局部 committed-context check。全部既有 response gate 通过后，成功响应原子发布新的 committed context／array／Map。
9. Full project/Session/locale/revision context reset clears both structures together. Session selection, project-result drill-down, and locale's existing `loadSessions()` → `selectSession()` reload clear both before first-page work; no-Session state clears both. / 完整 project／Session／locale／revision context reset 同时清空两种 structure。会话选择、项目结果下钻，以及 locale 既有的 `loadSessions()` → `selectSession()` reload 都在 first-page work 前同时清空二者；无 Session state 同时清空二者。
10. Temporary event reveal, Code Mode context rows, navigation-cache events, detail cache/pending/errors, selected-event state, search bindings, and DOM nodes are not Map writers. / Temporary event reveal、Code Mode context row、navigation-cache event、detail cache／pending／error、selected-event state、search binding 与 DOM node 都不是 Map writer。
11. `currentTimelineEvent(eventId, purpose = 'canonical')` reads the currently rendered committed Map even when a different desired context is pending, threads only the closed profiling purpose to that Map lookup, and then performs the existing O(1) exact-ID `temporaryEventReveal` fallback on a Map miss. The indexed enclosing-affordance callback must use this accessor rather than `canonicalTimelineEvent()` directly. Temporary reveal remains the fallback only in `currentTimelineEvent()` and never enters the Map; detached context and navigation cache remain explicit later fallbacks where they already exist; other canonical lookups return null/false and keep current error/close behavior. / 即使另一个 desired context 正在 pending，`currentTimelineEvent(eventId, purpose = 'canonical')` 仍读取当前已渲染的 committed Map，只把封闭 profiling purpose 传给该 Map lookup，并在 Map miss 后执行既有 O(1) 精确 ID `temporaryEventReveal` fallback。Indexed enclosing-affordance callback 必须使用此 accessor，而不能直接调用 `canonicalTimelineEvent()`。Temporary reveal 只在 `currentTimelineEvent()` 中继续作为 fallback，绝不进入 Map；detached context 与 navigation cache 只在既有位置继续作为显式后续 fallback；其它 canonical lookup 返回 null／false，并保留当前 error／close 行为。
12. Locale changes follow the inspected existing full `loadSessions()` → `selectSession()` reload: they retire the old pair before the localized first page and rebuild localized DTO objects plus the Map on success. Locale is presentation context, not event identity; Map keys remain machine event IDs and locale-only changes never rewrite IDs. This reviewer-approved correction preserves existing product behavior and supersedes the earlier planning assumption that locale retained the old pair while pending. / Locale 变化沿用经检查的既有完整 `loadSessions()` → `selectSession()` reload：在 localized first page 前 retire 旧 pair，并在成功时重建 localized DTO object 与 Map。Locale 属于 presentation context 而非 event identity；Map key 继续使用 machine event ID，纯 locale 变化绝不改写 ID。该 reviewer-approved 修正保留既有产品行为，并取代此前“locale pending 时保留旧 pair”的规划假设。
13. The suggestion optimization changes invocation ownership only. URL parameters, response acceptance, sorting/counting, final content, and the server route are byte/semantic parity requirements. / 候选优化只改变 invocation ownership。URL parameter、response acceptance、排序／计数、最终内容与 server route 都是 byte／semantic parity 要求。
14. New profiling hooks are content-free, inert when the Wave 0 init-script observer is absent, and expose only counts/booleans/backend class. Profiling-only parity snapshots must resolve a callable `recordStateSnapshot` before iterating the canonical array or reading the Map; absence, an observer without that method, or inaccessible method lookup produces `null`, zero canonical-array visits, and zero Map gets. The correctness-owned pre-append `assertTimelineEventState()` remains mandatory and linear. Hooks never expose IDs, URLs, query text, Session identity, event content, or user-visible diagnostics. / 新 profiling hook 必须不含内容，在 Wave 0 init-script observer 不存在时保持 inert，并且只暴露 count／boolean／backend class。仅用于 profiling 的 parity snapshot 必须先解析出可调用的 `recordStateSnapshot`，之后才能遍历 canonical array 或读取 Map；observer 缺席、缺少该方法或方法读取不可访问时返回 `null`，canonical-array visit 与 Map get 均为零。Correctness-owned 的 append 前 `assertTimelineEventState()` 继续保持强制且线性。Hook 绝不暴露 ID、URL、query text、Session identity、event content 或用户可见诊断。
15. The `selectSession()` handoff uses one closed internal result with exactly `suggestionRequestId`, `suggestionContext`, and `suggestionCommitted`. `suggestionRequestId` is the exact `state.fileSuggestionRequestId` generation allocated locally by the delegated `refreshFileSuggestions()` invocation, not a later sample of global state. Before either skipping or issuing the `loadSessions()` tail fallback, the outer Sessions request must pass six guards: its own request ID, original Sessions data context, delegated Session, Current Session Scope, returned suggestion context, and returned suggestion generation. A superseded outer request returns without starting, aborting, or duplicating newer suggestion work, including newer work with the same Session and context. / `selectSession()` handoff 使用一个精确只含 `suggestionRequestId`、`suggestionContext` 与 `suggestionCommitted` 的封闭 internal result。`suggestionRequestId` 是被委托 `refreshFileSuggestions()` invocation 在本地分配的精确 `state.fileSuggestionRequestId` generation，不是稍后读取 global state 得到的样本。在跳过或发出 `loadSessions()` tail fallback 前，外层 Sessions request 必须通过六项 guard：自身 request ID、原始 Sessions data context、被委托 Session、当前会话范围、返回 suggestion context 与返回 suggestion generation。已被取代的外层 request 直接返回，不启动、取消或复制更新 suggestion work，包括具有相同 Session 与 context 的更新 work。

## 7. File-suggestion ownership model / 文件候选 ownership 模型

### 7.1 Exact current and target call-chain table / 精确当前与目标 call-chain 表

| Boundary / 边界 | Current call chain at `afbb30eb…` / 当前调用链 | Wave 1A owner and required count / Wave 1A owner 与必需 count |
| --- | --- | --- |
| Initial selected-project state load / 初始已选项目 state load | `init()` → `/api/state` → `applyAppState()` → `loadSessions()` / 同左 | No direct suggestion owner; ownership is chosen by the resulting scope branch. / 不直接拥有 suggestion；由最终 scope 分支决定 ownership。 |
| Automatic initial Session selection / 自动初始会话选择 | `loadSessions()` → `selectSession()` → `Promise.all(..., refreshFileSuggestions())` → settle → tail `refreshFileSuggestions()` / 两次 | `selectSession()` returns the closed three-field suggestion result. The still-current outer Sessions request skips the tail only after all six handoff guards pass and that exact generation/context committed; it may issue one fallback only when all six guards pass and that still-latest delegated generation did not commit. Exactly one normal Session request. / `selectSession()` 返回封闭三字段 suggestion result。仍 current 的外层 Sessions request 只有在六项 handoff guard 全部通过且该精确 generation／context 已 commit 后才跳过 tail；只有六项 guard 全部通过且该仍 latest 的委托 generation 未 commit 时，才能发出一次 fallback。正常路径精确一次 Session 请求。 |
| Explicit later Session selection / 显式后续会话选择 | Session click → `selectSession()` → one `refreshFileSuggestions()` / 一次 | Unchanged: exactly one request for the selected Session/layer. / 不变：所选 Session／layer 精确一次请求。 |
| Same selected Session clicked while automatic selection settles / 自动 selection settle 期间再次点击同一已选 Session | Click → a newer `selectSession()` → a newer same-Session/same-context `refreshFileSuggestions()` generation that aborts/supersedes the delegated request / 两个 generation | The older outer `loadSessions()` sees `result.suggestionRequestId !== state.fileSuggestionRequestId` and returns without a third fallback, whether the newer request is pending or already committed. The newer click-owned request is not aborted and is the only request allowed to commit; the click still owns its intended single request. / 旧外层 `loadSessions()` 发现 `result.suggestionRequestId !== state.fileSuggestionRequestId` 后直接返回；无论更新 request 仍 pending 或已经 commit，都不发出第三次 fallback。更新的 click-owned request 不被 abort，并且是唯一允许 commit 的 request；该 click 仍只拥有其预期的一次请求。 |
| Project-result drill-down / 项目结果下钻 | `drillDownProjectResult()` → `Promise.all(loadAnalysis, loadTimelineThroughIndex, refreshFileSuggestions)` / 一次 | Unchanged: exactly one Session-scoped request. / 不变：精确一次 Session-scope 请求。 |
| Enter Project Scope / 进入项目范围 | `setSearchScope('project')` → `Promise.all(loadProjectResults, refreshFileSuggestions)` / 一次 | Unchanged: exactly one Project request without `sessionId`. / 不变：精确一次不带 `sessionId` 的 Project 请求。 |
| `loadSessions()` while Project Scope is already active / 项目范围已生效时执行 `loadSessions()` | `loadSessions()` → `loadProjectResults()` → tail `refreshFileSuggestions()` / 一次 | Unchanged: tail remains the Project owner. / 不变：tail 继续是 Project owner。 |
| Empty Session list / 空 Session list | `loadSessions()` switches scope to Project and reaches tail refresh / 一次 | Unchanged: one Project request, no Session request. / 不变：一次 Project 请求，零 Session 请求。 |
| Layer change / 事件层变化 | `changeLayer()` → `Promise.all(refreshActiveSearch, refreshFileSuggestions)` / 一次 | Unchanged in either scope; parameters reflect new layer and current scope. / 任一 scope 均不变；参数反映新 layer 与当前 scope。 |
| Locale reload / Locale 重载 | `changeLocale()` → `/api/state` → `applyAppState()` → `loadSessions()` / 当前 Session 自动路径会重复 | Apply the same automatic-selection outcome rule; one successful Session request, or the existing fallback only when the first did not commit. Project ownership stays one. / 应用同一自动 selection outcome 规则；成功路径一次 Session 请求，只有第一次未 commit 时才保留既有 fallback。Project ownership 保持一次。 |
| Source/project replacement / Source／project replacement | `finishProjectSelection()` resets all owners/state then `loadSessions()`; refresh completion aborts owners/contexts then `loadSessions()` / 当前 Session 自动路径会重复 | Reset invalidates prior requests; resulting scope owns exactly the same counts as initial load. / Reset 使先前 request 失效；结果 scope 拥有与初始 load 相同的精确 count。 |
| Revision recovery / Revision recovery | `recoverIndexRevision()` aborts all request owners, reloads state, and calls `loadSessions()` in a bounded loop / 可能有 intentional retry | Do not suppress or replace the existing bounded recovery owner. Skip/fallback is decided only after the outer request and complete returned suggestion result pass all six handoff guards under the accepted revision. / 不抑制或替换既有有界 recovery owner。只有外层 request 与完整返回 suggestion result 在已接受 revision 下通过全部六项 handoff guard 后，才能决定 skip／fallback。 |
| Stale/aborted selection / Stale／aborted selection | Request owner abort plus request ID/context/revision checks return `false`; a newer owner may commit / 最新者获胜 | Old suggestion work never commits. After delegated selection settles, a superseded outer `loadSessions()` fails at least one request-ID/original-context/Session/scope/suggestion-context/suggestion-generation guard and returns without issuing a fallback. The generation guard covers the otherwise-identical same-Session/same-context case. Only an outer request whose delegated generation is still latest may make one noncommitting fallback attempt. / 旧 suggestion work 绝不 commit。委托 selection settle 后，已被取代的外层 `loadSessions()` 至少无法通过 request ID／原始 context／Session／scope／suggestion context／suggestion generation 中一项 guard，并直接返回，不发出 fallback。Generation guard 覆盖其它条件完全相同的 same-Session／same-context 情形。只有其委托 generation 仍 latest 的外层 request 才能执行一次未 commit fallback 尝试。 |

There is no user-facing “refresh suggestions” button. “Explicit refresh” in this plan means the explicit source boundaries above—scope, Layer, project/source/locale/revision reload, Session click, and drill-down—that deliberately invoke `refreshFileSuggestions()`. / 当前没有面向用户的“刷新候选”按钮。本计划中的“显式刷新”是指上表中的显式 source boundary——scope、Layer、project／source／locale／revision reload、Session click 与 drill-down——它们会有意调用 `refreshFileSuggestions()`。

### 7.2 Narrow implementation decision / 窄化实施决定

`selectSession()` must return this closed internal, non-API shape and no extra authority-bearing fields: / `selectSession()` 必须返回以下封闭 internal、非 API shape，且不得增加其它携带 authority 的字段：

```js
Object.freeze({
  suggestionRequestId,
  suggestionContext,
  suggestionCommitted,
})
```

`suggestionRequestId` is the exact local `requestId` value allocated when the delegated `refreshFileSuggestions()` increments `state.fileSuggestionRequestId`; `suggestionContext` is the exact `fileSuggestionContextKey()` captured by that same invocation; `suggestionCommitted` is true only when that exact generation passed its request-owner, request-ID, context, and revision gates and committed `state.fileSuggestions`. The delegated invocation must return its own local generation/outcome to `selectSession()`; neither function may reconstruct `suggestionRequestId` by reading `state.fileSuggestionRequestId` after awaiting, because a newer same-context invocation may already own it. Existing non-handoff callers retain their current boolean behavior through a narrow internal outcome seam or opt-in mode. / `suggestionRequestId` 是被委托 `refreshFileSuggestions()` 递增 `state.fileSuggestionRequestId` 时分配的精确本地 `requestId`；`suggestionContext` 是同一次 invocation 捕获的精确 `fileSuggestionContextKey()`；只有该精确 generation 通过 request owner、request ID、context 与 revision gate 并 commit `state.fileSuggestions` 时，`suggestionCommitted` 才为 true。被委托 invocation 必须把自身本地 generation／outcome 返回给 `selectSession()`；两个函数都不得在 await 之后读取 `state.fileSuggestionRequestId` 来重建 `suggestionRequestId`，因为更新的 same-context invocation 可能已经拥有该值。其它非 handoff caller 通过窄化 internal outcome seam 或 opt-in mode 保留当前 boolean 行为。

`loadSessions()` captures its `requestId`, original `requestContext = sessionsDataContextKey()`, and delegated `sessionId` before awaiting `selectSession()`. Before either skipping the tail or issuing a fallback, it must newly require all of the following: / `loadSessions()` 在 await `selectSession()` 前捕获自身 `requestId`、原始 `requestContext = sessionsDataContextKey()` 与被委托 `sessionId`。在跳过 tail 或发出 fallback 前，必须重新要求以下条件全部成立：

1. `requestId === state.sessionsRequestId`; / 自身 Sessions request ID 仍 current；
2. the original `requestContext` still equals both committed `state.sessionsDataContext` and `sessionsDataContextKey()`; / 原始 `requestContext` 仍同时等于已提交 `state.sessionsDataContext` 与 `sessionsDataContextKey()`；
3. the delegated `sessionId === state.selectedSessionId`; / 被委托 `sessionId` 仍是 selected Session；
4. `state.searchScope === 'session'`; / 当前仍为当前会话范围；
5. `result.suggestionContext === fileSuggestionContextKey()`. / 返回 suggestion context 与当前 `fileSuggestionContextKey()` 相等。
6. `result.suggestionRequestId === state.fileSuggestionRequestId`. / 返回 suggestion generation 仍是当前最新 `state.fileSuggestionRequestId`。

If any of the six guards fails, this outer `loadSessions()` returns `false` without issuing a fallback and without starting or aborting whatever newer suggestion generation owns the UI—even when Session, scope, and context are identical. If all six guards pass and `suggestionCommitted` is true, it skips the tail. If all six pass, `suggestionCommitted` is false, and the delegated generation is therefore still latest, it may issue the existing one tail fallback, which keeps its own `refreshFileSuggestions()` owner/context/revision gates. This handoff must preserve the existing bounded revision-recovery owner; it must not introduce an unbounded retry or a second recovery coordinator. / 六项 guard 中任一失败时，该外层 `loadSessions()` 直接返回 `false`，不发出 fallback，也不启动或取消拥有 UI 的更新 suggestion generation——即使 Session、scope 与 context 完全相同。若六项 guard 全部通过且 `suggestionCommitted` 为 true，则跳过 tail。只有六项全部通过、`suggestionCommitted` 为 false、因而被委托 generation 仍 latest 时，才能发出既有的一次 tail fallback；该调用继续保留自身 `refreshFileSuggestions()` owner／context／revision gate。此 handoff 必须保留既有有界 revision-recovery owner；不得引入无界 retry 或第二个 recovery coordinator。

Do not add a shared promise, debounce window, cache, or “already loaded” flag detached from request context. Such approaches would conflate elimination, suppression, and coalescing and could drop legitimate Project/Layer/revision refreshes. / 不得增加脱离 request context 的 shared promise、debounce window、cache 或“already loaded” flag。这些做法会混淆 elimination、suppression 与 coalescing，并可能丢失合法 Project／Layer／revision refresh。

## 8. `currentEvents` / `currentEventsById` lifecycle model / `currentEvents`／`currentEventsById` 生命周期模型

### 8.1 Ownership relation / Ownership 关系

```text
committed timeline pair (the currently rendered interaction owner)
  -> context: state.timelineDataContext
  -> currentEvents: canonical ordered contiguous prefix [0, offset)
  -> currentEventsById: derived exact-object lookup index
       key   = canonical event.id
       value = the same object reference held by currentEvents

desired replacement context
  -> timelineDataContextKey()
  -> may differ while a request is pending; does not invalidate the rendered committed pair

presentation-only state (never indexed)
  -> temporaryEventReveal
  -> contextReveal/context rows
  -> navigationCache.events
  -> detail cache/pending/errors
  -> search bindings and DOM nodes
```

The three committed fields publish and retire together. `currentEvents` determines order, offset, loaded count, rendering order, target discovery, and traversal. `currentEventsById` answers only canonical exact-ID membership/retrieval for that same committed context. The currently rendered committed pair remains interactive while a different desired context is pending; the Map cannot decide paging, filtering, ordering, desired-query readiness, or presentation membership. / 三个 committed field 必须一起发布与 retire。`currentEvents` 决定顺序、offset、loaded count、rendering order、target discovery 与遍历。`currentEventsById` 只回答同一 committed context 中 canonical 精确 ID membership／retrieval。另一个 desired context pending 期间，当前已渲染的 committed pair 继续可交互；Map 不能决定 paging、filtering、ordering、desired-query readiness 或 presentation membership。

### 8.2 Transition table / 转换表

| Transition / 转换 | Array action / Array 动作 | Map action / Map 动作 | Commit/owner rule / Commit／owner 规则 |
| --- | --- | --- | --- |
| Initial state / 初始状态 | `[]` | empty `Map` | Pair exists from initialization. / 初始化即存在 pair。 |
| Full project reset / 完整项目 reset | clear | clear/recreate | `resetProjectViewState()` after aborting owners. / `resetProjectViewState()` 在取消 owner 后执行。 |
| No Sessions / 无会话 | clear | clear/recreate | `loadSessions()` current request/context only. / 仅限 current request／context。 |
| Session switch / 会话切换 | clear before requests | clear/recreate with array | `selectSession()` retires old canonical ownership before new work. / `selectSession()` 在新 work 前 retire 旧 canonical ownership。 |
| Project-result drill-down / 项目结果下钻 | clear before deep load | clear/recreate with array | `drillDownProjectResult()` owns the new Session. / `drillDownProjectResult()` 拥有新 Session。 |
| First page/replacement / 首页／replacement | publish validated response array | build complete Map from next array, then publish context/array/Map together | Request owner/ID/Session/context/revision must still match; no field publishes early. / Request owner／ID／Session／context／revision 必须仍匹配；任何 field 都不得提前发布。 |
| Valid append / 合法 append | concatenate ordered new page, preserving existing event objects | after full duplicate validation, `set` only new page objects; retain all prior entries/object identities | `appendOffset === offset === currentEvents.length`; page belongs to current context. / `appendOffset === offset === currentEvents.length`；page 属于 current context。 |
| `loadTimelineThroughIndex()` / 深前缀 load | publish locally accumulated ordered replacement | rebuild from full local array | No partial commit inside loop; stale result returns without either commit. / Loop 中不 partial commit；stale result 不提交任一 structure。 |
| `refreshTimelineFindState()` / Find refresh | publish locally accumulated replacement at prior depth | rebuild from replacement array | Same owner/context rule; prior pair remains last committed until success. / 相同 owner／context rule；成功前先前 pair 仍是最后 commit。 |
| Layer/filter/query replacement pending / Layer／filter／query replacement pending | retain the last committed context/array and its visible DOM | retain the matching committed Map; `currentTimelineEvent()` continues to read it | Old visible cards keep their existing click, selection, detail, and enclosing-operation behavior. Consumers that require desired-query readiness check committed-versus-desired context locally; stale response gates still prevent publish. / 旧可见 card 保留既有 click、selection、detail 与 enclosing-operation 行为。要求 desired-query ready 的 consumer 局部检查 committed 与 desired context；stale response gate 继续阻止 publish。 |
| Locale reload / Locale 重载 | clear through the existing `loadSessions()` → `selectSession()` full reload before localized first-page work; publish the localized array on success | clear/recreate with the array, then rebuild from unchanged machine IDs and new localized object identities | Locale participates in context, not identity. The prior pair is deliberately retired while the localized page loads; request/Session/context/revision gates still prevent stale publication. / Locale 属于 context，不属于 identity。Localized page 加载期间会有意 retire 先前 pair；request／Session／context／revision gate 继续阻止 stale publish。 |
| Project/revision retirement / Project／revision retire | reset immediately where current code has a full reset; otherwise retire through the existing owner/recovery transition before delegated Session reset/replacement | clear with the committed pair, then rebuild only from the accepted new owner | No desired-context-only global lookup gate is used; a stale response cannot repopulate any committed field. / 不使用只依赖 desired context 的全局 lookup gate；stale response 不能重新填充任何 committed field。 |
| Temporary reveal / 临时 reveal | no canonical mutation | no Map mutation | `currentTimelineEvent()` checks Map first, then current temporary reveal fallback. / `currentTimelineEvent()` 先查 Map，再查 current temporary reveal fallback。 |

### 8.3 Required executable invariants / 必需可执行不变量

For every committed pair, tests/probes must prove all of the following: / 对每个已提交 pair，test／probe 必须证明以下全部内容：

1. `state.offset === state.currentEvents.length`. / `state.offset === state.currentEvents.length`。
2. `currentEventsById.size === currentEvents.length === unique(currentEvents.map(id)).size`. / `currentEventsById.size === currentEvents.length === unique(currentEvents.map(id)).size`。
3. For every array entry `event`, `currentEventsById.get(event.id) === event`. / 对每个 array entry `event`，`currentEventsById.get(event.id) === event`。
4. Every Map entry has a matching array entry by both ID and object identity; no earlier-context object survives. / 每个 Map entry 都有 ID 与 object identity 同时匹配的 array entry；不保留较早 context object。
5. Append preserves all prior array and Map value identities and adds every new page event once. / Append 保留全部既有 array 与 Map value identity，并把每个新 page event 精确加入一次。
6. Replacement rebuilds from only the new canonical array; it never merges old and new Maps. / Replacement 只从新 canonical array 重建；绝不 merge 新旧 Map。
7. Duplicate within a page, duplicate against an existing prefix, missing/empty ID, or a parity mismatch fails before publish. / Page 内 duplicate、与既有 prefix duplicate、缺失／空 ID 或 parity mismatch 都必须在 publish 前失败。
8. Full Session/project/locale/revision reset makes old entries immediately unreachable. Layer/filter/query replacement keeps the rendered old committed pair reachable only until a successful atomic publish; after publish, the old pair is unreachable. Stale/aborted responses leave the current committed pair unchanged. / 完整 Session／project／locale／revision reset 使旧 entry 立即不可访问。Layer／filter／query replacement 只在成功原子 publish 前保持已渲染的旧 committed pair 可访问；publish 后旧 pair 不可访问。Stale／aborted response 保持当前 committed pair 不变。
9. Temporary reveal and context-only rows never change Map size or entries. / Temporary reveal 与 context-only row 绝不改变 Map size 或 entry。
10. Map miss never returns an event from an earlier Session/context and never manufactures a DTO. / Map miss 绝不返回较早 Session／context 的 event，也不制造 DTO。
11. While replacement is delayed, every still-mounted old canonical card resolves to the same old committed event object and retains click, selection, detail, and enclosing-operation behavior; no global desired-context mismatch may make that rendered DOM inert. / Replacement 延迟期间，每个仍 mounted 的旧 canonical card 都解析到同一个旧 committed event object，并保留 click、selection、detail 与 enclosing-operation 行为；不得因全局 desired-context mismatch 使该已渲染 DOM inert。

## 9. Confirmed lookup migration and operation-count evidence / 已确认 lookup 迁移与 operation-count evidence

### 9.1 Exact migration list / 精确迁移列表

The implementation must route the following through one canonical Map accessor, with a separate explicit temporary-reveal fallback only where current behavior has it: / 实施必须让以下 consumer 通过一个 canonical Map accessor；只有当前行为已有 temporary reveal fallback 的位置才保留单独显式 fallback：

- `currentTimelineEvent(eventId, purpose = 'canonical')`; this migrates `syncEnclosingOperationAffordances()` through the same accessor while preserving its exact temporary-event fallback, and continues to own `loadVisibleExpandedDetails()`, selected-event/detail reconciliation, timeline click handlers, and enclosing-context late-response checks. The enclosing callback passes the closed `enclosingAffordance` purpose through this accessor, so each mounted card performs one Map get and only a miss may perform the existing O(1) temporary-ID comparison. / `currentTimelineEvent(eventId, purpose = 'canonical')`；这会让 `syncEnclosingOperationAffordances()` 通过同一 accessor，同时保留其精确 temporary-event fallback，并继续拥有 `loadVisibleExpandedDetails()`、selected-event／detail reconciliation、timeline click handler 与 enclosing-context late-response check。Enclosing callback 通过此 accessor 传递封闭的 `enclosingAffordance` purpose，因此每张 mounted card 执行一次 Map get，且只有 miss 才可执行既有 O(1) temporary ID comparison。
- Direct canonical retrieval in `setActiveSearchTarget()`, `materializeSearchEvent()`, `resolveSearchTargetNode()`, `restoreFocus()` after `ensureEventLoaded()`, `drillDownProjectResult()`, `openInheritedSourceSession()`, structured-filter selected-event restoration in `loadTimeline()`, and `revealEnclosingOperation()`. / 上述函数中的直接 canonical retrieval。
- Exact canonical membership checks in `ensureEventLoaded()`, mounted-parent inspection, temporary-reveal collision cleanup, and `renderTimeline()`'s temporary-reveal canonical-membership decision, only where replacing `.some(id)` does not alter order or fallback semantics. / `ensureEventLoaded()`、mounted-parent inspection、temporary-reveal collision cleanup，以及 `renderTimeline()` 的 temporary-reveal canonical-membership decision 中的精确 canonical membership check；仅限替换 `.some(id)` 不改变顺序或 fallback 语义的位置。

Do not migrate ordered `findIndex()`/nearest-event traversal, `navigationCache.events`, category/filter/render traversal, search target discovery, page accumulation, or temporary-row insertion. The reviewed `renderTimeline()` membership decision above is an exact-ID lookup, not migration of the surrounding ordered render traversal. Any additional call site requires reviewer evidence that it is exact canonical-ID retrieval and does not depend on order or another owner. / 不得迁移有序 `findIndex()`／nearest-event 遍历、`navigationCache.events`、category／filter／render traversal、search target discovery、page accumulation 或 temporary-row insertion。上文经评审的 `renderTimeline()` membership decision 属于精确 ID lookup，不是迁移其周围有序 render traversal。任何额外 call site 都需要 reviewer evidence，证明它是 canonical 精确 ID retrieval 且不依赖顺序或其它 owner。

### 9.2 Smallest testable seam / 最小可测试 seam

`src/browser/timeline-event-state.js` must be a pure, browser-compatible CommonJS module used by `app.js` and directly imported by Node tests. It owns committed context/array/Map empty/reset and atomic replacement construction, validated append, exact Map get/has, the correctness parity snapshot, an observer-gated profiling snapshot seam, and a small indexed-card iteration helper used by `syncEnclosingOperationAffordances()`. The profiling seam must return before parity computation unless a callable `recordStateSnapshot` exists; the helper accepts an iterable of mounted cards and performs exactly one supplied Map-backed canonical lookup per card. Presentation/update callbacks remain in `app.js`. / `src/browser/timeline-event-state.js` 必须是由 `app.js` 使用、可被 Node test 直接 import 的纯 browser-compatible CommonJS module。它拥有 committed context／array／Map empty／reset 与原子 replacement construction、validated append、精确 Map get／has、correctness parity snapshot、observer-gated profiling snapshot seam，以及供 `syncEnclosingOperationAffordances()` 使用的小型 indexed-card iteration helper。Profiling seam 在存在可调用 `recordStateSnapshot` 前必须先返回、不得计算 parity；helper 接收 mounted card iterable，并对每张 card 精确执行一次传入的 Map-backed canonical lookup。Presentation／update callback 继续位于 `app.js`。

Focused Node operation proof must use a counting Map/get seam and many synthetic card objects to assert: / 聚焦 Node operation proof 必须使用 counting Map／get seam 与多张合成 card object 断言：

- A positive `C > 0` cards cause exactly `C` card iterations, `C` lookup requests, `C` Map-backend requests, and `C` Map gets. All-zero counters are structurally parseable diagnostic evidence but recompute `enclosingCardGetsMatch=false` and cannot support accepted worker or aggregate evidence. / 正数 `C > 0` 张 card 产生精确 `C` 次 card iteration、`C` 次 lookup request、`C` 次 Map-backend request 与 `C` 次 Map get。全零 counter 仍是结构可解析的诊断 evidence，但必须重算为 `enclosingCardGetsMatch=false`，不能支撑 accepted worker 或 aggregate evidence。
- No canonical array or array comparator is accepted by the helper, so the proof is runtime operation evidence, not a source-text search for removed `.find()`. / Helper 不接受 canonical array 或 array comparator，因此该证明是 runtime operation evidence，而不是搜索 `.find()` 是否被删除的 source-text assertion。
- Missing IDs preserve skip/fallback behavior and do not reuse the preceding event. / 缺失 ID 保持 skip／fallback 行为，不复用前一 event。
- A true `temporaryEventReveal` stays outside the canonical array/Map. Its mounted card still receives exactly one Map get, then the existing O(1) exact temporary-ID fallback; selection and `contextRevealPending` changes must update that card's enclosing affordance without any canonical array comparison. / 真正的 `temporaryEventReveal` 继续位于 canonical array／Map 之外。其 mounted card 仍精确执行一次 Map get，随后使用既有 O(1) 精确 temporary-ID fallback；selection 与 `contextRevealPending` 变化必须更新该 card 的 enclosing affordance，且不执行任何 canonical array comparison。
- Increasing canonical array length without increasing card count does not change the number of exact-ID lookup operations. / 增加 canonical array 长度但不增加 card count 时，精确 ID lookup operation 数不变。

### 9.3 Synthetic browser instrumentation / 合成浏览器 instrumentation

Extend `scripts/timeline-profile.js::installPageInstrumentation()` rather than creating a second profiler. The init script may install a content-free optional observer consumed by `app.js`/the state helper. `app.js::recordTimelineEventState()` must call only the gated profiling seam, so absent/incompatible observers impose no parity scan; the correctness parity function remains directly available to `assertTimelineEventState()`. At scenario reset/snapshot the observer records: / 扩展 `scripts/timeline-profile.js::installPageInstrumentation()`，不得创建第二套 profiler。Init script 可以安装由 `app.js`／state helper 消费的不含内容 optional observer。`app.js::recordTimelineEventState()` 只能调用 gated profiling seam，因此 observer 缺席／不兼容时不产生 parity scan；correctness parity function 继续直接供 `assertTimelineEventState()` 使用。在 scenario reset／snapshot 时 observer 记录：

- automatic-bootstrap Session and Project file-suggestion request counts, distinguished by presence/absence of `sessionId`; / 自动 bootstrap 的 Session／Project file-suggestion 请求 count，以是否存在 `sessionId` 区分；
- one zero-argument automatic-selection settlement marker emitted by `loadSessions()` immediately after its delegated `selectSession()` settles and before any six-guard skip/fallback decision; the page-owned fetch wrapper stores its current monotonic suggestion sequence as the watermark, so normal bootstrap is settlement count `1`, watermark `1`, and zero post-settlement Session requests without serializing identity or content; / `loadSessions()` 在被委托 `selectSession()` settle 后、任何六项 guard skip／fallback decision 前发出一次零参数 automatic-selection settlement marker；page-owned fetch wrapper 把其当前单调 suggestion sequence 存为 watermark，因此正常 bootstrap 的 settlement count 为 `1`、watermark 为 `1`、settle 后 Session request 为零，且不序列化 identity 或 content；
- per-scenario Session/Project suggestion counts and normalized unchanged parameters; / 逐 scenario 的 Session／Project suggestion count 与规范化且不变的参数；
- canonical exact-ID lookup requests, actual Map get operations, enclosing-affordance card iterations and Map gets, grouped by a closed content-free purpose alias; / canonical 精确 ID lookup request、实际 Map get operation、enclosing-affordance card iteration 与 Map get，并按封闭、不含内容的 purpose alias 分组；
- final array length, Map size, unique-ID count, object-identity parity verdict, committed-context binding verdict, pending-replacement state, and parity failure count, with no IDs serialized; a valid pending desired context must not be mislabeled as stale committed state; / 最终 array length、Map size、unique-ID count、object-identity parity verdict、committed-context binding verdict、pending-replacement state 与 parity failure count，且不序列化 ID；合法 pending desired context 不得被误标为 stale committed state；
- existing full render/card/highlight/discovery, DOM, Long Task, Resource Timing, materializer, request-family, functional, and causal evidence unchanged. / 既有 full render／card／highlight／discovery、DOM、Long Task、Resource Timing、materializer、request family、functional 与 causal evidence 保持不变。

Structural acceptance is relational per run: enclosing-affordance Map gets equal its card iterations, its array-comparison count is zero, all canonical lookup operations use the Map backend, and every parity verdict passes. Absolute lookup/iteration totals are recorded and may be observational across scheduling-sensitive scenarios; the relation and zero-scan verdict are hard exact. / 结构验收按每次 run 使用关系约束：enclosing-affordance Map get 等于其 card iteration、array comparison count 为零、全部 canonical lookup operation 使用 Map backend，且每个 parity verdict 通过。绝对 lookup／iteration total 会记录；在调度敏感场景中可以是 observational，但相等关系与零 scan verdict 属于 hard exact。

Do not accept the mere presence of a Map, a source-text grep, or a faster timing. The Node seam plus real-browser operation ledger and parity snapshot are jointly required. / 不接受仅存在 Map、source-text grep 或更快 timing。必须同时具备 Node seam、真实浏览器 operation ledger 与 parity snapshot。

## 10. Functional and causal preservation requirements / 功能与因果保持要求

Every focused test and every accepted profile run must retain the Wave 0 contracts: / 每个聚焦 test 与每次 accepted profile run 都必须保留 Wave 0 contract：

- selected Session role/identity; canonical loaded/card/mark counts; result summary and Load more state; / selected Session role／identity；canonical loaded／card／mark count；result summary 与 Load more state；
- late-hit target at fixture ordinal 1,650 and monotonic discovered target identity; / fixture ordinal 1,650 的 late-hit target 与单调 discovered target identity；
- deep structured filter page-zero replacement without residual append; / deep structured filter page-zero replacement，且无 residual append；
- temporary/context reveal isolation with zero canonical array/Map/target/count mutation; / temporary／context reveal 隔离，canonical array／Map／target／count 零 mutation；
- explicit cold Session switch to exactly 40 secondary cards, no primary/unknown post-selection DOM commit, one secondary materializer call, and no visible error; / 显式 cold Session switch 到精确 40 张 secondary card、selection 后无 primary／unknown DOM commit、secondary materializer 精确一次、无可见错误；
- no stale timeline, suggestion, analysis, detail, Raw Reference, context-reveal, or Map commit after owner/context retirement; / owner／context retire 后无 stale timeline、suggestion、analysis、detail、Raw Reference、context reveal 或 Map commit；
- delayed Layer/filter/query replacement leaves the still-visible committed cards fully interactive—click, selection, detail, and enclosing-operation behavior remains owned by the rendered committed pair—until a successful response atomically replaces it; / 延迟的 Layer／filter／query replacement 期间，仍可见的 committed card 保持完整交互能力——click、selection、detail 与 enclosing-operation 行为继续由已渲染 committed pair 拥有——直至成功响应将其原子替换；
- locale reload preserves the inspected product lifecycle by retiring the old pair through `loadSessions()` → `selectSession()` before publishing the new localized first page; it does not masquerade as a retained-pair pending replacement; / locale reload 通过 `loadSessions()` → `selectSession()` 在发布新的 localized first page 前 retire 旧 pair，从而保留经检查的产品 lifecycle；它不伪装成保留旧 pair 的 pending replacement；
- zero unintended materialization in warm scenarios; exact request offsets/limits/page counts and causal partial-order verdicts; / warm 场景零意外 materialization；精确 request offset／limit／page count 与因果 partial-order verdict；
- sibling analysis/timeline/file-suggestion requests remain logically unordered; observed order is descriptive only; / sibling analysis／timeline／file-suggestion request 继续无逻辑 total order；观测顺序只作描述；
- no change to API parameters, final suggestion content, error copy, folding overrides, detail ownership, locale identity, or responsive behavior. / API parameter、最终 suggestion content、error copy、folding override、detail ownership、locale identity 与 responsive behavior 均不变。

Suggestion-specific hard acceptance: / 候选专属 hard acceptance：

1. Normal automatic initial Session selection issues exactly one Session-scoped suggestion request, and no equivalent second request starts after `selectSession()` settles. / 正常自动初始会话选择精确发出一次 Session-scope suggestion request，且 `selectSession()` settle 后不再启动等价的第二次请求。
2. Explicit later Session selection and project-result drill-down each issue their intended single Session refresh. / 显式后续 Session selection 与项目结果下钻各自发出预期的一次 Session refresh。
3. Project Scope entry, Project-scope `loadSessions()`, empty-Session state, Layer change, source/project replacement, locale reload, and revision recovery retain their legitimate request ownership. / Project Scope entry、Project-scope `loadSessions()`、空 Session state、Layer change、source／project replacement、locale reload 与 revision recovery 保留合法 request ownership。
4. Superseded/aborted suggestions cannot commit to the wrong Session; a successful first commit cannot trigger a fallback duplicate. / 被取代／aborted suggestion 不能 commit 到错误 Session；成功的第一次 commit 不能触发 fallback duplicate。
5. Request parameters and final suggestion content/order/count are unchanged. / Request parameter 与最终 suggestion content／order／count 保持不变。
6. A superseded outer `loadSessions()` cannot issue a tail fallback for a newer suggestion generation, even when Sessions data context, selected Session, scope, and suggestion context are unchanged. Only an outer request passing all six handoff guards may skip or fall back, and bounded revision recovery remains the sole recovery owner. / 已被取代的外层 `loadSessions()` 不能为更新 suggestion generation 发出 tail fallback，即使 Sessions data context、selected Session、scope 与 suggestion context 全部不变。只有通过全部六项 handoff guard 的外层 request 才能 skip 或 fallback，且有界 revision recovery 继续作为唯一 recovery owner。
7. For same-Session/same-context re-selection, both schedules are hard acceptance: (A) the newer suggestion generation is still pending when the older outer call resumes; (B) the newer generation has already committed when the older outer call resumes. In both, there is no third fallback request, the old outer call does not abort the newer request, only the newer request may commit, and the explicit later Session selection retains its intended single request. / 对 same-Session／same-context re-selection，两种 schedule 都是 hard acceptance：（A）旧外层 call 恢复时，更新 suggestion generation 仍 pending；（B）旧外层 call 恢复时，更新 generation 已 commit。两种情形都要求：无第三次 fallback request；旧外层 call 不 abort 更新 request；只有更新 request 可以 commit；显式后续 Session selection 保留其预期的一次请求。

Map-specific hard acceptance is the invariant list in section 8 plus behavior parity for search navigation, enclosing-operation affordances, detail selection/ownership, inherited/fork target navigation, delayed Layer/filter/query replacement, append, Session switch, and the full-reset locale reload. Making the old visible DOM inert during a retained-pair replacement is an out-of-scope product decision and a mandatory stop, not an acceptable implementation shortcut. / Map 专属 hard acceptance 是第 8 节 invariant 列表，并要求 search navigation、enclosing-operation affordance、detail selection／ownership、inherited／fork target navigation、延迟 Layer／filter／query replacement、append、Session switch 与 full-reset locale reload 行为等价。在 retained-pair replacement 期间使旧可见 DOM inert 属于超范围产品决策与强制停止条件，不是可接受的实施捷径。

## 11. Profiling, calibration, baseline, and comparison protocol / Profiling、calibration、baseline 与比较协议

### 11.1 Evidence candidate and sequence / Evidence candidate 与顺序

After implementation and all focused/full gates pass, freeze one clean local evidence candidate commit. Record exact `candidateCommitSha`, live `targetSyncSha`, ancestry, exact target-to-candidate diff hash under the existing Wave 0 algorithm, `profiledImplementationTreeHash`, runtime asset hash, environment, and clean worktree. No implementation or expectation changes may occur between smoke, calibration, and five-run evidence. Documentation-only evidence recording stays separately identified. / 实施与全部聚焦／全量 gate 通过后，冻结一个干净本地 evidence candidate commit。按既有 Wave 0 algorithm 记录精确 `candidateCommitSha`、实时 `targetSyncSha`、ancestry、精确 target-to-candidate diff hash、`profiledImplementationTreeHash`、runtime asset hash、环境与干净工作树。Smoke、calibration 与五次 evidence 之间不得改变 implementation 或 expectation。纯文档 evidence 记录保持独立 identity。

Run in this order: / 按以下顺序运行：

1. One post-implementation synthetic browser smoke in a fresh process. / 一个 fresh process 中的实施后合成 browser smoke。
2. Three independent synthetic calibration processes, each exercising all five accepted scenarios and bootstrap capture once. / 三个独立合成 calibration process，各自执行全部五个已接受场景与一次 bootstrap capture。
3. Before seeing any five-run result, freeze only the existing eligible render/card/highlight/discovery counters whose three values agree and whose scenario semantics permit exactness. / 在查看任何五次 run 结果前，只冻结三次值一致且场景语义允许 exact 的既有 eligible render／card／highlight／discovery counter。
4. Structural suggestion counts, Map parity, zero-array-scan, and lookup/card relational verdicts are hard invariants, not calibration candidates. Scheduling-sensitive absolute lookup totals remain observational. / 结构性 suggestion count、Map parity、零 array scan 与 lookup／card 关系 verdict 是 hard invariant，不是 calibration candidate。调度敏感的绝对 lookup total 保持 observational。
5. Five independent synthetic browser baseline/comparison processes using the frozen exact set and identical fixture/environment. / 使用冻结 exact set 与相同 fixture／environment 的五个独立合成 browser baseline／comparison process。

Preserve invalid attempts and failed groups as diagnostic history. A run is invalid only for a documented environment/structure breach, hard-invariant failure, causal failure, or frozen-counter failure. Slow observations are retained. Never add/remove a frozen counter after seeing the five-run results; diagnosis requires a new labeled smoke/calibration/baseline group. / 保留 invalid attempt 与 failed group 作为诊断历史。只有已记录的 environment／structure breach、hard invariant 失败、causal 失败或 frozen counter 失败才能使 run invalid。较慢观测必须保留。看到五次 run 结果后绝不能增删 frozen counter；诊断后必须创建新的具名 smoke／calibration／baseline group。

Canonical M5–M8 evidence must never be stored under `%TEMP%`, `%TMP%`, the resolved Windows temporary directory, runner-owned scratch space, a sandbox/workspace ephemeral root, or the Git repository. Smoke, calibration, five-run comparison, frozen-decision records, and packet projections must be written directly to one preflighted durable repository-external root; Temp-to-durable copying is not the normal production path. / Canonical M5 至 M8 evidence 绝不得存放在 `%TEMP%`、`%TMP%`、解析后的 Windows 临时目录、runner-owned scratch space、sandbox／workspace ephemeral root 或 Git repository 下。Smoke、calibration、五次 comparison、frozen-decision record 与 packet projection 必须直接写入一个通过 preflight、位于 repository 外的 durable root；从 Temp 复制到 durable 位置不是正常 production path。

After each producing process exits and before an artificial review stop, a fresh independent process must enumerate canonical regular-file membership, reject links/reparse points/unsupported entries/duplicates/case collisions, bind every filename to raw byte length and lowercase SHA-256, reopen and rehash every file, and seal the completed stage. A directory ReadOnly attribute or checkbox is not a seal: use an enforceable ACL or equivalent read-only archive and byte-level verification that proves reads succeed while create, append, replacement, and deletion fail. / 每个 producing process 退出后、人工 review stop 前，必须由 fresh 独立 process 枚举 canonical regular-file membership，拒绝 link／reparse point／unsupported entry／duplicate／大小写冲突，按 raw byte length 与小写 SHA-256 绑定每个 filename，重新打开并重新 hash 每个文件，再封存已完成 stage。Directory ReadOnly attribute 或 checkbox 不构成 seal：必须使用可执行 ACL 或等价 read-only archive，并通过 byte-level verification 证明读取成功且 create、append、replacement 与 deletion 均失败。

The sealed native calibration artifacts and separate frozen decision must remain available and hash-equal immediately before M7. Every M6/M7 artifact must remain available and hash-equal immediately before M8. Before every later stage and reviewer stop, repeat complete existence/membership/length/hash verification from a fresh process. A missing, unreadable, membership-changing, or hash-changing canonical artifact is a fail-closed packet-source defect that supersedes the group. Summaries, console output, and recorded lengths/hashes cannot reconstruct missing raw bytes. / 在 M7 前一刻，已封存的 native calibration artifact 与独立 frozen decision 必须仍然可用且 hash 相等；在 M8 前一刻，全部 M6／M7 artifact 必须仍然可用且 hash 相等。在每个后续 stage 与 reviewer stop 前，必须由 fresh process 重复完整 existence／membership／length／hash 验证。Canonical artifact 缺失、不可读、membership 改变或 hash 改变，均是使该 group 被取代的 fail-closed packet-source defect。Summary、console output 与已记录 length／hash 不能重建缺失的 raw byte。

### 11.2 Required recorded dimensions / 必需记录维度

Each run/summary must retain: / 每次 run／summary 必须保留：

- normalized request family records/counts, Session/Project suggestion scope counts, outcomes, offsets/limits, detail/envelope counts, and causal verdicts; / normalized request family record／count、Session／Project suggestion scope count、outcome、offset／limit、detail／envelope count 与 causal verdict；
- bootstrap automatic-selection suggestion count and proof that no post-settlement equivalent request occurred; / bootstrap 自动 selection suggestion count，以及 settle 后没有等价请求的证明；
- exact-ID lookup operations, Map gets, enclosing-affordance card iterations/Map gets, zero-array-scan verdict, Map parity, committed-context binding, pending-replacement interaction, and stale-owner verdicts; / 精确 ID lookup operation、Map get、enclosing-affordance card iteration／Map get、零 array scan verdict、Map parity、committed-context binding、pending replacement 交互与 stale-owner verdict；
- full renders, card generations, highlight passes/marks/owners, target discovery, context-row insertions, DOM counts, materializer calls, Long Tasks, and Resource Timing; / full render、card generation、highlight pass／mark／owner、target discovery、context-row insertion、DOM count、materializer call、Long Task 与 Resource Timing；
- final functional state/digest and every Wave 0 causal constraint; / 最终 functional state／digest 与每个 Wave 0 causal constraint；
- exact ref/tree/fixture/runtime identity and process-isolation evidence. / 精确 ref／tree／fixture／runtime identity 与 process-isolation evidence。

Timing, Long Tasks, Resource Timing, and scheduling-sensitive absolute work remain observational distributions with repeat count, median, minimum, and maximum. There is no numerical latency gate. / Timing、Long Task、Resource Timing 与调度敏感绝对 work 继续作为包含 repeat count、median、minimum 与 maximum 的 observational distribution。不设置数值 latency gate。

### 11.3 Wave 0 comparison policy / Wave 0 比较 policy

- Reuse the accepted Wave 0 five-scenario functional/causal baseline and descriptive timing distributions. Do not recollect or reinterpret Wave 0. / 复用已接受 Wave 0 五场景 functional／causal baseline 与描述性 timing distribution。不得重新采集或重新解释 Wave 0。
- Exact structural before/after claim 1: the confirmed initial automatic-selection call chain changes from two equivalent Session suggestion invocations at `afbb30eb…` to one on the Wave 1A candidate; runtime after-count is five-run exact. / 精确结构 before／after claim 1：已确认的自动初始 selection 调用链从 `afbb30eb…` 上两次等价 Session suggestion invocation 变为 Wave 1A candidate 上一次；runtime after-count 在五次 run 中精确。
- Exact structural before/after claim 2: the confirmed enclosing-affordance path changes from one `Array.find(currentEvents)` per card to one Map-backed lookup per card, with runtime zero-scan/operation relation proof. / 精确结构 before／after claim 2：已确认 enclosing-affordance 路径从逐 card 一次 `Array.find(currentEvents)` 变为逐 card 一次 Map-backed lookup，并提供 runtime 零 scan／operation relation proof。
- Functional state and causal constraints must match exactly. Timing distributions may be described side by side but cannot establish a deterministic cross-run millisecond improvement. / Functional state 与 causal constraint 必须精确匹配。Timing distribution 可以并列描述，但不能据此建立确定性跨 run 毫秒改善。
- No claim may attribute improvement to cold source build, commit validation, cold materialization, exact warm-hit latency, ProjectQueryStore, or other server domains. / 不得把改善归因到 cold source build、commit validation、cold materialization、exact warm-hit latency、ProjectQueryStore 或其它 server domain。

### 11.4 Private server evidence reuse / 私有 server evidence 复用

The accepted Wave 0 private evidence remains bound only to the exact Wave 0 candidate, implementation tree, runtime/fixture identity, packet index/hash, schemas, and proof algorithms publicly recorded by `docs/exec-plans/completed/2026-08-24-performance-wave-0-baseline.md`. Wave 1A cites that public accepted identity as historical server-domain evidence; it never copies the private artifacts, relabels an old run as Wave 1A, inserts it into a Wave 1A manifest, or claims a Wave 1A private-corpus execution. / 已接受的 Wave 0 private evidence 只绑定到 `docs/exec-plans/completed/2026-08-24-performance-wave-0-baseline.md` 公开记录的精确 Wave 0 candidate、implementation tree、runtime／fixture identity、packet index／hash、schema 与 proof algorithm。Wave 1A 只把该公开 accepted identity 作为历史 server-domain evidence 引用；绝不复制 private artifact、把旧 run 重新标记为 Wave 1A、把它写入 Wave 1A manifest，或声称执行过 Wave 1A private-corpus run。

Reuse is allowed only because the expected implementation changes browser source, generated browser output, browser-focused tests/profiling, and docs. The exact target-to-candidate review must prove that no server-domain implementation path changed, including `server.js`, `src/session-query.js`, Materialized Session/ProjectQueryStore owners, canonical validation/fingerprint code, source adapters/parsers, cache, scheduler, revision lease, and prewarm. This is a path-and-function diff proof, not an inference from test results. / 只有因为预期实施仅改变 browser source、生成 browser output、browser 聚焦 test／profiling 与文档，才允许复用。精确 target-to-candidate review 必须证明没有 server-domain implementation path 变化，包括 `server.js`、`src/session-query.js`、Materialized Session／ProjectQueryStore owner、canonical validation／fingerprint code、source adapter／parser、cache、scheduler、revision lease 与 prewarm。这是 path 与 function diff 证明，不是从 test result 推断。

Although `scripts/performance-wave-0-runner.js` is in the Wave 1A envelope, additions there must be reachable only from the synthetic-browser command/schema/aggregation branch. Focused locked-fixture tests must prove that the private artifact schema, private manifest validation, private summary aggregation, failure tuples, and snapshot-proof semantics remain unchanged. Existing accepted private schema/algorithm version identifiers must not be bumped or reinterpreted by browser-only additions. / 虽然 `scripts/performance-wave-0-runner.js` 位于 Wave 1A envelope 内，但其中新增逻辑必须只能从 synthetic-browser command／schema／aggregation 分支到达。聚焦 locked-fixture test 必须证明 private artifact schema、private manifest validation、private summary aggregation、failure tuple 与 snapshot-proof 语义保持不变。Browser-only 新增不得提升或重新解释既有 accepted private schema／algorithm version identifier。

If implementation cannot keep those shared private paths unchanged, stop before smoke or evidence capture and choose one separately reviewed remedy: split the browser aggregation into an isolated browser-only helper with an explicit changed-path-envelope amendment, or recollect private evidence under a separately reviewed protocol bound to a new candidate/schema/packet identity. Historical Wave 0 evidence alone is then insufficient. / 若实施无法保持这些 shared private path 不变，必须在 smoke 或 evidence capture 前停止，并选择一个单独评审的补救方案：通过显式 changed-path-envelope amendment 把 browser aggregation 拆入隔离的 browser-only helper，或在绑定新 candidate／schema／packet identity 的单独评审 protocol 下重新采集 private evidence。此时仅有历史 Wave 0 evidence 不再充分。

## 12. Exact expected changed paths / 精确预期 changed path

### 12.1 Authorized implementation-candidate path set / 获授权 implementation-candidate path set

Source inspection establishes this exact expected candidate envelope: / 源码检查建立以下精确预期 candidate envelope：

1. `docs/exec-plans/active/2026-08-27-performance-wave-1a-browser-hot-path.md` — live plan/progress evidence. / 活跃计划／进度 evidence。
2. `AGENTS.md` — active-plan registration during execution only. / 仅用于执行期间 active-plan 登记。
3. `docs/design-docs/timeline-loading-and-rendering-performance.md` — bilingual browser Map, suggestion ownership, Wave 1A evidence, and later-wave boundary. / 双语 browser Map、suggestion ownership、Wave 1A evidence 与 later-wave boundary。
4. `src/browser/app.js` — the two product changes, integration with the state boundary, and inert content-free profiling calls. / 两项产品变更、state boundary 集成与 inert 不含内容 profiling call。
5. `src/browser/timeline-event-state.js` — new pure derived-index/transaction/operation-count seam. / 新增纯 derived-index／transaction／operation-count seam。
6. `test/timeline-event-state.test.js` — replacement, append, duplicate, parity, lookup, and linear-operation proof. / replacement、append、duplicate、parity、lookup 与线性 operation 证明。
7. `e2e/browser.test.js` — focused real-browser request ownership, Map lifecycle, stale owner, temporary reveal, and migrated-consumer behavior. / 聚焦真实浏览器 request ownership、Map lifecycle、stale owner、temporary reveal 与 migrated-consumer 行为。
8. `scripts/timeline-profile.js` — additive bootstrap suggestion, lookup-operation, and Map-parity instrumentation in the accepted harness. / 在已接受 harness 中增加 bootstrap suggestion、lookup operation 与 Map parity instrumentation。
9. `test/timeline-profile.test.js` — focused artifact/acceptance/counter relation tests. / 聚焦 artifact／acceptance／counter relation test。
10. `scripts/performance-wave-0-runner.js` — add the new structural paths/relations only to the synthetic-browser validation and independent-process aggregation branch, without changing the six-domain model or any private artifact/schema/validation/summary/failure/snapshot-proof path. / 只把新结构 path／relation 加入 synthetic-browser validation 与独立进程 aggregation 分支，不改变六域模型或任何 private artifact／schema／validation／summary／failure／snapshot-proof path。
11. `test/performance-wave-0-runner.test.js` — exact aggregation/calibration/invalid-group regressions for the added browser evidence plus locked-fixture proof that all private runner semantics remain unchanged. / 新增 browser evidence 的精确 aggregation／calibration／invalid-group regression，以及全部 private runner 语义保持不变的 locked-fixture 证明。
12. `public/assets/app.js` — generated only by `npm run build:client`, never edited manually. / 只通过 `npm run build:client` 生成，绝不手工编辑。

`scripts/timeline-profile-fixture.js` is not expected to change: it already supplies 1,800 primary Main events, 40 secondary Main events, late hit 1,650, and context reveal. `scripts/performance-wave-0-identity.js` is not expected to change: its implementation hash already covers `src`, `scripts`, `test`, `public`, and package inputs, and Wave 1A does not rebuild the sealed Wave 0 review packet. `src/browser/entry.js` is not expected to change because `app.js` can require the new module through the existing bundle graph. / `scripts/timeline-profile-fixture.js` 预期不变：它已提供 primary 1,800 个 Main event、secondary 40 个 Main event、late hit 1,650 与 context reveal。`scripts/performance-wave-0-identity.js` 预期不变：其 implementation hash 已覆盖 `src`、`scripts`、`test`、`public` 与 package input，且 Wave 1A 不重建密封 Wave 0 review packet。`src/browser/entry.js` 预期不变，因为 `app.js` 可以通过既有 bundle graph require 新 module。

No `server.js`, `src/session-query.js`, canonical contract/schema, source adapter/parser, ProjectQueryStore, Materialized Session owner, package/lockfile, workflow, README, product spec, or other generated/public path is expected. Any such need is a mandatory reviewer checkpoint, not implicit permission. / 不预期修改 `server.js`、`src/session-query.js`、canonical contract／schema、source adapter／parser、ProjectQueryStore、Materialized Session owner、package／lockfile、workflow、README、product spec 或其它 generated／public path。任何此类需要都必须进入 reviewer checkpoint，不构成隐式授权。

### 12.2 Closeout path transition / Closeout path 转换

Only after independent completion review accepts the exact candidate and evidence may closeout: / 只有独立完成评审接受精确 candidate 与 evidence 后，closeout 才能：

- move this plan from `docs/exec-plans/active/2026-08-27-performance-wave-1a-browser-hot-path.md` to `docs/exec-plans/completed/2026-08-27-performance-wave-1a-browser-hot-path.md`; / 把本计划从 active path 移到 completed path；
- change the performance-design reference from active to completed; / 把 performance design reference 从 active 改为 completed；
- remove the Wave 1A active entry from `AGENTS.md`; / 从 `AGENTS.md` 移除 Wave 1A active entry；
- preserve all implementation/evidence identities and rerun documentation/diff/currentness checks, without recollecting evidence unless executable inputs changed. / 保持全部 implementation／evidence identity，并重跑 documentation／diff／currentness check；除非 executable input 变化，否则不重新采集 evidence。

## 13. Milestones and mandatory reviewer stops / 里程碑与强制 reviewer stop

### M0 — Revalidate and register / 重新核验并登记

Work: resolve live default/target refs; reread target guidance and authorities; verify ancestry, exact relevant-path drift, toolchain, clean/known worktree, and the path envelope. After explicit implementation authorization, register this active plan in `AGENTS.md`. / 工作：解析实时 default／target ref；重读目标 guidance 与 authority；核验 ancestry、精确 relevant-path drift、toolchain、干净／已知 worktree 与 path envelope。取得明确实施授权后，在 `AGENTS.md` 登记本 active plan。

Gate: no relevant drift; no unowned changes; active registration exact. / 门槛：无相关 drift；无未归属变更；active 登记精确。

### M1 — Install the test/evidence seam without changing behavior / 在不改变行为的情况下建立 test／evidence seam

Work: add `timeline-event-state.js`, focused Node tests, and the minimal content-free Wave 0 instrumentation/runner extensions. First prove the current fixture/request classifier can distinguish Session versus Project suggestions and the card-iteration seam measures Map gets without IDs/content. Do not collect long groups. / 工作：增加 `timeline-event-state.js`、聚焦 Node test 与最小不含内容的 Wave 0 instrumentation／runner 扩展。先证明当前 fixture／request classifier 能区分 Session 与 Project suggestion，并证明 card-iteration seam 能在不输出 ID／内容的情况下量测 Map get。不得采集长 group。

Gate: independent reviewer accepts the seam as content-free, inert outside profiling, non-user-visible, and not a second profiling model. / 门槛：独立 reviewer 接受该 seam 不含内容、profiling 外 inert、不面向用户，且不是第二套 profiling model。

### M2 — Remove only the duplicate automatic Session suggestion / 只移除重复自动 Session suggestion

Work: implement the exact closed three-field `selectSession()` result, return the delegated refresh's own locally allocated generation, and apply all six outer `loadSessions()` handoff guards before either skip or fallback. Add focused browser coverage for normal initial selection, explicit later selection, Project Scope, project-result drill-down, Layer refresh, source/project/locale/revision reload, aborted/stale delegated work, and both pending/already-committed schedules for a newer same-Session/same-context generation. / 工作：实施精确封闭的三字段 `selectSession()` result，返回被委托 refresh 自身本地分配的 generation，并在 skip 或 fallback 前执行全部六项外层 `loadSessions()` handoff guard。增加聚焦 browser 覆盖：正常初始 selection、显式后续 selection、Project Scope、项目结果下钻、Layer refresh、source／project／locale／revision reload、aborted／stale 委托 work，以及更新 same-Session／same-context generation 仍 pending／已经 commit 的两种 schedule。

Gate: normal initial Session request count is exactly one; all six handoff guards are executable and covered; in both same-context schedules the superseded outer request creates no third fallback and does not abort the newer generation, only the newer generation commits, explicit later selection still owns one request, bounded revision recovery remains the only recovery owner, all other legitimate counts/parameters/content are exact, and no server/API change occurs. / 门槛：正常初始 Session request count 精确为一；全部六项 handoff guard 可执行且有覆盖；两种 same-context schedule 中被取代的外层 request 都不创建第三次 fallback、也不 abort 更新 generation，只有更新 generation commit，显式后续 selection 仍拥有一次请求；有界 revision recovery 继续作为唯一 recovery owner；其它合法 count／parameter／content 全部精确；无 server／API 变更。

### M3 — Add and migrate the browser event Map / 增加并迁移 browser event Map

Work: initialize `currentEventsById`; route committed context/array/Map mutations through reset/replace/append helpers; keep `currentTimelineEvent()` bound to the rendered committed pair during pending replacement; add only consumer-local desired-context checks where an existing contract requires them; preserve the current temporary fallback; migrate only approved consumers; add duplicate fail-closed behavior and parity probes. / 工作：初始化 `currentEventsById`；让 committed context／array／Map mutation 经过 reset／replace／append helper；replacement pending 期间让 `currentTimelineEvent()` 继续绑定已渲染 committed pair；只在既有契约要求时增加 consumer-local desired-context check；保留当前 temporary fallback；只迁移已批准 consumer；增加 duplicate fail-closed 行为与 parity probe。

Gate: focused Node/browser tests prove every lifecycle invariant, stale-owner isolation, migrated behavior, delayed-replacement old-card click/selection/detail/enclosing-operation continuity, atomic new-pair publish, and the `C` cards → `C` Map gets operation relation with zero array comparisons. / 门槛：聚焦 Node／browser test 证明每个 lifecycle invariant、stale-owner isolation、migrated 行为、延迟 replacement 期间旧 card 的 click／selection／detail／enclosing-operation 连续性、新 pair 原子 publish，以及 `C` cards → `C` Map get 且 array comparison 为零的 operation relation。

### M4 — Integrate browser behavior, generate, and document / 集成 browser 行为、生成并记录

Work: exercise existing browser transition/context/detail/search tests, update bilingual timeline performance design with fixed Wave 1A decisions and later-wave exclusions, run `npm run build:client`, and inspect generated diff. Do not manually edit the bundle. / 工作：执行既有 browser transition／context／detail／search test；用固定 Wave 1A 决策与 later-wave 排除项更新双语 timeline performance design；运行 `npm run build:client` 并检查 generated diff。不得手工编辑 bundle。

Gate: generated output is exactly reproducible/current; product spec remains unchanged because visible behavior is unchanged. Any visible contract change stops. / 门槛：generated output 精确可复现／current；由于可见行为不变，product spec 保持不变。任何可见 contract 变化都必须停止。

### M5 — Focused and full validation / 聚焦与全量验证

Work: run the focused commands in section 14, complete source/diff review, then all full repository/release gates on one exact clean candidate. Record actual test totals. Perform an explicit target-to-candidate server-domain path/function audit and locked-fixture review of private artifact schema, manifest validation, summary aggregation, failure tuples, and snapshot-proof semantics. / 工作：运行第 14 节聚焦命令、完成 source／diff review，然后在一个精确干净 candidate 上运行全部完整 repository／release gate。记录实际 test total。执行显式 target-to-candidate server-domain path／function audit，并用 locked fixture 评审 private artifact schema、manifest validation、summary aggregation、failure tuple 与 snapshot-proof 语义。

Gate: every gate passes; exact changed paths equal section 12; no unrelated diff or manual generated edit; no server-domain implementation path changed; runner changes are synthetic-browser-only; all shared private schema/validation/aggregation/failure/snapshot semantics are unchanged. Any failure requires the stop-and-split/recollect decision in section 11.4 before smoke. / 门槛：每个 gate 通过；精确 changed path 等于第 12 节；无无关 diff 或手工 generated edit；没有 server-domain implementation path 变化；runner 变更仅属于 synthetic-browser；全部 shared private schema／validation／aggregation／failure／snapshot 语义保持不变。任一失败都要求在 smoke 前执行第 11.4 节的停止并拆分／重新采集决策。

### M6 — Freeze candidate, smoke, and calibrate / 冻结 candidate、smoke 与 calibration

Work: freeze the clean local evidence candidate; run one smoke; then three fresh calibration processes. Review hard relations and freeze only eligible existing work counters before viewing a five-run group. / 工作：冻结干净本地 evidence candidate；运行一次 smoke；再运行三个 fresh calibration process。在查看五次 group 前评审 hard relation，并只冻结合格的既有 work counter。

Gate: smoke and all three calibration attempts are valid, share one identity/proof, pass every structural/functional/causal contract, and produce a reviewed frozen exact set. / 门槛：smoke 与三次 calibration attempt 全部 valid、共享一个 identity／proof、通过每个 structural／functional／causal contract，并产出经评审的 frozen exact set。

### M7 — Five-process synthetic comparison / 五进程合成比较

Work: run exactly five fresh independent synthetic browser processes with the frozen set. Retain all observations and invalid history. Cite only the public accepted identity of Wave 0 private server evidence as historical evidence; do not relabel or include its private artifacts. / 工作：使用 frozen set 运行精确五个 fresh 独立合成 browser process。保留全部 observation 与 invalid history。只引用 Wave 0 private server evidence 的公开 accepted identity 作为历史 evidence；不得重新标记或纳入其 private artifact。

Gate: five valid distinct PIDs, one candidate/tree/fixture identity, exact suggestion counts, zero-scan/Map parity relations, exact functional/causal state, no invalid accepted attempts, no Wave 0 private artifact/relabeling, and no numerical latency gate. / 门槛：五个 valid 不同 PID、一个 candidate／tree／fixture identity、精确 suggestion count、零 scan／Map parity relation、精确 functional／causal state、accepted attempt 中无 invalid、无 Wave 0 private artifact／重标，且无数值 latency gate。

### M8 — Independent review and closeout / 独立评审与收口

Work: assemble the closed Wave 1A review packet defined in section 19, including every named payload and recomputable sanitized run projection; request independent source/diff/test/evidence review; resolve findings without changing the frozen executable candidate; then perform the documentation lifecycle in section 12.2. If executable changes are required, create a new candidate and repeat M5–M7. / 工作：组装第 19 节定义的封闭 Wave 1A review packet，包括每个具名 payload 与可重算 sanitized run projection；请求独立 source／diff／test／evidence 评审；在不改变 frozen executable candidate 的前提下解决 finding；随后执行第 12.2 节文档生命周期。若需要 executable change，则创建新 candidate 并重跑 M5 至 M7。

Gate: `packet-index.json` validates every required payload and `reviewPacketHash`; the packet or declared read-only `SANITIZED_RUN_ROOT` independently recomputes all section 19 structural/causal/numeric results; M5's no-server-change and unchanged-private-runner proof is independently rechecked; the reviewer returns an explicit acceptance/integration-ready verdict with no unresolved blocker. Only then archive the plan and remove active registration. / 门槛：`packet-index.json` 校验每个必需 payload 与 `reviewPacketHash`；packet 或已声明 read-only `SANITIZED_RUN_ROOT` 可独立重算第 19 节全部 structural／causal／numeric result；独立复核 M5 的无 server change 与 private runner 不变证明；reviewer 返回明确 acceptance／integration-ready verdict 且无未解决 blocker。只有此后才能归档计划并移除 active 登记。

### Mandatory stop checkpoints / 强制停止检查点

Stop and request review before continuing if any of these occurs: / 出现以下任一情况时必须停止并请求评审：

1. Relevant descendant drift from `afbb30eb…` changes a confirmed call chain, state mutation site, harness schema, generated build, or governing contract. / `afbb30eb…` 之后的相关 drift 改变已确认 call chain、state mutation site、harness schema、generated build 或 governing contract。
2. Removing the duplicate requires coalescing, caching, debounce, server changes, or suppression of a Project/explicit refresh. / 移除 duplicate 需要 coalescing、cache、debounce、server 变更或抑制 Project／显式 refresh。
3. Map integration requires API/DTO/Materialized Session/ProjectQueryStore/source-adapter/validator/fingerprint changes. / Map 集成需要 API／DTO／Materialized Session／ProjectQueryStore／source adapter／validator／fingerprint 变更。
4. Duplicate ID handling cannot preserve the prior committed state and existing error/retry surface without a new user-visible contract. / Duplicate ID 处理无法在不引入新用户可见 contract 的情况下保持先前 committed state 与既有 error／retry surface。
5. A proposed lookup migration depends on order, navigation-cache ownership, presentation rows, or a noncanonical ID. / 拟迁移 lookup 依赖顺序、navigation-cache ownership、presentation row 或非 canonical ID。
6. Profiling needs permanent visible diagnostics, content/identity serialization, a new competing harness, or a numerical latency gate. / Profiling 需要永久可见 diagnostics、内容／identity 序列化、新的竞争 harness 或数值 latency gate。
7. Any changed path falls outside section 12, or a package/lockfile/workflow/product-spec/server path appears. / 任一 changed path 超出第 12 节，或出现 package／lockfile／workflow／product spec／server path。
8. Server-side domains 1–4 change. Stop before evidence and decide whether Wave 1A must be abandoned or separately amended with private recollection. / Server-side domain 1 至 4 变化。在 evidence 前停止，并决定放弃 Wave 1A 或通过单独 amendment 加入 private recollection。
9. Any implementation globally rejects the rendered committed pair merely because a different desired timeline context is pending, or intentionally makes the old visible DOM inert. Treat that as an out-of-scope product decision. / 任一实施只因另一个 desired timeline context pending 就全局拒绝已渲染 committed pair，或有意使旧可见 DOM inert。必须把它视为超范围产品决策。
10. Any shared private artifact schema, manifest validation, summary aggregation, failure tuple, snapshot-proof semantic, or accepted algorithm/version identifier changes. Stop and either isolate browser aggregation through a reviewed envelope amendment or define/recollect private evidence under a separately reviewed protocol. / 任一 shared private artifact schema、manifest validation、summary aggregation、failure tuple、snapshot-proof 语义或 accepted algorithm／version identifier 变化。必须停止，并通过经评审 envelope amendment 隔离 browser aggregation，或在单独评审 protocol 下定义／重新采集 private evidence。

## 14. Focused tests and full gates / 聚焦测试与完整 gate

### 14.1 New/strengthened focused coverage / 新增／强化聚焦覆盖

Node state/operation tests must cover: / Node state／operation test 必须覆盖：

- empty initialization and full reset; replacement Map build and exact object identity; valid append preserving old identities; / 空初始化与完整 reset；replacement Map build 与精确 object identity；合法 append 保留旧 identity；
- duplicate within replacement/page, duplicate against prefix, empty/missing ID, and fail-before-publish behavior; / replacement／page 内 duplicate、与 prefix duplicate、空／缺失 ID 与 publish 前失败行为；
- Map size/unique count/array identity parity; committed context/array/Map atomic publish; committed-pair miss/fallback semantics; consumer-local desired-context checks; temporary reveal exclusion; / Map size／unique count／array identity parity；committed context／array／Map 原子 publish；committed-pair miss／fallback 语义；consumer-local desired-context check；temporary reveal 排除；
- operation proof at multiple card and canonical-array sizes: linear card iterations, one Map get per card, zero array comparisons. / 多种 card 与 canonical-array size 下的 operation 证明：线性 card iteration、逐 card 一次 Map get、零 array comparison。

Real-browser tests must cover: / 真实 browser test 必须覆盖：

- automatic initial Session selection exactly one Session suggestion request and unchanged final suggestion data; / 自动初始 Session selection 精确一次 Session suggestion request，最终 suggestion data 不变；
- explicit later Session selection, project-result drill-down, Project Scope entry/refresh, empty Sessions, and Layer change counts/parameters; / 显式后续 Session selection、项目结果下钻、Project Scope entry／refresh、空 Sessions 与 Layer change count／parameter；
- aborted/stale Session suggestion cannot commit and the current Session still gets one intended refresh; revision mismatch recovery remains convergent; / aborted／stale Session suggestion 不能 commit，当前 Session 仍获得一次 intended refresh；revision mismatch recovery 继续收敛；
- the closed `selectSession()` result has exactly `suggestionRequestId`/`suggestionContext`/`suggestionCommitted`, with the generation captured from the delegated invocation rather than sampled after await; each of the six outer handoff guards is independently failed; / 封闭 `selectSession()` result 精确只含 `suggestionRequestId`／`suggestionContext`／`suggestionCommitted`，generation 来自被委托 invocation 而不是 await 后采样；逐项制造六项外层 handoff guard 失败；
- for a newer same-Session/same-context suggestion generation, delay the newer request in one test and let it commit before the old outer resumes in another; both assert no third fallback, no abort of the newer request, only the newer commit, and one intended request for the explicit later selection; / 对更新 same-Session／same-context suggestion generation，一个 test 延迟更新 request，另一个让它在旧外层恢复前 commit；两者都断言无第三次 fallback、不 abort 更新 request、只有更新者 commit，且显式后续 selection 只有一次 intended request；
- Map build on first page, rebuild on Layer/filter/query replacement, append, Session/project/revision reset, full locale reload, and stale response rejection; / first page Map build、Layer／filter／query replacement rebuild、append、Session／project／revision reset、完整 locale reload 与 stale response rejection；
- with a Layer/filter/query replacement response deliberately delayed, old visible cards retain click, selection, detail, and enclosing-operation behavior and resolve by old object identity until the new committed context/array/Map atomically replaces them; separately, locale reload proves an empty committed pair before the new localized first page; / 有意延迟 Layer／filter／query replacement response 时，旧可见 card 保留 click、selection、detail 与 enclosing-operation 行为，并持续解析到旧 object identity，直至新 committed context／array／Map 将其原子替换；locale reload 则单独证明在新的 localized first page 前存在 empty committed pair；
- temporary-reveal membership in `renderTimeline()` uses the Map-backed canonical membership helper, reports the closed Map backend operation, preserves presentation semantics, and adds zero canonical array comparisons; / `renderTimeline()` 中 temporary-reveal membership 使用 Map-backed canonical membership helper，报告封闭的 Map backend operation，保留 presentation 语义，并且不增加 canonical array comparison；
- a true out-of-prefix `temporaryEventReveal` with an enclosing Code Mode parent is rendered as a temporary reference card, becomes selected with a current affordance, disables that affordance while context reveal is pending, and leaves canonical array/Map parity and size unchanged; evidence isolation immediately before event-envelope fulfillment proves the specific `renderTimeline()` membership Map-has call, positive card/get equality, and zero array comparisons; / 带 enclosing Code Mode parent 的真正前缀外 `temporaryEventReveal` 会渲染为 temporary reference card，在被选中时得到 current affordance，在 context reveal pending 时禁用该 affordance，并保持 canonical array／Map parity 与 size 不变；在 event-envelope fulfill 前立即隔离 evidence，证明精确的 `renderTimeline()` membership Map-has call、正数 card／get 相等关系与零 array comparison；
- temporary event reveal and Code Mode context row do not change Map parity/size or become Map writers; / temporary event reveal 与 Code Mode context row 不改变 Map parity／size，也不成为 Map writer；
- search target navigation, inherited/fork target navigation, enclosing-operation affordances, visible detail loading, selected detail ownership, and Map miss fallback remain behaviorally identical; / search target navigation、inherited／fork target navigation、enclosing-operation affordance、visible detail loading、selected detail ownership 与 Map miss fallback 行为等价；
- generated bundle loads and exposes the same behavior; no source script is served directly. / generated bundle 加载并呈现相同行为；不直接服务 source script。

Profiler/runner tests must cover: / Profiler／runner test 必须覆盖：

- Session/Project suggestion classification from query parameters without storing Session IDs or URLs; bootstrap count `1` accepted and `0`/`2` rejected for the optimized normal path; / 从 query parameter 分类 Session／Project suggestion，且不存 Session ID／URL；优化后正常路径 bootstrap count `1` 接受、`0`／`2` 拒绝；
- Map parity failure, committed-context binding failure, an invalid stale publish, non-Map backend, nonzero array comparisons, or unequal enclosing card/get counts fail structural acceptance; a valid pending desired context with an interactive committed pair passes; / Map parity 失败、committed-context binding 失败、非法 stale publish、非 Map backend、非零 array comparison 或 enclosing card／get count 不等会使 structural acceptance 失败；存在可交互 committed pair 的合法 pending desired context 应通过；
- new hard paths aggregate exactly; scheduling-sensitive lookup totals remain observational; calibration still touches only the existing eligible six counters and preserves demonstrated-observational floors; / 新 hard path 精确 aggregate；调度敏感 lookup total 保持 observational；calibration 继续只处理既有六个 eligible counter，并保留 demonstrated-observational floor；
- invalid runs/groups remain retained and cannot be silently replaced; sibling request order remains unconstrained; privacy validator rejects identity/content additions. / invalid run／group 保留且不能静默替换；sibling request order 继续不受约束；privacy validator 拒绝 identity／content 添加。
- locked existing private fixtures prove byte/schema-compatible artifact parsing, identical manifest acceptance/rejection, identical summary aggregation, identical failure tuples, and identical snapshot-proof semantics; browser-only counters cannot enter private manifests/summaries or change accepted private algorithm/version identifiers. / 锁定的既有 private fixture 证明 artifact parsing 字节／schema 兼容、manifest 接受／拒绝相同、summary aggregation 相同、failure tuple 相同、snapshot-proof 语义相同；browser-only counter 不能进入 private manifest／summary，也不能改变 accepted private algorithm／version identifier。

### 14.2 Focused commands / 聚焦命令

Use the smallest relevant commands during editing, including at least: / 编辑期间使用最小相关命令，至少包括：

```text
node --check src/browser/app.js
node --check src/browser/timeline-event-state.js
node --check scripts/timeline-profile.js
node --check scripts/performance-wave-0-runner.js
node --test test/timeline-event-state.test.js test/navigation.test.js test/transition-safety.test.js test/search-targets.test.js test/timeline-profile.test.js test/performance-wave-0-runner.test.js test/build-boundary.test.js
node --test --test-name-pattern="file suggestions|Map|timeline|context|Session|Layer|stale|abort|search navigation" e2e/browser.test.js
```

If Node's test-name filter behavior differs in the exact supported runtime, run the complete `e2e/browser.test.js` rather than weakening coverage. / 若精确受支持 runtime 中 Node test-name filter 行为不同，应运行完整 `e2e/browser.test.js`，不得弱化覆盖。

### 14.3 Mandatory full candidate gates / 必需完整 candidate gate

On the exact clean evidence candidate, require: / 在精确干净 evidence candidate 上要求：

1. `npm test` / `npm test`
2. `npm run test:browser` / `npm run test:browser`
3. `npm run build:check` / `npm run build:check`
4. `npm run test:package` / `npm run test:package`
5. `npm run release:check` / `npm run release:check`
6. generated-asset byte equality/currentness after normal build, including no source map or extra generated path; / 正常 build 后 generated asset 字节相等／current，且无 source map 或额外 generated path；
7. `git diff --check`; / `git diff --check`；
8. exact target-to-candidate changed-path audit against section 12, plus `git status --short` cleanliness at evidence capture; / 针对第 12 节的精确 target-to-candidate changed-path audit，以及 evidence capture 时 `git status --short` 干净；
9. English/Chinese semantic-alignment and documentation-link audit; / 英文／中文语义对齐与文档 link 审计；
10. independent review. / 独立评审。

Do not hard-code Wave 0 totals. Record the actual passed/failed/skipped totals produced by the Wave 1A candidate, exact runtime/browser versions, and whether each result was executor-run or independently rerun. / 不得硬编码 Wave 0 total。记录 Wave 1A candidate 实际产生的 passed／failed／skipped total、精确 runtime／browser version，并说明每项结果属于 executor-run 还是 independent rerun。

## 15. Risk and rollback analysis / 风险与回滚分析

| Risk / 风险 | Failure mode / 失败模式 | Prevention/evidence / 防护／evidence | Rollback / 回滚 |
| --- | --- | --- | --- |
| Duplicate removal suppresses recovery / 移除 duplicate 抑制 recovery | First suggestion aborts/stales, tail is skipped, suggestions remain wrong/empty. / 第一次 suggestion abort／stale，tail 被跳过，候选错误／为空。 | Context-matched committed outcome; stale/abort/revision tests; final content parity. / Context-matched committed outcome；stale／abort／revision test；最终内容等价。 | Revert only M2 outcome/skip change; keep evidence seam if still useful only after review. / 只回滚 M2 outcome／skip；evidence seam 是否保留需重新评审。 |
| Same-context generation race issues a third fallback / Same-context generation race 发出第三次 fallback | A same-Session click advances `fileSuggestionRequestId` without changing context; the old delegated result reports the same context plus noncommit, and an old `loadSessions()` starts a fallback that duplicates or aborts the newer pending/already-committed request. / Same-Session click 在不改变 context 时推进 `fileSuggestionRequestId`；旧委托 result 报告相同 context 加未 commit，旧 `loadSessions()` 启动 fallback，复制或 abort 更新的 pending／已 commit request。 | Closed three-field result with invocation-local generation; six independently exercised guards; both newer-pending and newer-committed browser schedules assert no third request, no newer abort, and only newer commit. / 含 invocation-local generation 的封闭三字段 result；独立覆盖六项 guard；更新 request 仍 pending 与已经 commit 的两种 browser schedule 都断言无第三次 request、不 abort 更新者、且只有更新者 commit。 | Revert M2 and retain the existing duplicate until generation handoff is proven; do not add coalescing or weaken explicit later-selection ownership. / 回滚 M2 并保留既有 duplicate，直至 generation handoff 被证明；不增加 coalescing，也不削弱显式后续 selection ownership。 |
| Project request accidentally removed / Project 请求被误删 | Scope without `sessionId` no longer refreshes. / 不带 `sessionId` 的 scope 不再刷新。 | Ownership table assertions for scope/Layer/loadSessions/empty state. / Scope／Layer／loadSessions／empty state ownership 断言。 | Restore Project tail/call site; do not add cache. / 恢复 Project tail／call site；不增加 cache。 |
| Array/Map drift / Array／Map 漂移 | Wrong object, stale ID, wrong size, or old Session reachable after retirement. / 错误 object、stale ID、错误 size，或 retirement 后旧 Session 仍可访问。 | Single committed context/array/Map boundary, transactional prevalidation, parity probe, request/owner gates, atomic publish, and stale-response tests. / 单一 committed context／array／Map boundary、transactional 预校验、parity probe、request／owner gate、原子 publish 与 stale-response test。 | Revert Map integration and generated asset as one coherent change. / 将 Map 集成与 generated asset 作为整体回滚。 |
| Duplicate page partially commits / Duplicate page 部分 commit | Array extends before Map failure or Map overwrites old value. / Array 在 Map failure 前扩展，或 Map 覆盖旧 value。 | Validate complete page/prefix before either mutation; fail-closed tests. / 在任一 mutation 前校验完整 page／prefix；fail-closed test。 | Revert append helper; retain previous canonical pair. / 回滚 append helper；保留此前 canonical pair。 |
| Pending replacement makes old DOM inert / Pending replacement 使旧 DOM inert | A global desired-context gate disables click/selection/detail/affordance lookup for still-visible committed cards. / 全局 desired-context gate 禁用仍可见 committed card 的 click／selection／detail／affordance lookup。 | Explicit committed context/array/Map owner; `currentTimelineEvent()` uses rendered committed pair; delayed-response browser test covers all four interactions and atomic publish. / 显式 committed context／array／Map owner；`currentTimelineEvent()` 使用已渲染 committed pair；延迟 response browser test 覆盖四类交互与原子 publish。 | Revert the global gate immediately. If inert DOM is desired, stop for an out-of-scope product decision rather than changing Wave 1A. / 立即回滚全局 gate。若期望 inert DOM，必须因超范围产品决策停止，而不是改变 Wave 1A。 |
| Locale lifecycle is conflated with retained-pair replacement / Locale lifecycle 与 retained-pair replacement 混淆 | Plan or code claims the old pair remains interactive although the inspected `loadSessions()` → `selectSession()` reload retires it before localized page zero. / Plan 或 code 声称旧 pair 继续可交互，但经检查的 `loadSessions()` → `selectSession()` reload 会在 localized page zero 前 retire 它。 | Reviewer-approved classification keeps Layer/filter/query in retained-pair replacement and locale in full reset; focused browser evidence requires the empty committed pair during locale reload and parity after localized publish. / Reviewer-approved 分类把 Layer／filter／query 保留在 retained-pair replacement，把 locale 归入 full reset；聚焦 browser evidence 要求 locale reload 期间存在 empty committed pair，并在 localized publish 后恢复 parity。 | Restore the inspected full reload and correct documentation/tests together; any retained-locale proposal is new product semantics and requires a reviewer stop. / 一起恢复经检查的完整 reload 并修正文档／test；任何 retained-locale 提案都属于新产品语义，必须停下评审。 |
| Temporary/context rows pollute Map or lose their fallback / Temporary／context row 污染 Map 或丢失 fallback | Counts/targets/lookup ownership change, or a true temporary card misses the Map and retains stale enclosing-affordance visibility/ARIA/disabled state. / Count／target／lookup ownership 变化，或真正 temporary card 在 Map miss 后保留过期的 enclosing-affordance visibility／ARIA／disabled state。 | No writer path; `currentTimelineEvent(eventId, purpose)` performs one Map get then only the O(1) temporary-ID fallback; true-temporary selection/pending browser test; parity size before/during/after reveal; positive card/get equality and zero array comparisons. / 无 writer path；`currentTimelineEvent(eventId, purpose)` 执行一次 Map get，随后只执行 O(1) temporary-ID fallback；true-temporary selection／pending browser test；reveal 前／中／后 parity size；正数 card／get 相等且 array comparison 为零。 | Route the indexed callback back through `currentTimelineEvent()`; never insert presentation events into the Map. / 让 indexed callback 重新通过 `currentTimelineEvent()`；绝不把 presentation event 插入 Map。 |
| Instrumentation distorts or leaks / Instrumentation 扭曲或泄漏 | Observer-absent product paths still scan the full canonical prefix, or evidence serializes IDs/content or emits visible diagnostics. / Observer 缺席的产品路径仍扫描完整 canonical prefix，或 evidence 序列化 ID／内容、发出可见 diagnostics。 | Resolve callable `recordStateSnapshot` before parity work; Node operation test proves zero visits/gets for absent, method-missing, and inaccessible observers; content-free closed aliases/counts; privacy tests; timing observational. / 在 parity work 前解析可调用 `recordStateSnapshot`；Node operation test 证明 observer 缺席、缺少方法或方法不可访问时 visit／get 均为零；不含内容封闭 alias／count；privacy test；timing observational。 | Remove the profiling wrapper from product call sites or replace it with a narrower pre-gated pure seam; retain correctness assertions. / 从产品 call site 移除 profiling wrapper，或用更窄的前置 gated pure seam 替换；保留 correctness assertion。 |
| Enclosing proof is vacuous / Enclosing proof 空洞 | `0 === 0` counter equality lets a worker/group pass without observing the optimized path. / `0 === 0` counter equality让 worker／group 在未观测优化路径时通过。 | Profiler and runner both require positive card iterations before exact card/lookup/Map/get equality; accepted fixtures use positive counters; honest all-zero fixtures remain parseable but fail worker and aggregate acceptance. / Profiler 与 runner 均在 card／lookup／Map／get 精确相等前要求正数 card iteration；accepted fixture 使用正数 counter；诚实全零 fixture 保持可解析但无法通过 worker 与 aggregate acceptance。 | Reject the evidence and rerun only after restoring positive structural instrumentation; do not replace the gate with timing. / 拒绝 evidence，仅在恢复正数 structural instrumentation 后重跑；不得用 timing 取代该 gate。 |
| Generated asset divergence / Generated asset 偏离 | Source passes locally but shipped bundle is stale/manual. / Source 本地通过但 shipped bundle stale／手工修改。 | Normal build, `build:check`, byte equality, browser load assertion. / 正常 build、`build:check`、字节相等、browser load 断言。 | Regenerate from source; never patch minified file. / 从 source 重建；绝不 patch minified file。 |
| Wave 0 private evidence is relabeled or invalidated / Wave 0 private evidence 被重标或失效 | Shared runner changes alter private schema/validation/aggregation/proof semantics while old evidence is presented as Wave 1A evidence. / Shared runner 变化改变 private schema／validation／aggregation／proof 语义，同时旧 evidence 被呈现为 Wave 1A evidence。 | Historical-identity-only citation; server path/function audit; locked private fixtures; M5/M8 independent gates; no private artifacts in the Wave 1A packet. / 仅按历史 identity 引用；server path／function audit；锁定 private fixture；M5／M8 独立 gate；Wave 1A packet 不含 private artifact。 | Stop before smoke; isolate browser aggregation under reviewed envelope amendment or recollect under a separately reviewed private protocol. / 在 smoke 前停止；通过经评审 envelope amendment 隔离 browser aggregation，或按单独评审 private protocol 重新采集。 |
| Review packet is aggregate-only or mutable / Review packet 只有 aggregate 或可变 | Reviewer cannot recompute isolation, exact counters, structural relations, or numeric distributions and must trust executor summaries. / Reviewer 无法重算 isolation、exact counter、structural relation 或 numeric distribution，只能信任 executor summary。 | Closed filenames, byte lengths/hashes/schema versions, `reviewPacketHash`, sanitized projections, frozen set record, and read-only root declaration. / 封闭 filename、byte length／hash／schema version、`reviewPacketHash`、sanitized projection、frozen set record 与 read-only root 声明。 | Reject packet and regenerate it from the unchanged frozen candidate/runs; recollect only if raw evidence or executable identity changed. / 拒绝 packet，并从未变的 frozen candidate／run 重新生成；只有 raw evidence 或 executable identity 变化时才重新采集。 |
| Scope creep / 范围扩张 | Adjacent render/server debt enters candidate. / 相邻 render／server debt 进入 candidate。 | Exact path audit and mandatory stops. / 精确 path audit 与强制 stop。 | Remove out-of-scope changes; if inseparable, abandon Wave 1A candidate. / 移除超范围变更；若不可分离则放弃 Wave 1A candidate。 |

Rollback is code/document rollback only. There is no persisted user data migration, API migration, source-transcript mutation, or server cache format. Revert the suggestion change, state module/app integration, focused evidence extensions/tests, generated bundle, and bilingual documentation coherently; then rerun generated/full gates. / 回滚只涉及 code／documentation。不存在持久用户数据 migration、API migration、source transcript mutation 或 server cache format。应一致地回滚 suggestion 变更、state module／app 集成、聚焦 evidence 扩展／test、generated bundle 与双语文档；随后重跑 generated／full gate。

## 16. Documentation and `AGENTS.md` lifecycle / 文档与 `AGENTS.md` 生命周期

After implementation authorization, M0 adds this exact active path under `AGENTS.md` → Current anchors → Active execution plans. During execution, this plan records bilingual progress, exact candidate/evidence identities, actual test totals, invalid diagnostic groups, frozen calibration decisions, reviewer findings, and stop decisions. / 取得实施授权后，M0 在 `AGENTS.md` → Current anchors → Active execution plans 下加入本精确 active path。执行期间，本计划以双语记录 progress、精确 candidate／evidence identity、实际 test total、invalid diagnostic group、frozen calibration 决策、reviewer finding 与 stop 决策。

`docs/design-docs/timeline-loading-and-rendering-performance.md` must be updated bilingually in the same implementation change to: / `docs/design-docs/timeline-loading-and-rendering-performance.md` 必须在同一 implementation change 中双语更新：

- record the duplicate automatic Session-suggestion ownership correction and its exact one-request contract; / 记录重复自动 Session suggestion ownership 修正及其精确一次请求契约；
- define the committed timeline context/`currentEvents`/`currentEventsById` ownership, pending-replacement interaction continuity, atomic replacement/append/reset rules, duplicate rejection, temporary-row exclusion, and lookup migration boundary; / 定义 committed timeline context／`currentEvents`／`currentEventsById` ownership、pending replacement 交互连续性、原子 replacement／append／reset rule、duplicate rejection、temporary-row 排除与 lookup migration boundary；
- publish only reviewed aggregate structural/functional conclusions and descriptive timing from the accepted Wave 1A group; / 只发布已接受 Wave 1A group 中经评审的聚合 structural／functional 结论与描述性 timing；
- leave keyed rendering, batching, virtualization, server indexes, cursors, validators, and relationship scans in later stages. / 让 keyed rendering、batching、virtualization、server index、cursor、validator 与 relationship scan 继续属于后续 stage。

`docs/product-specs/session-transcript-analyzer.md` is not expected to change because Wave 1A preserves visible behavior. If implementation reveals a visible contract change, stop rather than normalizing it into the spec. `docs/design-docs/indexed-materialized-session-lifecycle.md` is not expected to change because server lifecycle remains unchanged and Wave 0 private evidence is cited only under its original historical identity. / `docs/product-specs/session-transcript-analyzer.md` 预期不变，因为 Wave 1A 保持可见行为。若实施暴露用户可见 contract 变化，应停止，而不是把它写入 spec 使之常态化。`docs/design-docs/indexed-materialized-session-lifecycle.md` 预期不变，因为 server lifecycle 保持不变，且 Wave 0 private evidence 只按其原始历史 identity 引用。

Only after M8 acceptance may the plan move to `completed/`, the design reference move with it, and `AGENTS.md` active registration be removed. English and Chinese changes must remain semantically aligned in each revision. / 只有 M8 接受后，计划才能移到 `completed/`、design reference 才能随之移动，并移除 `AGENTS.md` active 登记。每个 revision 中英文变更都必须保持语义对齐。

## 17. Completion criteria / 完成标准

Do not mark Wave 1A complete until every item is true: / 以下每项全部满足前，不得把 Wave 1A 标记为完成：

- [ ] The implementation candidate descends from the revalidated live target; exact ancestry, candidate/tree/runtime-asset identity, and target diff are recorded. / Implementation candidate 是重新核验实时 target 的后代；已记录精确 ancestry、candidate／tree／runtime asset identity 与 target diff。
- [ ] Exactly the two authorized product changes are present; section 12 is the complete changed-path set and no unexpected path remains. / 精确存在两项获授权产品变更；第 12 节是完整 changed-path set，且无意外 path。
- [ ] Normal automatic initial Session selection issues exactly one Session suggestion request, records exactly one content-free automatic-selection settlement after delegated `selectSession()` settles with watermark `1`, and records zero post-settlement Session requests; explicit later Session and drill-down refresh once without recording automatic settlement. / 正常自动初始 Session selection 精确一次 Session suggestion request；在被委托 `selectSession()` settle 后精确记录一次不含内容、watermark 为 `1` 的 automatic-selection settlement，并记录零次 settle 后 Session request；显式后续 Session 与 drill-down 各刷新一次，且不记录 automatic settlement。
- [ ] The closed `selectSession()` result contains exactly the delegated invocation's local `suggestionRequestId`, `suggestionContext`, and `suggestionCommitted`; all six outer handoff guards are tested. In both newer-pending and newer-committed same-Session/same-context schedules, the superseded `loadSessions()` issues no third fallback, does not abort the newer request, and its older delegated request cannot commit; explicit later Session selection retains one intended request, and bounded revision recovery remains the sole recovery owner. / 封闭 `selectSession()` result 精确包含被委托 invocation 的本地 `suggestionRequestId`、`suggestionContext` 与 `suggestionCommitted`；全部六项外层 handoff guard 均有测试。在更新 request 仍 pending 与已经 commit 的两种 same-Session／same-context schedule 中，被取代的 `loadSessions()` 不发出第三次 fallback、不 abort 更新 request，且其较早被委托 request 不能 commit；显式后续 Session selection 保留一次 intended request，且有界 revision recovery 继续作为唯一 recovery owner。
- [ ] Every legitimate Project/Layer/source/project/locale/revision refresh remains, parameters and final suggestion content are unchanged, and stale/aborted work cannot commit. / 每个合法 Project／Layer／source／project／locale／revision refresh 均保留，参数与最终 suggestion content 不变，stale／aborted work 不能 commit。
- [ ] `currentEvents` remains the canonical ordered contiguous prefix and every Map lifecycle/parity/identity/duplicate invariant passes. / `currentEvents` 继续作为 canonical 有序连续前缀，且每个 Map lifecycle／parity／identity／duplicate invariant 通过。
- [ ] Committed context, `currentEvents`, and `currentEventsById` publish/retire together. Full Session/project/locale/revision reset makes old state unreachable; delayed Layer/filter/query replacement preserves click/selection/detail/enclosing-operation behavior on the rendered committed pair until atomic success; locale reload proves retirement before localized page zero; stale responses cannot publish. / Committed context、`currentEvents` 与 `currentEventsById` 一起发布／retire。完整 Session／project／locale／revision reset 使旧 state 不可访问；延迟 Layer／filter／query replacement 在原子成功前保留已渲染 committed pair 的 click／selection／detail／enclosing-operation 行为；locale reload 证明在 localized page zero 前完成 retirement；stale response 不能 publish。
- [ ] Temporary reveal, context rows, navigation cache, detail state, selection, and DOM remain nonwriters; `renderTimeline()` resolves temporary-reveal canonical membership through the Map helper with zero canonical array comparisons, and evidence identifies that specific call rather than an unrelated navigation membership lookup. / Temporary reveal、context row、navigation cache、detail state、selection 与 DOM 继续不是 writer；`renderTimeline()` 通过 Map helper 解析 temporary-reveal canonical membership，canonical array comparison 为零，且 evidence 识别该精确 call，而不是无关 navigation membership lookup。
- [ ] The deterministic operation seam proves a positive card count, linear card iterations, exact card/lookup/Map-backend/get equality, and zero per-card array scans in the enclosing-affordance path; all-zero evidence is retained only as failed diagnostic evidence and cannot support worker/group acceptance. A true out-of-prefix temporary card uses only the O(1) fallback after its Map miss, updates affordance visibility/ARIA when selected, updates disabled state while context reveal is pending, and preserves canonical parity/size. / 确定性 operation seam 证明正数 card count、线性 card iteration、card／lookup／Map-backend／get 精确相等，以及 enclosing-affordance 路径零逐 card array scan；全零 evidence 只作为失败诊断 evidence 保留，不能支撑 worker／group acceptance。真正前缀外 temporary card 在 Map miss 后只使用 O(1) fallback，在被选中时更新 affordance visibility／ARIA，在 context reveal pending 时更新 disabled state，并保持 canonical parity／size。
- [ ] Profiling-only state snapshots do zero canonical-array visits and zero Map gets when `recordStateSnapshot` is absent or inaccessible, while an installed observer still receives the same closed snapshot and append prevalidation retains its mandatory linear correctness scan. / 当 `recordStateSnapshot` 缺席或不可访问时，仅用于 profiling 的 state snapshot 执行零 canonical-array visit 与零 Map get；安装 observer 时仍收到同一封闭 snapshot，且 append prevalidation 保留其强制线性 correctness scan。
- [ ] Wave 0 final functional state, request constraints, causal DOM ledger, warm materialization, context isolation, cold switch, no-visible-error, Long Task, and Resource Timing contracts remain complete. / Wave 0 final functional state、request constraint、causal DOM ledger、warm materialization、context isolation、cold switch、无可见 error、Long Task 与 Resource Timing contract 保持完整。
- [ ] One smoke, three valid independent calibration processes, a predeclared frozen exact set, and five valid independent synthetic browser processes exist for the exact candidate; invalid history is retained. / 精确 candidate 具备一次 smoke、三次 valid 独立 calibration process、预先声明 frozen exact set 与五次 valid 独立合成 browser process；invalid history 已保留。
- [ ] Accepted Wave 0 private server evidence is cited only by its original public candidate/tree/packet identity as historical evidence and is never copied or relabeled; exact target-to-candidate audit proves no server-domain path changed. / 已接受 Wave 0 private server evidence 只按其原始公开 candidate／tree／packet identity 作为历史 evidence 引用，绝不复制或重标；精确 target-to-candidate audit 证明没有 server-domain path 变化。
- [ ] Runner additions are synthetic-browser-only, and focused locked-fixture tests prove private artifact schema, manifest validation, summary aggregation, failure tuples, snapshot-proof semantics, and accepted algorithm/version identifiers remain unchanged; otherwise the reviewed split/recollection protocol has completed before evidence. / Runner 新增只属于 synthetic-browser，且聚焦 locked-fixture test 证明 private artifact schema、manifest validation、summary aggregation、failure tuple、snapshot-proof 语义与 accepted algorithm／version identifier 保持不变；否则必须在 evidence 前完成经评审的拆分／重新采集 protocol。
- [ ] Focused tests, full `npm test`, full `npm run test:browser`, `npm run build:check`, `npm run test:package`, `npm run release:check`, generated equality, `git diff --check`, and exact scope audit pass with actual totals recorded. / 聚焦 test、完整 `npm test`、完整 `npm run test:browser`、`npm run build:check`、`npm run test:package`、`npm run release:check`、generated equality、`git diff --check` 与精确 scope audit 通过，并记录实际 total。
- [ ] Bilingual performance design and plan logs are aligned; product/lifecycle docs remain unchanged for documented reasons. / 双语 performance design 与 plan log 对齐；product／lifecycle doc 因已记录理由保持不变。
- [ ] The complete closed section 19 packet validates: every payload is bound by canonical filename, raw length/SHA-256/schema version and `reviewPacketHash`; the packet or declared read-only `SANITIZED_RUN_ROOT` independently recomputes every required structural/causal/numeric result; no private Wave 0 artifact is included. / 完整封闭的第 19 节 packet 校验通过：每个 payload 由 canonical filename、raw length／SHA-256／schema version 与 `reviewPacketHash` 绑定；packet 或已声明 read-only `SANITIZED_RUN_ROOT` 可独立重算每个必需 structural／causal／numeric result；不包含 Wave 0 private artifact。
- [ ] An independent reviewer accepts exact source/diff/path scope, tests, structural/causal evidence, privacy, documentation, rollback, and integration readiness with no unresolved blocker. / 独立 reviewer 接受精确 source／diff／path scope、test、structural／causal evidence、privacy、documentation、rollback 与 integration readiness，且无未解决 blocker。
- [ ] Only after that acceptance, the plan is archived to `completed/`, its design link is updated, and its `AGENTS.md` active entry is removed. / 只有在该 acceptance 后，计划才归档到 `completed/`、design link 更新、`AGENTS.md` active entry 移除。

## 18. Progress and decision logs / 进度与决策日志

### Progress log / 进度日志

- 2026-08-28 — Durable replacement lineage and M5 acceptance: after canonical M6 evidence was lost under `%TEMP%` with no established deleting or visibility-loss actor, the implementation commit `7b17b4d826e19ffee44af74b7324d66d61df6e35` remained frozen and a plan-only child `b1c53157fc9837a4ec9f2303b2ea63b1b820ca12` established the durable evidence contract. Its tree is `cbac15f2ebbf131c619c4a2f52526316f723a8fe`; target `afbb30eb7d51ba2594b7dfdd5dbbe7b7991fe988`, target-to-candidate diff SHA-256 `6502a48897828a20c31de6bb9c4d6665831b6346410dd03ebdec3efe88626fdd`, implementation hash `950b2d05801884934f5eecafeb079b56ab5262f1a55fff32c2d7e6d50a70a8b5`, fixture proof `4b5a38f87544c5b3ef6bab0366f923554ce8ec8d2a07cf1acd4148efa002b373`, and runtime asset `282791` bytes／`de7d470954573d772f2c5163a9ad5a69721ddd088df82d45c6c00a944a97adff` are exact. Replacement M5 passed from the durable external root with the previously recorded syntax 4/4, focused Node 73/73, Wave 1A bundle 15/15, existing browser subset 45/45, full Node 782/782, full browser 136/136, locked private-runner 5/5, build/package/release/currentness, exact twelve-path, no-server, privacy, and generated-output gates. / 2026-08-28 — Durable replacement lineage 与 M5 接受：canonical M6 evidence 在 `%TEMP%` 下丢失且未能确定删除或可见性丢失 actor 后，implementation commit `7b17b4d826e19ffee44af74b7324d66d61df6e35` 保持冻结，并由仅含 plan 的 child `b1c53157fc9837a4ec9f2303b2ea63b1b820ca12` 建立 durable evidence contract。其 tree 为 `cbac15f2ebbf131c619c4a2f52526316f723a8fe`；target `afbb30eb7d51ba2594b7dfdd5dbbe7b7991fe988`、target-to-candidate diff SHA-256 `6502a48897828a20c31de6bb9c4d6665831b6346410dd03ebdec3efe88626fdd`、implementation hash `950b2d05801884934f5eecafeb079b56ab5262f1a55fff32c2d7e6d50a70a8b5`、fixture proof `4b5a38f87544c5b3ef6bab0366f923554ce8ec8d2a07cf1acd4148efa002b373` 与 runtime asset `282791` bytes／`de7d470954573d772f2c5163a9ad5a69721ddd088df82d45c6c00a944a97adff` 均精确。Replacement M5 从 durable external root 通过此前记录的 syntax 4／4、focused Node 73／73、Wave 1A bundle 15／15、existing browser subset 45／45、完整 Node 782／782、完整 browser 136／136、locked private-runner 5／5、build／package／release／currentness、精确十二路径、无 server 变更、privacy 与 generated-output gate。
- 2026-08-28 — Durable M6 acceptance: one smoke worker PID `20420` and three calibration worker PIDs `11872`, `20572`, and `6976` were valid, distinct, exit-zero processes with bootstrap plus all five scenarios accepted and no numerical latency gate. The native calibration is `1491` bytes／`0f2de563e3a629b0a55d3ff46672fc1e5bef6b14daed11da8f1808ebc4b41a93`. The separate frozen decision is `3976` bytes／`b89c9ab7245bfda81d232e0ce24e0c49002c1794c66dec77a2b455a0b97f4509`, timestamped `2026-08-28T10:41:50.500Z` before replacement M7 consumption. It froze only: preload full renders/card generations/highlight passes/marks/owners/discovery `4/1500/0/0/0/9`; late-hit highlight passes/owners `10/1`; deep filter `1/150/1/450/150/2`; context reveal all six zero; and cold-switch highlight passes/marks/owners all zero. Suggestion counts, positive cards, Map parity, zero scans, and card/lookup/Map/get relations remained hard invariants. Smoke and calibration stages were independently enumerated, byte-bound, reopened, and ACL-sealed. / 2026-08-28 — Durable M6 接受：一个 smoke worker PID `20420` 与三个 calibration worker PID `11872`、`20572`、`6976` 均为 valid、互异且 exit-zero 的 process；bootstrap 与全部五个 scenario 均通过，且无数值 latency gate。Native calibration 为 `1491` bytes／`0f2de563e3a629b0a55d3ff46672fc1e5bef6b14daed11da8f1808ebc4b41a93`。独立 frozen decision 为 `3976` bytes／`b89c9ab7245bfda81d232e0ce24e0c49002c1794c66dec77a2b455a0b97f4509`，时间戳 `2026-08-28T10:41:50.500Z`，早于 replacement M7 result consumption。它只冻结：preload full render／card generation／highlight pass／mark／owner／discovery `4/1500/0/0/0/9`；late-hit highlight pass／owner `10/1`；deep filter `1/150/1/450/150/2`；context reveal 六项全零；cold switch highlight pass／mark／owner 全零。Suggestion count、正数 card、Map parity、零 scan 与 card／lookup／Map／get 关系继续属于 hard invariant。Smoke 与 calibration stage 均经过独立枚举、byte binding、重新打开与 ACL seal。
- 2026-08-28 — Durable replacement M7 acceptance: exactly five valid attempts, zero invalid worker attempts, zero retries, and distinct PIDs `22252`, `1960`, `17296`, `16136`, and `24080` produced manifest `1627` bytes／`f513c5b24a0fba5ac3d5d79d851b78d224042d3d6e835bccbaf1141ac24c0813` and summary `105199` bytes／`25660d2ecf0e6ef654c74add276096eba6113b13597af54122e74149e77bbc43`. All 228 hard-exact paths, the frozen exact set, bootstrap Session/Project `1/0`, settlement count/watermark `1/1`, zero post-settlement Session request, normalized request ownership, array/Map/unique-ID/object/context/offset/pending parity, positive enclosing cards, exact card/lookup/Map/get equality, zero other backend, zero canonical array comparisons, and every five-scenario structural/functional/causal/privacy/cleanup contract passed 5/5. All 361 numeric observational series retain repeat count, median, minimum, and maximum; no latency threshold was enabled. / 2026-08-28 — Durable replacement M7 接受：精确五次 valid attempt、零 invalid worker attempt、零 retry，且 PID `22252`、`1960`、`17296`、`16136`、`24080` 互异；生成的 manifest 为 `1627` bytes／`f513c5b24a0fba5ac3d5d79d851b78d224042d3d6e835bccbaf1141ac24c0813`，summary 为 `105199` bytes／`25660d2ecf0e6ef654c74add276096eba6113b13597af54122e74149e77bbc43`。全部 228 个 hard-exact path、frozen exact set、bootstrap Session／Project `1/0`、settlement count／watermark `1/1`、settle 后 Session request 为零、规范 request ownership、array／Map／unique ID／object／context／offset／pending parity、正数 enclosing card、card／lookup／Map／get 精确相等、other backend 为零、canonical array comparison 为零，以及五个场景的每项 structural／functional／causal／privacy／cleanup contract 均通过 5／5。全部 361 条 numeric observational series 均保留 repeat count、median、minimum 与 maximum；未启用 latency threshold。
- 2026-08-28 — Replacement M7 descriptive observations and evidence boundary: duration medians `[min,max]` in milliseconds were preload `1351 [1288,1412]`, late hit `9615 [9460,9740]`, deep filter `1601 [1582,1631]`, context reveal `602 [566,617]`, and cold switch `1035 [985,1055]`. Long Task count／maximum／total medians `[min,max]` were preload `5 [5,6]`／`142 [113,143]`／`456 [408,569]`, late hit `32 [32,32]`／`607 [546,730]`／`9093 [9031,9174]`, deep filter `2 [2,2]`／`221 [218,231]`／`331 [322,339]`, and zero for context reveal/cold switch. Resource Timing `durationTotal` medians `[min,max]` remained descriptive by family: preload timeline `72.5 [67.6,76.1]`, late-hit detail/timeline `345.3 [299.8,472.1]`／`121 [115.1,155.9]`, deep-filter timeline `19.2 [16.6,20.2]`, context envelope `2.3 [2,4.1]`, and cold-switch analysis/detail/suggestions/timeline `46.9 [46.1,53]`／`44.5 [38.3,54]`／`46.6 [45.7,52.5]`／`89.5 [87.5,96.4]`. The former M7 group is preserved only as superseded diagnostic evidence with `finalGroupEligibility=false` and `replacementInputEligibility=false`; it was not read, copied, projected, imported, or relabeled into this lineage. / 2026-08-28 — Replacement M7 描述性 observation 与 evidence boundary：duration 中位数 `[min,max]`（毫秒）为 preload `1351 [1288,1412]`、late hit `9615 [9460,9740]`、deep filter `1601 [1582,1631]`、context reveal `602 [566,617]`、cold switch `1035 [985,1055]`。Long Task count／maximum／total 中位数 `[min,max]` 为 preload `5 [5,6]`／`142 [113,143]`／`456 [408,569]`、late hit `32 [32,32]`／`607 [546,730]`／`9093 [9031,9174]`、deep filter `2 [2,2]`／`221 [218,231]`／`331 [322,339]`，context reveal／cold switch 均为零。Resource Timing `durationTotal` 按 family 的中位数 `[min,max]` 保持描述性：preload timeline `72.5 [67.6,76.1]`、late-hit detail／timeline `345.3 [299.8,472.1]`／`121 [115.1,155.9]`、deep-filter timeline `19.2 [16.6,20.2]`、context envelope `2.3 [2,4.1]`、cold-switch analysis／detail／suggestions／timeline `46.9 [46.1,53]`／`44.5 [38.3,54]`／`46.6 [45.7,52.5]`／`89.5 [87.5,96.4]`。Former M7 group 只作为 superseded diagnostic evidence 保存，且 `finalGroupEligibility=false`、`replacementInputEligibility=false`；它未被读取、复制、投影、导入或重标到本 lineage。
- 2026-08-28 — Non-worker control history and M8 state: the corrected replacement-M7 preflight superseded a DateTime-kind-sensitive aggregate; the independent M7 validator preserved one `minimum/maximum` versus declared `min/max` implementation error; the reviewer-stop aggregate preserved one local-`DateTime` coercion error before raw-byte UTC revalidation passed. M8 preflight also preserved one PowerShell parser-only control failure before the corrected fresh process passed. None was a worker attempt, none changed runner/stage bytes, and none caused a replacement process. The authoritative M8 preflight independently rehashed M5 `25`, smoke `4`, calibration `7`, and M7 `8` files and re-proved all four ACLs; M8 packet assembly and independent packet/source review remain pending. / 2026-08-28 — 非 worker control history 与 M8 状态：修正后的 replacement-M7 preflight 取代一项 DateTime-kind-sensitive aggregate；独立 M7 validator 保留一项把已声明 `min/max` 误当作 `minimum/maximum` 的实现错误；reviewer-stop aggregate 在 raw-byte UTC 重验通过前保留一项本地 `DateTime` coercion 错误。M8 preflight 也在修正后的 fresh process 通过前保留一次仅由 PowerShell parser 引起的 control failure。它们都不是 worker attempt，均未改变 runner／stage byte，也未触发 replacement process。权威 M8 preflight 独立重算 M5 `25`、smoke `4`、calibration `7` 与 M7 `8` 个文件的 hash，并重新证明四个 ACL；M8 packet assembly 与独立 packet／source review仍待完成。
- 2026-08-27 — M0 repository/ref gate: reconfirmed `origin` as `git@github.com:Yijia-Zhou/session-analyzer.git` and `package.json.repository.url` as `git+https://github.com/Yijia-Zhou/session-analyzer.git`; live `main` resolved to `2f282a94cc9eb3ce03fded4f8867e0d120a599aa` and live `origin/towards-0.2.0` resolved to the reviewed `afbb30eb7d51ba2594b7dfdd5dbbe7b7991fe988`. `git merge-base --is-ancestor` passed. Because the live target equals the reviewed SHA, the complete descendant diff is empty: there are no descendant changes in browser Session selection, file suggestions, timeline state, profiling scripts/tests, generated assets, package/build inputs, or governing performance/lifecycle documents to classify. / 2026-08-27 — M0 仓库／ref gate：重新确认 `origin` 为 `git@github.com:Yijia-Zhou/session-analyzer.git`，`package.json.repository.url` 为 `git+https://github.com/Yijia-Zhou/session-analyzer.git`；实时 `main` 解析为 `2f282a94cc9eb3ce03fded4f8867e0d120a599aa`，实时 `origin/towards-0.2.0` 解析为经评审的 `afbb30eb7d51ba2594b7dfdd5dbbe7b7991fe988`。`git merge-base --is-ancestor` 通过。由于实时 target 等于 reviewed SHA，完整 descendant diff 为空：browser Session selection、file suggestion、timeline state、profiling script／test、generated asset、package／build input 与 governing performance／lifecycle document 均无 descendant change 需要分类。
- 2026-08-27 — M0 worktree/branch gate: the pre-switch tracked worktree at `0027b99949bec0dac7dda4e6357aeb272988fe73` had the same tree `997d910a64f9760025be11f9f6614a35ffe3d423` as the live target and no tracked diff. Its sole untracked path was this authorized reviewed plan, with SHA-256 `134e1c0cc45cf1c63c9739a1ef899c6c97f9208923765904854d96d19fcb9a53`; no unowned path existed. Created and switched safely to `perf/wave-1a-browser-hot-path` from `origin/towards-0.2.0`, preserving the plan, then registered its exact active path in `AGENTS.md`. / 2026-08-27 — M0 worktree／branch gate：切换前 tracked worktree 位于 `0027b99949bec0dac7dda4e6357aeb272988fe73`，其 tree `997d910a64f9760025be11f9f6614a35ffe3d423` 与实时 target 相同，且无 tracked diff。唯一 untracked path 是本获授权 reviewed plan，其 SHA-256 为 `134e1c0cc45cf1c63c9739a1ef899c6c97f9208923765904854d96d19fcb9a53`；不存在未归属 path。随后从 `origin/towards-0.2.0` 安全创建并切换到 `perf/wave-1a-browser-hot-path`，保留本计划，并在 `AGENTS.md` 登记其精确 active path。
- 2026-08-27 — M0 authority/toolchain gate: reread target `AGENTS.md`, `CONTEXT.md`, product/performance/lifecycle authorities, accepted Wave 0 plan, `README.md`, `package.json`, and `scripts/build-client.js`; confirmed the exact section 12 envelope, historical-only private-evidence boundary, and unchanged visible/server ownership contracts. The implementation environment is Node `v24.18.1` and npm `12.0.2`, satisfying the reviewed source-development policy. M0 passed with no mandatory-stop condition; M1 may begin, but no long group, production bundle regeneration, or M2 product change is authorized before independent M1 seam acceptance. / 2026-08-27 — M0 authority／toolchain gate：重读 target 上的 `AGENTS.md`、`CONTEXT.md`、product／performance／lifecycle authority、已接受 Wave 0 计划、`README.md`、`package.json` 与 `scripts/build-client.js`；确认第 12 节精确 envelope、只作历史引用的 private-evidence boundary，以及不变的可见／server ownership contract。实施环境为 Node `v24.18.1` 与 npm `12.0.2`，满足经评审的源码开发 policy。M0 通过，未触发强制停止条件；可以开始 M1，但在独立 M1 seam 接受前不得运行长 group、重建 production bundle 或实施 M2 product change。
- 2026-08-27 — M1 pure seam: added browser-compatible CommonJS `src/browser/timeline-event-state.js` without importing it from `app.js`. It provides empty/reset, validated replacement and append publication, exact-object Map get/has, committed context/array/Map parity snapshots, stable fail-closed duplicate/empty-ID errors, and the indexed-card iteration helper. Replacement-review repair removed the reverse Map-entry × array `.some()` scan: parity now makes one canonical-array pass and relies on Map-size, unique non-empty IDs, exact `Map.get()` object identity, committed Map/context binding, and offset parity. Operation tests at prefix sizes 0, 1, 64, and 10,000 prove exactly N visits/N Map gets; append prevalidation adds exactly page-length visits/Map `has` operations and invokes no `.some()`/`.every()`/`.find()`/`.findIndex()` comparator. Existing card tests retain `C` cards → `C` Map gets at canonical sizes 4 and 400, missing-ID non-reuse, content-free observer records, and inert/error-isolated non-profile behavior. The production `currentEvents` owner and `currentTimelineEvent()` remain unchanged. / 2026-08-27 — M1 纯 seam：新增 browser-compatible CommonJS `src/browser/timeline-event-state.js`，但未从 `app.js` 导入。它提供 empty／reset、经校验的 replacement 与 append publish、精确 object Map get／has、committed context／array／Map parity snapshot、稳定 fail-closed duplicate／empty-ID error，以及 indexed-card iteration helper。Replacement-review 修复移除了 Map entry × array `.some()` 的反向 scan：parity 现在只对 canonical array 做一次遍历，并依赖 Map size、唯一非空 ID、精确 `Map.get()` object identity、committed Map／context binding 与 offset parity。Prefix size 0、1、64 与 10,000 的 operation test 证明精确 N 次 visit／N 次 Map get；append prevalidation 只增加 page length 次 visit／Map `has` operation，且不调用 `.some()`／`.every()`／`.find()`／`.findIndex()` comparator。既有 card test 继续证明 canonical size 4 与 400 下 `C` 张 card → `C` 次 Map get、missing ID 不复用、content-free observer record，以及 non-profile inert／observer error isolation。Production `currentEvents` owner 与 `currentTimelineEvent()` 保持不变。
- 2026-08-27 — M1 synthetic-browser evidence seam: extended only `scripts/timeline-profile.js` and the synthetic branch of `scripts/performance-wave-0-runner.js`. The synthetic timeline artifact uses closed schema v4. Replacement-review repair moved suggestion-start order and the automatic-selection-settled marker into the same page init-script fetch owner: every suggestion receives a suggestion-only monotonic sequence, settlement stores the current sequence watermark, and post-settlement is recomputed as `sequence > watermark`; wall-clock fields are not used or serialized. Bootstrap evidence stores only sequence/watermark, `session|project` scope, closed Layer/outcome aliases, HTTP status, post-settlement boolean, exact scope counts, and settlement count. Timeline-event evidence stores only closed purpose/backend aliases, nonnegative operation counts, array/Map/unique counts, parity/context/offset/pending booleans, and recomputed relations. Validator arithmetic now requires `mapGets <= mapBackendRequests <= lookupRequests`, backend partition equality, and exact enclosing card/lookup/Map/get equality with zero other backend and zero array comparisons. `profileAcceptance()` requires passing bootstrap structural relations plus one successful 2xx Session suggestion; runner validation rejects `acceptance.passed=true` when bootstrap or scenario structural evidence fails. Negative fixtures cover forged watermarks/sequences, parity failure, failed/non-2xx suggestions, impossible Map tuples, card mismatch, other backend, and array comparisons. The optional `globalThis.__sessionAnalyzerProfileObserver` remains content-free, absent/inert outside profiling, and error-isolated. Runner manifest, calibration, summary, private artifact, and private algorithm/schema identifiers remain unchanged at v3. / 2026-08-27 — M1 synthetic-browser evidence seam：只扩展 `scripts/timeline-profile.js` 与 `scripts/performance-wave-0-runner.js` 的 synthetic branch。Synthetic timeline artifact 使用封闭 schema v4。Replacement-review 修复把 suggestion start order 与 automatic-selection-settled marker 放入同一个 page init-script fetch owner：每次 suggestion 获得 suggestion-only 单调 sequence，settlement 保存当前 sequence watermark，post-settlement 按 `sequence > watermark` 重算；不使用或序列化 wall-clock field。Bootstrap evidence 只存储 sequence／watermark、`session|project` scope、封闭 Layer／outcome alias、HTTP status、post-settlement boolean、精确 scope count 与 settlement count。Timeline-event evidence 只存储封闭 purpose／backend alias、非负 operation count、array／Map／unique count、parity／context／offset／pending boolean 与重算 relation。Validator arithmetic 现在要求 `mapGets <= mapBackendRequests <= lookupRequests`、backend partition equality，以及 enclosing card／lookup／Map／get 精确相等、other backend 与 array comparison 均为零。`profileAcceptance()` 要求 bootstrap structural relation 通过且存在唯一成功 2xx Session suggestion；runner validator 会拒绝 bootstrap 或 scenario structural evidence 失败却报告 `acceptance.passed=true` 的 artifact。Negative fixture 覆盖伪造 watermark／sequence、parity failure、failed／non-2xx suggestion、impossible Map tuple、card mismatch、other backend 与 array comparison。Optional `globalThis.__sessionAnalyzerProfileObserver` 继续保持 content-free、profiling 外 absent／inert 且隔离 error。Runner manifest、calibration、summary、private artifact 与 private algorithm／schema identifier 保持 v3 不变。
- 2026-08-27 — Independent M1 seam review returned Changes requested on four bounded findings: parity contained an O(N²) reverse scan; suggestion ordering compared browser and Node wall clocks; structural arithmetic/worker bootstrap acceptance were not closed tightly enough; and the planning-era next action was stale. Repairs are limited to the M1 seam files/tests and this bilingual plan. No M2 product behavior, production Map owner, generated asset, private-runner contract, long profile group, commit, push, PR, merge, or archive action occurred. / 2026-08-27 — 独立 M1 seam 评审针对四项限定 finding 返回 Changes requested：parity 含 O(N²) reverse scan；suggestion ordering 比较 browser 与 Node wall clock；structural arithmetic／worker bootstrap acceptance 封闭不足；planning-era next action 已过时。修复只限 M1 seam file／test 与本双语计划。未发生 M2 product behavior、production Map owner、generated asset、private-runner contract、长 profile group、commit、push、PR、merge 或归档 action。
- 2026-08-27 — Replacement M1 repair validation: inspected the complete repaired diff; syntax passed 4/4 for `src/browser/app.js`, `src/browser/timeline-event-state.js`, `scripts/timeline-profile.js`, and `scripts/performance-wave-0-runner.js`; the complete M1 focused Node command passed 72/72 across the state, navigation, transition-safety, search-target, profiler, runner, and build-boundary suites. `git diff --check` and separate new-file whitespace checks passed. The exact changed-path set remains the same eight-path M0/M1 envelope with no unexpected or missing path. `src/browser/app.js` and `public/assets/app.js` remain unchanged from `afbb30eb…`; the untouched bundle SHA-256 remains `0b024009f8eb7d3d617751c4de0d2e59d6d19d1d5474d88790b323a2ab4b37a3`. No browser smoke, calibration, baseline, full e2e group, bundle build, commit, push, PR, merge, M2 duplicate removal, or production Map integration occurred. Executor repair is complete and stopped for replacement independent M1 seam review. / 2026-08-27 — Replacement M1 修复验证：检查完整 repaired diff；`src/browser/app.js`、`src/browser/timeline-event-state.js`、`scripts/timeline-profile.js` 与 `scripts/performance-wave-0-runner.js` 的 syntax 4／4 通过；完整 M1 focused Node 命令在 state、navigation、transition-safety、search-target、profiler、runner 与 build-boundary suite 上通过 72／72。`git diff --check` 与独立 new-file whitespace check 通过。精确 changed-path set 继续是同一个八路径 M0／M1 envelope，不存在意外或缺失 path。`src/browser/app.js` 与 `public/assets/app.js` 相对 `afbb30eb…` 保持不变；未触碰 bundle 的 SHA-256 继续为 `0b024009f8eb7d3d617751c4de0d2e59d6d19d1d5474d88790b323a2ab4b37a3`。未运行 browser smoke、calibration、baseline、完整 e2e group、bundle build；未执行 commit、push、PR、merge、M2 duplicate removal 或 production Map integration。Executor 修复已完成，并停在 replacement independent M1 seam 评审点。
- 2026-08-27 — Replacement independent M1 seam review returned `ACCEPTED`, explicitly authorizing M2 while retaining the accepted content-free, private-runner-isolated, and behavior-neutral M1 seam as the baseline. / 2026-08-27 — Replacement independent M1 seam 评审返回 `ACCEPTED`，明确授权 M2，并继续以已接受、不含内容、隔离 private runner 且行为中性的 M1 seam 为基线。
- 2026-08-27 — M2 implementation and focused gate: `refreshFileSuggestions()` retains boolean behavior for every non-handoff caller and returns the exact frozen three-field local-generation result only for delegated `selectSession()` work. `loadSessions()` captures its own request/context/delegated Session and evaluates all six closed guards before skip or fallback; a failed handoff returns without starting or aborting newer suggestion work. Eight focused real-browser cases passed 8/8 using an in-memory source bundle without writing the production asset. They prove exact normal/explicit/Project/Layer/locale/project/source/drill-down/empty/revision ownership and parameters, exact final empty/nonempty content parity, every guard outcome, stale delegated noncommit, and newer-pending/newer-committed same-Session/same-context schedules with two requests, no third fallback, no newer abort, and only the newer commit. The accepted M1 Node command still passes 72/72; syntax passes 4/4; `git diff --check` passes. The exact M0–M2 envelope contains the prior eight paths plus `src/browser/app.js` and `e2e/browser.test.js`; no server/API/package path changed. `public/assets/app.js` remains untouched with SHA-256 `0b024009f8eb7d3d617751c4de0d2e59d6d19d1d5474d88790b323a2ab4b37a3`. No smoke, calibration, baseline, full E2E group, production build, commit, push, PR, merge, or archive action occurred. / 2026-08-27 — M2 实施与 focused gate：`refreshFileSuggestions()` 对全部非 handoff caller 保留 boolean 行为，只为被委托的 `selectSession()` work 返回精确、冻结且携带本地 generation 的三字段 result。`loadSessions()` 捕获自身 request／context／被委托 Session，并在 skip 或 fallback 前评估全部六项封闭 guard；handoff 失败时直接返回，不启动或取消更新 suggestion work。八项聚焦真实浏览器 case 使用不写入生产资产的内存 source bundle，通过 8／8；证明正常／显式／Project／Layer／locale／project／source／drill-down／empty／revision 路径的精确 ownership 与 parameter、最终 empty／nonempty content 精确等价、每项 guard outcome、stale delegated work 不 commit，以及更新 request pending／已 commit 的 same-Session／same-context 两种 schedule 均只有两次请求、无第三次 fallback、不取消更新者、且只有更新者 commit。已接受的 M1 Node 命令继续通过 72／72；syntax 4／4 通过；`git diff --check` 通过。精确 M0–M2 envelope 是此前八路径加 `src/browser/app.js` 与 `e2e/browser.test.js`；没有 server／API／package path 变化。`public/assets/app.js` 保持未触碰，SHA-256 为 `0b024009f8eb7d3d617751c4de0d2e59d6d19d1d5474d88790b323a2ab4b37a3`。未运行 smoke、calibration、baseline、完整 E2E group 或 production build；未执行 commit、push、PR、merge 或 archive。
- 2026-08-27 — M3 committed timeline pair and focused gate: `app.js` now initializes the accepted pure state owner and routes every canonical reset, replacement, and append through it, so committed context, ordered `currentEvents`, exact-object `currentEventsById`, and offset publish or retire together. Pending Layer/filter/query work records but does not retire the still-rendered committed pair; Session/project/revision full resets clear both structures. The locale case already proved the implementation's separate full `loadSessions()` → `selectSession()` reset, later recognized by M5 review as the intended lifecycle rather than a retained-pair replacement. Section 9 exact-ID consumers moved to the Map; the ordered `findIndex()` traversal remains array-owned, while the remaining temporary-reveal exact membership is migrated by the replacement-M5 repair. `syncEnclosingOperationAffordances()` uses the indexed-card seam. Six focused real-browser M3 cases passed 6/6; the combined M2/M3 source-candidate group passed 14/14. Evidence covers first-page 150, append 300/320, Session switch 40, query/locale/project/revision rebuilds, delayed committed-pair click/selection/detail/enclosing behavior, duplicate fail-before-publish plus retry, stale Layer rejection, Code Mode context-row nonownership, and exact positive 150 card iterations = 150 lookup requests = 150 Map gets with zero array comparisons. Every snapshot passed Map size/unique count/exact identity/context binding/offset parity. The accepted Node set passed 72/72 and syntax passed 4/4; `git diff --check`, no-server-path audit, and exact writer/consumer scan passed. `public/assets/app.js` remains untouched at the pre-M4 SHA-256 `0b024009f8eb7d3d617751c4de0d2e59d6d19d1d5474d88790b323a2ab4b37a3`. / 2026-08-27 — M3 committed timeline pair 与 focused gate：`app.js` 现在初始化已接受的纯 state owner，并让全部 canonical reset、replacement 与 append 通过该边界，因此 committed context、有序 `currentEvents`、保存精确 object 的 `currentEventsById` 与 offset 会一起发布或 retire。Pending Layer／filter／query work 只记录 pending，不 retire 仍在渲染的 committed pair；Session／project／revision full reset 同时清空两种结构。Locale case 当时已经证明 implementation 使用单独的完整 `loadSessions()` → `selectSession()` reset；M5 评审后来确认这是预期 lifecycle，而非 retained-pair replacement。第 9 节精确 ID consumer 改用 Map；有序 `findIndex()` traversal 继续由 array 拥有，剩余 temporary-reveal 精确 membership 则由 replacement-M5 修复迁移。`syncEnclosingOperationAffordances()` 使用 indexed-card seam。六项聚焦真实浏览器 M3 case 通过 6／6；M2／M3 source candidate 合并 group 通过 14／14。Evidence 覆盖 first page 150、append 300／320、Session switch 40、query／locale／project／revision rebuild、延迟期间 committed-pair 的 click／selection／detail／enclosing 行为、duplicate 在 publish 前失败并 retry、stale Layer rejection、Code Mode context row 非 owner，以及精确正数 150 次 card iteration = 150 次 lookup request = 150 次 Map get，array comparison 为零。每个 snapshot 的 Map size／unique count／精确 identity／context binding／offset parity 均通过。已接受 Node set 通过 72／72，syntax 4／4 通过；`git diff --check`、无 server path 审计与精确 writer／consumer scan 通过。`public/assets/app.js` 在 M4 前继续未触碰，SHA-256 为 `0b024009f8eb7d3d617751c4de0d2e59d6d19d1d5474d88790b323a2ab4b37a3`。
- 2026-08-27 — M4 generated integration and design documentation: updated `docs/design-docs/timeline-loading-and-rendering-performance.md` bilingually with the committed context/array/Map/offset owner, atomic validated lifecycle, six-guard suggestion handoff, positive structural operation proof, and explicit later-wave exclusions. The product spec and lifecycle design remain unchanged because visible and server ownership contracts did not change. Ran only the normal `npm run build:client`; `public/assets/app.js` is the sole changed generated path, SHA-256 `c7e4b3815262aee7c1399dbcd240ab6367602492631e5de6938aba6b1966513c`, raw length 282,459 bytes, with no source map; the vendor asset stayed byte-identical. `npm run build:check` passed and reproduced the asset. The plan-defined existing transition/context/detail/search focused browser group then ran against the production bundle and passed 44/44. No visible contract change or mandatory stop appeared. / 2026-08-27 — M4 generated integration 与 design documentation：双语更新 `docs/design-docs/timeline-loading-and-rendering-performance.md`，记录 committed context／array／Map／offset owner、原子且经校验的 lifecycle、六项 guard suggestion handoff、正数结构 operation proof 与显式 later-wave 排除项。由于可见与 server ownership contract 未改变，product spec 与 lifecycle design 保持不变。只运行正常 `npm run build:client`；`public/assets/app.js` 是唯一 changed generated path，SHA-256 为 `c7e4b3815262aee7c1399dbcd240ab6367602492631e5de6938aba6b1966513c`，raw length 为 282,459 bytes，无 source map；vendor asset 保持逐字节相同。`npm run build:check` 通过并重现该资产。随后针对 production bundle 运行计划定义的既有 transition／context／detail／search focused browser group，通过 44／44。未出现可见 contract 变化或强制停止条件。
- 2026-08-27 — M5 final executor validation and audit: on Node `v24.18.1`／V8 `13.6.233.17-node.50`, npm `12.0.2`, Playwright `1.60.0`, and Chromium `148.0.7778.96` on Windows x64, syntax passed 4/4; the focused Node set passed 72/72; exact production-bundle Wave 1A cases passed 14/14; the plan-defined existing browser subset passed 44/44; full `npm test` passed 781/781; final full `npm run test:browser` passed 135/135; `npm run test:package` passed Codex, Claude Code, and DeepSeek Harness; and final `npm run release:check` passed build currentness, another 781/781 Node run, and the three-adapter package smoke. `git diff --check`, separate new-file trailing-whitespace/final-newline checks, build reproducibility, and generated equality passed. Exact paths equal all twelve section 12 paths with no missing/unexpected path; the active registration is exact. Target-to-working-candidate audits show no server/query/Materialized Session/source-adapter/parser/package/lockfile/workflow/product-spec/lifecycle-design path change. Runner hunks add validation/aggregation only after the unchanged private branch returns and gate browser structural output on `profileKind === 'synthetic-browser'`; the focused locked audit passed 5/5 for sealed-copy manifest, canonical private boundaries, closed synthetic schema, private shape, and snapshot semantics. Private artifact/manifest/summary/failure/proof schema and version identifiers remain v3. The generated bundle remains SHA-256 `c7e4b3815262aee7c1399dbcd240ab6367602492631e5de6938aba6b1966513c`, length 282,459, with no extra generated path. HEAD remains reviewed target `afbb30eb…`; all twelve paths are known implementation work, with no unowned path. The explicit no-commit instruction means no clean frozen candidate identity, smoke, calibration, baseline group, evidence packet, commit, push, PR, merge, archive, or closeout action occurred; execution stops before M6 for independent review and separate freeze authorization. / 2026-08-27 — M5 final executor validation 与 audit：在 Windows x64 上使用 Node `v24.18.1`／V8 `13.6.233.17-node.50`、npm `12.0.2`、Playwright `1.60.0` 与 Chromium `148.0.7778.96`，syntax 4／4 通过；focused Node set 通过 72／72；精确 production-bundle Wave 1A case 通过 14／14；计划定义的既有 browser subset 通过 44／44；完整 `npm test` 通过 781／781；最终完整 `npm run test:browser` 通过 135／135；`npm run test:package` 的 Codex、Claude Code 与 DeepSeek Harness 全部通过；最终 `npm run release:check` 通过 build currentness、另一次 781／781 Node run 与三 adapter package smoke。`git diff --check`、独立 new-file trailing-whitespace／final-newline check、build 可复现性与 generated equality 全部通过。精确 path 等于第 12 节全部十二条路径，无缺失／意外 path；active registration 精确。Target 到 working candidate 审计显示没有 server／query／Materialized Session／source adapter／parser／package／lockfile／workflow／product spec／lifecycle design path 变化。Runner hunk 只在不变 private branch 返回后增加 validation／aggregation，并以 `profileKind === 'synthetic-browser'` 门控 browser structural output；聚焦 locked audit 对 sealed-copy manifest、canonical private boundary、封闭 synthetic schema、private shape 与 snapshot semantic 通过 5／5。Private artifact／manifest／summary／failure／proof schema 与 version identifier 继续为 v3。Generated bundle SHA-256 继续为 `c7e4b3815262aee7c1399dbcd240ab6367602492631e5de6938aba6b1966513c`，length 282,459，无额外 generated path。HEAD 继续是 reviewed target `afbb30eb…`；全部十二路径均为已知 implementation work，无 unowned path。由于明确 no-commit 指令，未产生 clean frozen candidate identity，也未执行 smoke、calibration、baseline group、evidence packet、commit、push、PR、merge、archive 或 closeout action；执行在 M6 前停止，等待独立评审与另行 freeze 授权。
- 2026-08-28 — Independent M5 review returned `Changes requested` on three bounded findings: the profiler's page-owned automatic-selection settlement method was never invoked by `app.js`; the plan incorrectly grouped locale with retained-pair Layer/filter/query replacement despite the inspected implementation and E2E evidence using a full `loadSessions()` → `selectSession()` reset; and `renderTimeline()` retained one canonical temporary-reveal `.some(id)` membership scan. The reviewer approved preserving the existing full-reset locale product behavior and correcting the plan/design rather than introducing new retained-locale semantics. Repairs remain inside the accepted twelve-path envelope; M6, candidate freeze/commit, smoke, calibration, baseline, review packet, push, PR, merge, archive, and closeout remain prohibited. / 2026-08-28 — 独立 M5 评审针对三项限定 finding 返回 `Changes requested`：profiler 的 page-owned automatic-selection settlement method 从未被 `app.js` 调用；尽管经检查的 implementation 与 E2E evidence 使用完整 `loadSessions()` → `selectSession()` reset，计划却错误地把 locale 与 retained-pair Layer／filter／query replacement 归为一类；`renderTimeline()` 还保留一处 canonical temporary-reveal `.some(id)` membership scan。Reviewer 批准保留既有 full-reset locale 产品行为并修正计划／design，而不是引入新的 retained-locale 语义。修复继续位于已接受十二路径 envelope 内；M6、candidate freeze／commit、smoke、calibration、baseline、review packet、push、PR、merge、archive 与 closeout 继续被禁止。
- 2026-08-28 — Replacement-M5 repairs and executor validation: `loadSessions()` now invokes a zero-argument, error-isolated marker immediately after delegated `selectSession()` settlement and before rendering or any six-guard skip/fallback decision; the page-owned monotonic ledger proves the normal bootstrap tuple Session requests `1`, settlement count `1`, settlement watermark `1`, post-settlement Session requests `0`. The explicit Session-click path does not emit the marker. `profileAcceptance()` and accepted-worker validation require watermark `1`; a structurally closed artifact with the marker removed is rejected despite `acceptance.passed=true`. Locale documentation/tests now classify Layer/filter/query as retained-pair replacements and locale as the reviewer-approved full reset that proves an empty pair before localized page zero. `renderTimeline()` temporary-reveal membership uses `hasCanonicalTimelineEvent()`; a dedicated real-browser fork-point case observes a Map-backed membership operation and zero canonical array comparisons while preserving reveal/fold behavior. One initial diagnostic Wave 1A run passed 13/14 because the empty-Session test still expected the old seam object; after updating that content-free Project-ledger expectation, the exact production-bundle group passed 14/14. Syntax passed 4/4; focused Node passed 72/72; the plan-defined existing browser subset passed 44/44; the dedicated fork-point membership case passed 1/1; full `npm test` passed 781/781; full `npm run test:browser` passed 135/135; `npm run build:check`, `npm run test:package` for Codex/Claude Code/DeepSeek Harness, and `npm run release:check` all passed, with release check repeating build currentness, 781/781 Node tests, and all three package checks. The locked private-runner audit passed 5/5. The reproducible production bundle SHA-256 is `f5b713e41b6a0652bd8faac320f734c9e6696fb62ffdb5f00bd54aba2dc00b95`, length 282,543 bytes. Runtime remains Node `v24.18.1`, V8 `13.6.233.17-node.50`, npm `12.0.2`, Playwright `1.60.0`, Chromium `148.0.7778.96`, Windows x64. Final `git diff --check`, three-new-file whitespace/final-newline validation, exact twelve-path comparison, reviewed-SHA ancestry, active-plan registration, generated-path/hash, stale-wording/direct-writer/source-route scans, and prohibited server/API/package/lockfile/workflow/product-spec/lifecycle path audits passed. HEAD remains `afbb30eb…`, and all twelve paths are owned working-candidate changes. No Wave 1A smoke/calibration/baseline, packet, freeze, commit, push, PR, merge, archive, or closeout action occurred. / 2026-08-28 — Replacement-M5 修复与 executor validation：`loadSessions()` 现在会在被委托 `selectSession()` settle 后、render 或任何六项 guard skip／fallback decision 前，调用零参数且隔离 error 的 marker；page-owned 单调 ledger 证明正常 bootstrap tuple 为 Session request `1`、settlement count `1`、settlement watermark `1`、settle 后 Session request `0`。显式 Session-click 路径不会发出 marker。`profileAcceptance()` 与 accepted-worker validation 要求 watermark `1`；移除 marker 后，即使 structurally closed artifact 报告 `acceptance.passed=true` 也会被拒绝。Locale 文档／test 现在把 Layer／filter／query 归类为 retained-pair replacement，把 locale 归类为 reviewer-approved full reset，并证明 localized page zero 前存在 empty pair。`renderTimeline()` temporary-reveal membership 使用 `hasCanonicalTimelineEvent()`；专门的真实浏览器 fork-point case 观测到 Map-backed membership operation 与零 canonical array comparison，同时保留 reveal／fold 行为。首次 diagnostic Wave 1A run 通过 13／14，原因是 empty-Session test 仍精确期待旧 seam object；更新该不含内容的 Project-ledger expectation 后，精确 production-bundle group 通过 14／14。Syntax 通过 4／4；focused Node 通过 72／72；计划定义的 existing browser subset 通过 44／44；专门 fork-point membership case 通过 1／1；完整 `npm test` 通过 781／781；完整 `npm run test:browser` 通过 135／135；`npm run build:check`、Codex／Claude Code／DeepSeek Harness 的 `npm run test:package` 与 `npm run release:check` 全部通过，release check 再次通过 build currentness、781／781 Node test 与三项 package check。Locked private-runner audit 通过 5／5。可复现 production bundle SHA-256 为 `f5b713e41b6a0652bd8faac320f734c9e6696fb62ffdb5f00bd54aba2dc00b95`，length 282,543 bytes。Runtime 继续为 Node `v24.18.1`、V8 `13.6.233.17-node.50`、npm `12.0.2`、Playwright `1.60.0`、Chromium `148.0.7778.96`、Windows x64。最终 `git diff --check`、三个 new-file whitespace／final-newline validation、精确十二路径 comparison、reviewed SHA ancestry、active-plan registration、generated path／hash、stale wording／direct writer／source route scan，以及禁止的 server／API／package／lockfile／workflow／product spec／lifecycle path audit 均通过。HEAD 继续为 `afbb30eb…`，全部十二路径都是有 owner 的 working-candidate change。未执行 Wave 1A smoke／calibration／baseline、packet、freeze、commit、push、PR、merge、archive 或 closeout action。
- 2026-08-28 — Latest replacement M5 review returned `Changes requested` on one bounded P1: `syncEnclosingOperationAffordances()` called `canonicalTimelineEvent()` directly, so a mounted true `temporaryEventReveal` still paid its Map get but received `null` instead of the existing O(1) temporary fallback, allowing visibility, `aria-hidden`, or disabled state to remain stale after selection or `contextRevealPending` changes. The review also found that the prior fork-point operation assertion was indirect because another navigation consumer could produce the observed membership record; that assertion is superseded as specific render-membership evidence. The accepted settlement marker/watermark, locale full reset, `renderTimeline()` Map membership, six handoff guards, committed state boundary, and synthetic/private isolation remain unchanged. M6 and every freeze/profile/packet/repository-lifecycle action remain blocked. / 2026-08-28 — 最新 replacement M5 评审针对一项限定 P1 返回 `Changes requested`：`syncEnclosingOperationAffordances()` 直接调用 `canonicalTimelineEvent()`，因此 mounted 的真正 `temporaryEventReveal` 虽然仍执行 Map get，却得到 `null` 而不是既有 O(1) temporary fallback，使 visibility、`aria-hidden` 或 disabled state 可能在 selection 或 `contextRevealPending` 变化后保持 stale。评审还发现此前 fork-point operation 断言属于间接 evidence，因为其它 navigation consumer 也可能产生观测到的 membership record；该断言不再作为精确 render-membership evidence。已接受的 settlement marker／watermark、locale full reset、`renderTimeline()` Map membership、六项 handoff guard、committed state boundary 与 synthetic／private isolation 均保持不变。M6 与全部 freeze／profile／packet／repository lifecycle action 继续被阻止。
- 2026-08-28 — Focused P1 implementation and isolated browser proof: `currentTimelineEvent(eventId, purpose = 'canonical')` now passes the closed purpose to the canonical Map get and preserves its exact temporary-event fallback; the indexed enclosing-affordance callback uses it. No temporary or presentation event enters `currentEventsById`. A new production-bundle true-temporary case passed 1/1: the event-envelope response is paused, operation evidence is reset immediately before fulfillment, the first subsequent lookup is the specific `renderTimeline()` Map membership call, the out-of-prefix card becomes selected with a visible/ARIA-current affordance, pending parent reveal disables it, completion re-enables it, canonical parity/size stays unchanged, and positive card iterations equal Map gets with zero array comparisons. The old fork-point test now explicitly proves its navigation reveal is not a `temporaryEventReveal` probe. Full replacement-M5 validation remains pending. / 2026-08-28 — 聚焦 P1 implementation 与隔离 browser proof：`currentTimelineEvent(eventId, purpose = 'canonical')` 现在把封闭 purpose 传给 canonical Map get，并保留其精确 temporary-event fallback；indexed enclosing-affordance callback 使用该函数。没有任何 temporary 或 presentation event 进入 `currentEventsById`。新增 production-bundle true-temporary case 通过 1／1：暂停 event-envelope response，在 fulfill 前立即重置 operation evidence，随后第一次 lookup 是精确的 `renderTimeline()` Map membership call；前缀外 card 被选中后得到可见／ARIA-current affordance，parent reveal pending 时禁用，完成后重新启用；canonical parity／size 保持不变，且正数 card iteration 等于 Map get，array comparison 为零。旧 fork-point test 现在明确证明其 navigation reveal 不是 `temporaryEventReveal` probe。完整 replacement-M5 validation 仍待执行。
- 2026-08-28 — Focused replacement-M5 validation stop: the complete twelve-path diff was re-inspected; syntax passed 4/4; the focused Node command passed 72/72; the complete production-bundle Wave 1A group passed 15/15, including the new true-temporary case; the relevant existing browser subset passed 45/45; full `npm test` passed 781/781; and full `npm run test:browser` passed 136/136. `npm run build:check`, the Codex/Claude Code/DeepSeek Harness `npm run test:package`, and `npm run release:check` all passed; release check repeated generated-currentness, 781/781 Node tests, and all three package smokes. The locked private-runner audit passed 5/5. Reproducible `public/assets/app.js` is SHA-256 `722a3825e21f0db045b6b636d723d47e23ce10b61e7028681ffb507b631aacb7`, length 282,559 bytes, with no extra generated asset or source map. Final `git diff --check`, three-new-file whitespace/final-newline checks, exact twelve-path comparison, reviewed-SHA ancestry, active registration, direct-writer scan, and prohibited server/API/package/lockfile/workflow/product-spec/lifecycle-path audit passed. HEAD and `origin/towards-0.2.0` remain `afbb30eb…`; all paths are owned. No smoke, calibration, baseline, packet, freeze, commit, push, PR, merge, archive, or closeout action occurred. Execution stops before M6 for focused replacement M5 review. / 2026-08-28 — Focused replacement-M5 validation 停止点：重新检查完整十二路径 diff；syntax 通过 4／4；focused Node command 通过 72／72；包含新增真正 temporary case 的完整 production-bundle Wave 1A group 通过 15／15；相关既有 browser subset 通过 45／45；完整 `npm test` 通过 781／781；完整 `npm run test:browser` 通过 136／136。`npm run build:check`、覆盖 Codex／Claude Code／DeepSeek Harness 的 `npm run test:package` 与 `npm run release:check` 全部通过；release check 重复通过 generated-currentness、781／781 Node test 与全部三项 package smoke。Locked private-runner audit 通过 5／5。可复现的 `public/assets/app.js` SHA-256 为 `722a3825e21f0db045b6b636d723d47e23ce10b61e7028681ffb507b631aacb7`，length 为 282,559 bytes，无额外 generated asset 或 source map。最终 `git diff --check`、三个 new-file whitespace／final-newline check、精确十二路径 comparison、reviewed-SHA ancestry、active registration、direct-writer scan，以及禁止的 server／API／package／lockfile／workflow／product spec／lifecycle path audit 全部通过。HEAD 与 `origin/towards-0.2.0` 均继续为 `afbb30eb…`，全部路径均有 owner。未执行 smoke、calibration、baseline、packet、freeze、commit、push、PR、merge、archive 或 closeout action。执行在 M6 前停止，等待 focused replacement M5 review。
- 2026-08-28 — Follow-up replacement M5 review returned `Changes requested` on two bounded P1 evidence/performance findings. First, `app.js::recordTimelineEventState()` invoked the full linear parity snapshot even when no callable `recordStateSnapshot` observer existed, adding evidence-only O(N) work after replacement/append and before retained-pair replacement requests. Second, profiler and runner accepted vacuous all-zero enclosing-affordance equality despite the positive-`C` design contract. The accepted temporary fallback, settlement watermark, locale lifecycle, six handoff guards, committed pair, and synthetic/private boundary remain unchanged; M6 remains blocked. / 2026-08-28 — Follow-up replacement M5 评审针对两项限定 P1 evidence／performance finding 返回 `Changes requested`。第一，`app.js::recordTimelineEventState()` 即使不存在可调用的 `recordStateSnapshot` observer，仍调用完整线性 parity snapshot，在 replacement／append 后及 retained-pair replacement request 前增加仅用于 evidence 的 O(N) work。第二，尽管 design contract 要求正数 `C`，profiler 与 runner 仍接受空洞的 enclosing-affordance 全零相等。已接受的 temporary fallback、settlement watermark、locale lifecycle、六项 handoff guard、committed pair 与 synthetic／private boundary 均保持不变；M6 继续被阻止。
- 2026-08-28 — Focused follow-up repair: added `profileTimelineEventStateSnapshot()`, which resolves a callable state-snapshot observer before any parity work and otherwise returns `null`; `app.js` uses only this gated seam, while append retains `assertTimelineEventState()`. A 1,000-event operation test proves zero visits/gets for absent, method-missing, and inaccessible observers, and exact 1,000/1,000 work plus the unchanged closed snapshot for a valid observer. Profiler and runner now require `cardIterations > 0` before exact enclosing arithmetic. Accepted fixtures use positive 1/1/1/1 counters; honest all-zero fixtures recompute false, remain structurally parseable, and cannot pass bootstrap worker, scenario worker, or aggregate acceptance. The three focused Node files passed 42/42; full validation remains pending. / 2026-08-28 — 聚焦 follow-up 修复：新增 `profileTimelineEventStateSnapshot()`，在任何 parity work 前解析可调用的 state-snapshot observer，否则返回 `null`；`app.js` 只使用该 gated seam，而 append 保留 `assertTimelineEventState()`。1,000-event operation test 证明 observer 缺席、缺少方法或方法不可访问时 visit／get 均为零；有效 observer 时精确执行 1,000／1,000 work 并产生不变的封闭 snapshot。Profiler 与 runner 现在要求 `cardIterations > 0`，之后才检查 enclosing 精确 arithmetic。Accepted fixture 使用正数 1／1／1／1 counter；诚实全零 fixture 重算为 false、保持结构可解析，且不能通过 bootstrap worker、scenario worker 或 aggregate acceptance。三个 focused Node file 通过 42／42；完整 validation 仍待执行。
- 2026-08-28 — Follow-up replacement-M5 validation stop: syntax passed 4/4; the exact focused Node command passed 73/73; the complete production-bundle Wave 1A group passed 15/15; the relevant existing browser subset passed 45/45; full `npm test` passed 782/782; and full `npm run test:browser` passed 136/136. `npm run build:check`, the Codex/Claude Code/DeepSeek Harness package smoke, and `npm run release:check` passed; release check repeated generated currentness, 782/782 Node tests, and all three package checks. The locked private-runner audit passed 5/5. Reproducible `public/assets/app.js` is SHA-256 `de7d470954573d772f2c5163a9ad5a69721ddd088df82d45c6c00a944a97adff`, length 282,791 bytes, with one generated asset and no source map. The complete diff was re-inspected; final `git diff --check`, three-new-file whitespace/final-newline checks, exact twelve-path comparison, reviewed-SHA ancestry, active registration, direct-writer scan, and prohibited server/API/package/lockfile/workflow/product-spec/lifecycle-path audit passed. HEAD and `origin/towards-0.2.0` remain `afbb30eb…`; all paths are owned. No smoke, calibration, baseline, packet, freeze, commit, push, PR, merge, archive, or closeout action occurred. Execution stops before M6 for another replacement M5 review. / 2026-08-28 — Follow-up replacement-M5 validation 停止点：syntax 通过 4／4；精确 focused Node command 通过 73／73；完整 production-bundle Wave 1A group 通过 15／15；相关既有 browser subset 通过 45／45；完整 `npm test` 通过 782／782；完整 `npm run test:browser` 通过 136／136。`npm run build:check`、Codex／Claude Code／DeepSeek Harness package smoke 与 `npm run release:check` 通过；release check 重复通过 generated currentness、782／782 Node test 与全部三项 package check。Locked private-runner audit 通过 5／5。可复现的 `public/assets/app.js` SHA-256 为 `de7d470954573d772f2c5163a9ad5a69721ddd088df82d45c6c00a944a97adff`，length 为 282,791 bytes，只有一个 generated asset 且无 source map。重新检查完整 diff；最终 `git diff --check`、三个 new-file whitespace／final-newline check、精确十二路径 comparison、reviewed-SHA ancestry、active registration、direct-writer scan，以及禁止的 server／API／package／lockfile／workflow／product spec／lifecycle path audit 全部通过。HEAD 与 `origin/towards-0.2.0` 均继续为 `afbb30eb…`，全部路径均有 owner。未执行 smoke、calibration、baseline、packet、freeze、commit、push、PR、merge、archive 或 closeout action。执行在 M6 前停止，等待再次 replacement M5 review。
- 2026-08-28 — Evidence-retention recovery: canonical M6 evidence was lost while stored under `%TEMP%`; the exact deleting or visibility-loss actor was not established. Control review classifies this as a packet-source/evidence-retention defect, not an executable defect. The still-available former M7 group may be preserved only in a separately labeled `superseded-diagnostic` archive with `finalGroupEligibility=false` and `replacementInputEligibility=false`; it cannot enter replacement calibration, M7, `SANITIZED_RUN_ROOT`, or the final packet. Replacement M5–M6 must use direct canonical writes plus fresh-process sealing under one preflighted durable external root. Replacement M7 remains blocked. / 2026-08-28 — Evidence-retention 恢复：canonical M6 evidence 在存放于 `%TEMP%` 期间丢失；未能确定精确的删除或可见性丢失 actor。Control review 将其分类为 packet-source／evidence-retention defect，而非 executable defect。仍可用的 former M7 group 只能保存到单独标记的 `superseded-diagnostic` archive，并记录 `finalGroupEligibility=false` 与 `replacementInputEligibility=false`；它不得进入 replacement calibration、M7、`SANITIZED_RUN_ROOT` 或最终 packet。Replacement M5 至 M6 必须在一个通过 preflight 的 durable external root 下执行 direct canonical write 与 fresh-process seal。Replacement M7 继续被阻止。
- 2026-08-27 — M1 focused evidence: syntax passed 4/4 for `src/browser/app.js`, the new state seam, timeline profiler, and shared runner. The exact M1 Node command passed 71/71 across the new state tests plus navigation, transition safety, search targets, profiler, runner, and build-boundary suites. Runner coverage retained closed private manifest acceptance/rejection, canonical private boundaries, unchanged private summary shape with no browser-structural field, failure tuples, invalid-attempt retention, and six-checkpoint snapshot-proof semantics; synthetic fixtures separately proved hard aggregation and rejection of count/relation/privacy violations. `git diff --check` passed. The exact changed-path set is the eight-path M0/M1 subset: `AGENTS.md`, this plan, the two scripts, the new state module, and the three focused test files; no unexpected or missing path exists. `src/browser/app.js` and `public/assets/app.js` are unchanged from `afbb30eb…`; the untouched bundle SHA-256 is `0b024009f8eb7d3d617751c4de0d2e59d6d19d1d5474d88790b323a2ab4b37a3`. No browser smoke/calibration/baseline, e2e group, bundle generation, M2 duplicate removal, or production Map integration ran. There is no plan deviation; the deliberate synthetic-only schema v4 bump is the closed additive schema boundary described above, while every private contract/version remains unchanged. M1 is stopped for independent seam review. / 2026-08-27 — M1 聚焦 evidence：`src/browser/app.js`、新 state seam、timeline profiler 与 shared runner 的 syntax 4／4 通过。精确 M1 Node 命令在新 state test 加 navigation、transition safety、search target、profiler、runner 与 build-boundary suite 上通过 71／71。Runner coverage 保留封闭 private manifest 接受／拒绝、规范 private boundary、无 browser-structural field 的不变 private summary shape、failure tuple、invalid-attempt 保留与六 checkpoint snapshot-proof 语义；独立 synthetic fixture 证明 hard aggregation，以及对 count／relation／privacy violation 的拒绝。`git diff --check` 通过。精确 changed-path set 是八条 M0／M1 subset：`AGENTS.md`、本计划、两个 script、新 state module 与三个聚焦 test file；不存在意外或缺失 path。`src/browser/app.js` 与 `public/assets/app.js` 相对 `afbb30eb…` 保持不变；未触碰 bundle 的 SHA-256 为 `0b024009f8eb7d3d617751c4de0d2e59d6d19d1d5474d88790b323a2ab4b37a3`。未运行 browser smoke／calibration／baseline、e2e group、bundle generation、M2 duplicate removal 或 production Map integration。不存在计划偏离；有意的 synthetic-only schema v4 bump 是上述封闭 additive schema boundary，而全部 private contract／version 保持不变。M1 已停在独立 seam 评审点。
- 2026-08-27 — Planning: verified exact repository identity, live `main@2f282a94…`, live `towards-0.2.0@afbb30eb…`, and local/tree equality; read required docs, complete relevant browser/server/query functions, generated output, Wave 0 scripts/fixture/tests, focused Node/browser tests, and exact target CI checks. / 2026-08-27 — 规划：核验精确仓库 identity、实时 `main@2f282a94…`、实时 `towards-0.2.0@afbb30eb…` 与本地／tree 相等；读取必需文档、完整相关 browser／server／query function、generated output、Wave 0 script／fixture／test、聚焦 Node／browser test 与精确 target CI check。
- 2026-08-27 — Planning findings: confirmed the post-settlement duplicate Session suggestion request, distinct Project ownership, `currentTimelineEvent()` linear scan, and the per-card enclosing-affordance `O(L^2)` subpath as labeled source facts/inference. No implementation, test run, profile group, generated change, commit, or push occurred. / 2026-08-27 — 规划 finding：确认 settle 后重复 Session suggestion request、独立 Project ownership、`currentTimelineEvent()` 线性 scan，以及逐 card enclosing-affordance `O(L^2)` 子路径，并按 source fact／inference 标注。未发生 implementation、test run、profile group、generated change、commit 或 push。
- 2026-08-27 — Planning closeout: inspected the complete plan/diff, checked English/Chinese semantic alignment and exact path/function attribution for every major source claim, passed the tracked and new-file whitespace checks, and confirmed the target plan is the only changed path. / 2026-08-27 — 规划收口：检查完整计划／diff，核对英文／中文语义对齐与每项主要源码 claim 的精确 path／function 归属，通过 tracked 与新文件 whitespace check，并确认目标计划是唯一 changed path。
- 2026-08-27 — Reviewer revision: corrected pending-timeline ownership to an interactive committed context/array/Map; closed the outer Sessions handoff guards; isolated historical Wave 0 private evidence from browser runner changes; and defined the closed, hash-bound, independently recomputable Wave 1A packet. Planning-only boundary and exact source/ref/scope findings remain unchanged. / 2026-08-27 — Reviewer revision：把 pending timeline ownership 修正为可交互的 committed context／array／Map；封闭外层 Sessions handoff guard；隔离历史 Wave 0 private evidence 与 browser runner 变更；定义封闭、hash-bound、可独立重算的 Wave 1A packet。Planning-only 边界与精确 source／ref／scope finding 保持不变。
- 2026-08-27 — Same-context race revision: added the delegated invocation's exact file-suggestion generation as the third closed result field and sixth handoff guard; required pending/already-committed same-context browser schedules. No implementation, test/profile run, generated change, commit, push, or PR occurred. / 2026-08-27 — Same-context race revision：把被委托 invocation 的精确 file-suggestion generation 加为第三个封闭 result field 与第六项 handoff guard；要求 same-context 更新 request 仍 pending／已经 commit 的 browser schedule。未发生 implementation、test／profile run、generated change、commit、push 或 PR。
- Next action: assemble, independently self-validate, and seal the closed section 19 packet directly under the accepted durable `m8-packet` owner, then stop for independent M8 source/diff/test/evidence review. Do not claim integration readiness or perform lifecycle closeout before that reviewer returns an explicit acceptance verdict. / 下一步：直接在已接受 durable `m8-packet` owner 下组装、独立自验并封存第 19 节 closed packet，随后停下等待独立 M8 source／diff／test／evidence review。在 reviewer 返回明确接受 verdict 前，不得声明 integration readiness，也不得执行 lifecycle closeout。

### Fixed decisions log / 固定决策日志

- 2026-08-27 — Bind all planning findings to `afbb30eb7d51ba2594b7dfdd5dbbe7b7991fe988`; future execution uses ancestry plus relevant-path drift review, not an assumption that the remote stays fixed. / 2026-08-27 — 把全部规划 finding 绑定到 `afbb30eb7d51ba2594b7dfdd5dbbe7b7991fe988`；未来执行使用 ancestry 加 relevant-path drift 评审，不假设远端保持不变。
- 2026-08-27 — Delete only the successful automatic Current Session Scope duplicate invocation; preserve noncommitting fallback and every Project/explicit owner. / 2026-08-27 — 只删除成功自动当前会话范围的重复 invocation；保留未 commit fallback 与每个 Project／显式 owner。
- 2026-08-27 — Keep `currentEvents` canonical and ordered; commit its context and derived exact-object `currentEventsById` together. Pending desired replacement does not disable the rendered committed pair; replacement/append is transactional and duplicate handling fails closed. / 2026-08-27 — 保持 `currentEvents` canonical 且有序；其 context 与派生 exact-object `currentEventsById` 一起 commit。Pending desired replacement 不禁用已渲染 committed pair；replacement／append 使用 transaction，duplicate 处理 fail closed。
- 2026-08-27 — Migrate only confirmed canonical exact-ID consumers; retain all ordered, navigation-cache, rendering, filtering, paging, and presentation traversals. / 2026-08-27 — 只迁移已确认 canonical 精确 ID consumer；保留全部有序、navigation-cache、rendering、filtering、paging 与 presentation 遍历。
- 2026-08-27 — Use a pure browser state module plus additive content-free Wave 0 instrumentation as joint operation proof; no Map-presence, grep-only, or timing-only acceptance. / 2026-08-27 — 使用纯 browser state module 加不含内容的 Wave 0 instrumentation 作为联合 operation proof；不接受只存在 Map、只做 grep 或只看 timing。
- 2026-08-27 — Cite accepted Wave 0 private server evidence only by its original public identity as historical evidence; keep runner additions synthetic-browser-only and prove private schema/validation/aggregation/failure/snapshot semantics unchanged. Retain the five-scenario model; timing remains descriptive and no numerical latency gate is introduced. / 2026-08-27 — 只按原始公开 identity 把已接受 Wave 0 private server evidence 作为历史 evidence 引用；runner 新增只属于 synthetic-browser，并证明 private schema／validation／aggregation／failure／snapshot 语义不变。保留五场景模型；timing 继续只作描述，不引入数值 latency gate。
- 2026-08-27 — Expected code paths are fixed in section 12. Any server/API/package/product-spec/source-adapter/ProjectQueryStore path is a stop, not scope. / 2026-08-27 — 预期 code path 固定于第 12 节。任何 server／API／package／product spec／source adapter／ProjectQueryStore path 都是停止条件，不属于 scope。
- 2026-08-28 — Canonical M5–M8 evidence is owned only by one preflighted durable repository-external root and is written there directly. Every completed stage is independently byte-bound, reopened, enforceably sealed, and reverified before later stages/reviewer stops; temporary namespaces and recorded summaries/hashes are not evidence-retention mechanisms. / 2026-08-28 — Canonical M5 至 M8 evidence 只由一个通过 preflight、位于 repository 外的 durable root 持有，并直接写入该位置。每个完成 stage 都必须独立绑定 byte、重新打开、执行可强制的 seal，并在后续 stage／reviewer stop 前重验；临时 namespace 与已记录 summary／hash 都不是 evidence-retention 机制。
- 2026-08-27 — A pending desired Layer/filter/query timeline context does not retire the visible committed pair; only successful ownership-gated publication or an existing full reset retires it. Making old visible DOM inert during those retained-pair replacements requires a separate product decision. / 2026-08-27 — Pending desired Layer／filter／query timeline context 不会 retire 可见 committed pair；只有成功且通过 ownership gate 的发布或既有完整 reset 才会 retire 它。在这些 retained-pair replacement 期间使旧可见 DOM inert 需要单独产品决策。
- 2026-08-28 — Locale uses the existing full `loadSessions()` → `selectSession()` reset and retires the old pair before localized page zero. This reviewer-approved correction supersedes the earlier retained-pair locale assumption while preserving the inspected product behavior. / 2026-08-28 — Locale 使用既有完整 `loadSessions()` → `selectSession()` reset，并在 localized page zero 前 retire 旧 pair。该 reviewer-approved 修正取代此前 retained-pair locale 假设，同时保留经检查的产品行为。
- 2026-08-28 — Automatic-selection settlement is a zero-argument profiling signal emitted only by the automatic `loadSessions()` delegation boundary after `selectSession()` settles and before skip/fallback; the page-owned fetch ledger alone owns sequence and watermark. Explicit Session clicks never emit it. / 2026-08-28 — Automatic-selection settlement 是零参数 profiling signal，只由自动 `loadSessions()` delegation boundary 在 `selectSession()` settle 后、skip／fallback 前发出；sequence 与 watermark 只由 page-owned fetch ledger 拥有。显式 Session click 绝不发出该 signal。
- 2026-08-28 — `renderTimeline()`'s temporary-reveal decision is exact canonical-ID membership and therefore uses the Map helper; the surrounding ordered render traversal and presentation semantics remain array-owned and unchanged. / 2026-08-28 — `renderTimeline()` 的 temporary-reveal decision 属于精确 canonical-ID membership，因此使用 Map helper；其周围有序 render traversal 与 presentation 语义继续由 array 拥有且保持不变。
- 2026-08-28 — Every mounted enclosing-affordance card resolves through `currentTimelineEvent(eventId, 'enclosingAffordance')`: exactly one Map get occurs first, and only a miss may use the existing O(1) exact temporary-event fallback. This preserves temporary presentation behavior without making temporary/context rows Map writers. / 2026-08-28 — 每张 mounted enclosing-affordance card 都通过 `currentTimelineEvent(eventId, 'enclosingAffordance')` 解析：先精确执行一次 Map get，只有 miss 才可使用既有 O(1) 精确 temporary-event fallback。该规则保留 temporary presentation 行为，同时不让 temporary／context row 成为 Map writer。
- 2026-08-28 — Product state call sites use only `profileTimelineEventStateSnapshot()`: a callable `recordStateSnapshot` is the precondition for profiling parity work, while `assertTimelineEventState()` remains the correctness-owned append precondition. / 2026-08-28 — 产品 state call site 只使用 `profileTimelineEventStateSnapshot()`：可调用的 `recordStateSnapshot` 是执行 profiling parity work 的前置条件，而 `assertTimelineEventState()` 继续作为 correctness-owned append 前置条件。
- 2026-08-28 — Enclosing-affordance acceptance is non-vacuous: profiler and runner both require `cardIterations > 0` plus exact card/lookup/Map-backend/get equality and zero other backend/array comparisons. Honest all-zero evidence remains parseable only as failed diagnostic evidence. / 2026-08-28 — Enclosing-affordance acceptance 不得空洞：profiler 与 runner 均要求 `cardIterations > 0`，并要求 card／lookup／Map-backend／get 精确相等、other backend／array comparison 为零。诚实全零 evidence 只能作为失败诊断 evidence 保持可解析。
- 2026-08-27 — Wave 1A review evidence uses the fixed section 19 packet tree and hash algorithm; sanitized projections are the recomputation authority, and Wave 0 private artifacts never enter the packet. / 2026-08-27 — Wave 1A review evidence 使用第 19 节固定 packet tree 与 hash algorithm；sanitized projection 是重算权威，Wave 0 private artifact 绝不进入 packet。
- 2026-08-27 — Context equality is insufficient suggestion ownership: the delegated invocation's local `suggestionRequestId` is the sixth guard, and fallback is allowed only while that noncommitting generation remains latest. / 2026-08-27 — Context 相等不足以证明 suggestion ownership：被委托 invocation 的本地 `suggestionRequestId` 是第六项 guard，只有该未 commit generation 仍 latest 时才允许 fallback。

## 19. Independent review and closeout requirements / 独立评审与收口要求

Wave 1A has one closed, content-safe review-packet contract. “Equivalent evidence index,” executor-only aggregate, or an ad hoc directory is not acceptable. The packet is evidence output outside the repository and does not add a changed path to section 12. / Wave 1A 只有一个封闭、content-safe 的 review-packet contract。不接受“等价 evidence index”、只有 executor aggregate 或临时目录。Packet 是仓库外 evidence output，不会向第 12 节增加 changed path。

### 19.1 Required packet payloads / 必需 packet payload

The logical packet must expose this canonical relative file tree; filenames and run ordinals are fixed. Projection files may be embedded or resolved through the indexed read-only root rule in section 19.2. / 逻辑 packet 必须提供以下 canonical relative file tree；filename 与 run ordinal 固定。Projection file 可以内嵌，也可以通过第 19.2 节已索引 read-only root rule 解析。

```text
packet-index.json
target-to-evidence-doc.diff
implementation-identity.json
post-run-documentation-identity.json
changed-path-generated-asset-gates.json
smoke/run-projection.json
calibration/manifest.json
calibration/summary.json
calibration/run-01.json
calibration/run-02.json
calibration/run-03.json
calibration/exact-counter-set.json
synthetic/manifest.json
synthetic/summary.json
synthetic/run-01.json
synthetic/run-02.json
synthetic/run-03.json
synthetic/run-04.json
synthetic/run-05.json
review-safe-summary.md
```

- `target-to-evidence-doc.diff` is the exact byte diff from the revalidated target to the post-run documentation state, not a summary or path-only list. / `target-to-evidence-doc.diff` 是从重新核验 target 到 post-run documentation state 的精确 byte diff，不是 summary 或只有 path 的 list。
- `implementation-identity.json` binds live target, candidate commit, ancestry, target-to-candidate diff hash, implementation tree, fixture/runtime/tool versions, generated `public/assets/app.js` raw length/SHA-256, and clean capture. / `implementation-identity.json` 绑定实时 target、candidate commit、ancestry、target-to-candidate diff hash、implementation tree、fixture／runtime／tool version、生成 `public/assets/app.js` raw length／SHA-256 与干净 capture。
- `post-run-documentation-identity.json` binds the evidence-doc commit/tree or content hashes after results are recorded and proves executable inputs did not change after candidate freeze. / `post-run-documentation-identity.json` 绑定记录结果后的 evidence-doc commit／tree 或 content hash，并证明 candidate freeze 后 executable input 未变化。
- `changed-path-generated-asset-gates.json` records the exact section 12 path comparison, target-to-candidate server-domain path/function audit, locked-fixture private-runner compatibility verdicts, normal-build command/result, generated byte-equality/currentness result, source-map/extra-asset absence, `git diff --check`, and full gate results with actual totals and executor/reviewer attribution. / `changed-path-generated-asset-gates.json` 记录针对第 12 节的精确 path comparison、target-to-candidate server-domain path／function audit、locked-fixture private-runner compatibility verdict、正常 build command／result、generated 字节相等／currentness result、无 source map／额外 asset、`git diff --check`，以及含实际 total 与 executor／reviewer 归属的完整 gate result。
- The smoke and eight run projections contain only the sanitized raw fields required to revalidate identity, PID/process isolation, request/counter records, hard distinct values, functional/causal verdicts, and observational numeric samples. They are not pre-aggregated summaries. / Smoke 与八个 run projection 只包含重新校验 identity、PID／process isolation、request／counter record、hard distinct value、functional／causal verdict 与 observational numeric sample 所需的 sanitized raw field。它们不是预先 aggregate 的 summary。
- `calibration/exact-counter-set.json` is a separately written, timestamped and identity-bound frozen decision produced after the three calibration projections and before any synthetic five-run result is read. It must not be embedded only inside either summary. / `calibration/exact-counter-set.json` 是在三个 calibration projection 之后、读取任何 synthetic 五次 run 结果之前单独写入、带 timestamp 并绑定 identity 的 frozen decision。它不得只嵌在任一 summary 内。
- `review-safe-summary.md` is bilingual English/Chinese and reports only recomputed aggregate structural/functional conclusions, descriptive timing, invalid history, public Wave 0 historical identity, reviewer attribution, and unresolved findings. / `review-safe-summary.md` 必须为英文／中文双语，并且只报告重算后的 aggregate structural／functional 结论、描述性 timing、invalid history、公开 Wave 0 历史 identity、reviewer 归属与未解决 finding。

### 19.2 `packet-index.json` binding and packet hash / `packet-index.json` 绑定与 packet hash

`packet-index.json` uses schema/algorithm ID `session-analyzer-wave-1a-review-packet-v1`. For every payload other than the index itself, it records: canonical slash-separated relative filename; storage location (`packet` or `SANITIZED_RUN_ROOT`); raw byte length; lowercase raw SHA-256; payload schema ID/version; and the producing/verification algorithm version. Entries are sorted by canonical filename and duplicate/case-colliding names fail closed. / `packet-index.json` 使用 schema／algorithm ID `session-analyzer-wave-1a-review-packet-v1`。对 index 自身之外的每个 payload，它记录：使用 slash 的 canonical relative filename；存储位置（`packet` 或 `SANITIZED_RUN_ROOT`）；raw byte length；小写 raw SHA-256；payload schema ID／version；producing／verification algorithm version。Entry 按 canonical filename 排序；duplicate／大小写冲突 name 必须 fail closed。

`reviewPacketHash` is SHA-256 over UTF-8 bytes of one record line per sorted entry, with fields `filename`, decimal byte length, raw SHA-256, payload schema/version, algorithm version, and storage location joined by NUL (`\0`) and each record terminated by LF (`\n`). The index declares this algorithm verbatim and records the resulting lowercase digest. Recomputing every payload hash and `reviewPacketHash` is the first reviewer gate. / `reviewPacketHash` 是对每个已排序 entry 的一行 record 所做的 SHA-256：字段依次为 `filename`、十进制 byte length、raw SHA-256、payload schema／version、algorithm version 与 storage location，字段以 NUL（`\0`）连接，每条 record 以 LF（`\n`）结束，再对全部 UTF-8 byte 求 hash。Index 必须逐字声明此 algorithm 并记录所得小写 digest。重算每个 payload hash 与 `reviewPacketHash` 是第一项 reviewer gate。

Required projections may live inside the packet or under one separately declared read-only `SANITIZED_RUN_ROOT`. If the latter is used, the index must declare external-read-only mode and bind every root payload by the same canonical relative filename/length/hash/schema record; the reviewer receives read-only access to that exact root. Moving projections outside the packet does not permit an unindexed file or mutable aggregate. / 必需 projection 可以位于 packet 内，或位于一个单独声明的 read-only `SANITIZED_RUN_ROOT` 下。若使用后者，index 必须声明 external-read-only mode，并用相同 canonical relative filename／length／hash／schema record 绑定 root 中每个 payload；reviewer 获得该精确 root 的只读访问。把 projection 放到 packet 外不允许出现未索引文件或可变 aggregate。

### 19.3 Independent recomputation contract / 独立重算 contract

Using only the indexed packet payloads and, if declared, read-only `SANITIZED_RUN_ROOT`, the reviewer must independently recompute and compare with both summaries: / Reviewer 必须只使用已索引 packet payload，以及已声明时的 read-only `SANITIZED_RUN_ROOT`，独立重算并与两个 summary 比较：

1. smoke/calibration/synthetic PID isolation, distinct-process counts, run ordinals, candidate/tree/fixture/runtime identity, and manifest membership; / smoke／calibration／synthetic PID isolation、distinct process count、run ordinal、candidate／tree／fixture／runtime identity 与 manifest membership；
2. every hard distinct value and per-run functional/causal pass count, including invalid/failed attempt preservation; / 每个 hard distinct value 与逐 run functional／causal pass count，包括 invalid／failed attempt 保留；
3. the three-run eligible counter observations and the exact frozen `exactCounterSet`, plus proof that its separate record predates five-run result consumption; / 三次 run 的 eligible counter observation 与精确 frozen `exactCounterSet`，以及其独立 record 早于五次 run result consumption 的证明；
4. automatic Session/Project suggestion request counts, absence of the post-settlement duplicate, request parameter normalization, and stale/fallback verdicts; / 自动 Session／Project suggestion request count、无 settle 后 duplicate、request parameter normalization 与 stale／fallback verdict；
5. committed array/Map size, unique-ID/object-identity parity, committed-context binding, stale-publish absence, zero array scans, enclosing card-to-Map-get equality, and Map backend verdicts; / committed array／Map size、unique ID／object identity parity、committed-context binding、无 stale publish、零 array scan、enclosing card 与 Map get 相等以及 Map backend verdict；
6. numeric repeat count, median, minimum, and maximum for timing, Long Tasks, Resource Timing, and every observational numeric work field using the declared Wave 0 aggregation algorithm. / 使用已声明 Wave 0 aggregation algorithm，重算 timing、Long Task、Resource Timing 与每个 observational numeric work field 的 repeat count、median、minimum 与 maximum。

Executor-only aggregates are insufficient. A missing projection, non-recomputable field, mutable root, summary mismatch, or packet hash mismatch fails review; it cannot be waived by prose. / 只有 executor aggregate 不充分。缺少 projection、field 不可重算、root 可变、summary mismatch 或 packet hash mismatch 都会使 review 失败；不能用文字说明豁免。

### 19.4 Wave 0 private reference boundary / Wave 0 private reference 边界

The packet names only the public accepted Wave 0 candidate/tree/packet identity from the completed Wave 0 plan and the historical-reuse rationale from section 11.4. It must contain no Wave 0 private artifact, private projection, path, digest not already public, or relabeled private result. M5's target-to-candidate server-path proof and locked-fixture private-runner compatibility results are indexed Wave 1A gate records, not copies of Wave 0 evidence. / Packet 只命名 completed Wave 0 plan 中公开 accepted Wave 0 candidate／tree／packet identity，以及第 11.4 节的历史复用理由。它不得包含 Wave 0 private artifact、private projection、path、尚未公开的 digest 或重标 private result。M5 的 target-to-candidate server-path 证明与 locked-fixture private-runner compatibility result 是已索引的 Wave 1A gate record，不是 Wave 0 evidence 副本。

### 19.5 Review actions and closeout / Review action 与收口

The independent reviewer must verify, without relying only on executor summaries: / 独立 reviewer 必须核验以下内容，不能只依赖 executor summary：

1. the complete packet contract and independent recomputation above; exact live target, candidate, ancestry, implementation/runtime/documentation identities, clean capture, and exact diff; / 上述完整 packet contract 与独立重算；精确 live target、candidate、ancestry、implementation／runtime／documentation identity、干净 capture 与精确 diff；
2. full source functions and complete changed files for suggestion handoff/guards, committed timeline pair, canonical lookup, enclosing-affordance iteration, instrumentation, and browser-only runner aggregation; / suggestion handoff／guard、committed timeline pair、canonical lookup、enclosing-affordance iteration、instrumentation 与 browser-only runner aggregation 的完整 source function／changed file；
3. exact changed-path/generated-output proof and the M5 no-server-change plus unchanged-private-runner proof; / 精确 changed-path／generated-output 证明，以及 M5 的无 server change 与 private runner 不变证明；
4. focused/full test logs with actual totals, executor/reviewer attribution, exact runtime/browser versions, build/package/release/currentness results, and `git diff --check`; / 含实际 total、executor／reviewer 归属、精确 runtime／browser version、build／package／release／currentness result 与 `git diff --check` 的聚焦／完整 test log；
5. bilingual semantic alignment, active/completed/`AGENTS.md` lifecycle state, risk/rollback review, public-only Wave 0 reference, and every mandatory-stop resolution. / 双语语义对齐、active／completed／`AGENTS.md` 生命周期状态、风险／回滚评审、只公开引用 Wave 0，以及每个 mandatory stop 的处理。

The reviewer independently reruns at least syntax, the new state/operation Node tests, profiler/runner focused tests including private-compatibility fixtures, a focused real-browser request/Map/pending-replacement test set, generated currentness, diff/path checks, packet validation, and evidence-summary recomputation. Broader full-suite/package/release results may remain executor-run only if clearly labeled and accepted; any reviewer doubt requires rerun rather than assumption. / Reviewer 独立重跑至少 syntax、新 state／operation Node test、包含 private compatibility fixture 的 profiler／runner 聚焦 test、聚焦真实 browser request／Map／pending replacement test set、generated currentness、diff／path check、packet validation 与 evidence summary 重算。更广 full-suite／package／release result 可以仅由 executor 运行，但必须明确标注并被接受；reviewer 有任何疑问时必须重跑，不得假设。

Closeout is documentation-only and cannot repair or reinterpret failed executable evidence. If independent review finds an executable or packet-source defect, return to a new candidate or evidence group as identity rules require and repeat M5–M7. If it finds only bilingual/log/link issues, correct them, recompute post-run documentation identity and packet bindings/checks, and keep the accepted implementation/run identity unchanged. / Closeout 只能是 documentation-only，不能修补或重新解释失败的 executable evidence。若独立评审发现 executable 或 packet-source defect，必须按 identity rule 返回新 candidate 或 evidence group 并重跑 M5 至 M7。若只发现双语／log／link 问题，则修正后重算 post-run documentation identity 与 packet binding／check，同时保持已接受 implementation／run identity 不变。
