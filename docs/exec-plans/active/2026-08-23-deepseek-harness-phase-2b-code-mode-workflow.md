# DeepSeek Harness Phase 2B — Code Mode and tool-workflow structured support / DeepSeek Harness 第二阶段 B —— Code Mode 与 tool-workflow 结构化支持

## Status / 状态

- Status: active; M0 inventory and focused upstream audit complete; the Phase 2A `session/end-seed` correction passed all local gates and corrected remote CI, and Phase 2A is archived. Phase 2B production work is unblocked but has not started in this correction pass. / 状态：活跃；M0 语料清点与聚焦上游审计已完成；Phase 2A `session/end-seed` 修正已通过全部本地门禁与修正版远程 CI，且 Phase 2A 已归档。Phase 2B 生产工作已解除阻断，但本次修正流程尚未启动它。
- Branch: `support-dsh` / 分支：`support-dsh`
- Repository baseline: `c58a52b7804921d3aed680f8f2e8da9f0d105bdf` (clean; `origin/support-dsh` at the same commit). / 仓库基线：`c58a52b7804921d3aed680f8f2e8da9f0d105bdf`（干净；`origin/support-dsh` 指向同一提交）。
- Expected historical starting point: `f59dbf5f5549a03f1965c83ce3f183dd33a58baf`; the actual baseline legitimately contains the later cold-materialization, bounded-prewarm, and weighted-cache integration ending in merge `c58a52b`. No branch operation was performed. / 预期历史起点：`f59dbf5f5549a03f1965c83ce3f183dd33a58baf`；实际基线已合法包含后续 cold materialization、bounded prewarm 与 weighted cache 集成，并结束于 merge `c58a52b`。未执行任何分支操作。
- Phase 2A historical accepted head `f59dbf5f5549a03f1965c83ce3f183dd33a58baf` passed GitHub Actions run `32340719998` but was semantically wrong for resume markers. Corrected head `08e078ecb3bb2cfb0b829af42d121c61c0eee1a1` passed all local gates, six-artifact replay, and GitHub Actions run `32628383363`; Phase 2A is archived at `docs/exec-plans/completed/2026-08-20-deepseek-harness-phase-2a-session-semantics.md`. / Phase 2A 历史验收 head `f59dbf5f5549a03f1965c83ce3f183dd33a58baf` 通过 GitHub Actions run `32340719998`，但其 resume marker 语义错误。修正版 head `08e078ecb3bb2cfb0b829af42d121c61c0eee1a1` 通过全部本地门禁、六工件重放与 GitHub Actions run `32628383363`；Phase 2A 已归档至 `docs/exec-plans/completed/2026-08-20-deepseek-harness-phase-2a-session-semantics.md`。

## Objective / 目标

Add source-owned structured support for durable DeepSeek Harness Code Mode dispatch and, where evidence permits, `tool-workflow/*`, while preserving the accepted source-neutral Indexed／Materialized, Raw Reference, query parity, and Detail purpose × responsibility contracts. Code Mode correlation must come from DeepSeek's durable IDs rather than the Codex JavaScript inference parser. Workflow must not be treated as Code Mode or attached to a tool operation without sufficient source identity. / 在保持已接受的来源中立 Indexed／Materialized、Raw Reference、查询等价及 Detail purpose × responsibility 契约的前提下，为 DeepSeek Harness 持久化 Code Mode dispatch 增加来源自有的结构化支持，并在证据充分时支持 `tool-workflow/*`。Code Mode 关联必须来自 DeepSeek 持久化 ID，而不是 Codex JavaScript 推断 parser。Workflow 不得被当作 Code Mode，也不得在缺少充分来源 identity 时附着到某个工具操作。

## Evidence precedence / 证据优先级

1. Accepted Session Analyzer source-neutral contracts, designs, source, and committed tests remain the application specification. / 已接受的 Session Analyzer 来源中立契约、设计、源码与已提交测试继续作为应用规格。
2. Current DeepSeek Harness source defines current intended semantics. / 当前 DeepSeek Harness 源码定义当前预期语义。
3. The copied read-only real corpus defines only what those six artifacts actually persisted. / 复制的只读真实语料只定义这六个工件实际持久化了什么。
4. Historical `0.1.0-rc.6` writer fixtures define their exact bytes and retain their historical provenance. / 历史 `0.1.0-rc.6` writer fixture 定义其精确字节并保留历史 provenance。
5. Codex is a mature presentation reference, not a source-correlation authority for DSH. Claude remains a secondary source-neutral pressure reference. / Codex 是成熟呈现参考，而不是 DSH 来源关联权威；Claude 继续作为次级来源中立压力参考。

## M0 real-corpus inventory / M0 真实语料清点

The read-only structural report is `tmp/dsh-structural-inventory.json`; its generator and report are Git-ignored and must remain uncommitted. All six artifacts use the JSONL backend as independently framed `session.jsonl.zstd`, header format version `0`, complete untorn committed prefixes, and one physical storage row per Raw Record. None records a writer package version, so package identity cannot be inferred beyond format/vocabulary compatibility. / 只读结构报告位于 `tmp/dsh-structural-inventory.json`；生成器与报告均被 Git 忽略，必须保持未提交。六个工件都使用 JSONL backend 的独立分帧 `session.jsonl.zstd`，header format version 为 `0`，committed prefix 完整且无撕裂，并保持一个物理存储行对应一条 Raw Record。它们都没有记录 writer package version，因此只能判断 format／vocabulary 兼容，不能推断精确 package identity。

### Per-Session physical and semantic facts / 逐会话物理与语义事实

1. `tmp/dsh-real-sessions/--home-joejack-dsh_playground--/session-82b92006-083e-4c81-8a2d-8c79de28e927/session.jsonl.zstd`
   - Coarse provenance: playground Standard example; header/effective preset `standard` → `standard` after four selections (`minimal`, `minimal`, `minimal`, `standard`). / 粗粒度 provenance：playground Standard 示例；header／effective preset 为 `standard` → `standard`，期间四次选择依次为 `minimal`、`minimal`、`minimal`、`standard`。
   - Lineage: no `parentSession`, `origin`, or `seedLength`; `delegationDepth:0`; no `session/end-seed`. / Lineage：无 `parentSession`、`origin` 或 `seedLength`；`delegationDepth:0`；无 `session/end-seed`。
   - Physical: 7,185 records = 2,445 ordinary event rows + 4,739 packed rows + header. Packed rows: reasoning 2,721 rows／38,194 members; text 104／1,516; tool-call 1,914／37,147. / 物理：7,185 条记录＝2,445 条普通事件＋4,739 条 packed row＋header。Packed row：reasoning 2,721／38,194 members；text 104／1,516；tool-call 1,914／37,147。
   - Event histogram: `agent-preset/selected:4`, `agent/inbox/spliced:18`, `approval/policy:1`, `assistant/chunk:1501`, `assistant/message:167`, `goal/change:2`, `permission/preset:1`, `request/context:1`, `request/header:1`, `sandbox/mode:1`, `session/title:1`, `step/start:167`, `step/end:167`, `todo/write:3`, `tool/call:199`, `tool/result:199`, `turn/start:1`, `turn/end:1`, `user/message:10`. / 事件直方图如上。
   - Main tool calls: `bash:157`, `edit:10`, `read:12`, `write:7`, `job_output:7`, `job_list:1`, `create_goal:1`, `update_goal:1`, `todo_write:3`; all 199 have exact result pairs and no recorded result error. / Main 工具调用如上；199 次全部有精确结果配对，未记录结果错误。
   - No Code dispatch, workflow, compaction, descriptor, unknown type, interruption, open turn/step, unmatched tool call, or partial assistant step. / 无 Code dispatch、workflow、compaction、descriptor、未知类型、中断、开放 turn／step、未配对工具调用或 partial assistant step。

2. `tmp/dsh-real-sessions/--home-joejack-dsh_playground--/session-f39dabea-f4a9-477d-afbc-c63d0357df76/session.jsonl.zstd`
   - Coarse provenance: playground PTC／Code example; header/effective preset `standard` → `code`. / 粗粒度 provenance：playground PTC／Code 示例；header／effective preset 为 `standard` → `code`。
   - Lineage: no `parentSession`, `origin`, or header `seedLength`; `delegationDepth:0`; one trailing `session/end-seed` at seq 57,035 after `turn/end`. / Lineage：无 `parentSession`、`origin` 或 header `seedLength`；`delegationDepth:0`；seq 57,035 在 `turn/end` 后有一条尾随 `session/end-seed`。
   - Physical: 3,344 records = 480 ordinary + 2,863 packed + header. Packed rows: reasoning 2,370／43,371; text 138／2,718; tool-call 355／10,467. / 物理：3,344＝480＋2,863＋header；packed 统计如上。
   - Event histogram: `agent-preset/selected:1`, `agent/inbox/spliced:2`, `approval/policy:1`, `assistant/chunk:331`, `assistant/message:13`, `permission/preset:1`, `request/context:1`, `request/header:1`, `sandbox/mode:1`, `session/end-seed:1`, `session/title:2`, `session/title-llm-request:1`, `step/start:13`, `step/end:13`, `tool/call:12`, `tool/code-dispatch-start:35`, `tool/code-dispatch:35`, `tool/result:12`, `turn/start:1`, `turn/end:1`, `user/message:2`. / 事件直方图如上。
   - Main tool calls: `run_code:12`, all exactly paired and successful. Code dispatch topology is detailed below. / Main 工具调用：`run_code:12`，全部精确配对且成功；Code dispatch 拓扑见下文。
   - No workflow, compaction, descriptor, unknown type, interruption, open turn/step, unmatched tool call, or partial assistant step. / 无 workflow、compaction、descriptor、未知类型、中断、开放 turn／step、未配对工具调用或 partial assistant step。

3. `tmp/dsh-real-sessions/--home-joejack-session-analyzer--/session-244d9853-e5c8-4321-8c79-c4f043ca9976/session.jsonl.zstd`
   - Coarse provenance: real Minimal development; preset `standard` → `minimal`; no parent/origin/header seed; depth 0; one trailing end-seed marker at seq 121,206. / 粗粒度 provenance：真实 Minimal 开发；preset `standard` → `minimal`；无 parent／origin／header seed；depth 0；seq 121,206 有一条尾随 end-seed marker。
   - Physical: 11,050 = 4,032 ordinary + 7,017 packed + header. Packed reasoning 3,906／47,710; text 110／1,748; tool-call 3,001／67,717. / 物理与 packed 统计如上。
   - Event histogram: `agent-preset/selected:1`, `agent/inbox/spliced:4`, `approval/policy:1`, `assistant/chunk:2568`, `assistant/message:288`, `llm/retry-started:5`, `llm/retry:5`, `permission/preset:1`, `request/context:1`, `request/header:1`, `sandbox/mode:1`, `session/end-seed:1`, `session/title:1`, `step/start:288`, `step/end:288`, `tool/call:287`, `tool/result:287`, `turn/start:1`, `turn/end:1`, `user/message:2`. / 事件直方图如上。
   - Main tools: `bash:246`, `str_replace_editor:41`; all paired, with seven editor error results. / Main 工具：`bash:246`、`str_replace_editor:41`；全部配对，其中七次 editor 结果为错误。
   - No Code dispatch, workflow, compaction, descriptor, unknown type, interruption, or open lifecycle. / 无 Code dispatch、workflow、compaction、descriptor、未知类型、中断或开放 lifecycle。

4. `tmp/dsh-real-sessions/--home-joejack-session-analyzer--/session-8d76ca1e-b682-4f8a-bb4d-b50a4e1a819d/session.jsonl.zstd`
   - Real Minimal development; preset `standard` → `minimal`; no parent/origin/header seed; depth 0; trailing end-seed at seq 148,218. / 真实 Minimal 开发；preset `standard` → `minimal`；无 parent／origin／header seed；depth 0；seq 148,218 有尾随 end-seed。
   - Physical: 11,757 = 4,023 ordinary + 7,733 packed + header. Packed reasoning 4,054／55,531; text 130／2,364; tool-call 3,549／86,301. / 物理与 packed 统计如上。
   - Event histogram: `agent-preset/selected:1`, `agent/inbox/spliced:2`, `approval/policy:1`, `assistant/chunk:2584`, `assistant/message:270`, `permission/preset:1`, `request/context:1`, `request/header:1`, `sandbox/mode:1`, `session/end-seed:1`, `session/title:1`, `step/start:270`, `step/end:270`, `tool/call:308`, `tool/result:308`, `turn/start:1`, `turn/end:1`, `user/message:1`. / 事件直方图如上。
   - Main tools: `bash:308`, all paired and successful. No Code/workflow/compaction/descriptor/unknown/interruption/open lifecycle. / Main 工具：`bash:308`，全部配对成功；无 Code／workflow／compaction／descriptor／未知／中断／开放 lifecycle。

5. `tmp/dsh-real-sessions/--home-joejack-session-analyzer--/session-97c229e1-2be3-49ee-ad4a-64562d17ebcd/session.jsonl.zstd`
   - Real Minimal development; preset `standard` → `minimal`; no parent/origin/header seed; depth 0. Two end-seed markers occur at seq 18,795 and 20,244; the first is followed by further durable activity and the second trails the final `turn/end`. / 真实 Minimal 开发；preset `standard` → `minimal`；无 parent／origin／header seed；depth 0。seq 18,795 与 20,244 有两条 end-seed marker；第一条之后仍有持久活动，第二条尾随最终 `turn/end`。
   - Physical: 1,693 = 912 ordinary + 780 packed + header. Packed reasoning 445／8,814; text 23／725; tool-call 312／9,794. / 物理与 packed 统计如上。
   - Event histogram: `agent-preset/selected:1`, `agent/inbox/spliced:4`, `approval/policy:1`, `assistant/chunk:537`, `assistant/message:71`, `permission/preset:1`, `request/context:1`, `request/header:2`, `sandbox/mode:1`, `session/end-seed:2`, `session/title:2`, `session/title-llm-request:1`, `step/start:71`, `step/end:71`, `tool/call:70`, `tool/result:70`, `turn/start:2`, `turn/end:2`, `user/message:2`. / 事件直方图如上。
   - Main tools: `bash:70`, all paired; one error result. Turn endings: one `aborted`, one `completed`; no open turn/step/call or partial assistant step. / Main 工具：`bash:70`，全部配对，其中一次错误；turn 结束为一次 `aborted`、一次 `completed`；无开放 turn／step／call 或 partial assistant step。
   - No Code/workflow/compaction/descriptor/unknown. / 无 Code／workflow／compaction／descriptor／未知类型。

6. `tmp/dsh-real-sessions/--home-joejack-session-analyzer--/session-bf2d4e08-a4a1-40ab-86f7-414b944140cf/session.jsonl.zstd`
   - Real Minimal development; preset `standard` → `minimal`; no parent/origin/header seed; depth 0; trailing end-seed at seq 92,083. / 真实 Minimal 开发；preset `standard` → `minimal`；无 parent／origin／header seed；depth 0；seq 92,083 有尾随 end-seed。
   - Physical: 7,826 = 2,819 ordinary + 5,006 packed + header. Packed reasoning 3,116／46,096; text 217／4,474; tool-call 1,673／38,695. / 物理与 packed 统计如上。
   - Event histogram: `agent-preset/selected:1`, `agent/inbox/spliced:8`, `approval/policy:1`, `assistant/chunk:1802`, `assistant/message:197`, `permission/preset:1`, `request/context:1`, `request/header:1`, `sandbox/mode:1`, `session/end-seed:1`, `session/title:1`, `step/start:197`, `step/end:197`, `tool/call:199`, `tool/result:199`, `turn/start:4`, `turn/end:4`, `user/message:4`. / 事件直方图如上。
   - Main tools: `bash:199`, all paired and successful. Four completed turns; no Code/workflow/compaction/descriptor/unknown/open lifecycle. / Main 工具：`bash:199`，全部配对成功；四个完成 turn；无 Code／workflow／compaction／descriptor／未知／开放 lifecycle。

### Real PTC Code Mode topology / 真实 PTC Code Mode 拓扑

- All 70 dispatch rows belong to 12 exact outer `run_code` calls and form 35 subcall nodes. Every outer has one exact `tool/call` and one later exact `tool/result`. / 70 条 dispatch row 全部属于 12 个精确外层 `run_code` call，并形成 35 个 subcall node；每个外层都有一条精确 `tool/call` 与一条稍后的精确 `tool/result`。
- For every observed node, `rootCallId === parentCallId === outer callId`; `subCallId` is `${parentCallId}:code:<n>`; depth is exactly 1. / 每个已观察 node 都满足 `rootCallId === parentCallId === outer callId`；`subCallId` 为 `${parentCallId}:code:<n>`；depth 恰为 1。
- Every node has exactly one start and one settled row, identical dispatch name on both sides, start before settle, `isError:false`, and settlement before the owning outer result. Every outer result is present, successful, and aligned after its dispatch tree. / 每个 node 都恰有一条 start 与一条 settled row，两侧 dispatch name 相同，start 先于 settle，`isError:false`，且 settlement 位于所属 outer result 之前；每个 outer result 都存在、成功并与 dispatch tree 对齐。
- The 12 outer operations contain respectively 3, 2, 4, 2, 1, 1, 1, 4, 3, 6, 7, and 1 subcalls. Dispatch names observed are `bash`, `grep`, `read`, and `write`. Four outer operations show parallel-style physical interleaving where a later subcall starts before an earlier subcall settles. / 12 个 outer operation 分别包含 3、2、4、2、1、1、1、4、3、6、7、1 个 subcall；观察到的 dispatch name 为 `bash`、`grep`、`read`、`write`。其中四个 outer operation 呈现并行式物理交错：后一个 subcall 在较早 subcall settle 前开始。
- No real dispatch error, start-only incomplete node, settle-only historical node, depth >1, failed outer result, or missing outer result occurs in this corpus. These remain source-backed or synthetic-test cases, not real-corpus claims. / 本语料没有真实 dispatch error、仅 start 的 incomplete node、仅 settle 的历史 node、depth >1、失败 outer result 或缺失 outer result；这些仍属于源码支持或合成测试 case，不能声称已被真实语料验证。

## Playground versus real Minimal coverage / Playground 与真实 Minimal 覆盖比较

- Playground-only families: `goal/change:2`, `todo/write:3`, `tool/code-dispatch-start:35`, and `tool/code-dispatch:35`. Code dispatch is confined to the PTC example in this corpus. / 仅 playground 出现：`goal/change:2`、`todo/write:3`、`tool/code-dispatch-start:35`、`tool/code-dispatch:35`；在本语料中 Code dispatch 仅限 PTC 示例。
- Minimal-only families: `llm/retry-started:5` and `llm/retry:5`, both in one long development Session. / 仅 Minimal 出现：`llm/retry-started:5` 与 `llm/retry:5`，都位于一个长开发会话。
- Real Minimal work heavily exercises packed rows, `tool/call`／`tool/result` pairing (864 pairs), assistant streaming/finalization, 826 steps, eight turns, one aborted turn, `agent/inbox/spliced:18`, and repeated resume markers. The deferred retry/inbox families should be prioritized later, but remain Phase 2B non-goals. / 真实 Minimal 工作大量覆盖 packed row、`tool/call`／`tool/result` 配对（864 对）、assistant streaming／finalization、826 个 step、八个 turn、一个 aborted turn、`agent/inbox/spliced:18` 与重复 resume marker。推迟的 retry／inbox family 应在后续优先处理，但继续作为 Phase 2B 非目标。
- `tool-workflow/*`, all compaction events, `subagent/descriptor`, and unknown event types occur in 0/6 copied Sessions. This is negative evidence for this corpus, not a claim that DSH never writes them. / `tool-workflow/*`、所有 compaction event、`subagent/descriptor` 与未知事件类型在六份复制会话中均为 0；这是本语料的负面证据，不是“DSH 从不写入”的结论。
- The long Sessions validate Phase 1's packed physical-row and exact tool-pairing assumptions, and expose no new storage format. They contradict Phase 2A's marker-only seed-ownership assumption as recorded in the blocker below. / 长会话验证了 Phase 1 的 packed 物理 row 与精确工具配对假设，没有暴露新存储格式；但它们反驳了 Phase 2A 仅凭 marker 推断 seed ownership 的假设，详见下述 blocker。

## Focused current-upstream audit / 聚焦当前上游审计

### Checkout and package relationship / Checkout 与 package 关系

- Read-only checkout: `tmp/deepseek-harness-current` (Git-ignored). / 只读 checkout：`tmp/deepseek-harness-current`（Git 忽略）。
- HEAD: `b150a551b8d465e31e418e1b2eaf5e79bbb7d28e`, branch `master`, clean, tracking `origin/master`. / HEAD、branch 与 clean 状态如上。
- HEAD is the merge `Merge pull request #2908 from deepseek-harness/release/dsh-0.1.1-rc.2`, dated 2026-08-21. Root and `apps/cli` versions are both `0.1.1-rc.2`. The npm registry reports `@deepseek-ai/dsh` `latest` and `next` as `0.1.1-rc.2`; no registry `gitHead` was returned, so byte identity between npm and this Git commit is not claimed. / HEAD 是 2026-08-21 的 release merge；root 与 `apps/cli` 均为 `0.1.1-rc.2`。npm registry 的 `latest` 与 `next` 都指向 `0.1.1-rc.2`；registry 未返回 `gitHead`，因此不声称 npm 与该 Git commit 逐字节相同。
- Historical reference `47f943859bef60e4160492346772ded9b24f765a` remains immutable provenance for Phase 1／2A and is an ancestor of current HEAD. / 历史参考 `47f943859bef60e4160492346772ded9b24f765a` 继续作为 Phase 1／2A 的不可变 provenance，并且是当前 HEAD 的祖先。

### Code Mode delta / Code Mode 差异

- `CodeDispatchStartEventData`, `CodeDispatchEventData`, the durable event declarations, dispatch ancestry invariant, and client `ToolCallTree` are unchanged between the historical reference and current HEAD. / `CodeDispatchStartEventData`、`CodeDispatchEventData`、持久事件声明、dispatch ancestry invariant 与 client `ToolCallTree` 在历史参考与当前 HEAD 之间未变化。
- The only relevant `code-mode.ts` source delta adds deferred model context for successful image-bearing nested results and adjusts model-facing wording. It does not change `rootCallId`, `parentCallId`, `subCallId`, name/arguments, start/settled ordering, or `content + isError` representation. / 唯一相关的 `code-mode.ts` 源码差异是为成功且含图片的 nested result 增加延迟 model context，并调整模型可见文案；未改变三类 ID、name／arguments、start／settled 顺序或 `content + isError` 表示。
- Current `run_code` appends start only when the scheduler actually enters a queued call; an abandoned queued call writes nothing. Subcall IDs are deterministic submission-order `${outerCallId}:code:<n>`. The start and settle rows repeat the same durable IDs/name/JSON-normalized arguments. Settle adds `isError` and rendered `content`. The run drains in-flight dispatch/log work before the outer result returns, so complete writer output encloses settlements before outer `tool/result`; a committed-prefix crash may still expose a start-only incomplete node. / 当前 `run_code` 只有在 scheduler 实际进入 queued call 时才追加 start；被放弃的 queued call 不写任何内容。Subcall ID 按提交顺序确定性生成。Start／settle 重复相同持久 ID、name 与 JSON-normalized arguments；settle 增加 `isError` 和 rendered `content`。Run 会在 outer result 返回前 drain in-flight dispatch／log work，因此完整 writer 输出把 settlement 包在 outer `tool/result` 之前；committed-prefix crash 仍可能只暴露 start-only incomplete node。
- `rootCallId` is the root execution identity and `parentCallId` is the immediate containing execution. The invariant accepts an edge only when a non-root parent already belongs to the same root. Current SDK assembly excludes `run_code` from its own bindings, and the current client comments describe emitted bindings as one-level while safely supporting recursive trees up to a fixed depth. Real corpus depth is only 1; Phase 2B must not claim deeper writer validation. / `rootCallId` 是根 execution identity，`parentCallId` 是直接包含 execution；invariant 仅在非 root parent 已属于同一 root 时接纳 edge。当前 SDK assembly 会从自身 binding 中排除 `run_code`，current client 注释把当前输出描述为单层，同时以固定上限安全支持递归 tree。真实语料 depth 只有 1；Phase 2B 不得声称更深 writer 验证。
- The current DSH client projects dispatches as recursive subcalls of the existing outer tool card. That is presentation evidence only; Session Analyzer retains its own accepted canonical event/Raw ownership rules. / 当前 DSH client 把 dispatch 投影为既有 outer tool card 的递归 subcall；这只是呈现证据，Session Analyzer 继续遵守自身已接受的 canonical event／Raw ownership 规则。

### Tool-workflow delta / Tool-workflow 差异

- `tool-workflow` types, recorder, invariant, and client workflow definition are unchanged between `47f9438…` and `b150a55…`. There is no major redesign. / `tool-workflow` types、recorder、invariant 与 client workflow definition 在两次提交之间未变化，不存在重大重设计。
- One run is keyed by non-empty `runId`. `run-start` adds a non-empty display `name`; `agent-start` adds positive unique member `seq`, label, optional phase, and exact child `SessionId`; `agent-end` pairs by `runId + seq` and uses `completed|failed|cancelled`; `run-end` pairs by `runId`, requires every member closed, and uses `completed|cancelled|error`. / 一个 run 由非空 `runId` 标识；四类事件的字段与终态集合如上。
- Only a top-level direct workflow tool execution (`exec.parent === undefined`) records this lifecycle. A workflow invoked as a nested Code Mode subcall and a direct `WorkflowEngine` consumer do not emit `tool-workflow/*`. / 只有顶层 direct workflow tool execution（`exec.parent === undefined`）记录该 lifecycle；作为 Code Mode nested subcall 调用的 workflow 与直接 `WorkflowEngine` consumer 不发出 `tool-workflow/*`。
- Workflow rows do not carry the enclosing tool `callId`. Current source and its keyless application snapshot show the lifecycle physically inside one direct workflow call/result interval, but parallel direct calls can make interval-based ownership ambiguous and the tool name is configurable. The current DSH client therefore builds an independent Chat node keyed by `runId` and leaves the existing workflow tool card unchanged. / Workflow row 不携带 enclosing tool `callId`。当前源码及其 keyless application snapshot 展示 lifecycle 物理上位于一个 direct workflow call／result 区间内，但并行 direct call 会使基于区间的 ownership 含糊，而且工具名可配置。当前 DSH client 因此按 `runId` 构建独立 Chat node，并保持既有 workflow tool card 不变。
- No copied real artifact contains this family. Current-source semantics and upstream synthetic snapshot evidence are sufficient to model its own run/member lifecycle, but not to claim real-session validation or exact ownership by another Logical Event. / 复制的真实工件都不含该 family。当前源码语义与上游合成 snapshot 证据足以建模 run／member 自身 lifecycle，但不足以声称真实会话验证或由另一个 Logical Event 精确拥有。

### Relevant persistence vocabulary / 相关持久 vocabulary

- The two Code Mode and four workflow types remain in the current generated known-event catalog and format version remains `0`. Current HEAD adds four unrelated `team/*` types; they are outside Phase 2B. / 两类 Code Mode 与四类 workflow type 继续存在于当前 generated known-event catalog，format version 仍为 `0`；当前 HEAD 新增四个无关 `team/*` type，超出 Phase 2B。
- No relevant source change requires a historical/current version distinction for these six event types. Fixtures and docs must still label historical `0.1.0-rc.6`, current `0.1.1-rc.2` source-only evidence, and real unknown-package format-v0 evidence separately. / 这六类事件没有相关源码变化要求区分 historical／current version；fixture 与文档仍须分别标注历史 `0.1.0-rc.6`、当前 `0.1.1-rc.2` source-only 证据，以及真实未知 package 的 format-v0 证据。

## Resolved post-acceptance contradiction / 已解决的验收后冲突

Historical and current upstream both define `Session.firstLiveSeq` as the constructor-seed length for replay, fork, **or resume**, and persist that in-process construction fact as the last `session/end-seed` marker. They separately define header `seedLength` as the durable fork-lineage boundary; a resumed Session's constructor seed is its full stored log while the header keeps the original fork value. / 历史与当前上游都把 `Session.firstLiveSeq` 定义为 replay、fork **或 resume** 的 constructor-seed 长度，并用最后一条 `session/end-seed` 持久化该进程内构造事实；同时单独把 header `seedLength` 定义为持久 fork-lineage 边界：恢复会话的 constructor seed 是完整已存日志，而 header 继续保留原始 fork 值。

The adapter accepted at `f59dbf5…` returned the last marker as an ownership boundary when header `seedLength` was absent and rejected any marker later than header `seedLength`. Both rules were invalid for resume. The 2026-08-23 adapter-local correction removes both rules; focused regressions, all applicable local gates, replay of all six real artifacts, and corrected remote CI are green. All five marker-bearing top-level Sessions remain ordinary and the repeated-marker Session retains six Main events between its markers. Phase 2A is archived and this prerequisite no longer blocks Phase 2B. / 在 `f59dbf5…` 验收的 adapter 会在 header `seedLength` 缺失时把最后一个 marker 作为 ownership boundary，并拒绝任何晚于 header `seedLength` 的 marker；两条规则对 resume 都不成立。2026-08-23 的 adapter 本地修正已移除这两条规则；聚焦回归、全部适用本地门禁、六份真实工件重放与修正版远程 CI 均通过。五个含 marker 的顶层会话全部保持普通会话，重复 marker 会话在 marker 之间保留六个 Main 事件。Phase 2A 已归档，该前置条件不再阻断 Phase 2B。

This must be corrected and protected by minimal synthetic ordinary-resume, repeated-resume, initial-fork, and resumed-fork fixtures before Phase 2B production code begins. The correction is adapter-local: inherited ownership comes only from valid header `seedLength`; `session/end-seed` remains Raw/Protocol construction-boundary evidence and can help close standalone lifecycle interpretation, but never independently proves parent ownership. No shared canonical or browser change is authorized. / 在 Phase 2B 生产代码开始前，必须用最小合成的 ordinary resume、repeated resume、initial fork 与 resumed fork fixture 修正并保护该行为。修正应保持 adapter 本地：继承 ownership 只来自有效 header `seedLength`；`session/end-seed` 继续作为 Raw／Protocol construction-boundary 证据，并可帮助解释独立 lifecycle closure，但永远不能独立证明 parent ownership。不得因此修改共享 canonical 或 browser。

## Phase 2B evidence and support decisions / Phase 2B 证据与支持决定

1. **Code Mode admission after the blocker is fixed.** Historical/current source and the real PTC artifact agree on the durable ID graph. Outer exact `run_code` call/result becomes one `code_mode_operation`; dispatch start/settled pairs become independently owned Observed Nested Activity Logical Events with their own Raw References and status. `eventRefs` records exact parent-child Logical relationships derived only from `rootCallId`／`parentCallId`／`subCallId`. Outer Raw References remain outer call/result only; nested Raw References and search text are never copied into the outer operation. / **阻断修复后准入 Code Mode。** 历史／当前源码与真实 PTC 工件对持久 ID 图一致；outer 精确 `run_code` call／result 成为一个 `code_mode_operation`，dispatch start／settled pair 成为独立 ownership、拥有自身 Raw Reference 与 status 的 Observed Nested Activity Logical Event；`eventRefs` 只根据三类持久 ID 记录精确 parent-child Logical 关系。Outer Raw Reference 只包含 outer call／result；绝不把 nested Raw Reference 或 search text 复制到 outer operation。
2. **No Codex inference parser.** DSH does not parse outer code to prove execution or declared requests. Its `presentationIndexes.codeModeDeclaredRequests` remains empty unless a future independently approved source fact exists. Codex is reused only for source-neutral `code_mode_operation`, detail hierarchy, observation/outcome distinction, event-reference navigation, and existing renderer vocabulary. / **不使用 Codex 推断 parser。** DSH 不解析 outer code 来证明执行或声明 request；除非未来有独立批准的来源事实，其 `presentationIndexes.codeModeDeclaredRequests` 保持为空。Codex 只作为来源中立 `code_mode_operation`、detail 层级、observation／outcome 区分、event-reference 导航及既有 renderer vocabulary 的参考。
3. **Code Mode evidence tiers.** Success pairing, physical interleaving, outer enclosure, and depth 1 are real-corpus validated. Nested `isError`, caught-error versus successful outer result, start-only incomplete, settle-only historical fallback, failed/incomplete outer, and depth >1 require current-source or sanitized synthetic coverage and must be labeled accordingly. / **Code Mode 证据分级。** 成功配对、物理交错、outer enclosure 与 depth 1 已由真实语料验证；nested `isError`、捕获错误但 outer 成功、仅 start incomplete、仅 settle 历史 fallback、失败／不完整 outer 与 depth >1 需要当前源码或脱敏合成覆盖，并须相应标注。
4. **Workflow starts as one grouped Protocol provenance event, not Main duplication.** The four rows may be grouped exactly by `runId`, with member pairing by `runId + seq`, child Session IDs and lifecycle outcomes in Structured Detail, and exact ordered Raw References. Because no durable call ID relates the run to the direct workflow tool call and no real artifact validates the family, Phase 2B must not merge it into or supplement a tool operation, nor emit a second Main operation beside the generic tool call. Promotion to Main or association with an owner requires separate real evidence or a future upstream identity field. / **Workflow 初始投影为单个 grouped Protocol provenance event，而不是 Main 重复。** 四类 row 可按 `runId` 精确归组，member 按 `runId + seq` 配对，Structured Detail 展示 child Session ID 与 lifecycle outcome，并保留精确有序 Raw Reference。由于没有持久 call ID 把 run 关联到 direct workflow tool call，也没有真实工件验证该 family，Phase 2B 不得把它合并进或补充到某个工具操作，也不得在 generic tool call 旁再发出第二个 Main operation。提升到 Main 或关联 owner 需要独立真实证据或未来上游 identity 字段。
5. **Workflow incompleteness is source observation, not repair.** Missing run/member endings in a committed prefix produce an incomplete grouped Protocol event; the analyzer does not synthesize rows. Contradictory/duplicate identities fail closed to separate Protocol/Raw fallback rather than manufacturing success. / **Workflow incomplete 属于来源 observation，而不是修复。** Committed prefix 缺少 run／member ending 时生成 incomplete grouped Protocol event；analyzer 不合成 row。矛盾／重复 identity 会保守回退为分离 Protocol／Raw，而不是制造 success。
6. **Version boundary.** The selected Code/workflow fields are compatible across historical reference and current HEAD under format v0; support both without a package-version branch. Preserve evidence labels and fail closed on future header format versions. / **版本边界。** 所选 Code／workflow 字段在 format v0 下兼容历史参考与当前 HEAD；无需 package-version branch 即可同时支持。继续保留证据标签，并对未来 header format version 保守失败。

## Architecture-pressure log / 架构压力记录

| Finding / 发现 | Evidence source / 证据来源 | Existing abstraction / 既有抽象 | Friction / 摩擦 | Adapter-local clean? / adapter 本地可解决？ | Shared change? / 共享修改？ | Severity / 严重性 | Disposition / 处置 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Marker-only resume was misclassified as inherited ownership / 仅 marker 的 resume 曾被误判为继承 ownership | Historical/current upstream + five real Sessions / 历史／当前上游＋五个真实会话 | Phase 2A seed segmentation / Phase 2A seed 分段 | Main history disappeared; resumed fork could be rejected / Main 历史消失；恢复 fork 可能被拒绝 | Yes: header `seedLength` only / 是：仅 header `seedLength` | No / 否 | Resolved / 已解决 | Regressions + six-artifact replay + full local/remote gates green; Phase 2A archived / 回归＋六工件重放＋完整本地／远程门禁通过；Phase 2A 已归档 |
| Explicit DSH dispatch graph versus Codex interval inference / DSH 显式 dispatch 图与 Codex 区间推断 | Current source + real PTC / 当前源码＋真实 PTC | `code_mode_operation` + `eventRefs` | Reusing Codex parser would replace stronger provenance / 复用 Codex parser 会替换更强 provenance | Yes / 是 | No / 否 | High design importance / 高设计重要性 | Use exact DSH IDs only / 只用 DSH 精确 ID |
| Outer operation and nested activity need disjoint Raw ownership / Outer operation 与 nested activity 需要不相交 Raw ownership | Real PTC + accepted Code Mode contract / 真实 PTC＋已接受 Code Mode 契约 | One Logical Event can have many Raw refs; `eventRefs` are relations / 一个 Logical Event 可含多 Raw ref；`eventRefs` 是关系 | Copying nested refs/text would double metrics/search/evidence / 复制 nested ref／text 会重复指标／搜索／证据 | Yes / 是 | No / 否 | High / 高 | Outer owns call/result; nested owns dispatch pair / Outer 拥有 call／result；nested 拥有 dispatch pair |
| Parallel dispatch order is not strict pair adjacency / 并行 dispatch 顺序不保证 pair 相邻 | Real PTC / 真实 PTC | Ordered Raw refs + typed IDs / 有序 Raw ref＋typed ID | Four operations interleave starts/settles / 四个 operation 交错 start／settle | Yes / 是 | No / 否 | Medium / 中 | Pair by `subCallId`, preserve physical order / 按 `subCallId` 配对并保留物理顺序 |
| Current source schema permits ancestry, observed writer is depth 1 / 当前源码 schema 允许 ancestry，实际 writer 只观察到 depth 1 | Current invariant/client + real PTC / 当前 invariant／client＋真实 PTC | Recursive `eventRefs` / 递归 `eventRefs` | Deep support could overclaim evidence / 深层支持可能过度声称证据 | Yes / 是 | No / 否 | Medium / 中 | Bound traversal; label deeper cases synthetic/source-only / 限制遍历；更深 case 标为合成／仅源码 |
| Workflow run has no enclosing tool call ID / Workflow run 没有 enclosing tool call ID | Current source; 0/6 real / 当前源码；真实 0／6 | Exact call-pair Logical operations / 精确 call-pair Logical operation | Temporal association can be ambiguous under parallel calls / 并行 call 下时间关联可能含糊 | Yes: standalone grouped Protocol / 是：独立 grouped Protocol | No / 否 | High evidence gap / 高证据缺口 | Do not attach or promote in Phase 2B / Phase 2B 不附着、不提升 |
| One workflow run can reference many child Sessions / 一个 workflow run 可引用多个 child Session | Current source only / 仅当前源码 | Derived-session navigation + Structured Detail / Derived Session 导航＋Structured Detail | Child IDs are exact but owner relation is run-specific / child ID 精确，但 owner 关系属于 run | Yes / 是 | No / 否 | Medium / 中 | Show bounded member facts in Supplemental Detail; no cross-Session inference / 在补充详情展示有界 member fact；不做跨会话推断 |
| Temptation to add DSH browser branching / 增加 DSH browser 分支的诱惑 | Existing Codex presentation + current DSH UI / 既有 Codex 呈现＋当前 DSH UI | Source-neutral canonical kind/detail/renderers / 来源中立 canonical kind／detail／renderer | DSH tree UI is convenient but not the app contract / DSH tree UI 方便但不是本应用契约 | Adapter/detail local / adapter／detail 本地 | **No** / **否** | Blocking if proposed / 若提出则阻断 | Reuse neutral surfaces only / 只复用中立 surface |
| Source-only workflow fixture versus real validation / 仅源码 workflow fixture 与真实验证 | Current upstream snapshot; real corpus negative / 当前上游 snapshot；真实语料负面 | Fixture evidence labeling / Fixture 证据标注 | Tests can accidentally imply observed writer validation / 测试可能误示已观察 writer 验证 | Yes / 是 | No / 否 | Medium / 中 | Label fixture source-only; docs state 0/6 / 标明 fixture 仅源码；文档注明 0／6 |

## Checkpoints / 检查点

- [x] M0 — verify repository baseline, accepted Phase 2A CI, all six real artifacts, group comparison, current upstream HEAD/package relation, and focused semantic delta. / 核验仓库基线、Phase 2A CI、六份真实工件、组间比较、当前上游 HEAD／package 关系及聚焦语义差异。
- [x] Corrective prerequisite — fixed and independently validated `session/end-seed` versus header `seedLength` semantics; Phase 2A archived after corrected remote CI. / 修正前置条件——已修复并独立验证 `session/end-seed` 与 header `seedLength` 语义；修正版远程 CI 通过后 Phase 2A 已归档。
- [ ] M1 — curate the smallest sanitized Code Mode fixtures preserving physical writer order: observed success/interleaving from real topology, current-source nested error/caught outer success, start-only incomplete, and malformed/ambiguous ancestry fail-closed cases. / 精选最小脱敏 Code Mode fixture，保留物理 writer 顺序，并覆盖真实成功／交错、当前源码 nested error／outer 捕获成功、start-only incomplete 与畸形／歧义 ancestry 保守失败。
- [ ] M2 — implement DSH-owned outer/nested correlation, canonical `code_mode_operation`, independent Observed Nested Activity, exact Raw ownership, status/observation semantics, query parity, and source freshness. / 实现 DSH 自有 outer／nested 关联、canonical `code_mode_operation`、独立 Observed Nested Activity、精确 Raw ownership、status／observation 语义、查询等价与来源 freshness。
- [ ] M3 — implement adapter-owned Code Mode Structured Detail using existing purposes, responsibilities, renderer vocabulary, and `eventRefs`; add no DSH browser branch and no Codex AST parsing. / 使用既有 purpose、responsibility、renderer vocabulary 与 `eventRefs` 实现 adapter-owned Code Mode Structured Detail；不增加 DSH browser branch，不解析 Codex AST。
- [ ] M4 — curate one minimal current-source workflow fixture, group valid lifecycle rows as one Protocol event, preserve incomplete/failure/member facts, and explicitly retain the 0/6 real-session evidence label. / 精选一个最小当前源码 workflow fixture，把合法 lifecycle row 归组为单个 Protocol event，保留 incomplete／failure／member fact，并明确标注真实会话 0／6。
- [ ] M5 — run focused DeepSeek, source-adapter conformance, Code Mode/shared presentation, full Node, `release:check`, browser when user-visible detail changes, package smoke, then remote CI after an authorized push. / 运行 DeepSeek 聚焦、source-adapter conformance、Code Mode／共享呈现、完整 Node、`release:check`、用户可见 detail 变化时的 browser、package smoke，并在获授权 push 后运行远程 CI。

## Fixture policy / Fixture 策略

- Never read `tmp/dsh-real-sessions` from committed tests and never commit real prompts, source, output, paths, IDs beyond harmless structural provenance, or wholesale artifacts. / 已提交测试绝不读取 `tmp/dsh-real-sessions`，绝不提交真实 prompt、源码、输出、路径、超出无害结构 provenance 的 ID 或完整工件。
- Derive the minimum shape from the structural report and regenerate equivalent synthetic artifacts through the applicable DSH writer where practical. Preserve packed rows and physical order only when the behavior depends on them. / 从结构报告提取最小 shape，并在可行时通过相应 DSH writer 重新生成等价合成工件；仅在行为依赖时保留 packed row 与物理顺序。
- Label every fixture as historical rc.6 writer, current rc.2 source/snapshot, or sanitized structural derivation. A source-only workflow fixture must not be described as real-session validated. / 每个 fixture 都须标注为历史 rc.6 writer、当前 rc.2 source／snapshot 或脱敏结构派生；仅源码 workflow fixture 不得描述为已由真实会话验证。
- Protect byte-exact uncompressed physical fixtures through `.gitattributes` when required. / 必要时通过 `.gitattributes` 保护逐字节精确的未压缩物理 fixture。

## Validation contract / 验证契约

The eventual implementation must prove: explicit durable Code topology; interleaved ordering; exact disjoint Raw References; success/error/incomplete nested states where evidence exists; no duplicate generic outer event; stable `eventRefs`; outer versus caught nested outcome independence; Index／Materialized query parity; accepted-snapshot Detail/Raw freshness; adapter-owned Detail purpose × responsibility; grouped workflow run/member status without fabricated call ownership; no browser source branch; and Codex/Claude regression safety. / 最终实现必须证明：显式持久 Code 拓扑、交错顺序、精确且不重叠的 Raw Reference、有证据支持的 nested success／error／incomplete 状态、不产生重复 generic outer event、稳定 `eventRefs`、outer 与被捕获 nested outcome 相互独立、Index／Materialized 查询等价、accepted-snapshot Detail／Raw freshness、adapter-owned Detail purpose × responsibility、不虚构 call ownership 的 grouped workflow run／member status、无 browser 来源分支，以及 Codex／Claude 回归安全。

No Phase 2B production validation has run yet because production implementation has not started. Historical M0 results remain evidence: baseline/source checks and structural scan passed, while replay through the then-accepted `f59dbf5…` adapter failed the user-visible contract for 5/6 marker-bearing Sessions and the existing 49/49 focus lacked ordinary/repeated-resume coverage. The corrective prerequisite later passed its new regressions, six-artifact replay, full Node 682/682, `release:check` with package smoke, browser 121/121, and corrected remote run `32628383363`; those are Phase 2A closure gates, not Phase 2B behavior validation. Phase 2B behavior tests remain NOT RUN. / Phase 2B 生产实现尚未开始，因此尚无 Phase 2B 生产验证。历史 M0 结果继续作为证据：基线／源码检查与结构扫描通过，而当时已接受的 `f59dbf5…` adapter 对五个含 marker 会话的重放未满足用户可见契约，既有 49／49 聚焦测试也缺少普通／重复 resume 覆盖。此后的修正前置条件已通过新回归、六工件重放、完整 Node 682／682、含 package smoke 的 `release:check`、browser 121／121 与修正版远程 run `32628383363`；这些属于 Phase 2A 关闭门禁，不是 Phase 2B 行为验证。Phase 2B 行为测试仍为 NOT RUN。

## Non-goals / 非目标

Phase 2B does not begin hooks, approvals, retries, `goal/change`, `todo/write`, slash-command lifecycle, schedule/change, feedback/record, web-search auxiliary LLM requests, future-format raw-preserving inspection, SQLite DSH backend support, whole-file Detail/Raw optimization, mixed-source indexing, or a shared nested-operation redesign. Interesting real occurrences remain inventory evidence only. / Phase 2B 不启动 hook、approval、retry、`goal/change`、`todo/write`、slash-command lifecycle、schedule/change、feedback/record、web-search 辅助 LLM request、未来格式 raw-preserving 检查、SQLite DSH backend、whole-file Detail／Raw 优化、mixed-source indexing 或共享 nested-operation 重设计；真实语料中的相关事件只作为 inventory 证据记录。

## Progress log / 进展记录

- 2026-08-23 (M0): verified actual repository baseline `c58a52b…`, clean `support-dsh`, and remotes; reconciled the expected `f59dbf5…` with three later accepted performance milestones. / 核验实际仓库基线、clean 分支与 remote，并对齐预期 `f59dbf5…` 与三个后续已接受性能里程碑。
- 2026-08-23 (Phase 2A closeout check): independently verified GitHub Actions run `32340719998` as fully successful, then left the plan active because the real-corpus resume-marker contradiction prevents honest archival. / 独立复核 GitHub Actions run `32340719998` 全部成功；但真实语料的 resume-marker 冲突阻止诚实归档，因此计划保持 active。
- 2026-08-23 (real corpus): produced the ignored structural report, inventoried all six Sessions without transcript content, and compared playground Standard/PTC with real Minimal persistence coverage. / 生成忽略的结构报告，在不暴露转录内容的情况下清点六个会话，并比较 playground Standard／PTC 与真实 Minimal 持久覆盖。
- 2026-08-23 (upstream): cloned current official upstream at `b150a55…`, verified clean `0.1.1-rc.2`, compared the six target event types and their implementation/presentation against `47f9438…`, and found no relevant incompatible redesign. / 克隆当前官方上游 `b150a55…`，核验 clean `0.1.1-rc.2`，对比六类目标事件及实现／呈现与 `47f9438…`，未发现相关不兼容重设计。
- 2026-08-23 (decision gate): Code Mode and workflow assumptions remain source-compatible, but Phase 2B implementation stops at M0 because accepted marker-only seed ownership deletes real top-level history. / Code Mode 与 workflow 假设保持源码兼容，但已接受的仅 marker seed ownership 会删除真实顶层历史，因此 Phase 2B 实现在 M0 停止。
- 2026-08-23 (corrective prerequisite complete): adapter-local head `08e078e…` passed deterministic resume/seed regressions, six-artifact replay, all applicable local gates, and GitHub Actions run `32628383363`; Phase 2A moved to `completed/`. Phase 2B is unblocked and remains at M0 because this correction pass intentionally stops before production implementation. / 修正前置条件完成：adapter 本地 head `08e078e…` 通过确定性 resume／seed 回归、六工件重放、全部适用本地门禁与 GitHub Actions run `32628383363`；Phase 2A 已移至 `completed/`。Phase 2B 已解除阻断，并因本次修正流程有意止于生产实现前而继续停留在 M0。
