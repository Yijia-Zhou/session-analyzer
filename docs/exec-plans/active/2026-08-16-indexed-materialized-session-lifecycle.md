# Indexed Session and Materialized Session lifecycle execution plan / Indexed Session 与 Materialized Session 生命周期执行计划

## Status / 状态

- Status: active; M1 through M3 are accepted through exact implementation head `d2ff26e92104c83b17e46a11f3bb1566a1c65fb2`; the M4 Codex candidate is complete and stopped for an independent checkpoint; M5 has not started / 状态：活跃；M1 至 M3 已接受至精确实现 head `d2ff26e92104c83b17e46a11f3bb1566a1c65fb2`；M4 Codex 候选已完成并停在独立检查点；M5 尚未开始
- Started: 2026-08-16 / 开始日期：2026-08-16
- Working branch: `indexed-materialized-session-lifecycle` / 工作分支：`indexed-materialized-session-lifecycle`
- Baseline branch: `towards-0.2.0` / 基线分支：`towards-0.2.0`
- Exact baseline commit: `6f8fa085fbd18344a973c8169be1998b5a71943d` / 精确基线 commit：`6f8fa085fbd18344a973c8169be1998b5a71943d`
- Current review artifact: accepted M0/M1 documentation and M2 seam through exact head `7eaa317da1e6ca7fb8a6bcea6f24f0770a9f3530`; accepted M3 implementation through exact head `d2ff26e92104c83b17e46a11f3bb1566a1c65fb2`; working-tree M4 Codex source-backed candidate pending an immutable-head checkpoint / 当前评审 artifact：已接受至精确 head `7eaa317da1e6ca7fb8a6bcea6f24f0770a9f3530` 的 M0／M1 文档与 M2 seam；已接受至精确 head `d2ff26e92104c83b17e46a11f3bb1566a1c65fb2` 的 M3 实现；工作树中的 M4 Codex 来源回读候选正等待不可变 head 检查点
- Design packet: `docs/design-docs/indexed-materialized-session-lifecycle.md` / 设计包：`docs/design-docs/indexed-materialized-session-lifecycle.md`

## Objective / 目标

Replace the resident complete-Session Index with a source-neutral lifecycle in which Indexed Sessions support project-wide browsing and query behavior, while complete Materialized Sessions are reconstructed, validated, coalesced, and cached on demand within one committed Index revision. Preserve existing source selection, query, timeline, detail, Raw, image, relationship, cancellation, and reindex semantics for Codex and Claude Code. / 用来源中立生命周期替换常驻完整 Session 的 Index：Indexed Session 支持项目级浏览与查询，而完整 Materialized Session 在单个已提交 Index revision 内按需重建、校验、合并并缓存。保持 Codex 与 Claude Code 既有来源选择、查询、时间线、详情、Raw、图片、关系、cancellation 与 reindex 语义。

## Guardrails / 护栏

1. Milestone stops are mandatory. M2 production code cannot start until a fresh independent M1 architecture review explicitly accepts the packet. M4 cannot start until the M3 implementation checkpoint is accepted. / Milestone 停点是强制的。Fresh 独立 M1 架构评审显式接受前不得开始 M2 生产代码；M3 实现检查点接受前不得开始 M4。
2. The implementation stays source-neutral in shared contracts, server state, materialization ownership, and project query. Adapters own source parsing, evidence, freshness, and reconstruction. / 实现必须在共享契约、server state、物化 ownership 与项目查询中保持来源中立；adapter 拥有来源解析、证据、freshness 与重建。
3. Aggregate profiling must not record transcript content or paths. Diagnostic heap increases are evidence only and never become a product-default change. / 聚合 profiling 不得记录转录内容或路径。诊断性提高 heap 只作为证据，绝不成为产品默认变更。
4. Existing detail purpose/responsibility, complete ordered Raw References, source-owned locators, sanitization, fork/derived ownership, replacement atomicity, and cancellation behavior remain contractual. / 既有详情用途／职责、完整有序 Raw Reference、来源拥有 locator、sanitization、fork／derived ownership、替换原子性与 cancellation 行为继续属于契约。
5. First implementation has no eviction. It must nevertheless isolate materialization by Index revision and release the whole old cache on successful replacement. / 首版不实现 eviction，但必须按 Index revision 隔离物化，并在成功替换时释放整个旧 cache。

## Milestone plan / 里程碑计划

| Milestone / 里程碑 | Deliverable / 交付物 | Status / 状态 | Gate / 门槛 |
| --- | --- | --- | --- |
| M0 — Baseline, consumer map, retained-memory audit / 基线、consumer map、常驻内存审计 | Exact branch/baseline, full baseline tests, complete consumer/residency map, aggregate heap/search evidence, stop-condition decision / 精确 branch／baseline、全量基线测试、完整 consumer／常驻矩阵、聚合 heap／search 证据、停止条件决定 | Complete / 已完成 | Naive event-shaped query projection rejected after measured 82.5% retained heap / 量测仍保留 82.5% heap 后拒绝朴素 event-shaped 查询投影 |
| M1 — Lifecycle architecture packet / 生命周期架构评审包 | Closed Indexed/Materialized/descriptor/dependency/query contracts, route matrix, data flow, scheduler/failure semantics, Codex/Claude sketches, compatibility, non-goals, review dispositions / 封闭 Indexed／Materialized／descriptor／dependency／query 契约、route 矩阵、数据流、scheduler／failure 语义、Codex／Claude 草图、兼容性、非目标、评审处理 | Complete; contract and authoritative sequence accepted / 已完成；契约与权威顺序已接受 | Pass 5 accepted exact reconciliation head `7c3db8dfd9895a57a63020035a6278e0c410f1a0`; M2 may start / 第 5 轮接受精确对齐 head `7c3db8dfd9895a57a63020035a6278e0c410f1a0`；M2 可开始 |
| M2 — Lifecycle seam without memory change / 不改变内存的生命周期 seam | Discriminated `resident-complete-v1`/`indexed-materialized-v1` canonical/adapter seam; `materializeSession` required in both modes; both production adapters use compatibility materialization of their existing complete Sessions; conformance traverses Indexed → Materialized / 判别式两种 canonical／adapter seam；两种 mode 都要求 `materializeSession`；两个生产 adapter 对既有完整 Session 使用兼容物化；conformance 穿过 Indexed → Materialized | Complete; read-only checkpoint pass 4 accepted exact head `7eaa317da1e6ca7fb8a6bcea6f24f0770a9f3530` / 已完成；读取型 checkpoint 第 4 轮接受精确 head `7eaa317da1e6ca7fb8a6bcea6f24f0770a9f3530` | Focused 55/55 and full 535/535; exact current analysis/presentation compatibility, exception-safe non-mutation, closed descriptor/retainer/error contracts; no user-visible or resident-memory change / 聚焦 55／55 与全量 535／535；精确当前 analysis／presentation 兼容、异常安全不可 mutation、封闭 descriptor／retainer／error 契约；无用户可见或常驻内存变化 |
| M3 — Shared project-query independence / 共享项目查询独立 | Build/adopt one shared packed query store for both legacy adapters; move list/sort/project search/filter/suggestions/catalog/navigation off complete events; event APIs explicitly consume compatibility-materialized Sessions; install the source-neutral no-cache `IndexRevisionLease` for async query/materialization retirement safety / 为两个 legacy adapter 构建／采用同一共享紧凑查询存储；列表／排序／项目搜索／filter／建议／catalog／导航脱离完整 event；event API 显式消费兼容物化 Session；为异步 query／materialization retirement 安装来源中立、无 cache 的 `IndexRevisionLease` | Complete; exact Sol checkpoint accepted implementation head `d2ff26e92104c83b17e46a11f3bb1566a1c65fb2` / 已完成；精确 Sol 检查点接受实现 head `d2ff26e92104c83b17e46a11f3bb1566a1c65fb2` | Negative array-free/getter-bomb fixtures, exact query oracle/cancellation/revision-retirement gates, bounded legacy Raw owner lookup, all revision-bearing browser query responses checked, and mandatory implementation checkpoint accepted before M4 / 无 event array／getter-bomb fixture、精确 query oracle／cancellation／revision-retirement 门槛、有界 legacy Raw owner lookup、所有带 revision 的 browser query response 均被校验；M4 前强制实现检查点接受 |
| M4 — Codex source-backed Session / Codex 来源回读 Session | Switch Codex to Indexed build output; transient full parse, fork/query/summary derivation, uncached source-backed materialization protected by the M3 lease, detail/Raw/image parity, default-heap corpus evidence / 把 Codex 切换为 Indexed build output；瞬态完整解析、fork／query／summary 派生、受 M3 lease 保护的无 cache 来源回读物化、detail／Raw／image 等价、默认 heap 语料证据 | Candidate complete; independent checkpoint pending; M5 blocked / 候选已完成；等待独立检查点；M5 被阻塞 | M3 checkpoint accepted first; Codex structural/fork/stale/cancel/reindex/retirement/conformance tests and real-corpus memory gates / 必须先接受 M3 检查点；Codex 结构／fork／stale／cancel／reindex／retirement／conformance 测试与真实语料内存门槛 |
| M5 — Claude source-backed Session / Claude 来源回读 Session | Switch Claude to the same lifecycle with lease-protected uncached materialization, interned positive/negative/directory dependencies, and preserved primary/derived/pointer/materialized-fork semantics / 把 Claude 切换到同一生命周期，使用 lease 保护的无 cache 物化、intern 正向／负向／directory 依赖，并保持 primary／derived／pointer／materialized-fork 语义 | Pending / 待开始 | Pairing/detail/analysis/derived/fork/reindex/retirement conformance, no reachable parsed graph, available-corpus memory gate; no legacy production adapter remains / pairing／detail／analysis／derived／fork／reindex／retirement conformance、parsed 图不可达、可用语料内存门槛；不再有 legacy 生产 adapter |
| M6 — Runtime materialization owner and integration / Runtime 物化 owner 与集成 | Extend each M3 revision lease with an unbounded cache, active-one/queued-32 scheduler, coalescing, waiter cancellation, stale errors, source switch/replacement isolation, and minimal browser recovery / 在每个 M3 revision lease 上扩展无界 cache、active-one／queued-32 scheduler、合并、waiter cancellation、stale error、来源切换／替换隔离与最小 browser recovery | Pending / 待开始 | Cold/warm/event/detail/Raw/image integration, replacement/cache-release, browser/build/package/full Node gates / 冷／热 event／detail／Raw／image 集成、替换／cache release、browser／build／package／全量 Node 门槛 |
| M7 — Release readiness and closeout / 发布准备与收尾 | Product/design docs synchronized, final corpus measurements, external review findings closed, plan archived only when shipped / product／design 文档同步、最终语料量测、外部评审 finding 关闭、只在已交付时归档计划 | Pending / 待开始 | Release checks pass; no unresolved blocking findings; explicit completion evidence / 发布检查通过；无未解决阻断 finding；有显式完成证据 |

## M0 record / M0 记录

### Repository and baseline / 仓库与基线

- The original worktree was clean at `6f8fa085fbd18344a973c8169be1998b5a71943d`; work moved to dedicated branch `indexed-materialized-session-lifecycle`. / 原 worktree 在 `6f8fa085fbd18344a973c8169be1998b5a71943d` 上干净；工作已移至专用分支 `indexed-materialized-session-lifecycle`。
- Baseline command: `npm test`. Result: 528 passed, 0 failed, 0 skipped, about 9.85 seconds reported test duration. / 基线命令：`npm test`。结果：528 passed、0 failed、0 skipped，报告测试时长约 9.85 秒。
- Runtime: Node `v24.18.1`, npm `12.0.2`. / Runtime：Node `v24.18.1`、npm `12.0.2`。

### Consumer/residency findings / Consumer／常驻结论

- `state.index` is the sole server owner. `sessions` and `sessionsById` share exact Session object identities; they do not duplicate event bodies. / `state.index` 是唯一 server owner。`sessions` 与 `sessionsById` 共享精确 Session object identity，不重复事件 body。
- Session summaries need metadata, counts, relationships, top-five tools/files, failed-command count, and protocol count; requiring event arrays is a validator/length artifact. / Session 摘要只需要 metadata、count、关系、前五 tools／files、失败命令 count 与 protocol count；对事件 array 的要求来自 validator／length 偶合。
- Project filters/search, file suggestions, and catalogs currently scan full Raw/Logical arrays. Search-result navigation depends on event ID plus structural timeline ordinal and currently lacks a revision token. / 项目 filter／search、文件建议与目录当前扫描完整 Raw／Logical array。搜索结果导航依赖 event ID 与结构 timeline ordinal，且当前缺少 revision token。
- Timeline limit controls response size but not full-array scans; Raw timeline creates DTOs before slicing; browser navigation can accumulate complete filtered timelines and detail caches. / Timeline limit 只控制 response size，不控制完整 array 扫描；Raw timeline 在切片前创建 DTO；浏览器导航会累积完整过滤时间线与 detail cache。
- Codex commits compact Raw plus complete Logical, hidden fork/presentation/analysis derivations, while source-backed detail hydration is already bounded and freshness-checked. / Codex 提交紧凑 Raw 加完整 Logical、隐藏 fork／presentation／analysis 派生；来源回读 detail hydration 已有界且校验 freshness。
- Claude commits parsed Raw graphs, normalized duplicates, Logical arrays, analysis, and relationship/presentation state. Its structured detail directly consumes resident parsed records. / Claude 提交 parsed Raw 图、normalized 副本、Logical array、analysis 与关系／presentation 状态；结构详情直接消费常驻 parsed record。
- Fork/derived inference is a build-time global consumer, but its committed navigation result is compact. Production migration needs compact cross-session evidence and a materialization boundary descriptor rather than resident parent/child bodies. / Fork／derived 推断是构建期全局 consumer，但其已提交导航结果紧凑。生产迁移需要紧凑跨 Session 证据与物化边界 descriptor，而不是常驻 parent／child body。

The detailed consumer matrix and source-specific retained-field audit are normative inputs to M1 and are recorded in `docs/design-docs/indexed-materialized-session-lifecycle.md`. / 详细 consumer 矩阵与来源专属常驻字段审计是 M1 的规范输入，记录在 `docs/design-docs/indexed-materialized-session-lifecycle.md`。

### Memory and search evidence / 内存与搜索证据

The aggregate-only corpus profiling found 666 files, 482 indexed Sessions, approximately 1.193 GB of candidates, approximately 200,820 Logical Events, and approximately 282,998 Raw Records. The default-heap cold build OOMed near the V8 limit. A diagnostic-only 4 GiB build committed at 2,098,809,064 bytes forced-GC heap and 2,674,880,512 bytes RSS. / 仅聚合语料 profiling 发现 666 个文件、482 个已索引 Session、约 1.193 GB 候选、约 200,820 个 Logical Event 与约 282,998 个 Raw Record。默认 heap 冷构建在 V8 限制附近 OOM。仅诊断的 4 GiB 构建在提交后强制 GC 得到 2,098,809,064 bytes heap 与 2,674,880,512 bytes RSS。

Dropping event bodies while retaining ordinary per-event query objects left 1,731,440,608 bytes forced-GC heap, about 82.5% of the complete graph. The stop condition therefore rejected that representation. Packed length-framed text plus scalar metadata measured 384,223,656 bytes when every layer was compressed, but cold search regressed to Main 2.126 s, Protocol 1.001 s, and Raw 4.173 s and added about 13.9 s of build work. M1 therefore proposes uncompressed packed Main text and compressed Protocol/Raw text, estimated at about 601 MB total packed query data, subject to M3 runtime measurement and parity/performance gates. / 删除 event body、保留普通逐 event 查询 object 后，强制 GC heap 仍有 1,731,440,608 bytes，约为完整图的 82.5%，因此停止条件拒绝该表示。每层都压缩时，紧凑长度分帧正文加 scalar metadata 量测为 384,223,656 bytes，但冷搜索退化到 Main 2.126 s、Protocol 1.001 s、Raw 4.173 s，并增加约 13.9 s 构建工作。M1 因而提议 Main 使用未压缩紧凑正文，Protocol／Raw 使用压缩正文，估算查询数据总计约 601 MB；最终仍受 M3 runtime 量测及等价／性能门槛约束。

## M1 architecture checkpoint / M1 架构检查点

The review packet includes all mandatory inputs: exact base/head description, consumer matrix, memory evidence and rejected stop-condition representation, Indexed/Materialized canonical contracts, runtime/data-flow diagram, Codex and Claude migration sketches, a concrete project-search representation, cache/reindex/cancellation/stale-source semantics, compatibility, non-goals, conformance gates, and unresolved questions. / 评审包包含全部强制输入：精确 base／head 描述、consumer 矩阵、内存证据与被停止条件拒绝的表示、Indexed／Materialized canonical 契约、runtime／data-flow 图、Codex 与 Claude 迁移草图、具体项目搜索表示、cache／reindex／cancellation／stale-source 语义、兼容性、非目标、conformance 门槛与待决问题。

### Independent review log / 独立评审日志

No production M2 work may be recorded below until the review log contains an explicit accepted verdict. / 在评审日志包含显式 accepted verdict 前，下方不得记录任何生产 M2 工作。

| Review / 评审 | Exact scope / 精确范围 | Verdict / 结论 | Findings and disposition / Finding 与处理 |
| --- | --- | --- | --- |
| M1 fresh architecture review, pass 1 / M1 fresh 架构评审，第 1 轮 | Working-tree documentation diff against `6f8fa085...`; independent `gpt-5.6-sol`, xhigh, fresh context / 相对 `6f8fa085...` 的 working-tree 文档 diff；独立 `gpt-5.6-sol`、xhigh、fresh context | `CHANGES_REQUIRED` / `CHANGES_REQUIRED` | Accepted M0 stop-condition direction; eight blockers: executable milestone transition, closed fields/budgets/equality, filtered `timelineIndex`, async query semantics/cancellation, staged adapter APIs, Codex fork descriptor, Claude dependency closure, bounded scheduler/error transitions. All are addressed in the revised design; exact re-review pending. / 接受 M0 停止条件方向；八项 blocker：可执行 milestone 过渡、封闭字段／budget／等值、过滤后 `timelineIndex`、异步查询语义／cancellation、staged adapter API、Codex fork descriptor、Claude dependency 闭包、有界 scheduler／error transition。修订设计已逐项处理；等待精确复审。 |
| M1 architecture re-review, pass 2 / M1 架构复审，第 2 轮 | Exact immutable head `a65778e7ac5a7ae14c16d3e11b16b0698c3f8fc1` against `6f8fa085...` / 相对 `6f8fa085...` 的精确不可变 head `a65778e7ac5a7ae14c16d3e11b16b0698c3f8fc1` | `CHANGES_REQUIRED` / `CHANGES_REQUIRED` | Closed blockers 1, 3, 4, 5, 7, and 8. Reopened field-specific unresolved relationship compatibility and Codex's valid three-segment membership; new blocker removed an unconsumed Raw query segment fact. Atomic replacement wording and committed-head description were also normalized. / 关闭 blocker 1、3、4、5、7、8。重新打开按字段允许 unresolved relationship 的兼容性与 Codex 有效三 segment membership；新 blocker 要求移除无人消费的 Raw query segment fact。同时统一原子替换措辞与已提交 head 描述。 |
| M1 architecture re-review, pass 3 / M1 架构复审，第 3 轮 | Exact immutable head `e29fb566116eb1f30bdf5e8660ebc72a2c156e38` against `6f8fa085...` / 相对 `6f8fa085...` 的精确不可变 head `e29fb566116eb1f30bdf5e8660ebc72a2c156e38` | `ACCEPTED`; M2 may start / `ACCEPTED`；M2 可开始 | Closed field-specific unresolved relationship compatibility, exact Codex three-segment membership/drop-only-inherited behavior, and removal of unconsumed Raw query segment facts. Confirmed atomic replacement wording, clean documentation-only diff, and no new blockers. This plan stops before M2 by instruction. / 关闭按字段 unresolved relationship 兼容、精确 Codex 三 segment membership／只删除 inherited 行为，以及无人消费 Raw query segment fact 的移除。确认原子替换措辞、干净纯文档 diff，且无新 blocker。本计划按指令停在 M2 之前。 |
| Authoritative-goal completion audit / 权威目标完成审计 | Re-read all 572 lines of the user-provided objective before M2 / M2 前重新读取用户提供目标的全部 572 行 | Sequence discrepancy found; no production code affected / 发现顺序偏差；未影响生产代码 | Pass 3 had accepted detailed contracts but the plan moved Codex/runtime work into M3. The authoritative goal requires M2 seam with compatibility materialization, M3 shared-query independence and mandatory stop, M4 Codex, M5 Claude, M6 owner. Documentation was reconciled before implementation. / 第 3 轮已接受详细契约，但计划把 Codex／runtime 工作提前到 M3。权威目标要求 M2 兼容物化 seam、M3 共享查询独立与强制停点、M4 Codex、M5 Claude、M6 owner。已在实现前对齐文档。 |
| M1 milestone-sequence delta review, pass 4 / M1 milestone 顺序 delta 评审，第 4 轮 | Exact immutable head `470c71332e633f603a96878485832828dbac46a9` against `23534bbd5cc47c025b9c0ed2525390bb29a304b3`; independent `gpt-5.6-sol`, xhigh, fresh context / 相对 `23534bbd5cc47c025b9c0ed2525390bb29a304b3` 的精确不可变 head `470c71332e633f603a96878485832828dbac46a9`；独立 `gpt-5.6-sol`、xhigh、fresh context | `CHANGES_REQUIRED` / `CHANGES_REQUIRED` | Restored order accepted, but two blockers remained: the M2 compatibility projection required fields absent from current complete Sessions, and M4/M5 direct materialization lacked pre-M6 revision retirement protection. The revised packet now defines an exact legacy complete-Session branch plus an M3 no-cache `IndexRevisionLease`; exact re-review is pending. / 已接受恢复后的顺序，但仍有两个 blocker：M2 兼容投影要求当前完整 Session 不具备的字段；M4／M5 直接物化缺少 M6 前 revision retirement 保护。修订包现已定义精确 legacy 完整 Session 分支与 M3 无 cache 的 `IndexRevisionLease`；等待精确复审。 |
| M1 milestone-sequence delta re-review, pass 5 / M1 milestone 顺序 delta 复审，第 5 轮 | Exact immutable head `7c3db8dfd9895a57a63020035a6278e0c410f1a0` against `470c71332e633f603a96878485832828dbac46a9`; same independent `gpt-5.6-sol`, xhigh reviewer / 相对 `470c71332e633f603a96878485832828dbac46a9` 的精确不可变 head `7c3db8dfd9895a57a63020035a6278e0c410f1a0`；同一独立 `gpt-5.6-sol`、xhigh reviewer | `ACCEPTED`; M2 may start / `ACCEPTED`；M2 可开始 | Closed both pass-4 blockers. Confirmed exact current complete-Session compatibility without future fields, strict synthetic coverage, M3 no-cache lease retirement safety for M4/M5, and cache/coalescing/scheduling deferred to M6. No new milestone-order, ownership, query, freshness, Detail, Raw-reference, or source-ownership contradiction. / 关闭第 4 轮两个 blocker。确认不要求未来字段的精确当前完整 Session 兼容、严格合成覆盖、M3 无 cache lease 为 M4／M5 提供 retirement 安全，以及 cache／coalescing／scheduling 推迟到 M6。没有新的 milestone 顺序、ownership、query、freshness、Detail、Raw-reference 或 source-ownership 矛盾。 |

## M2 lifecycle-seam candidate / M2 生命周期 seam 候选

- `src/source-adapter-contract.js` now closes the two lifecycle values, requires `materializeSession` in both, and requires strict-mode descriptor/legacy-owner/private-state validators plus declared private fields. / `src/source-adapter-contract.js` 现已封闭两个 lifecycle 值、在两种 mode 都要求 `materializeSession`，并要求 strict mode 的 descriptor／legacy-owner／private-state validator 与已声明私有字段。
- `src/canonical-contract.js` preserves the exact resident complete-Session validator and adds strict Indexed/Materialized validation, copied-fact/snapshot equality, forbidden retainers, bounded descriptor/dependency/legacy-owner envelopes, unique event identity, and complete Raw-reference ownership. / `src/canonical-contract.js` 保持精确 resident 完整 Session validator，并新增 strict Indexed／Materialized validation、copied-fact／snapshot 等值、禁止 retainer、有界 descriptor／dependency／legacy-owner envelope、唯一 event identity 与完整 Raw-reference ownership。
- Codex and Claude both declare `resident-complete-v1`; compatibility materialization returns the exact captured Index object after pre/post-await cancellation and ownership checks. The shared conformance suite now materializes before complete event/detail/Raw assertions. No buildIndex, event route, query, or resident-memory behavior changed. / Codex 与 Claude 都声明 `resident-complete-v1`；兼容物化在 await 前后 cancellation 与 ownership 检查后返回捕获 Index 中的精确 object。共享 conformance suite 现会在完整 event／detail／Raw assertion 前先物化。没有改变 buildIndex、event route、query 或常驻内存行为。
- Focused gate: `node --test test/canonical-contract.test.js test/source-adapter-contract.test.js test/source-adapter-conformance.test.js test/source-switch.test.js` — 55 passed. Full gate: `npm test` — 535 passed, 0 failed. An initial full run exposed one existing error-precedence expectation; dispatch ordering was restored and both focused and full gates then passed. / 聚焦门槛：`node --test test/canonical-contract.test.js test/source-adapter-contract.test.js test/source-adapter-conformance.test.js test/source-switch.test.js`——55 passed。全量门槛：`npm test`——535 passed、0 failed。首次全量运行暴露一项既有 error precedence 预期；恢复 dispatch 顺序后，聚焦与全量门槛均通过。
- M2 remains a candidate until a read-only exact-head review finds no blocking lifecycle/compatibility/conformance issue. / 在读取型精确 head 评审确认没有 lifecycle／compatibility／conformance blocker 前，M2 继续是候选。
- Read-only checkpoint pass 1 inspected exact delta `16227ddffce4c98731f10d8f641fb2fbe6476137..2852e1404f664823263bd917991f08dec12e5cda` and returned `CHANGES_REQUIRED`. Four blockers were reproduced: shallow resident mutation detection; strict callbacks could mutate the passed Index; bounded Arrays admitted accessors/symbol/extra keys; strict Materialized closure admitted an undeclared non-private retainer. The correction fingerprints the complete callback input graph without retaining a clone, validates Array property descriptors without invoking accessors, closes every Materialized top-level field, and adds an exact payload-byte check for the legacy owner envelope. Delta tests and exact re-review are required before M2 can complete. / 读取型 checkpoint 第 1 轮检查了精确 delta `16227ddffce4c98731f10d8f641fb2fbe6476137..2852e1404f664823263bd917991f08dec12e5cda`，结论为 `CHANGES_REQUIRED`。复现了四个 blocker：resident mutation 检测过浅；strict callback 可修改传入 Index；有界 Array 接受 accessor／symbol／额外 key；strict Materialized 闭包接受未声明的非私有 retainer。修正方案会在不常驻 clone 的前提下 fingerprint 完整 callback 输入图、在不调用 accessor 的情况下校验 Array property descriptor、封闭每个 Materialized 顶层字段，并为 legacy owner envelope 增加精确 payload byte 校验。M2 完成前必须通过 delta 测试与精确复审。
- Read-only checkpoint pass 2 inspected exact delta `2852e1404f664823263bd917991f08dec12e5cda..a0de9522f6fdb071e65d989298e5350598e7d7ac` and again returned `CHANGES_REQUIRED`. Normal-return mutation, bounded Array, top-level retainer, payload-byte, Map/Set/cycle/identity-replacement, and accessor-safety probes passed. Three blockers remained: mutation checks were skipped when a callback rejected; nested parsed-correlation/alternate-event retainers passed inside `analysis` or `presentationIndexes`; validator rejections escaped without the materialization contract code. The second correction makes fingerprint verification exception-safe with mutation precedence and retained cause, closes the exact analysis and presentation-index schemas, normalizes validator failures, preserves unchanged-input source failures, and records an M3 gate for the nonblocking full-Index fingerprint cost. / 读取型 checkpoint 第 2 轮检查了精确 delta `2852e1404f664823263bd917991f08dec12e5cda..a0de9522f6fdb071e65d989298e5350598e7d7ac`，再次返回 `CHANGES_REQUIRED`。正常返回 mutation、有界 Array、顶层 retainer、payload byte、Map／Set／cycle／identity replacement 与 accessor safety 探针均通过。仍有三个 blocker：callback reject 时跳过 mutation 检查；嵌套 parsed-correlation／alternate-event retainer 可进入 `analysis` 或 `presentationIndexes`；validator rejection 逸出且没有 materialization contract code。第二次修正让 fingerprint verification 对异常安全、以 mutation 为优先并保留 cause，封闭精确 analysis 与 presentation-index schema，归一化 validator failure，保留输入未变化的 source failure，并为非阻塞的完整 Index fingerprint 成本记录 M3 门槛。
- Second-correction gates: the focused lifecycle suite passes 55/55 and the full Node suite passes 535/535 with `git diff --check` clean. M2 remains a candidate pending exact read-only re-review. / 第二次修正门槛：聚焦生命周期套件 55／55 通过，全量 Node 套件 535／535 通过，且 `git diff --check` 干净。M2 在精确读取型复审前继续保持候选状态。
- Read-only checkpoint pass 3 inspected exact delta `a0de9522f6fdb071e65d989298e5350598e7d7ac..489ae19466625eb3b88bd693c9ac0a71fb8470b9` and returned `CHANGES_REQUIRED`. Exception-safe synchronous mutation/rejection handling, nested retainer closure, presentation compatibility, validator error codes, and unchanged-input source errors passed. Three blockers remained: exact current Codex analysis includes legitimate own-`undefined` optional `exitCode` leaves; native async and returned-Promise validators were not executably rejected; and an unprintable non-`Error` thrown object could make error formatting throw. The third correction admits explicit `undefined` only in the closed analysis tree, statically rejects async/generator validators, rejects every non-`undefined` validator return, suppresses native rejected-Promise fallout, and uses a no-throw formatter while retaining the original thrown cause. / 读取型 checkpoint 第 3 轮检查了精确 delta `a0de9522f6fdb071e65d989298e5350598e7d7ac..489ae19466625eb3b88bd693c9ac0a71fb8470b9`，结论为 `CHANGES_REQUIRED`。异常安全的同步 mutation／rejection 处理、嵌套 retainer 闭包、presentation 兼容、validator error code 与输入未变化的 source error 均通过。仍有三个 blocker：当前 Codex 精确 analysis 含有合法的 own-`undefined` optional `exitCode` 叶子；原生 async 与返回 Promise 的 validator 未被可执行地拒绝；不可打印的非 `Error` thrown object 可导致 error formatter 再次抛错。第三次修正只在封闭 analysis 树中允许显式 `undefined`，静态拒绝 async／generator validator，拒绝 validator 的每个非 `undefined` 返回值，抑制原生 rejected Promise 的后续影响，并使用 no-throw formatter 同时保留原始 thrown cause。
- Third-correction gates: focused lifecycle tests pass 55/55, the full Node suite passes 535/535, syntax checks pass, and `git diff --check` is clean. Exact re-review remains required before M2 completion. / 第三次修正门槛：聚焦生命周期测试 55／55 通过，全量 Node 套件 535／535 通过，语法检查通过，且 `git diff --check` 干净。M2 完成前仍需精确复审。
- Read-only checkpoint pass 4 inspected exact delta `489ae19466625eb3b88bd693c9ac0a71fb8470b9..7eaa317da1e6ca7fb8a6bcea6f24f0770a9f3530` and returned `ACCEPTED`. It reproduced exact checked-in Codex analysis parity including own-`undefined` leaves; static rejection of async/generator/async-generator validators across all three hooks; rejection and normalization of Promise/thenable/iterator/scalar returns without unhandled native rejection; exact cause retention for non-`Error` throws; and all earlier mutation, retainer, presentation, source-error, lifecycle, and no-drift guarantees. M2 is complete and M3 may start, subject to its recorded full-Index fingerprint performance gate. / 读取型 checkpoint 第 4 轮检查了精确 delta `489ae19466625eb3b88bd693c9ac0a71fb8470b9..7eaa317da1e6ca7fb8a6bcea6f24f0770a9f3530`，结论为 `ACCEPTED`。复现并确认了检入 Codex 精确 analysis 等价（含 own-`undefined` 叶子）；三个 hook 全部静态拒绝 async／generator／async-generator validator；Promise／thenable／iterator／scalar 返回值被拒绝并归一化且没有原生未处理 rejection；非 `Error` throw 保留精确 cause；以及此前所有 mutation、retainer、presentation、source-error、lifecycle 与无 drift 保证。M2 已完成，M3 可开始，但必须遵守已记录的完整 Index fingerprint 性能门槛。

## M3 shared-query implementation candidate / M3 共享查询实现候选

- `src/project-query-store.js` implements one closed source-neutral schema for both adapters: one shared UTF-8 dictionary, exactly three per-Session Main／Protocol／Raw shards, typed scalar/list columns, and separate length-framed preview/search text. Main is identity-coded; Protocol／Raw chunks below 3.5 MiB are identity-coded and larger chunks use independent level-1 gzip. Validation closes fields, ownership, dictionary IDs, ordered unique event IDs, layer coverage, exact codec threshold, chunk bounds, whole-store row/byte caps, and exact encoded-byte accounting. / `src/project-query-store.js` 为两个 adapter 实现同一封闭且来源中立的 schema：一个共享 UTF-8 dictionary、每个 Session 恰好三个 Main／Protocol／Raw shard、typed scalar／list column，以及独立长度分帧 preview／search 正文。Main 使用 identity codec；低于 3.5 MiB 的 Protocol／Raw chunk 使用 identity，较大 chunk 使用独立 level-1 gzip。Validation 封闭字段、ownership、dictionary ID、有序唯一 event ID、layer coverage、精确 codec 阈值、chunk bound、全 store row／byte cap 与精确 encoded-byte accounting。
- Both legacy production adapters install the shared store while complete Sessions are still resident, and replace the list-facing Session shells with compact summary projections. Session list/sort/date, project free text and same-row filters, project file suggestions, catalogs, latest-result identity, filtered ordinal, and both locales read only the projections/store. Array-free strict fixtures and getter-bomb legacy fixtures prove those paths do not reach `rawEvents`, `logicalEvents`, `analysis`, or `presentationIndexes`; an absent bounded Code Mode catalog fails closed to empty. Codex additionally builds the canonical file/line legacy Raw owner projection before commit, so request-time ownership lookup returns only `sessionId`/`rawIdHint`/`line` before explicit materialization. Event/timeline/session-suggestion APIs require an explicit compatibility-materialized Session. / 两个 legacy 生产 adapter 都会在完整 Session 仍常驻时安装共享 store，并把列表面向的 Session shell 替换为紧凑 summary projection。Session 列表／排序／日期、项目自由文本与同 row filter、项目文件建议、catalog、latest-result identity、过滤后 ordinal 与两种 locale 只读取 projection／store。无 array 的严格 fixture 与 getter-bomb legacy fixture 证明这些路径不会触达 `rawEvents`、`logicalEvents`、`analysis` 或 `presentationIndexes`；有界 Code Mode catalog 缺失时 fail closed 为空。Codex 还会在 commit 前构建 canonical file／line legacy Raw owner projection，因此 request-time ownership lookup 在显式物化前只返回 `sessionId`／`rawIdHint`／`line`。Event／timeline／session suggestion API 要求显式兼容物化 Session。
- `IndexRevisionLease` adds process-local monotonic revisions, synchronous retirement on successful replacement or identity clear, joined caller/retirement cancellation, and post-await validation without cache, coalescing, queue, or scheduler. Every asynchronous project query and materializing route captures one lease and never refetches `state.index`. HTTP 409 `INDEX_REVISION_RETIRED` and response `indexRevision` drive one coalesced browser state/query reload. Ordinary Session lists, project results, and file suggestions are rejected before commit on mismatch; stale cards and ordinals are never exposed to timeline drill-down. / `IndexRevisionLease` 新增进程内单调 revision、成功替换或 identity clear 时同步 retirement、caller／retirement cancellation 合并与 await 后校验，但不含 cache、coalescing、queue 或 scheduler。每个异步项目查询与物化 route 都捕获一个 lease，绝不重新读取 `state.index`。HTTP 409 `INDEX_REVISION_RETIRED` 与 response `indexRevision` 驱动一次合并的 browser state／query reload。普通 Session 列表、项目结果与文件建议会在 mismatch 时于 commit 前被拒绝；过期 card 与 ordinal 绝不会暴露给 timeline 下钻。
- The M2 full-Index fingerprint cost is removed from trusted resident event requests by an exact internal compatibility-materializer boundary that validates captured Index fields, exact `sessionsById` ownership, the selected Session graph, returned identity, and post-call ownership. Custom/future materializers retain the full graph fingerprint, so the narrowed fast path cannot be selected by an adapter merely declaring resident mode. Executable tests prove unrelated Index collections are not inspected and selected-session mutation/ownership failures remain closed. / M2 完整 Index fingerprint 成本通过精确的内部兼容 materializer 边界从受信任 resident event request 中移除；该边界校验捕获的 Index 字段、精确 `sessionsById` ownership、所选 Session 图、返回 identity 与调用后 ownership。自定义／未来 materializer 继续执行完整 graph fingerprint，因此 adapter 不能仅靠声明 resident mode 选择该快速路径。可执行测试证明不会检查无关 Index collection，同时所选 Session mutation／ownership failure 继续封闭。
- Deterministic profiling uses 4,097 rows per layer and two chunks per scan. The latest focused run records 1,602,040 accounted bytes and packed Main／Protocol／Raw scans of 29.8／18.5／18.1 ms versus oracle scans of 21.8／17.3／14.4 ms. The aggregate-only real-corpus command was `node --expose-gc --max-old-space-size=4096 scripts/project-query-store-profile.js --source codex --repo <redacted> --repeats 3` on Node v24.18.1/V8 13.6.233.17-node.50. It covered 484 Sessions and 72,070／133,046／288,161 rows, recorded 999,810,398 query-store bytes and packed medians/ranges of Main 535.9 ms [532.3, 545.4], Protocol 722.6 ms [715.6, 741.7], and Raw 1,847.5 ms [1,815.7, 1,887.5], versus oracle medians 342.0／395.5／944.6 ms. Peak over committed memory was 372.0 MB heap, 230.5 MB RSS, 211.4 MB external, and 244.5 MB ArrayBuffer. The initial always-gzip Protocol／Raw candidate was rejected at 573,918,577 bytes because Raw took 4,070.2 ms; 2 MiB and 3 MiB adaptive candidates were also rejected at Raw 2,052.2 ms and at 1,901.6 ms but 2.002× oracle. The accepted 3.5 MiB threshold is below both 2 seconds and 2× on every layer. Cancellation and revision retirement remain under 100 ms. This diagnostic run does not claim the M4 lifecycle-memory win. / 确定性 profiling 每层使用 4,097 rows，每次扫描两个 chunk。最新聚焦运行记录 1,602,040 accounted bytes；紧凑 Main／Protocol／Raw 扫描为 29.8／18.5／18.1 ms，oracle 为 21.8／17.3／14.4 ms。只输出聚合数据的真实语料命令为 `node --expose-gc --max-old-space-size=4096 scripts/project-query-store-profile.js --source codex --repo <redacted> --repeats 3`，运行于 Node v24.18.1／V8 13.6.233.17-node.50。它覆盖 484 个 Session 与 72,070／133,046／288,161 rows，记录 999,810,398 query-store bytes；紧凑 median／range 分别为 Main 535.9 ms [532.3, 545.4]、Protocol 722.6 ms [715.6, 741.7]、Raw 1,847.5 ms [1,815.7, 1,887.5]，oracle median 为 342.0／395.5／944.6 ms。相对 committed memory 的 peak 为 heap 372.0 MB、RSS 230.5 MB、external 211.4 MB、ArrayBuffer 244.5 MB。初始 Protocol／Raw 始终 gzip 的候选虽为 573,918,577 bytes，但因 Raw 需 4,070.2 ms 被拒绝；2 MiB 与 3 MiB 自适应候选也分别因 Raw 2,052.2 ms，以及虽为 1,901.6 ms 却达到 oracle 的 2.002× 而被拒绝。接受的 3.5 MiB 阈值让每层同时低于 2 秒与 2×。Cancellation 与 revision retirement 继续低于 100 ms。此次诊断运行不声称 M4 生命周期内存收益。
- First immutable candidate gates at `b57af1e0dd717fc9083ff7ecf4d86e5a49b7ffa1`: syntax checks and focused query/store/revision/conformance/profile suites passed; `npm test` passed 552/552; `build:check` and Codex/Claude `test:package` smoke passed; the complete browser suite passed 113/113; and `git diff --check` was clean. One preceding full browser run had a single 30-second deep-drill-down timeout after 112 passes; the exact test immediately passed 1/1 in 1.42 seconds and two required complete reruns then passed 113/113. No M4 production change was in scope. / 首个不可变候选 `b57af1e0dd717fc9083ff7ecf4d86e5a49b7ffa1` 的门槛：语法检查与聚焦 query／store／revision／conformance／profile suite 通过；`npm test` 552／552 通过；`build:check` 与 Codex／Claude `test:package` smoke 通过；完整 browser suite 113／113 通过；且 `git diff --check` 干净。此前一次完整 browser run 在 112 项通过后出现唯一一次 30 秒 deep-drill-down timeout；精确单测随即以 1.42 秒通过 1／1，随后要求的两次完整复跑均通过 113／113。范围内没有 M4 生产变更。
- The mandatory Luna max sweep independently accepted exact candidate `b57af1e0dd717fc9083ff7ecf4d86e5a49b7ffa1`: it found no hidden project-query materialization, source split, lease mixing, codec/accounting/cancellation/concurrency defect, or premature M4/M6 work. A fresh Sol xhigh architecture checkpoint then returned `CHANGES_REQUIRED` on the same exact head. It found three missed boundaries: legacy file/line Raw ownership still scanned resident complete Sessions; the Codex resident presentation catalog could fall back to Session `presentationIndexes`; and ordinary Session-list/file-suggestion responses could commit a newer revision under older browser state. / 强制 Luna max 扫描独立接受精确候选 `b57af1e0dd717fc9083ff7ecf4d86e5a49b7ffa1`：没有发现隐藏的项目 query 物化、来源分裂、lease 混用、codec／accounting／cancellation／concurrency 缺陷或过早的 M4／M6 工作。Fresh Sol xhigh 架构检查点随后对同一精确 head 返回 `CHANGES_REQUIRED`。它发现三个遗漏边界：legacy file／line Raw ownership 仍扫描常驻完整 Session；Codex 常驻 presentation catalog 可能回退读取 Session `presentationIndexes`；普通 Session 列表／文件建议 response 可能在旧 browser state 下提交较新 revision。
- The correction builds and validates a compact Codex owner table during indexing, changes shared lookup to return only canonical owner hints before materialization, removes the resident presentation fallback, and routes every revision-bearing browser query response through the same pre-commit/coalesced-recovery check. Getter-bomb owner/presentation tests and three focused browser mismatch scenarios pass. A current aggregate-only large-corpus owner audit covered 486 Sessions/290,258 Raw rows: 290,258 unique owners and 16,601,515 accounted bytes, below both canonical budgets. The corrected full gates pass: `npm test` 553/553, complete browser suite 115/115, `build:check`, Codex/Claude `test:package`, and clean `git diff --check`. Immutable-head Sol re-review remains required; M4 stays blocked. / 修正在索引期间构建并校验紧凑 Codex owner table，把共享 lookup 改为只在物化前返回 canonical owner hint，删除常驻 presentation fallback，并让每个带 revision 的 browser query response 通过同一套 pre-commit／合并恢复检查。Owner／presentation getter-bomb 测试与三个聚焦 browser mismatch 场景均通过。当前只输出聚合数据的大型语料 owner 审计覆盖 486 个 Session／290,258 条 Raw row：290,258 个唯一 owner、16,601,515 accounted bytes，低于两项 canonical budget。修正后的全量门槛均通过：`npm test` 553／553、完整 browser suite 115／115、`build:check`、Codex／Claude `test:package`，且 `git diff --check` 干净。仍需不可变 head Sol 复审；M4 继续阻塞。
- Exact Sol re-review of first correction head `2dc2178f918244025abcf956aff8e69e095eb10c` returned `CHANGES_REQUIRED`. The Raw owner and presentation blockers were closed, and every response had a pre-commit check, but a second revision detected by nested project results or file suggestions during a pending recovery was discarded without propagating failure; `loadSessions()` then reported success and could leave the browser on the intermediate revision. The second correction propagates nested failure, records another recovery request while coalescing is active, retries inside the bounded loop, and queues one successor when detection lands at the completion boundary. A focused overlapping-revision browser test proves two state reloads converge without exposing either injected marker. Parser-produced owner rows are also tightened to positive JSONL lines, closing the review's nonblocking line-zero observation. Exact full gates and immutable-head re-review remain required; M4 stays blocked. / 对首轮修正 head `2dc2178f918244025abcf956aff8e69e095eb10c` 的精确 Sol 复审返回 `CHANGES_REQUIRED`。Raw owner 与 presentation blocker 已关闭，所有 response 也都有 commit 前校验；但 pending recovery 期间若嵌套项目结果或文件建议检测到第二个 revision，response 虽被丢弃，失败却未向上传播，随后 `loadSessions()` 会报告成功，使浏览器可能停留在中间 revision。第二轮修正会传播嵌套失败，在合并恢复活跃时记录另一项恢复请求，在有界循环内重试，并在检测发生于完成边界时排队一次后继恢复。聚焦重叠 revision 浏览器测试证明两次 state reload 会收敛，且两个注入 marker 均不会暴露。Parser 产生的 owner row 也被收紧为正 JSONL line，关闭该评审的非阻塞 line-zero 观察。仍需精确全量门槛与不可变 head 复审；M4 继续阻塞。
- Second-correction gates pass: focused mismatch tests 4/4 and owner-boundary test 1/1; `npm test` 553/553; complete browser suite 116/116; `build:check`; Codex/Claude `test:package`; syntax checks; generated-client parity; and clean `git diff --check`. Immutable-head Sol re-review remains required; M4 stays blocked. / 第二轮修正门槛均通过：聚焦 mismatch 测试 4／4、owner 边界测试 1／1、`npm test` 553／553、完整 browser suite 116／116、`build:check`、Codex／Claude `test:package`、语法检查、生成客户端等价，以及干净的 `git diff --check`。仍需不可变 head Sol 复审；M4 继续阻塞。
- Exact Sol re-review accepted immutable implementation head `d2ff26e92104c83b17e46a11f3bb1566a1c65fb2`. It verified nested mismatch/retirement propagation, deadlock-free active-recovery signalling, bounded retry plus completion-edge successor recovery, non-commit of both mismatched DTOs, the genuine two-revision browser scenario, positive owner lines, generated-client parity, bilingual documentation, and no M4/M6 drift. It independently reran the overlapping-revision and owner tests plus `build:check`; supplied full Node 553/553, browser 116/116, package, syntax, and diff gates were consistent with inspection. The mandatory M3 checkpoint is complete; M4 may start in a later turn, but no M4 work starts in this checkpoint turn. / 精确 Sol 复审接受不可变实现 head `d2ff26e92104c83b17e46a11f3bb1566a1c65fb2`。复审确认了嵌套 mismatch／retirement 传播、无死锁的 active-recovery 信号、有界重试加完成边界后继恢复、两个 mismatch DTO 均不提交、真实的双 revision 浏览器场景、正数 owner line、生成客户端等价、双语文档，以及没有 M4／M6 drift。它独立复跑了重叠 revision 与 owner 测试以及 `build:check`；提供的全量 Node 553／553、browser 116／116、package、语法与 diff 门槛也与检查一致。强制 M3 检查点已完成；M4 可在后续 turn 开始，但本检查点 turn 不启动任何 M4 工作。

## M4 Codex source-backed candidate / M4 Codex 来源回读候选

- Production Codex now declares `indexed-materialized-v1`; Claude remains `resident-complete-v1`. `buildSourceBackedIndex()` performs candidate inspection, a transient relationship-evidence pass, global fork/review/earlier-branch inference, and an accepted-prefix finalization pass that incrementally builds the shared query store, legacy Raw owners, catalogs, dependency sets, and strict Indexed Sessions. Complete Raw/Logical/analysis/presentation graphs are absent from the committed Index. / 生产 Codex 现声明 `indexed-materialized-v1`；Claude 继续为 `resident-complete-v1`。`buildSourceBackedIndex()` 执行 candidate inspection、瞬态关系证据 pass、全局 fork／review／earlier-branch 推断，以及逐 accepted-prefix 的 finalize pass；后者会增量构建共享 query store、legacy Raw owner、catalog、dependency set 与严格 Indexed Session。已提交 Index 中不存在完整 Raw／Logical／analysis／presentation 图。
- Codex materialization validates the accepted transcript identity/prefix, ignores append-only suffix bytes for the captured revision, applies copied relationship and `session_index.jsonl` title/time facts, reconstructs exact current Raw/Logical/analysis/presentation state, and returns a distinct Materialized Session with only registered `_forkSegmentsByRawId` private state. Rewrite, truncation, removal, prefix mismatch, and same-content identity replacement fail `INDEXED_SOURCE_STALE`. / Codex 物化会校验已接受 transcript identity／prefix、对捕获 revision 忽略 append-only suffix byte、应用复制的关系与 `session_index.jsonl` title／time fact、重建精确当前 Raw／Logical／analysis／presentation state，并返回只有已注册 `_forkSegmentsByRawId` private state 的独立 Materialized Session。Rewrite、truncate、remove、prefix mismatch 与同内容 identity replacement 均以 `INDEXED_SOURCE_STALE` 失败。
- Exact unchanged reindex performs no parse and reuses the prior Indexed collection, query store, dependencies, owner projection, and catalogs. Changed reindex rebuilds the affected source snapshot and may reuse only deeply equal Indexed/dependency identities; fork relationship changes retract stale supersession correctly and no Materialized Session crosses revisions. / 完全未变化的 reindex 不执行 parse，并复用上一 revision 的 Indexed collection、query store、dependency、owner projection 与 catalog。变化 reindex 会重建受影响来源 snapshot，并且只可复用深度相等的 Indexed／dependency identity；fork 关系变化会正确撤回过期 supersession，任何 Materialized Session 都不会跨 revision。
- The exact repository-owned Codex materializer/validator set uses a frozen bounded Index view plus selected Indexed/dependency fingerprints and owning-Map identity checks. This closes the M3 O(total corpus) request-time fingerprint gate without permitting custom adapters to select the fast path. Pre-abort, in-flight abort, callback failure, and successful-return mutation checks remain exception-safe and non-mutating. / 仓库拥有且 identity 精确的 Codex materializer／validator set 使用 frozen 有界 Index view，加上所选 Indexed／dependency fingerprint 与 owning Map identity check。这关闭了 M3 的 request-time O(total corpus) fingerprint 门槛，同时不允许自定义 adapter 选择快速路径。Pre-abort、in-flight abort、callback failure 与成功返回 mutation check 继续保持异常安全且不可 mutation。
- Focused M4 coverage adds 10 strict lifecycle tests plus fork/reindex parity: no reachable full event graph or parsed source, repeated uncached parity, unrelated-Index getter bombs, append/rewrite/truncate/missing/identity-replacement freshness, no-parse unchanged reuse, copied-metadata revision behavior, cancellation, exact materialized-fork Raw/Logical/analysis/presentation parity, no parent materialization, packed Main/Raw ownership, and partial-reindex supersession. The latest full Node suite passes 565/565; targeted M4/fork/conformance/canonical tests, `build:check`, and `git diff --check` pass. / 聚焦 M4 coverage 新增 10 个严格生命周期测试及 fork／reindex 等价覆盖：完整 event 图与 parsed source 不可达、重复无 cache 等价、无关 Index getter bomb、append／rewrite／truncate／missing／identity replacement freshness、无 parse 的 unchanged reuse、复制 metadata 的 revision 行为、cancellation、materialized-fork Raw／Logical／analysis／presentation 精确等价、不物化 parent、紧凑 Main／Raw ownership 与 partial-reindex supersession。最新全量 Node suite 通过 565／565；聚焦 M4／fork／conformance／canonical 测试、`build:check` 与 `git diff --check` 均通过。
- The aggregate-only default-heap profile on the corpus visible on 2026-08-16 covers 48 Sessions and 8,142／8,239／29,610 Main／Protocol／Raw rows. It completes without a heap override in 23,691.9 ms; observed peak heap/RSS/external/ArrayBuffer are 147,380,496／475,918,336／126,831,562／124,669,702 bytes; forced-GC heap/RSS are 15,518,952／408,629,248 bytes; query-store／recorded-total accounted bytes are 96,603,125／98,395,861. Packed scan medians are 50.13／29.99／126.18 ms. The current corpus is not the historical 486-Session M3 snapshot, so no same-corpus historical ratio is claimed. A diagnostic 4 GiB legacy build over the exact same current candidates confirms 48 Sessions／29,610 Raw rows and ends at 312,442,232 heap bytes. / 只输出聚合数据的默认 heap profile 覆盖 2026-08-16 当时可见语料中的 48 个 Session 与 8,142／8,239／29,610 条 Main／Protocol／Raw row。它不提高 heap 上限并在 23,691.9 ms 内完成；观测 peak heap／RSS／external／ArrayBuffer 为 147,380,496／475,918,336／126,831,562／124,669,702 bytes；强制 GC 后 heap／RSS 为 15,518,952／408,629,248 bytes；query-store／已记录总 accounted bytes 为 96,603,125／98,395,861。紧凑 scan median 为 50.13／29.99／126.18 ms。当前语料不是历史 M3 的 486 Session snapshot，因此不声称同语料历史比例。对完全相同当前 candidate 的诊断性 4 GiB legacy build 确认 48 Session／29,610 Raw row，并以 312,442,232 heap bytes 结束。
- Browser fixtures now construct strict Codex indexes and materialize only local assertion data. The strict suite passes 114/116. The two deterministic long-search/pagination failures expose the deliberately deferred M6 boundary: a correct 171-row project drill-down loads/selects the deep target, then the default expanded profile issues 171 independent detail requests and M4 reparses the accepted Session for each because cache/coalescing is forbidden until M6; old-context work also extends into one pagination transition. Assertions remain unchanged, no M6 owner is implemented early, and this stage-specific result is recorded for the M6 full-browser gate. / Browser fixture 现构建严格 Codex index，只在测试进程内物化断言所需数据。严格套件通过 114／116。两个确定性的长搜索／分页失败暴露了有意推迟到 M6 的边界：正确的 171-row 项目下钻会加载／选中深层目标，随后默认展开 profile 发出 171 个独立 detail request；由于 M6 前禁止 cache／合并，M4 会为每个 request 重解析已接受 Session；旧 context work 也会延续到一次 pagination transition。断言保持不变，不提前实现 M6 owner，并把这一阶段性结果记录给 M6 的全量 browser 门槛。
- This candidate is not accepted until an independent immutable-head M4 review closes every blocking finding. M5 must not start before that verdict. / 在独立、基于不可变 head 的 M4 评审关闭所有阻断 finding 前，本候选不算接受；该 verdict 前不得启动 M5。

## Planned verification / 计划验证

### M2 seam and M3 shared-query boundary / M2 seam 与 M3 共享查询边界

- Focused canonical/source-adapter/query-store contract tests. / 聚焦 canonical／source-adapter／query-store 契约测试。
- Exact query-oracle parity for both sources and both locales. / 两种来源、两种 locale 的精确 query oracle 等价。
- M2 keeps both production adapters resident-complete. Its compatibility validators apply the exact current complete-Session contract and Index object identity without deriving future fields, while `materializeSession` adds non-mutation/cancellation/identity validation and conformance through the seam. / M2 让两个生产 adapter 继续 resident-complete。兼容 validator 精确应用当前完整 Session 契约与 Index object identity，不派生未来字段；`materializeSession` 则加入不可 mutation／cancellation／identity validation，以及穿过 seam 的 conformance。
- M3 exact query-oracle parity covers both sources/locales, array-free Indexed fixtures, asynchronous query cancellation, filtered navigation ordinal/revision, explicit Materialized inputs for event-level APIs, and lease retirement before/during/after await. / M3 精确 query-oracle 等价覆盖两种来源／locale、无 event array 的 Indexed fixture、异步 query cancellation、过滤后导航 ordinal／revision、event-level API 的显式 Materialized input，以及 await 前／中／后的 lease retirement。
- M3 ends with the mandated Luna max hidden-dependency sweep and implementation review. No Codex source-backed build or default-heap lifecycle claim occurs before acceptance. / M3 以强制 Luna max 隐藏依赖扫描与实现评审结束；接受前不进行 Codex 来源回读 build，也不声称默认 heap 生命周期收益。

### M4 Codex source semantics / M4 Codex 来源语义

- Compact Indexed shape, no reachable complete event graph, cold/warm materialization equality, source append/rewrite matrix, detail/Raw/image parity, and hydration coordination. / 紧凑 Indexed shape、完整事件图不可达、冷／热物化等值、来源 append／rewrite 矩阵、detail／Raw／image 等价与 hydration 协调。
- Materialized fork continuation ownership, inherited-context summary, Raw segment presentation, earlier-branch supersession, and unchanged-reindex query/evidence reuse. / Materialized fork continuation ownership、inherited-context 摘要、Raw segment presentation、earlier-branch supersession 与未变化 reindex 的 query／evidence 复用。
- Cold/warm here means repeated uncached M4 reconstructions with equality/parity checks; cache identity and coalescing are deliberately deferred to M6. Every direct reconstruction is guarded by the captured M3 lease. / 此处 cold／warm 指重复执行无 cache 的 M4 重建并检查等值／等价；cache identity 与合并明确推迟到 M6。每次直接重建都受捕获的 M3 lease 保护。

### M5 Claude / M5 Claude

- No reachable parsed Raw/source graph in the committed Index; exact message/tool pairing, plan/lifecycle/detail/analysis parity after materialization. / 已提交 Index 中 parsed Raw／来源图不可达；物化后 message／tool pairing、plan／lifecycle／detail／analysis 精确等价。
- Pointer/materialized fork, primary/derived/subagent/workflow ownership, sidecar dependency invalidation, and Indexed reuse without complete-array sharing. / Pointer／materialized fork、primary／derived／subagent／workflow ownership、sidecar 依赖失效，以及不共享完整 array 的 Indexed 复用。
- Claude direct reconstruction remains uncached and M3-lease-protected until M6 extends the same revision state with the owner/cache/scheduler. / Claude 直接重建在 M6 以 owner／cache／scheduler 扩展同一 revision state 前保持无 cache，并受 M3 lease 保护。

### M6–M7 runtime integration and closure / M6–M7 runtime 集成与收尾

- Full Node and browser suites, client build, package/release checks, source switch, project replacement, navigation revision, cancellation, and error/loading behavior. / 全量 Node 与 browser suite、client build、package／release 检查、来源切换、项目替换、导航 revision、cancellation 与 error／loading 行为。
- Final aggregate corpus measurements compared with M0, complete bilingual docs, accepted independent closeout review, and plan archive only after shipped completion. / 最终聚合语料量测与 M0 比较、完整双语文档、已接受独立收尾评审，以及只在已交付完成后归档 plan。

## Documentation updates / 文档更新

When behavior changes in later milestones, update the product spec if the external contract changes, this design doc for lifecycle/tradeoff changes, `transcript-source-adapters.md` for adapter-boundary changes, `logical-event-timeline.md` and the performance design for loading/query changes, and this active plan after each real checkpoint. English and Chinese text must change together. / 后续 milestone 改变行为时：若外部契约变化则更新产品规格；生命周期／tradeoff 变化更新本设计文档；adapter 边界变化更新 `transcript-source-adapters.md`；loading／query 变化更新 `logical-event-timeline.md` 与性能设计；每个真实检查点后更新本活跃计划。英文与中文必须同步变更。

This plan moves to `docs/exec-plans/completed/` only after M7 is genuinely complete. / 只有 M7 真正完成后，本计划才移至 `docs/exec-plans/completed/`。
