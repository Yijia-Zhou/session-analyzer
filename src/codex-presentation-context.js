'use strict';

// Compatibility boundary for existing imports. The implementation is shared
// because Codex and DeepSeek both project the same canonical eventRefs relation.
module.exports = require('./shared/code-mode-presentation-context');
