{
  "name": "session-analyzer",
  "version": "0.0.1",
  "private": true,
  "description": "Local interactive viewer for Codex session transcripts.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test"
  },
  "dependencies": {
    "markdown-it": "^14.1.0"
  },
  "engines": {
    "node": ">=18"
  }
}
